# Current State — Autosmartweby

> **Kanonicky dokument.** Aktualizuje se pri kazdem tasku, ktery meni stav systemu.
> **Posledni aktualizace:** 2026-04-22 (C-11)

---

## Souhrn

Autosmartweby je poloautomatizovany CRM system pro oslovovani ceskych zivnostniku bez webovych stranek.

**Apps Script backend** (~6700 LOC, 16 souboru): kvalifikace leadu, generovani briefu a emailovych draftu, per-lead odesilani pres GmailApp, mailbox sync, write-back z odvozeneho sheetu pres lead_id lookup (Variant B). Pipeline bezi v rezimu DRY_RUN=true a standardne se zastavi ve stavu BRIEF_READY. Webhook pipeline existuje v kodu, je vypnuta (ENABLE_WEBHOOK=false).

**CRM frontend** (Next.js 16, React 19, TypeScript): prihlasen stránka (email+heslo), dashboard s KPI widgety, leads tabulka s filtrovanim a detail drawerem, pipeline kanban (read-only, 6 sloupcu), follow-up timeline, editace 5 poli per lead se zapisem zpet do Sheets. Frontend bezi lokalne, neni nasazen na verejne URL. Data cte z Google Sheets pres service account, zapis pres Apps Script Web App endpoint (doPost handler implementovan v BX1, TEST runtime verified; Web App UI deployment pending).

**Vstup dat** je rucni. System nescrapuje leady, negeneruje webove stranky a nema hosting pipeline.

**Governance vrstva** je definovana v repu a vynucovana na GitHubu:
- branch protection na main je aktivni (require PR, 1 review, status check docs-governance, dismiss stale, strict),
- CI workflow docs-governance.yml kontroluje aktuálnost generovanych souboru a dokumentacni sync,
- check-doc-sync.mjs vraci 43 pass / 0 warn / 0 fail,
- task records system s 5 zaznamy, 4 automatizacni skripty, PR template,
- enforce_admins je false — owner muze obejit branch protection.

CI validuje aktuálnost generated files a existenci governance souboru. Nevaliduje povinnost task recordu pro kodove zmeny — to je konvence, ne enforcement.

## Co dnes existuje

### Apps Script backend (12 souboru, ~6600 radku)
- Kvalifikace leadu (evaluateQualification_) — rucni spusteni z menu
- Deduplikace pres company_key (ICO > domena > email > normalizovane jmeno + mesto)
- Template selection (12+ variant podle segmentu)
- Preview brief generovani (JSON: headline, subheadline, CTA, benefits, sections) — cesky s lokativy
- Email draft generovani (personalizovany predmet + telo, situacne zavisly uvod)
- Web check pres Serper API (hledani chybejicich webu) — manualni z menu
- **Auto web check hook A-06** (`apps-script/AutoWebCheckHook.gs`) — automaticky web check pro nove LEADS radky bez website_url. Reusuje `findWebsiteForLead_()`, batch size 20, LockService guard, double-run prevence pres `website_checked_at`, per-row error isolation. Dva trigger mody: casovy (15min, auto-install pres installProjectTriggers) a post-import (lead_id cileny z processRawImportBatch_). **Stav: TEST runtime overeno** (2026-04-17, kontrolovany delta test na 3 radcich: 1 FOUND + 2 NOT_FOUND, live Serper API + Sheets zapis potvrzeny).
- Odvozeny list "Ke kontaktovani" s KPI dashboardem, prioritou, editovatelnymi sloupci
- Write-back Varianta B (lead_id lookup, imunni vuci posunu radku)
- Per-lead Gmail draft/send z "Ke kontaktovani" sheetu
- Mailbox sync (read-only: thread_id, reply_type, timestamps, CRM labely)
- **Auto qualify hook A-07** (`apps-script/AutoQualifyHook.gs`) — automaticka kvalifikace po web checku. Reusuje `evaluateQualification_()`, batch size 20, LockService guard, double-run prevence pres `lead_stage`. Dva trigger mody: casovy (15min, auto-install pres installProjectTriggers) a post-web-check (volany z A-06 runAutoWebCheckInner_). **Stav: TEST runtime overeno** (2026-04-17, QUALIFIED + DISQUALIFIED + REVIEW + SKIPPED guard provereny diagnostickymi funkcemi, 23 lokalnich asserti). Bug fix: `extractDomainFromUrl_` vyzaduje tecku v domene (prevence `dom:nenalezeno`).
- **Preview queue → BRIEF_READY A-08** (`apps-script/AutoQualifyHook.gs` + pre-existing `apps-script/PreviewPipeline.gs`) — uzavira cestu QUALIFIED → BRIEF_READY. `processPreviewQueue()` vybira radky s `qualified_for_preview=TRUE` a `preview_stage ∈ {'', NOT_STARTED, FAILED, REVIEW_NEEDED, BRIEF_READY}`, zapisuje `preview_brief_json` (B-01 kontrakt, 18 poli), `preview_slug`, `preview_headline/subheadline/cta`, `template_type`, a pokud `send_allowed=TRUE` tak i `email_subject_draft` + `email_body_draft` + `outreach_stage=DRAFT_READY`. Per-row `try/catch` zajistuje, ze failure jednoho radku neshodi batch (FAILED + preview_error). Transition `lead_stage: QUALIFIED → IN_PIPELINE`. Dva trigger mody: casovy (15min timer) a post-qualify hook (volany z `runAutoQualify_` po uspesne kvalifikaci, non-fatal). **Stav: LOCAL VERIFIED** (2026-04-20, 6 scenaru / 38 asserti: happy path, send_allowed=FALSE, skip gates, per-row fail isolation, BRIEF_READY idempotence).
- **Ingest quality report A-09** (`apps-script/IngestReport.gs`) — reportovaci vrstva nad existujicim ingest funnellem. Pro kazdy `source_job_id` generuje 1 radek do append-only `_ingest_reports` sheet (41 sloupcu vcetne snapshot_stage) + full JSON payload do `_asw_logs`. Metriky: raw/imported/error/duplicate/pending counts, LEADS funnel (web_checked, qualified_or_beyond, brief_ready), derived rates (normalization_success, import, qualification, brief_ready, contact_completeness), `bottleneck_stage` detection, `summary_status` (OK/DEGRADED/PARTIAL/FAILED), `fail_reason_breakdown_json` grouping. Strict semantics: `duplicate_count=rejected_duplicate only`, `brief_ready_count=preview_stage='BRIEF_READY' only`, `qualified_or_beyond_count` jako canonical funnel metric (A-08 posouva QUALIFIED→IN_PIPELINE), `duration_ms_approx` labeled jako derived approximation. Dva trigger mody: post-batch hook v `processRawImportBatch_()` (per distinct source_job_id, non-fatal) a manualni menu ("Ingest report → …"). **Stav: LOCAL VERIFIED** (2026-04-20, 12 scenaru / 136 asserti: happy, empty, high-duplicate, missing-contacts, errors-dominate, partial, OK, schema sanity, report_id uniqueness, header validation, snapshot_stage differentiation, type preservation).
- **Preview URL return + statusy B-05** (`apps-script/PreviewPipeline.gs` + `apps-script/EnvConfig.gs` + `apps-script/Config.gs`) — uzavira CRM-side smycku mezi A-08 a B-04. Webhook payload doplnen o `preview_slug` (B-04 mandatory) a header `X-Preview-Webhook-Secret` (B-04 auth). `preview_stage` enum rozsiren o operator-facing lifecycle: `GENERATING` (in-flight, nahrazuje legacy QUEUED+SENT_TO_WEBHOOK), `READY_FOR_REVIEW` (URL zapsana, ceka operator, nahrazuje READY+REVIEW_NEEDED), `APPROVED` (operator manual terminal). Response parsing + write-back beze zmen (`preview_url, preview_screenshot_url, preview_generated_at, preview_version, preview_quality_score, preview_needs_review`). `FAILED` zustava v `eligibleStages` → natural-loop retry pres 15-min timer. `READY_FOR_REVIEW` a `APPROVED` nejsou eligible — operator-owned. Novy helper `getPreviewWebhookSecret_()` cte Script Property `PREVIEW_WEBHOOK_SECRET` (fail-closed empty fallback). Dva call sites: `processPreviewQueue()` (timer) a `runWebhookPilotTest()` (menu) — symetricke. **Stav: LOCAL VERIFIED** (2026-04-21, 10 scenaru / 42 asserti: success write-back, 200+ok:false, HTTP 400/401/500, network exception, retry eligibility, APPROVED preservation, per-row fail isolation, needs_review flag propagation). TEST RUNTIME not verified (vyzaduje clasp push + `PREVIEW_WEBHOOK_SECRET` Script Property + `WEBHOOK_URL` + B-04 endpoint deployed).
- Triggery: 15min timer (processPreviewQueue, autoWebCheckTrigger, autoQualifyTrigger), onOpen (menu), onEdit (write-back), post-qualify hook A-08 (inline), post-batch A-09 ingest report hook (inline v processRawImportBatch_)
- DRY_RUN defaultne zapnuty

### CRM frontend (Next.js 16 + React 19)
- Login (email+heslo, auth pres HMAC-SHA256 session cookie)
- Dashboard (KPI: k osloveni, high priority, follow-upy, pipeline breakdown)
- Leads tabulka s filtrovanim, razenim, detail drawer
- Pipeline kanban (6 sloupcu, read-only, bez drag-drop)
- Follow-up timeline (po terminu, dnes, zitra, tento tyden)
- Editace 5 poli per lead se zapisem zpet do Sheets
- Data: Google Sheets pres service account (read), Apps Script Web App (write — doPost implementovan BX1, Web App deployment pending)
- Mock service pro lokalni vyvoj bez Sheets pripojeni
- Preview renderer (B-02): route `/preview/[slug]`, renderuje MVP landing page z hardcoded sample briefu. 6 sekci (hero, services, contact, reviews, location, faq) rizenych polem `suggested_sections` z B-01 contractu. Verejne pristupny bez auth.
- Template family mapping (B-03): `crm-frontend/src/lib/domain/template-family.ts` mapuje runtime `template_type` na 4 MVP family (`emergency`, `community-expert`, `technical-authority`, `generic-local`) + render hints. Renderer zatim zustava template-agnostic; family vrstva je pripravena pro nasledne family-specificke layouty.
- Preview render endpoint (B-04): `POST /api/preview/render` (`crm-frontend/src/app/api/preview/render/route.ts`) prijima payload z Apps Scriptu, validuje proti B-01 MinimalRenderRequest, upsertne brief do in-memory preview store (`src/lib/preview/preview-store.ts`) a vrati `MinimalRenderResponseOk` s `preview_url = ${PUBLIC_BASE_URL}/preview/${preview_slug}`. Auth: header `X-Preview-Webhook-Secret` vs env `PREVIEW_WEBHOOK_SECRET` (timing-safe compare). Unknown template base fallback → `preview_needs_review=true`. B-02 route `/preview/[slug]` nyni prednostne cte runtime store a fallbackuje na hardcoded fixtures. Zive GAS propojeni zatim chybi — GAS payload jeste neobsahuje `preview_slug` (B-05 gap fix).
- Bezi lokalne, neni nasazen na verejne URL

### Datove kontrakty a staging
- Scraping Job Input v1.0 (`docs/contracts/scraping-job-input.schema.json`) — kanonicky kontrakt pro jeden scraping job (1 job = 1 query na 1 portalu v 1 meste/segmentu). Definuje 12 poli, deterministicky `source_job_id` a lifecycle stavy.
- RAW_IMPORT staging layer v1.0 (`docs/contracts/raw-import-staging.md`) — kontrakt pro `_raw_import` system sheet, staging buffer mezi scraperem a LEADS. 16 sloupcu, 5-stavovy status model, 4-hodnotovy decision model.
- Normalization raw -> LEADS rules v1.0 (`docs/contracts/normalization-raw-to-leads.md`) — kontrakt pro transformaci surovych dat z `_raw_import` na LEADS radek. 23-field mapping, cleaning rules, reject policy, lead_id generation, 6 novych source_* metadata sloupcu.
- **Scraper runtime A-04 (firmy.cz)** (`scripts/scraper/firmy-cz.mjs`) — Node ESM CLI, cte A-01 job input a produkuje pole A-02 RAW_IMPORT rows. Parsing strategy: JSON-LD schema.org primary + Open Graph a regex fallbacks. Per-record try/catch, rate-limited live mode, fixture mode pro deterministicky offline test. Zapis do `_raw_import` sheetu je mimo scope A-04.
- **Ingest runtime bridge A-10** (`apps-script/Normalizer.gs`, `apps-script/RawImportWriter.gs`) — runtime vrstva pro `_raw_import`: normalizer implementuje A-03 cleaning rules vcetne CZ phone prefix normalizace, reject policy a segment slug-to-label mapping (SETTINGS!A2:A11 kompatibilni); staging writer zajistuje sheet creation, append-only zapis, in-place status update, batch orchestrator (raw → normalize → dedupe → import/reject) a LEADS append pres HeaderResolver. **Stav: TEST runtime overeno** (2026-04-17, leadsBefore=799, leadsAfter=800, leadsAppended=1; lokalni proof: 7 rows, 1 reject, 2 hard dup, 4 imported).
- Zbyvajici automatizace (zivefirmy.cz scraper, review UI) nejsou jeste implementovane.

### Dokumentace
- Governance s validacnim scriptem
- 8 decisions (D-1 az D-8), 6 hotovych

## Co neexistuje

- Automaticky scraping kontaktu z portalu
- Formular pro vyhledavani na portalech
- Generovani a deploy skutecnych preview webu
- Hromadne odesilani emailu
- Automaticky trigger na novy radek v LEADS
- End-to-end automatizace bez lidskeho zasahu
- CI/CD pipeline pro kod (existuje jen docs-governance check)
- Testy (zadne unit, integration ani e2e testy)
- Frontend deployment (Vercel/Netlify)
- ~~Apps Script Web App doPost handler~~ — **BX1 DONE** (doPost + handleUpdateLead_, inner logic TEST runtime verified; Web App UI deployment + frontend e2e pending)

## Specifikace

- **Lead Lifecycle State Machine (CS1):** Autoritativni specifikace end-to-end lifecycle stavu leadu — viz `docs/21-business-process.md`. Definuje 18 canonical stavu (4 terminal, 3 review), 24 povolenych prechodu, derivacni pravidla z existujicich stage poli. Scope: od importu po reakci; WON/LOST jsou downstream mimo lifecycle. Implementace sloupce `lifecycle_state` neni soucasti CS1.
- **Workflow Orchestrator (CS2):** Logicka orchestracni vrstva nad lifecycle — viz `docs/24-automation-workflows.md`. Hybrid model (poll + manual + reactive), 17 eventu, formalni step kontrakt, run history design. Kompatibilni s existujicimi Apps Script triggery.
- **Reliability & Idempotency (CS3):** Idempotency keys, retry politika, dead-letter handling a locking pro vsechny automaticke workflow kroky — viz `docs/24-automation-workflows.md`. Definuje 12 kroku s idempotency strategii, retry matici (transient/permanent/ambiguous), dead-letter design v dedickovany `_asw_dead_letters` sheet (append-only, separatni od `_asw_logs` run history), lock pravidla pro Apps Script LockService.
- **Sendability Gate (C-04):** Autoritativni specifikace gate mezi preview a outreach fazi — viz `docs/24-automation-workflows.md`. Definuje 3 gate outcomes (AUTO_SEND_ALLOWED / MANUAL_REVIEW_REQUIRED / SEND_BLOCKED) — gate outcomes NEJSOU lifecycle states — 21 blocking reasons ve 4 kategoriich (canonical-lifecycle / compliance / outbound-signal / data-deficit) + 3 review reasons se stable reason codes, 19 hard conditions pro auto-send, 8-ORDER precedence a deterministic evaluator pseudocode. CS1 konzistence: `TERMINAL_STATE_*` reason codes pokryvaji pouze CS1 canonical terminals (DISQUALIFIED / REPLIED / BOUNCED / UNSUBSCRIBED); `outreach_stage` hodnoty `WON`/`LOST` jsou downstream auxiliary outcomes (mimo CS1), fallback je derivuje na REPLIED. Scope: pouze SPEC, zadny runtime sender / queue / UI. Implementace a PROPOSED sloupce (`sendability_outcome`, `unsubscribed`, `suppressed`) jsou handoff na navazujici task.
- **Outbound Queue (C-05):** Autoritativni specifikace datove vrstvy mezi C-04 gatem a budoucim senderem — viz `docs/24-automation-workflows.md`. Definuje `_asw_outbound_queue` sheet schema (32 poli, 15 povinnych per zadani + 17 auditability rozsireni), 5 queue statusu (QUEUED / SENDING / SENT / FAILED / CANCELLED — queue statusy NEJSOU CS1 canonical lifecycle states, jsou ortogonalni dimenze nad queue radkem), povolene/zakazane transitions s 5 invarianty (vcetne ireverzibility `SENDING → QUEUED`), deterministicky pseudocode pro queue create / worker claim / cancel / fail, send payload kontrakt v1.0 (sekce 6), immediate vs scheduled pravidla, failure design s 6 povinnymi poli pro diagnostiku, audit cross-ref graph (LEADS ↔ queue ↔ `_asw_logs` ↔ `_asw_dead_letters`), 4 sample rows. CS3 alignment: idempotency key pattern `send:{lead_id}:{SHA256(email + subject + body)}` (reuse S12), max_attempts=1 + okamzity dead-letter, retry = novy queue row ne prepis stavajici. CS1 alignment: zadny novy canonical state; T17/T18 transitions mapovane na queue row insert / SENT. C-04 alignment: pouze AUTO_SEND_ALLOWED vytvari queue radek, outcome je snapshotovan v `created_from_sendability_outcome`. Scope: pouze SPEC, zadny runtime sender / queue worker / UI / ESP integration / Config.gs changes. Vsechny nove sloupce jsou PROPOSED FOR C-05 a budou materializovany implementacnim taskem.
- **Provider abstraction + sender interface (C-06):** Autoritativni specifikace provider-agnosticke vrstvy mezi `_asw_outbound_queue` a konkretnim ESP — viz `docs/24-automation-workflows.md`. Definuje `EmailSender` interface (1 metoda `send(request: SendRequest) → NormalizedSendResponse`), `SendRequest` kontrakt (17 poli — 13 immutable snapshot z C-05 payload + 4 runtime-derived), `NormalizedSendResponse` kontrakt (17 poli — 7 povinnych per zadani + 10 auditability rozsireni), `NormalizedProviderStatus` enum (7 hodnot: ACCEPTED / QUEUED_BY_PROVIDER / REJECTED / THROTTLED / TIMEOUT / AUTH_FAILED / UNKNOWN), `NormalizedSendErrorClass` enum (8 hodnot: TIMEOUT / RATE_LIMIT / INVALID_RECIPIENT / AUTH_FAILED / PROVIDER_UNAVAILABLE / PROVIDER_REJECTED / INVALID_REQUEST / UNKNOWN) s deterministickym 1:N mappingem na CS3 `failure_class`, 4-vrstvovou separaci statusu (provider raw / NormalizedProviderStatus / QUEUE_SEND_STATUS / CS1 lifecycle) s fixnim lookup table, 3 fail scenare (TIMEOUT → AMBIGUOUS HOLD, RATE_LIMIT → TRANSIENT, INVALID_RECIPIENT → PERMANENT), Gmail vs generic ESP sample mapping s comparison tabulkou, sender selection via Script Property `EMAIL_PROVIDER` (multi-provider fallback out-of-scope), PII safety invariant pro `provider_response_excerpt` + `error_message`. CS3 alignment: `EmailSender.send` nikdy neretryuje (respect S12 max_attempts=1), `retryable` hint + `rate_limit_reset_at` jsou pouze diagnostika; retry = novy queue row (C-05 pravidlo). CS1 alignment: zadny novy canonical state; T18 (EMAIL_QUEUED → EMAIL_SENT) je triggered queue workerem pri success, C-06 samotny nezapisuje do LEADS. C-05 alignment: `SendRequest` je stavebni derivat payload v1.0, `NormalizedSendResponse.provider_message_id` + `sent_at` jsou zdroj pro queue row `SENT` transition. Scope: pouze SPEC, zadny runtime sender / Gmail adapter / ESP adapter / queue worker / factory / Config.gs changes / frontend UI. Vsechny nove artefakty jsou PROPOSED FOR C-06 a budou materializovany implementacnim taskem.
- **Follow-up engine (C-08):** Autoritativni specifikace follow-up automation — viz `docs/24-automation-workflows.md`. Definuje 3-stage sekvenci (`initial` / `follow_up_1` / `follow_up_2`), max 2 follow-upy po initial (total 3 queue rows per sekvence), T+3 / T+7 business days timing pocitany z actual `sent_at` predchozi stage (ne scheduled_at / queued_at / created_at), quiet hours 09:00–17:00 Europe/Prague s deterministickym posunem, 5 stop condition kategorii (REPLY / UNSUBSCRIBE / BOUNCE / MANUAL_BLOCK / REVIEW_FLAG|UNKNOWN_INBOUND) + 5 composite stop invariants (CS1 terminal lookup; C-04 gate re-check s `is_followup=true` context; C-07 inbound event store check; manual block flag highest-priority po CS1 terminal; OOO hold pause 14 dni ne stop), triple-redundant reply guard (CS1 REPLIED + C-04 B3 + inbound event store → garantuje "lead s reply uz nikdy nedostane follow-up"), 3 decision outcomes (AUTO_INSERT / REVIEW_REQUIRED / STOP), pregenerate-vs-regenerate tabulku (IMMUTABLE recipient/sender_identity/preview_url/personalization vs REGENERATED subject/body/CTA vs STAGE-DERIVED thread_hint/scheduled_at/priority/idempotency_key), 5-vrstvou tvrdou separaci identity (stage / lifecycle / queue status / inbound event / review flag) s 5 zakazanymi kolapsi, idempotency pattern `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}` + 4 invariants (unique per stage×sekvence×lead, safe-to-run-twice, race-safe, ne-rollback-safe), manual block model + 6-dim differentiation od UNSUBSCRIBE (zdroj / scope / CS1 state change / reversibilita / storage / compliance), 5 sample lead timelines (silent happy path / reply → stop / bounce → stop / unsubscribe → stop / unknown-inbound → review → manual_stop) s plnymi per-row tabulkami, sample queue rows (initial / follow_up_1 / follow_up_2) s field-level INHERITED/REGENERATED/STAGE-DERIVED/STAGE-OVERRIDE markers. CS1 alignment: zadny novy canonical state (reuse EMAIL_SENT / REPLIED / BOUNCED / UNSUBSCRIBED + T20/T21/T22); engine cte lifecycle_state, nikdy nezapisuje. CS2 alignment: engine je novy CS2 step `followup_engine_run` (batch daily + optional reactive). CS3 alignment: LockService per engine run, failure_class mapping (ENGINE_TIMEOUT→TRANSIENT, C04_GATE_FAIL→PERMANENT, PARENT_NOT_SENT→TRANSIENT, MAX_RETRIES_EXCEEDED→PERMANENT), dead-letter append-only. C-04 alignment: PROPOSED `is_followup` context parameter pro bypass B16 `ALREADY_SENT`; respektuje B3/B4/B5/B7/B8 + PROPOSED `ADDRESS_BOUNCED`. C-05 alignment: engine je producer (vytvari nove queue rows pres insert contract), PROPOSED schema extensions `sequence_stage` / `parent_queue_id` / `sequence_root_queue_id` / `sequence_position` / `created_from_followup_engine_run` jsou dodatky ne mutace. C-06 alignment: engine populuje `SendRequest.thread_hint` (forward-compat pole); cte `NormalizedSendResponse.sent_at` / `provider_message_id` / `provider_thread_id`. C-07 alignment: engine cte `_asw_inbound_events` pro stop detection; respektuje 3-tier stop model (Tier 1 per thread, Tier 2 per address, Tier 3 lead-wide); OOO → pauze 14 dni ne stop. B6 (operator reply-handling UI) NENI blocker — operator muze flag `followup_manual_block` / `followup_review_required` / `reply_needs_manual` resolve primo v Google Sheets bunce. Scope: pouze SPEC, zadny scheduler runtime / cron / queue worker / mailbox sync / ESP webhook handler / frontend UI / text-generation / Config.gs zapisy. Vsechny nove artefakty (4 C-05 schema extensions, 3 LEADS flag extensions, C-04 `is_followup` context, 8 `_asw_logs` event types, 10-value stop_reason enum, decision outcome enum, idempotency pattern, engine batch run artifact `FU-RUN-*`, priority defaults 100/90/80, CS3 failure_class mapping, auto-terminate thresholds OOO=14d / review stale=30d / soft-bounce=3, 8 Script Properties) jsou PROPOSED FOR C-08 a budou materializovany implementacnim taskem.
- **Inbound event ingest (C-07):** Autoritativni specifikace ingest vrstvy mezi mailbox syncem / ESP webhookem a CS1 lifecycle — viz `docs/24-automation-workflows.md`. Definuje 5-family event taxonomy (REPLY, BOUNCE, UNSUBSCRIBE primarni; UNKNOWN_INBOUND, COMPLAINT rezervni), 3 event schemata (`reply_event` 18 poli, `bounce_event` 15 poli, `unsubscribe_event` 14 poli), 4-vrstvovou separaci (raw source → normalized event → lifecycle transition → review flag) s nezamenitelnym assignment per vrstvu, lifecycle mapping tabulku (8 radku) s 4 invarianty (vcetne explicit "NEEDS_MANUAL_REPLY je **review flag** ne CS1 state" + multi-event priority "UNSUBSCRIBE > COMPLAINT > BOUNCE > REPLY > UNKNOWN_INBOUND"), 3-tier stop rule model (Tier 1 follow-up stop per thread pro REPLY/UNKNOWN; Tier 2 address stop per email pro BOUNCE; Tier 3 lead stop napric kanaly pro UNSUBSCRIBE/COMPLAINT), idempotency kontrakt (event-level `ingest_event_id` = `gmail:{msg_id}` / `esp:{provider}:{event_id}` / `manual:{op}:{lead}:{type}:{hash}`; lifecycle-level transition guards), source variants (GMAIL_THREAD / GMAIL_DSN_THREAD / ESP_WEBHOOK / MANUAL_OPERATOR_INPUT), 3 sample lifecycle scenare (positive reply / hard bounce DSN 5.1.1 / unsubscribe via List-Unsubscribe + reply body intent). CS1 alignment: reuse existing REPLIED (#15) / BOUNCED (#16) / UNSUBSCRIBED (#17) terminals + T20/T21/T22 transitions, zadny novy canonical state, `reply_needs_manual` je boolean flag nad lifecycle state. CS3 alignment: ingest job lock pattern, failure_class mapping (GMAIL_API_TIMEOUT→TRANSIENT, ESP_WEBHOOK_AUTH→PERMANENT, CLASSIFIER_AMBIGUOUS→AMBIGUOUS), dead-letter append-only. C-04 alignment: Tier 3 reuses existing B7 `UNSUBSCRIBED`; Tier 2 vyzaduje PROPOSED novy block reason `ADDRESS_BOUNCED`. C-05 + C-06 alignment: C-07 jen **cte** `outreach_queue_id` / `provider_message_id` / `provider_thread_id` pro thread pairing, NEMUTUJE queue schema ani sender interface. B6 (operator reply-handling UI) NENI blocker — C-07 definuje kontrakt, B6 implementuje UI. Scope: pouze SPEC, zadny runtime mailbox ingest / ESP webhook handler / Gmail `List-Unsubscribe` detekce / `_asw_inbound_events` sheet creation / reply classifier / follow-up engine / frontend UI / Config.gs zapisy. Vsechny nove artefakty (`_asw_inbound_events` sheet, 3 event schemata, `reply_class`/`bounce_class`/`unsubscribe_source`/`raw_source`/`event_type` enumy, LEADS extension sloupce `reply_needs_manual`/`bounce_class`/`unsubscribed`/`unsubscribed_at`/`unsubscribe_source`/`soft_bounce_count`, `EMAIL_SYNC_STATUS` BOUNCED+UNSUBSCRIBED extensions, `EMAIL_REPLY_TYPE` UNSUBSCRIBE+COMPLAINT extensions, C-05 `last_inbound_event_id` backlink, C-04 `ADDRESS_BOUNCED` block reason, `rule-based-v1` classifier) jsou PROPOSED FOR C-07 a budou materializovany implementacnim taskem. C-07 je **prerekvizita** pro jakykoli follow-up cadence engine (engine potrebuje vedet, kdy NEposilat dalsi email).
- **Exception queue & human-in-the-loop (C-09):** Autoritativni specifikace centralizovane review queue + operator resolution kontraktu pro pripady, ktere automat neumi / nesmi rozhodnout sam — viz `docs/24-automation-workflows.md`. Definuje 10-type exception taxonomy (`preview_render_fail`, `missing_email`, `ambiguous_duplicate`, `broken_personalization`, `provider_fail_after_max_retries`, `unclear_reply`, `sendability_manual_review`, `compliance_hard_stop`, `normalization_error`, `followup_stale_review`) — 6 kanonickych z user briefu + 4 rozsireni grounded v repo. 4-tier priority model (P1 compliance > P2 delivery > P3 content > P4 data-quality) + SLA targets (P1 < 24h, P2 < 2 business days, P3 < 5 business days, P4 < 10 business days) + auto-priority-bump pravidla (pending > 2 SLA → promote; P4 pending > 14 dni → CLOSED_STALE; C-08 OOO hold > 7 dni → promote P2) + compliance precedence invariant (P1 nesmi byt merged/approved/retried — jediny outcome reject + audit note). PROPOSED `_asw_exceptions` sheet schema (24 poli append-only-with-resolution-update): `exception_id` (format `EX-{YYYY-MM-DD}-{NNNNN}`), `lead_id` + `outreach_queue_id` + `source_job_id` + `inbound_event_id` + `cs2_run_id` + `related_dead_letter_id` FK cross-ref, `exception_type`/`exception_priority`/`exception_status`, `detected_at`/`detected_by_step`/`summary`/`diagnostic_payload_json` (PII-masked max 4KB), `operator_decision`/`operator_note`/`operator_edited_fields_json`/`resolved_at`/`resolved_by`, `resolution_outcome`/`retry_reference_queue_id`/`retry_reference_exception_id`/`next_action`/`sla_target_at`. Minimal sheet-row-based review interface (read-only / editable / derived fields + on-edit trigger, B6-less interim). 4 discrete operator resolution outcomes (`approve` / `reject` / `retry` / `edit_and_continue`) s kompletnim kontraktem per outcome + resolution outcome × exception type compatibility matrix (napr. `compliance_hard_stop` + `approve` = validation error). 5-status model (OPEN → IN_REVIEW → RESOLVED → CLOSED; CANCELLED auto-resolved parallel; state diagram + 4 invariants vcetne CLOSED/CANCELLED terminal no-reopen + OPEN → RESOLVED forbidden). 6 next_action re-entry paths (`RETURN_TO_C04_GATE` / `CREATE_NEW_QUEUE_ROW` / `RESUME_C08_SEQUENCE` / `UPDATE_CS1_LIFECYCLE` / `LEAD_RE_INGEST` / `TERMINAL_STOP`). 5 sample exception rows (preview_render_fail / missing_email / ambiguous_duplicate / unclear_reply / compliance_hard_stop) + 4 sample resolutions (full operator workflows). Auditability: 9 `_asw_logs` event types (`exception_created`, `exception_claimed`, `exception_released`, `exception_resolved`, `exception_flow_reentry`, `exception_closed`, `exception_cancelled`, `exception_priority_bumped`, `exception_retry_chain_broken`) + cross-ref graph (LEADS ↔ exceptions ↔ queue ↔ inbound events ↔ logs ↔ dead_letters) + observability query patterns bez B6 UI. Idempotency per-type dedup_key pattern (napr. `exc:preview_render_fail:{queue_id}`, `exc:unclear_reply:{inbound_event_id}`, `exc:compliance_hard_stop:{lead_id}:{reason_code}`) + recent-closed window=7 dni + reopen rules (vzdy novy row + `retry_reference_exception_id`) + retry chain depth limit=3. **Dependency narrowing C-03 → CS3:** puvodni task brief uvadel `C-03`, ale v repo source-of-truth `C-03.md` NEEXISTUJE a `C3.md` je governance hardening (unrelated). Dependency narrowing na `CS3` (reliability prerekvizita — dead-letter, retry matice, LockService, failure_class) je explicitne dokumentovano. CS1 alignment: reuse existing `REVIEW_REQUIRED` #18 non-terminal + canonical terminals DISQUALIFIED/REPLIED/BOUNCED/UNSUBSCRIBED; zadny novy canonical state. CS2 alignment: nove steps `exception_detector` (hook do A-02/A-03/A-05/A-06/A-07/A-08/B-04/C-04/C-05/C-06/C-07/C-08 pri fail) + `exception_resolution_dispatcher`. CS3 alignment: C-09 je downstream konzument dead-letter, FK cross-ref pres `related_dead_letter_id`, zadna mutace CS3 schema. C-04 alignment: `MANUAL_REVIEW_REQUIRED` → `sendability_manual_review`; `SEND_BLOCKED` B7/B8 → `compliance_hard_stop`; PROPOSED `MANUAL_REVIEW_OVERRIDE` context parameter po approve. C-05 alignment: FAILED → CS3 dead-letter → `provider_fail_after_max_retries`; retry = novy queue row (C-05 invariant), idempotency_key extension `retry_of=exception_id` suffix. C-06 alignment: `NormalizedSendErrorClass` + `failure_class=PERMANENT` exhausted → CS3 → C-09; zadna mutace sender interface. C-07 alignment: `reply_class=UNCLASSIFIED` + `reply_needs_manual=TRUE` → `unclear_reply`; operator reclassification z resolve aktualizuje LEADS. C-08 alignment: `REVIEW_REQUIRED` → exception; `followup_stale_review_abandoned` → `followup_stale_review`; resolution resumes/stops sequence. B6 (operator exception dashboard UI) NENI blocker — operator v interim resolvuje primo v Google Sheets bunce. Future C-10 (suppression list aggregation) zadna direktni dependency; C-09 poskytuje audit trail jako source data. Scope: pouze SPEC, zadny runtime review worker / cron / scheduler / frontend UI / `detectException()` hooky / sheet creation / Config.gs zapisy / notification system. Vsechny nove artefakty (24-field sheet, 6 enumu, idempotency pattern, 9 `_asw_logs` event types, C-04 `MANUAL_REVIEW_OVERRIDE` context, C-05 idempotency extension, LEADS `open_exceptions_count` backref, SLA auto-bump rules, retry chain depth limit=3, stale abandonment=14 dni, compatibility matrix validation, CS2 2 nove steps, CS3 failure_class mapping pro engine errors, 7 Script Properties) jsou PROPOSED FOR C-09 a budou materializovany implementacnim taskem.
- **Config, secrets, limity a budget guardrails (C-11):** Autoritativni specifikace configuration vrstvy nad celou automatizaci — viz `docs/24-automation-workflows.md`. Definuje **6 ortogonalnich configuration planes** s tvrdou separaci invariantem `CONFIG ≠ SECRET ≠ LIMIT ≠ BUDGET ≠ KILL_SWITCH ≠ FEATURE` + 12 zakazanych kolapsu (NIKDY `SERPER_API_KEY` jako CONFIG — patri do SECRET; NIKDY `BATCH_SIZE` jako BUDGET — patri do LIMIT; NIKDY `DRY_RUN=true` jako CONFIG forever — patri do KILL_SWITCH `KILL_SWITCH_ALL_WRITES`; NIKDY `EMAIL_SYNC_ENABLED` jako CONFIG — patri do FEATURE `FEATURE_EMAIL_SYNC`; NIKDY `KILL_SWITCH_GMAIL` jako FEATURE — kill switch je emergency ne product optionality; NIKDY `BUDGET_GMAIL_DAILY_QUOTA` jako LIMIT — budget je cost/quota s ledger ne hard cap). Kazdy config artefakt patri do prave jedne role. Canonical 13-column config table s 17 VERIFIED IN REPO entries (ASW_ENV, ASW_SPREADSHEET_ID, PREVIEW_WEBHOOK_SECRET, SERPER_API_KEY, DRY_RUN const, ENABLE_WEBHOOK const, WEBHOOK_URL const, BATCH_SIZE const, EMAIL_MAILBOX_ACCOUNT const, EMAIL_SYNC_* consts, sheet name consts) + PROPOSED 4 CONFIG + 9 LIMIT_* + 7 BUDGET_* + 14 KILL_SWITCH_* + 10 FEATURE_* + 2 SECRET entries. **DRY_RUN → KILL_SWITCH_ALL_WRITES reklasifikace** (dnesni hybrid artifact migruje na Script Property s backward-compat fallback na `DRY_RUN` const). **EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC reklasifikace** (CONFIG const migruje na feature flag s rollout lifecycle). 10-field secrets inventory contract (`key`/`provider`/`rotation_policy`/`redaction_mask_pattern`/`used_in_step[]`/`access_pattern`/`timing_insensitive_compare`/`pii_class`/`last_rotated_at`/`owner`) s 5 VERIFIED + 2 PROPOSED (SENDGRID_API_KEY, MAILGUN_API_KEY conditional na `EMAIL_PROVIDER`). Per-stage limits pro 9 pipeline stages (A-04 firmy.cz scraper, A-05 dedupe, A-06 auto web check, A-07 auto qualify, A-08 preview queue, B-04 preview render endpoint, C-05 outbound queue worker, C-06 provider send, C-08 follow-up engine) — 9 LIMIT_* Script Properties (A-05/A-07/A-08 reuse VERIFIED `BATCH_SIZE=100` bez noveho PROPOSED entry). Invariant: limits jsou hard caps per stage — pri prekroceni stage safe-stopne s `limit_exceeded` event, nesmi retryovat proti limitu. **Budget guardrails** 10-field contract (`budget_id`/`category`/`provider_or_api`/`scope`/`reset_policy`/`warning_threshold_pct=80`/`critical_threshold_pct=100`/`hard_stop_on_critical`/`current_consumption`/`last_reset_at`) + **5 kategorii**: BG-PROV-OPS (Gmail daily send quota, Serper monthly quota, Gemini token budget) / BG-RETRY-EXPLOSION (max retries per step per day) / BG-COST-EXPLOSION (Serper per-query cost, Gemini tokens) / BG-RUNAWAY-BATCH (batch size explosion prevention) / BG-SILENT-DEGRADATION (Gmail soft bounce 7d rolling trend). PROPOSED `_asw_budget_ledger` 12-field append-only sheet (ledger_id/budget_id/cs2_run_id/stage/delta_amount/delta_unit/cumulative_at_time/threshold_status/triggering_step/triggering_event_id/timestamp/note). **Kill switch model** s 5-scope taxonomy: GLOBAL (KILL_SWITCH_ALL_WRITES celosystemovy shutdown) / ENV (KILL_SWITCH_PROD_ENFORCE) / CATEGORY (KILL_SWITCH_EMAIL_ALL / SCRAPE_ALL / WEBHOOK_ALL) / PROVIDER (KILL_SWITCH_GMAIL / SENDGRID / MAILGUN / SERPER) / API_SURFACE (KILL_SWITCH_DOPOST_WRITE / PREVIEW_WEBHOOK). Safe-stop semantics: inflight operace dokonci transakcni celek; nove operace vidi kill switch a safe-stopnou s `kill_switch_triggered` event + `cs2_run_id` + `triggering_step` + `intended_sheet_writes[]` audit trail. Manual reset pres Script Property update + `kill_switch_reset` event. Auto-escalate CATEGORY > 24h → GLOBAL (via `KILL_SWITCH_AUTO_ESCALATE=true`). Idempotentni check per CS2 step entry. Feature flag 8-field contract (flag_id/rollout_stage/default_value_per_env/target_audience/owner_stream/related_task/sunset_date/label) + 5 rollout stages: draft → pilot (TEST only) → rollout (TEST + 10% PROD) → stable → deprecated. 10 PROPOSED feature flags (FEATURE_EMAIL_SYNC, FEATURE_PREVIEW_WEBHOOK_V2, FEATURE_AUTO_QUALIFY_HOOK, FEATURE_AUTO_WEB_CHECK_HOOK, FEATURE_DOPOST_WRITE, FEATURE_FOLLOWUP_ENGINE, FEATURE_INBOUND_EVENT_INGEST, FEATURE_PERFORMANCE_REPORT, FEATURE_EXCEPTION_QUEUE, FEATURE_PROVIDER_ABSTRACTION). State safety rules (5): config change atomicity (snapshot na CS2 run start); kill switch activation timing (mezi stepy ne uprostred); budget threshold visibility (WARNING event i bez hard_stop); feature flag snapshot na run start; secret rotation dual-read window pro long-running steps. 9 PROPOSED `_asw_logs` event types: `config_value_changed`, `secret_rotated`, `limit_exceeded`, `budget_warning_crossed`, `budget_critical_crossed`, `kill_switch_triggered`, `kill_switch_reset`, `feature_flag_enabled`, `feature_flag_disabled`. 4 PROPOSED diagnostic tools: `diagConfigState` / `diagKillSwitchState` / `diagBudgetLedger` / `diagSecretInventory`. PROPOSED runtime API surface: `getConfigValue_(key, default)` / `getSecret_(key)` / `getLimit_(stage_key)` / `getBudgetState_(budget_id)` / `isKillSwitchActive_(scope)` / `isFeatureEnabled_(flag_id)` / `configSnapshot_()`. 7 sample scenarios (Gmail reputation incident → KILL_SWITCH_EMAIL_ALL safe-stop; Serper quota 100% → hard_stop; transient Gmail 500 → CS3 retry → NOT counted toward BUDGET; feature flag rollout pilot → rollout snapshot; DRY_RUN migration; secret rotation dual-read; config drift detection). 12-row anti-pattern table. 13-row handoff tabulka (CS2/C-04..C-10/BX1/A-04/A-06/future runtime/future UI). **Dependency narrowing `C-02, C-06, C-10` → `CS2, C-06, C-10`**: puvodni task brief uvadel `C-02`, ale v repo source-of-truth `C-02.md` NEEXISTUJE (`C2.md` je governance hardening — unrelated; precedent C-10 dependency narrowing). CS2 alignment: step entry hook (isKillSwitchActive_ check + checkLimit_ + checkBudget_), `cs2_run_id` je klic pro per-run budget accounting v `_asw_budget_ledger`, PROPOSED `config_snapshot_capture` + `config_snapshot_verify` nove CS2 steps; zadna mutace CS2 step taxonomy ani event model. C-06 alignment: `EMAIL_PROVIDER` Script Property je kanonicka vzorovy CONFIG entry pro provider selection (gmail / sendgrid / mailgun); `NormalizedSendErrorClass` je zdroj rozhodovani pro budget accounting (transient fails NE accounted, permanent fails + success accounted); provider-level kill switch `KILL_SWITCH_GMAIL` prestupne pres C-06 sender interface bez mutace `EmailSender` / `SendRequest` / `NormalizedSendResponse` kontraktu. C-10 alignment: 9 C-11 event types feed do `_asw_perf_reports` operational block; `data_completeness_flags_json` zaznamena degradaci zpusobenou C-11 guardraily (napr. `kill_switch_email_all_active=true` → `delivery_yield=null` + `c11_kill_switch_degraded=true` flag); C-10 5 event types + C-11 9 event types sdileji jednu observability vrstvu (`_asw_logs`) bez kolize. Navazuje na existujici infra (`EnvConfig.gs` + `envGuard_` + Script Properties pattern + doPost timing-insensitive compare v BX1) — nepisu od nuly, rozsiruji tak aby dostala strukturu. Scope: pouze SPEC, zadny runtime `ConfigManager` / `getConfigValue_()` / `isKillSwitchActive_()` / `checkBudget_()` helper / `_asw_budget_ledger` sheet creation / apps-script/Config.gs zapisy / PROPOSED Script Properties runtime setup / frontend UI pro kill-switch toggling / feature flag SDK integrace / secret rotation automation. Vsechny nove artefakty (4 CONFIG + 9 LIMIT + 7 BUDGET + 14 KILL_SWITCH + 10 FEATURE + 2 SECRET Script Properties = 46 celkem, `_asw_budget_ledger` 12-field sheet, 9 `_asw_logs` event types, 4 diagnostic tools, runtime API surface s 7 helper functions, DRY_RUN → KILL_SWITCH_ALL_WRITES migrace plan, EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC migrace plan, 5-scope kill switch taxonomy, safe-stop semantics, 5 budget kategorii, 5 rollout stages, 5 state safety rules, 12-row anti-pattern table, 13-row handoff tabulka, 6-plane ortogonalni taxonomie + hard-separation invariant, 12 zakazanych kolapsu, configSnapshot_ pattern) jsou PROPOSED FOR C-11 a budou materializovany implementacnim taskem.
- **Automation performance report (C-10):** Autoritativni specifikace reportovaci vrstvy nad celou automatizaci — viz `docs/24-automation-workflows.md`. Definuje 5 reporting grains (G1 per-job / G2 per-day / G3 per-run / G4 per-stage / G5 per-segment) s invariants "jedna zprava = jeden grain" a "grains jsou ortogonalni ne nested". **3-dimension reporting model (hard separace taxonomie):** **Dim A — Funnel** (10 canonical progression stages F1-F10: F1 raw_scraped → F2 normalized → F3 deduped_imported → F4 web_checked → F5 qualified → F6 brief_ready → F7 preview_generating → F8 preview_ready_for_review → F9 preview_approved → F10 outreach_ready) — cista monotonic progrese pres CS1 18-state lifecycle az k `OUTREACH_READY` (CS1 #12); **Dim B — Queue** (5 operational statusu QUEUED/SENDING/SENT/FAILED/CANCELLED, ortogonalni dimenze nad queue radkem per C-05 invariant, NEJSOU lifecycle states ani funnel stages); **Dim C — Outcome** (4 terminalni outcomy DISQUALIFIED/REPLIED/BOUNCED/UNSUBSCRIBED mapovane na CS1 terminaly #6/#15/#16/#17 resp. C-07 event_type; NEJSOU funnel stages — jsou end-of-lifecycle metrics). Tvrde oddeleni invariantem: funnel stage ≠ lifecycle state ≠ queue status ≠ outcome ≠ review flag ≠ alert state (review flags REVIEW_REQUIRED/PREVIEW_READY_FOR_REVIEW/FAILED jsou separatni dimenze; alert states jsou report-level interpretace; ZADNE mixing — napr. "email_queued funnel stage" neexistuje, je to queue_queued_count v Bloku B). Funnel metrics (dim A): 10 stage counts + 9 funnel-internal conv rates F_n→F_{n+1} pro n=1..9 + `funnel_yield_to_outreach_ready` (F10/F1) + drop-off counts. Queue metrics (dim B): queue_queued/sending/sent/failed/cancelled_count + queue_status_breakdown_json + send_success_rate (dim B interna: sent / (sent + failed)) + queue_latency_avg_ms. Outcome metrics (dim C): outcome_disqualified/replied/bounced/unsubscribed_count + reply_class/bounce_class breakdowns. Cross-dimension rates (Blok D): send_yield (A→B: queue_sent / f10_outreach_ready), reply_yield / bounce_yield / unsubscribe_yield (B→C: outcome_* / queue_sent_count), delivery_yield (A+B end-to-end: (sent − bounced) / f10_outreach_ready). Review load (Blok E, ortogonalni): blocked_by_sendability_count + review_queue_load + manual_review_entered_count + sequence_followup_reach_count. Operational metrics: processing_time (avg/p50/p95/max per step), fail_rate, retry_count + retry_success_rate, dead_letter_count + dead_letter_rate, review_queue_load, review_sla_compliance_rate per tier, stale_pending_count, queue_latency_avg_ms, exception_rate per 1000 leadu. Quality metrics: bounce_rate, hard_bounce_rate, soft_bounce_rate, reply_rate, positive_reply_rate, unclear_reply_rate, unsubscribe_rate, followup_yield_rate, preview_approval_rate, exception_rate_per_stage, compliance_hard_stop_rate. Metric definition contract template (15 poli: metric_id/name/grain/unit/formula/source_fields[]/null_handling/min_sample_size/value_range/baseline_ref/interpretation/alert_enabled/warning_threshold/critical_threshold/threshold_type) + 2 plne priklady. Alert threshold model: WARNING/CRITICAL severity × ABS/REL/COMBO threshold typy + min_sample invariant (pod min_sample → `summary_status=INCOMPLETE` misto alert) + baseline reference pattern (previous_day / previous_week / rolling_7d / rolling_30d / absolute_number). Bottleneck detection 3-lens algoritmus: funnel lens (stage s conv_rate < baseline - threshold, weight by drop-off count) + latency lens (step s p95_ms > baseline * multiplier, weight by throughput) + review lens (priority tier s SLA compliance < threshold, weight by open count); priority tiebreaker funnel > latency > review. PROPOSED `_asw_perf_reports` sheet schema (67 fields append-only) v blokove strukture podle dimenze: metadata (8) + Blok A funnel/dim A (14: F1-F10 counts + 9 conv rates + funnel_yield + 3 drop-off) + Blok B queue/dim B (8: 5 statusy + breakdown_json + send_success_rate + queue_latency) + Blok C outcome/dim C (4: disqualified/replied/bounced/unsubscribed counts) + Blok D cross-dim rates (5: send_yield / reply_yield / bounce_yield / unsubscribe_yield / delivery_yield) + Blok E review load (4: blocked/review_queue/manual_review_entered/sequence_followup_reach) + operational (9) + quality aliases (8) + synthetic summary (7). Per-field VERIFIED/INFERRED/PROPOSED label. `perf_report_id` format `perf-{grain}-{unit_id}-{ts14}-{uuid8}`. Auditability: 5 PROPOSED `_asw_logs` event types (`performance_report_started` / `performance_report_generated` / `performance_report_failed` / `performance_alert_threshold_crossed` / `performance_bottleneck_detected`) + cross-ref graph (`_ingest_reports` A-09 ↔ `_asw_perf_reports` C-10 ↔ LEADS ↔ queue ↔ inbound_events ↔ exceptions ↔ dead_letters ↔ logs). Comparison rules per grain: G1 job-vs-job same portal/segment, G2 day-vs-day weekday-matching (DOW grouping), G3 run-vs-run same step, G4 stage-vs-stage rolling, G5 segment-vs-segment within day. Outlier handling >3 stddev excluded. Known limitations: `data_completeness_flags_json` pattern (pokud C-05/C-06/C-07/C-09 nejsou runtime, null values + `summary_status=INCOMPLETE` + per-layer audit trail — graceful degradation). **Dependency narrowing `A-09, C-01 az C-09` → `A-09, CS1, CS2, CS3, C-04..C-09`:** puvodni task brief uvadel `C-01..C-09`, ale v repo source-of-truth `C-01.md`/`C-02.md`/`C-03.md` NEEXISTUJI (`C2.md`/`C3.md`/`C4.md` jsou governance hardening — unrelated). Dependency narrowing na 10 upstream metric-source tasku (A-09 + CS1 + CS2 + CS3 + C-04..C-09) je explicitne dokumentovano. A-09 alignment: C-10 je **rozsireny** report pokryvajici cely funnel (A-09 zustava ingest-only per-job); report pattern (summary_status, append-only, JSON payload do `_asw_logs`, bottleneck detection) dedden z A-09 a rozsiren. CS1 alignment: CS1 18-state lifecycle je zdroj funnel dim A F2-F10; terminal states #6/#15/#16/#17 jsou zdroj outcome dim C (disqualified/replied/bounced/unsubscribed) — outcome je SEPARATNI dimenze, NE funnel stage; zadny novy canonical state. CS2 alignment: `cs2_run_id` klic pro G3 per-run; PROPOSED novy CS2 step `performance_report_generator` (post-run hook). CS3 alignment: `_asw_dead_letters` + `failure_class` enum zdroj operational metrics; LockService pattern dedden. C-04 alignment: gate outcomes zdroj manual_review_required_count + compliance_hard_stop_rate + blocking_reason_breakdown. C-05 alignment: `_asw_outbound_queue.status` je zdroj pro celou queue dimenzi B (5 statusu); NE funnel stages. C-06 alignment: `NormalizedSendResponse` + `NormalizedSendErrorClass` zdroj provider_status + send_latency. C-07 alignment: `_asw_inbound_events` zdroj outcome dim C (replied/bounced/unsubscribed events) + reply_class/bounce_class breakdowns; NE funnel stages. C-08 alignment: 3-stage sekvence zdroj followup_yield_rate + stop_reason breakdown. C-09 alignment: `_asw_exceptions` zdroj review_queue_load + SLA compliance + exception_rate + per-type breakdown. Future dashboard / alerting / BI export tasks konzumuji stable `_asw_perf_reports` schema jako read-only data source. Scope: pouze SPEC, zadny runtime worker / cron / scheduler / `buildPerformanceReport_()` helper / sheet creation / Config.gs zapisy / frontend dashboard UI / alerting integration / BI export / `apps-script/PerfReport.gs` kod. Vsechny nove artefakty (67-field `_asw_perf_reports` sheet, `grain` enum 5 values, `summary_status` enum 5 values, funnel stage enum 10 values F1-F10 dim A, queue status enum 5 values QUEUED/SENDING/SENT/FAILED/CANCELLED dim B, outcome type enum 4 values DISQUALIFIED/REPLIED/BOUNCED/UNSUBSCRIBED dim C, alert threshold contract, bottleneck 3-lens algoritmus, `alert_summary_json` + `bottleneck_summary_json` + `data_completeness_flags_json` structures, `comparison_baseline_ref` lookup, 5 `_asw_logs` event types, 6 Script Properties `PERF_REPORT_*`, threshold defaults, future runtime worker kontrakt) jsou PROPOSED FOR C-10 a budou materializovany implementacnim taskem.

## Co je rozpracovane

- Webhook pipeline pro preview weby — kod pripraveny, ENABLE_WEBHOOK=false, zadna cilova sluzba
- Google Auth Phase 1 — kod na feature branch task/B3-auth-phase1, ceka na .env.local a merge
- Email sending pres ESP (Phase 2) — architektura navrzena, implementace 0%
