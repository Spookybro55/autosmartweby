# Task Record: C-04

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | C-04 |
| **Title** | Sendability Gate pravidla — autoritativni SPEC gate mezi preview a outreach |
| **Owner** | Claude |
| **Status** | done |
| **Date** | 2026-04-21 |
| **Stream** | C |
| **Dependencies** | C-01 (lifecycle state machine, existuje jako CS1 v `docs/21-business-process.md`); CS2 orchestrator (`docs/24-automation-workflows.md`) |

## Scope

Formalizuje rozhodovaci logiku, ktera stoji mezi fazi "preview hotove" a fazi "outreach queued". Definuje jedine autoritativni pravidlo pro kazdy lead, zda smi jit do auto-send, zda potrebuje manualni review, nebo zda je blokovan. Scope je **SPEC-only** — zadny runtime sender, queue, UI, webhook ani observability pipeline se v tomto tasku neimplementuje.

Task dodava:
- 3 **gate outcomes** (ne lifecycle states): `AUTO_SEND_ALLOWED` / `MANUAL_REVIEW_REQUIRED` / `SEND_BLOCKED`
- 19 hard conditions (H1–H19) pro pripusteni auto-sendu
- 21 blocking reasons (B1–B21) se stabilnimi reason codes, **rozclenenymi do 4 kategorii** (canonical-lifecycle / compliance / outbound-signal / data-deficit)
- 3 review reasons (R1–R3)
- 8-ORDER precedence rules (terminal > compliance > already-sent > qualifier > identity > content > review > allow)
- Deterministicky pseudocode evaluatoru (lookup-only, zadny side-effect)
- 5 sample leadu (2x AUTO_SEND + 2x BLOCK + 1x REVIEW) pro acceptance
- Observability contract (reason codes, log schema) a boundary rules proti double-send
- Handoff do C-05 (outbound queue), C-06 (ESP abstrakce), C-08/C-09 (rate limit / suppression)

**CS1 konzistence (2026-04-21 fix round):**
- `TERMINAL_STATE_*` block reasons (B2–B5) pokryvaji **pouze** CS1 canonical terminals: `DISQUALIFIED`, `REPLIED`, `BOUNCED`, `UNSUBSCRIBED`.
- `WON` a `LOST` jsou downstream sales outcomes (hodnoty auxiliary pole `outreach_stage`), **NE** canonical lifecycle states. CS1 sekce 10.4 je derivuje na `effective_lifecycle_state = REPLIED` → B3.
- `DEAD` neni canonical lifecycle state, neni to aux hodnota a neni to gate outcome. V C-04 spec se nepouziva.
- Sample leady pouzivaji pouze CS1 canonical states.

Task NEDODAVA:
- Runtime sender, ESP provider, queue, retry stroj, outbound rate limiter
- UI pro review frontu ani Sheets sloupec `sendability_outcome` jako editable pole
- Apps Script zmeny, webhooky, cron scheduler
- Frontend zmeny (read-only preview renderer zustava)
- Zadne PROPOSED pole se **nezapisuji** do LEADS v ramci C-04 — navrh je pouze v SPEC sekci "Implementation notes" jako podklad pro C-05/C-06

## Code Changes

Zadne. Task je pure specifikace.

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| — | — | Zadne code changes |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/24-automation-workflows.md | modified | Primarni spec location — cela sekce "Sendability Gate — C-04" (15 podsekci, vcetne pseudocode evaluatoru, 21 blocking reasons, 3 review reasons, 5 sample leadu, VERIFIED / INFERRED / PROPOSED labely). Fix round 2026-04-21: sekce 2 prejmenovana na "Gate outcomes (authoritative set)", pridana subsekce 2.1 "Terminologicka vrstvena mapa (3 layers)" oddelujici CS1 canonical lifecycle / gate outcomes / auxiliary downstream; outreach_stage input row flaguje WON/LOST jako downstream; block reasons dostaly column "Kategorie" (canonical-lifecycle / compliance / outbound-signal / data-deficit); invariant 4 explicitne vylucuje `TERMINAL_STATE_WON`/`LOST`/`DEAD`; Sample 4 hlavicka a prose zpresneny na "CS1 canonical terminal `BOUNCED`"; pridana "CS1 konformita samplu" poznamka. |
| docs/21-business-process.md | modified | Cross-reference odstavec v CS1 lifecycle sekci — prechod `PREVIEW_APPROVED / OUTREACH_READY → EMAIL_QUEUED` je gated C-04 evaluatorem; CS1 pouze definuje lifecycle stavy jako vstup |
| docs/20-current-state.md | modified | Zapis do sekce "Specifikace" — odkaz na C-04 spec v docs/24 s vyctem outcomes, blocking/review reason countu a poznamkou "scope: pouze SPEC". Fix round 2026-04-21: terminology update — "canonical outcomes" → "gate outcomes"; pridana CS1 konzistencni veta o canonical terminals vs downstream WON/LOST. |

## Contracts Changed

**SPEC level — ano, runtime level — ne.**

- Noveho **sendability evaluatoru** kontrakt je formalizovan v docs/24 (inputs, outputs, pseudocode, precedence, reason codes). Evaluator je deterministicka pure-function specifikace; implementace je handoff do C-05.
- PROPOSED sloupce (`sendability_outcome`, `sendability_primary_reason`, `sendability_evaluated_at`, `unsubscribed`, `suppressed`) jsou v SPEC explicitne oznaceny jako **PROPOSED FOR C-04** — do `docs/23-data-model.md` a do LEADS headers se zapisuji az v C-05 (nebo nasledujicim tasku, ktery bude implementovat evaluator a persistenci outcome).
- Zadny existujici datovy kontrakt (A-01 job input, A-02 raw import, A-03 normalization, B-01 brief) neni tuto SPEC dotcen.

## Tests

| Test | Vysledek |
|------|----------|
| check-doc-sync | OK |
| tsc --noEmit | N/A (doc-only task) |
| npm run build | N/A (doc-only task) |
| 5 sample leadu rucne projde evaluatorem | OK (2x AUTO_SEND, 2x BLOCK, 1x REVIEW — presna sekce "Sample evaluations" v docs/24) |
| Precedence 8-ORDER projde rucnim trace na 5 sample leadech | OK |

## Output for Audit

- **Canonical outcomes:** 3 (AUTO_SEND_ALLOWED, MANUAL_REVIEW_REQUIRED, SEND_BLOCKED)
- **Hard conditions:** 19 (H1–H19)
- **Blocking reasons:** 21 (B1–B21) — stable reason codes
- **Review reasons:** 3 (R1–R3) — stable reason codes
- **Precedence rules:** 8-ORDER (terminal > compliance > already-sent > qualifier > identity > content > review > allow)
- **Deterministic evaluator:** pseudocode v docs/24 sekce "Evaluator algorithm (deterministic)"
- **Sample leadu:** 5 (`SAMPLE-01` AUTO_SEND, `SAMPLE-02` AUTO_SEND, `SAMPLE-03` BLOCK:B1 (`INVALID_STATE`), `SAMPLE-04` BLOCK:B4 (`TERMINAL_STATE_BOUNCED`), `SAMPLE-05` REVIEW:R1 (`PREVIEW_NEEDS_REVIEW`))
- **VERIFIED IN REPO labely:** existujici pole `email`, `email_valid`, `lead_stage`, `preview_stage`, `outreach_stage`, `email_sync_status`, `email_reply_type`, `first_sent_at`, `company_key` — viz existujici LEADS EXTENSION_COLUMNS v `apps-script/Config.gs`
- **INFERRED FROM EXISTING SYSTEM labely:** double-send window (reusovan z `OUTBOUND_DOUBLE_SEND_MINUTES = 5min` v `apps-script/OutboundEmail.gs`), identity guard (reusovan z `verifyOutboundIdentity_` logiky), email regex (z `apps-script/Normalizer.gs` line 137)
- **PROPOSED FOR C-04 labely:** `sendability_outcome`, `sendability_primary_reason`, `sendability_evaluated_at`, `unsubscribed`, `suppressed`, `manual_review_reason`

## Known Limits

- SPEC neimplementuje evaluator v kodu — pouze definuje kontrakt. Implementace je handoff do C-05.
- PROPOSED sloupce nejsou v ramci C-04 pridany do LEADS — C-05 musi rozhodnout o persistenci (Apps Script write-back vs. recompute per-run).
- Rate limit a quiet hours nejsou soucasti C-04 core gate — jsou handoff do C-08 (rate limiting) a C-09 (suppression list + scheduling window).
- Suppression list struktura (dedicated sheet vs. boolean kolona v LEADS) je nerozhodnuta — SPEC pouze zavadi PROPOSED `suppressed` flag.
- Evaluator nepocita s multi-recipient scenarem (per-contact uvnitr jednoho leadu) — SPEC explicitne pracuje s "one lead = one primary email" modelem, ktery odpovida soucasnemu stavu LEADS.
- Gate pokryva pouze **prvni odchozi email** v outbound sekvenci. Follow-up logika (2., 3. zprava v thread) neni C-04 scope — bude resena v C-07 (follow-up cadence).

## Next Dependency

- **C-05 Outbound queue + evaluator runtime** — implementuje SENDABILITY_* enums v `apps-script/Config.gs`, persistuje `sendability_outcome` do LEADS, introduces `_asw_outbound_queue` sheet dle CS3 contractu
- **C-06 Provider abstraction (ESP)** — konzumuje outcome `AUTO_SEND_ALLOWED` jako gate pro ESP send
- **C-07 Follow-up cadence** — rozsiruje gate o follow-up specific rules (reply-based cancel, cadence window)
- **C-08 Rate limiting & quiet hours** — extends gate o casove pravidla nad evaluator outcome
- **C-09 Suppression list management** — implementuje PROPOSED `suppressed` flag a dedicated `_asw_suppressions` sheet

Do té doby zustava gate SPEC-only. Existujici Apps Script pipeline bezi beze zmeny v `DRY_RUN=true` rezimu; `OutboundEmail.gs` pouziva soucasne ad-hoc guardy (identity, double-send 5min, valid email, non-empty subject/body), ktere C-04 SPEC formalizuje jako H-podminky a B-reasony.
