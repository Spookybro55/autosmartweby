# Task Record: C-08

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | C-08 |
| **Title** | Follow-up engine — SPEC-only sekvence / časování / stop podmínky pro follow-up automation |
| **Owner** | Claude |
| **Status** | done |
| **Date** | 2026-04-21 |
| **Stream** | C |
| **Dependencies** | C-07 (inbound event ingest definuje REPLY / BOUNCE / UNSUBSCRIBE / UNKNOWN_INBOUND signal + `reply_needs_manual` review flag + 3-tier stop model, bez kterého engine neví, kdy NEposilat); C-06 (`NormalizedSendResponse.sent_at` je zdroj pro timing offset computation, `SendRequest.thread_hint` je forward-compat pole, které C-08 populuje pro follow_up_1/2); C-05 (`_asw_outbound_queue` je cilovy sheet pro follow-up queue rows, idempotency key pattern sdili s C-08 pres rozsireni stage scope); C-04 (re-call sendability gate s `is_followup=true` context pred kazdym follow-up insert, PROPOSED bypass B16 `ALREADY_SENT`); CS1 (zadne nove canonical states — reuse `EMAIL_SENT` / `REPLIED` / `BOUNCED` / `UNSUBSCRIBED` / `DISQUALIFIED` terminals + T20/T21/T22); CS2 (engine je novy CS2 step `followup_engine_run`, batch daily orchestrator); CS3 (lock pattern, failure_class, dead-letter pro engine run idempotency) |

## Scope

Formalizuje follow-up engine — logickou vrstvu mezi C-05 queue a budoucim scheduler/worker runtime — definuje **3-stage sekvenci** (`initial` → `follow_up_1` → `follow_up_2`), maximum **2 follow-upy** po initial (total 3 queue rows per sekvence), **T+3 / T+7 business days** rozestupy pocitane z actual `sent_at` predchozi stage, **quiet hours 09:00–17:00 Europe/Prague**, **5 stop condition kategorii** (REPLY / UNSUBSCRIBE / BOUNCE / MANUAL_BLOCK / REVIEW_FLAG|UNKNOWN_INBOUND) + **5 composite stop invariants** (vcetne C-04 gate re-check s `is_followup=true` + C-07 inbound event store check + CS1 terminal lookup), **triple-redundant reply guard** (CS1 REPLIED + C-04 B3 + inbound event store), **3 decision outcomes** (AUTO_INSERT / REVIEW_REQUIRED / STOP), **pregenerate-vs-regenerate** tabulku (IMMUTABLE recipient/sender_identity/preview_url/personalization vs REGENERATED subject/body/CTA vs STAGE-DERIVED thread_hint/scheduled_at/priority/idempotency_key), idempotency pattern `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}`, manual block model diferencovany od UNSUBSCRIBE, **5-vrstvovou tvrdou separaci** (stage / lifecycle / queue status / inbound event / review flag) s 5 zakazanymi kolapsi, sample queue rows pro vsechny 3 stage s field-level INHERITED/REGENERATED/STAGE-DERIVED/IMMUTABLE markers, **5 sample lead timelines** (silent happy path / reply → stop / bounce → stop / unsubscribe → stop / unknown-inbound → review → manual_stop), auditability approach (`_asw_logs` 8 event types + cross-ref graph + observability bez B6 UI), handoff tabulku na C-04/C-05/C-06/C-07/CS1/CS2/CS3/scheduler/B6/copy-gen/C-09/implementation task (12 radku).

Scope je **SPEC-only** — neimplementuje scheduler runtime, cron trigger, queue worker claim loop, mailbox sync runtime, ESP webhook HTTP handler, frontend UI (B6), text-generation engine, holiday calendar detail ani zapisy do `apps-script/Config.gs`. Vsechny nove artefakty jsou oznacene **PROPOSED FOR C-08** a budou materializovany implementacnim taskem. B6 (operator reply-handling UI) **NENI blocker** pro C-08 SPEC — operator muze `followup_manual_block` flag nastavit rucne v Google Sheets bunce.

Task dodava:
- 3-stage follow-up sequence definition (`initial` / `follow_up_1` / `follow_up_2`) s per-stage tabulkou (purpose, timing, input, output, queue row, thread hint)
- Max follow-up count invariant (max=2, initial nepocita, total 3 rows per sekvence) + 4 enforcement mechanisms (sequence counter check / idempotency key stage-aware / stage progression deterministic / lookup na poslední SENT row v sekvenci)
- Timing rules: T+3 business days od `initial.sent_at` → `follow_up_1.scheduled_at`; T+7 business days od `follow_up_1.sent_at` → `follow_up_2.scheduled_at`; vzdy od **actual** `sent_at` (ne scheduled/queued/created); business days = Po-Pá, stat. svatky mimo v1.0; quiet hours 09:00–17:00 Europe/Prague deterministic posun
- 5 stop condition kategorii s tabulkou (co aktivuje / scope / zastavi jen automatiku nebo i manual / z ktereho tasku pochazi)
- 5 composite stop invariants (CS1 terminal lookup / C-04 gate re-check s `is_followup=true` / C-07 inbound event store check / manual block flag / OOO review hold s PROPOSED 14 dni pauze)
- **Triple-redundant reply guard** (garantuje acceptance criteria #4 "lead s reply uz nikdy nedostane follow-up") pres (a) CS1 state check, (b) C-04 gate re-check B3, (c) inbound event store check
- Pregenerate vs regenerate rules per field (14-radkova tabulka s Initial / follow_up_1 / follow_up_2 / pravidlo sloupci + 3 klicove invariants)
- 3 decision outcomes (AUTO_INSERT / REVIEW_REQUIRED / STOP) s explicit podminkami pro kazdy + role `reply_needs_manual` + role `unknown_inbound` + dalsi review guardy
- **5-vrstva tvrda separace** (1. Follow-up stage / 2. Lifecycle state / 3. Queue status / 4. Inbound event / 5. Review flag) s tabulkou "co to je / kde zije / hodnoty / kdo meni" + 5 zakazanych kolapsi + engine konzumacni mapa
- Sample queue rows (initial, follow_up_1, follow_up_2) s field-level rozlisenim INHERITED / REGENERATED / STAGE-DERIVED / STAGE-OVERRIDE / IMMUTABLE
- **5 sample lead timelines:**
  - (1) silent happy path (initial → follow_up_1 → follow_up_2 bez reakce → SEQUENCE_COMPLETE)
  - (2) reply → stop (initial → reply → CS1 REPLIED → `followup_skip` stop_reason=REPLY)
  - (3) bounce → stop (initial → hard bounce DSN 5.1.1 → CS1 BOUNCED → Tier 2 address stop, PROPOSED C-04 `ADDRESS_BOUNCED`)
  - (4) unsubscribe → stop (initial → follow_up_1 → reply "unsubscribe" → CS1 UNSUBSCRIBED → Tier 3 lead stop, `followup_skip` stop_reason=UNSUBSCRIBE)
  - (5) unknown-inbound → review → manual_stop (initial → reply s UNCLASSIFIED class → `reply_needs_manual=TRUE` → `followup_review_required` → operator rozhodne stop_followup → `followup_manual_stop`)
- Auditability: `_asw_logs` events pro C-08 (8 PROPOSED event types: `followup_insert`, `followup_skip`, `followup_review_required`, `followup_pause_ooo`, `followup_manual_stop`, `followup_stale_review_abandoned`, `followup_engine_run_summary`, `followup_auto_block_soft_bounce`), cross-ref graph (LEADS ↔ queue ↔ inbound events ↔ logs ↔ dead_letters), observability bez B6 UI (query patterns)
- Idempotency pattern `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}` + 4 invariants (unique per stage×sekvence×lead, safe-to-run-twice, race-safe, ne-rollback-safe) + design rationale (proc ne obsahovy hash pro follow-upy)
- Manual block model: `followup_manual_block` boolean flag na LEADS + kdo nastavi (operator manual / B6 budouci / engine auto-set edge case) + co presne zastavi (jen automatiku, ne existing QUEUED rows bez C-05 CANCELLED) + reversibilita + diferenciace od UNSUBSCRIBE (6-dimension table: zdroj / scope / CS1 state change / reversible / storage / compliance)
- Handoff tabulka (12 radku) na C-04/C-05/C-06/C-07/CS1/CS2/CS3/scheduler/B6/copy-gen/C-09/implementation task s per-row popisem "jak C-08 konzumuje" + "jak C-08 prispiva"
- Non-goals explicit (14 polozek)
- Acceptance checklist (16 polozek) + PROPOSED/INFERRED/VERIFIED label summary (sekce 18)

**CS1 kompatibilita:**
- C-08 NIKDY neemituje novy canonical CS1 state. Pouziva existujici terminaly REPLIED (#15), BOUNCED (#16), UNSUBSCRIBED (#17), DISQUALIFIED + T20/T21/T22 transitions z `docs/21-business-process.md`.
- Engine **cte** lifecycle state pres LEADS (pro terminal check) ale **NIKDY nezapisuje** do `lifecycle_state` — to dela C-05 queue worker (T17, T18) + C-07 ingest (T20, T21, T22) + operator.
- Triple-redundant reply guard: (a) CS1 terminal check `lifecycle_state IN terminal_states`; (b) C-04 gate re-check B3 `TERMINAL_STATE_REPLIED`; (c) C-07 inbound event store `event_type=REPLY` existence check.

**CS2 kompatibilita:**
- Engine je novy CS2 step: `followup_engine_run` — batch daily orchestrator (typically 23:00 Europe/Prague cron).
- Jeden run eviduje `run_id` → `_asw_logs` event `followup_engine_run_summary` s `run_id`, `leads_evaluated`, `inserts`, `skips`, `reviews`, `pauses`, `duration_ms`.
- Respektuje CS2 event-driven pattern: engine se spusti periodicky + reactivly (po inbound event klidne hned re-vyhodnoti dotcene leady — PROPOSED optimalizace, v1.0 postaci daily batch).

**CS3 kompatibilita:**
- Engine run respektuje CS3 `LockService.getScriptLock()` pattern (stejne jako C-05 worker + C-07 ingest).
- `failure_class` mapping pro engine errors: `ENGINE_TIMEOUT` → TRANSIENT, `C04_GATE_FAIL` → PERMANENT (lead-level, operator musi resolve), `PARENT_NOT_SENT` → TRANSIENT (wait pro SENT), `MAX_RETRIES_EXCEEDED` → PERMANENT.
- Dead-letter pattern: pokud engine run failne pri inserting konkretni follow-up, row se presune do `_asw_dead_letters` s diagnostikou (retry = novy engine run, idempotency klic garantuje ze nedojde k duplicitnimu insertu).
- Idempotency key `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}` respektuje CS3 S1-S12 deterministic-first-then-hash pattern (stage scope = deterministic, content hash se nepouziva — viz design rationale).

**C-04 kompatibilita:**
- Engine **re-vola** C-04 sendability gate pred kazdym follow-up insert. Predava `is_followup=true` context → C-04 bypasses B16 `ALREADY_SENT` (initial uz byl odeslan, to je expected).
- Gate vrati `AUTO_SEND_ALLOWED` / `MANUAL_REVIEW_REQUIRED` / `SEND_BLOCKED` — engine route na AUTO_INSERT / REVIEW_REQUIRED / STOP.
- Gate zohlednuje existing B3/B4/B5 (CS1 terminal), B7 (UNSUBSCRIBED), B8 (SUPPRESSED), PROPOSED C-07 `ADDRESS_BOUNCED`.
- PROPOSED C-04 extension: `is_followup` context parameter — do materializace pouziva operator interim "initial je odeslan = nevytvaret follow-up" pres C-04 fallback logic (degraded, vyzaduje manual handling).

**C-05 kompatibilita:**
- Engine je **C-05 producer** — vytvari nove queue rows stejne jako C-04 pro initial. Respektuje C-05 insert contract (sekce 5 docs/24).
- PROPOSED C-05 schema extensions: `sequence_stage` (string enum), `parent_queue_id` (string nullable), `sequence_root_queue_id` (string), `sequence_position` (integer 1-indexed), `created_from_followup_engine_run` (string audit ref).
- Do materializace PROPOSED fields engine pracuje s 3 samostatnymi queue rows per sekvence (initial + follow_up_1 + follow_up_2) bez explicitniho stage metadata — funkcni, ale ztraci audit granularitu.
- Engine **nezapisuje** do existing queue row (parent row je immutable po SENT per C-05). Kazda stage = novy row.
- Idempotency: `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}` respektuje C-05 unique-per-idempotency_key invariant.

**C-06 kompatibilita:**
- Engine populuje `SendRequest.thread_hint` (C-06 merged PR #29 forward-compat pole):
  - initial: `thread_hint = null`
  - follow_up_1: `thread_hint = {thread_id: initial.provider_thread_id, in_reply_to_message_id: initial.provider_message_id}`
  - follow_up_2: `thread_hint = {thread_id: initial.provider_thread_id, in_reply_to_message_id: follow_up_1.provider_message_id}`
- Engine **nezapisuje** do C-06 sender interface ani `NormalizedSendResponse`. Jen **cte** `provider_message_id` + `provider_thread_id` + `sent_at` z C-06 response (propsany do queue row pri SENT transition per C-05).
- Gmail adapter pouzije `GmailApp.getThreadById(thread_id).reply(body)` nebo ESP adapter prevede na `In-Reply-To` + `References` headers — to je C-06 implementacni task, ne C-08.

**C-07 kompatibilita:**
- Engine **cte** `_asw_inbound_events` pro stop detection. Respektuje C-07 3-tier stop model:
  - Tier 1 (REPLY, UNKNOWN_INBOUND): sekvence stop (per thread / sekvence)
  - Tier 2 (BOUNCE): sekvence stop + Tier 2 address stop siri na dalsi sekvence na tu adresu (pres C-04 PROPOSED `ADDRESS_BOUNCED`)
  - Tier 3 (UNSUBSCRIBE, COMPLAINT): celkovy lead stop napric kanaly (pres C-04 B7 + B8)
- OOO (`reply_class=OOO`) = **pauze** (ne stop) — engine odlozi next stage o PROPOSED 14 kalendarnich dni, pak re-vyhodnoti.
- `unknown_inbound` → `reply_needs_manual=TRUE` → REVIEW_REQUIRED. Engine NIKDY auto-inserts pri review flag.
- Engine **nezapisuje** do `_asw_inbound_events` (append-only, jen C-07 ingest).

**B6 vztah:**
- B6 (operator reply-handling UI) **NENI blocker** pro C-08 SPEC. Operator muze v interim manage follow-up manual block + review resolution primo v Google Sheets bunce.
- B6 (budouci task) agreguje do per-lead view: queue sekvence rows, `_asw_logs` follow-up events, LEADS flagy (`reply_needs_manual`, `followup_manual_block`, `followup_review_required`, `unsubscribed`), pause/stop buttons.
- PROPOSED operator actions pro B6 (mimo C-08 scope): `resolve_review → continue_followup`, `resolve_review → stop_followup`, `set_manual_block`, `clear_manual_block`.

Task NEDODAVA:
- Runtime scheduler / cron trigger / daily batch job v Apps Script
- Queue worker claim loop (C-05 implementacni task)
- Mailbox sync runtime (C-07 implementacni task)
- ESP webhook HTTP handler (C-07 implementacni task)
- Frontend UI pro follow-up management (B6)
- Text-generation engine pro stage-specific copy (follow-up copy-gen task)
- Holiday calendar implementaci (ops config)
- Timezone-per-lead personalizaci (v1.0 single timezone)
- Multi-channel follow-up (SMS, phone, LinkedIn)
- A/B testing / experiment harness
- Zapisy do `apps-script/Config.gs` (PROPOSED enumy + Script Properties)
- Mutaci C-05 queue schema (PROPOSED extensions pouze)
- Mutaci C-06 sender interface
- Mutaci C-07 inbound event schema
- Novy canonical CS1 state

## Code Changes

Zadne. Task je pure specifikace.

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| — | — | Zadne code changes |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/24-automation-workflows.md | modified | Primarni spec location — nova sekce "Follow-up engine — C-08 (sekvence, časování, stop podmínky)" (18 podsekci) pridana za C-07 sekci. Obsahuje: ucel + ASCII architekturu diagram (C-04 gate → C-05 queue → C-06 sender → SENT → C-07 inbound events → C-08 engine decision), boundary / non-goals + B6 vztah, 3-stage sequence definition (table initial/follow_up_1/follow_up_2 s purpose/timing/input/output/queue row/thread hint), timing rules (T+3/T+7 business days od actual sent_at, quiet hours 09:00–17:00 Europe/Prague, business days skip logic), max follow-up count (max=2 + 4 enforcement mechanisms), 5 stop condition categories + 5 composite stop invariants (vcetne C-04 gate re-check s `is_followup=true` + triple-redundant reply guard), pregenerate-vs-regenerate rules (14-radkova tabulka + 3 klicove invariants), 3 decision outcomes (AUTO_INSERT/REVIEW_REQUIRED/STOP), 5-vrstvovou tvrdou separaci identity (stage/lifecycle/queue status/inbound event/review flag) + 5 zakazanych kolapsi, sample queue rows pro vsechny 3 stage s field-level INHERITED/REGENERATED/STAGE-DERIVED markers, **5 sample lead timelines** (silent / reply / bounce / unsubscribe / unknown-review), auditability (`_asw_logs` 8 event types + cross-ref graph + observability bez B6), idempotency pattern `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}` + 4 invariants + design rationale, manual block model + 6-dimension differentiation od UNSUBSCRIBE, handoff tabulka (12 radku) na C-04/C-05/C-06/C-07/CS1/CS2/CS3/scheduler/B6/copy-gen/C-09/implementacni task, non-goals (14 polozek), acceptance checklist (16 polozek), PROPOSED/INFERRED/VERIFIED label summary. |
| docs/20-current-state.md | modified | Nova polozka v sekci "Specifikace" — odkaz na C-08 spec v docs/24 s vyctem 3-stage sekvence + max count + timing rules + 5 stop conditions + 5-vrstva separace + 5 sample timelines + idempotency pattern + CS1/CS2/CS3/C-04/C-05/C-06/C-07/B6 alignment + scope disclosure. Update "Posledni aktualizace" na `2026-04-21 (C-08)`. |

## Contracts Changed

**SPEC level — ano, runtime level — ne.**

- Novy **follow-up engine** kontrakt je formalizovan v docs/24: 3-stage sequence (initial/follow_up_1/follow_up_2), max=2 follow-upy invariant, T+3/T+7 business days timing, 5 stop condition kategorii + 5 composite invariants, triple-redundant reply guard, pregenerate-vs-regenerate tabulka, 3 decision outcomes, 5-vrstva identity separace + 5 zakazanych kolapsi, idempotency pattern, manual block model s 6-dim diferenciaci od UNSUBSCRIBE.
- PROPOSED artefakty jsou v SPEC oznacene jako **PROPOSED FOR C-08**:
  - **C-05 queue schema extensions:** `sequence_stage` (enum `initial` / `follow_up_1` / `follow_up_2`), `parent_queue_id` (string nullable), `sequence_root_queue_id` (string), `sequence_position` (integer 1-indexed), `created_from_followup_engine_run` (string audit ref)
  - **LEADS schema extensions:** `followup_manual_block` (boolean), `followup_review_required` (boolean, optional), `last_operator_action` (string + timestamp, for audit)
  - **C-04 gate extension:** `is_followup` context parameter (boolean) for bypass B16 `ALREADY_SENT`
  - **`_asw_logs` event types (8):** `followup_insert`, `followup_skip`, `followup_review_required`, `followup_pause_ooo`, `followup_manual_stop`, `followup_stale_review_abandoned`, `followup_engine_run_summary`, `followup_auto_block_soft_bounce`
  - **Stop reason enum (10):** `REPLY`, `UNSUBSCRIBE`, `BOUNCE_HARD`, `BOUNCE_SOFT_ESCALATED`, `MANUAL_BLOCK`, `REVIEW_FLAG_PENDING`, `OOO_PAUSE`, `C04_GATE_BLOCK:{reason}`, `MAX_FOLLOWUPS_REACHED`, `PARENT_NOT_SENT`
  - **Decision outcome enum:** `AUTO_INSERT` / `REVIEW_REQUIRED` / `STOP`
  - **Idempotency pattern:** `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}`
  - **Engine batch run artifact:** `FU-RUN-{YYYY-MM-DD}-{NNN}` identifier + summary row v `_asw_logs`
  - **Priority defaults per stage:** initial 100, follow_up_1 90, follow_up_2 80
  - **CS3 failure_class mapping:** `ENGINE_TIMEOUT`→TRANSIENT, `C04_GATE_FAIL`→PERMANENT, `PARENT_NOT_SENT`→TRANSIENT, `MAX_RETRIES_EXCEEDED`→PERMANENT
  - **Auto-terminate thresholds:** OOO pause = 14 calendar days, review stale abandonment = 30 days, soft-bounce consecutive block = 3 bounces (aligned with C-07)
  - **Script Properties:** `FOLLOWUP_OFFSET_1_BUSINESS_DAYS=3`, `FOLLOWUP_OFFSET_2_BUSINESS_DAYS=7`, `FOLLOWUP_QUIET_HOURS_START=09:00`, `FOLLOWUP_QUIET_HOURS_END=17:00`, `FOLLOWUP_TIMEZONE=Europe/Prague`, `FOLLOWUP_MAX_PER_SEQUENCE=2`, `FOLLOWUP_OOO_PAUSE_DAYS=14`, `FOLLOWUP_REVIEW_STALE_ABANDON_DAYS=30`
  - Tyto budou materializovany implementacnim taskem v Apps Script (`runFollowupEngine()` + Config.gs zapisy + sheet schema migration).
- Zadny existujici datovy / behavioralni kontrakt (A-01 az A-10, B-01 az B-05, BX1, C-04, C-05, C-06, C-07, CS1, CS2, CS3) neni touto SPEC mutovan. C-08 jen **cte** z C-05 queue (parent row + sequence metadata) + C-06 response (`sent_at` + `provider_message_id` + `provider_thread_id`) + C-07 events (stop detection) + CS1 lifecycle state (terminal check).

## Tests

| Test | Vysledek |
|------|----------|
| check-doc-sync | OK (43 pass / 0 warn / 0 fail, 27 task records) |
| tsc --noEmit | N/A (doc-only task) |
| npm run build | N/A (doc-only task) |
| 5 sample lead timelines (silent / reply / bounce / unsubscribe / unknown-review) pokryvaji hlavni vetve sekvence | OK (docs/24 sekce 11, per-timeline full tabulka s cas / udalosti / lifecycle / queue rows / inbound events / operator view) |
| Max follow-up count = 2 invariant vynucen 4 mechanismy | OK (docs/24 sekce 5: sequence counter check / idempotency key stage-aware / stage progression deterministic / lookup na posledni SENT row) |
| Triple-redundant reply guard (acceptance criteria #4: lead s reply uz nikdy nedostane follow-up) | OK (docs/24 sekce 6 invariant explicit: CS1 terminal lookup + C-04 gate re-check B3 + C-07 inbound event store check) |
| 5-vrstva separace identity (stage / lifecycle / queue status / inbound event / review flag) bez kolapsu | OK (docs/24 sekce 9: 5-radkova tabulka + 5 zakazanych kolapsi + engine konzumacni mapa) |
| Timing rules pocitaji od **actual** `sent_at` (ne scheduled/queued/created) | OK (docs/24 sekce 4 invariant "od ceho se pocita" + 3 konkretni examples) |
| Pregenerate-vs-regenerate per-field tabulka bez ambiguity | OK (docs/24 sekce 7, 14 radku field tabulka + 3 klicove invariants — IMMUTABLE sekvencne vs REGENERATED per stage vs STAGE-DERIVED) |
| Idempotency pattern `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}` + 4 invariants (unique per stage×sekvence×lead, safe-to-run-twice, race-safe, ne-rollback-safe) | OK (docs/24 sekce 13 + design rationale proc ne obsahovy hash pro follow-upy) |
| Auditability: 8 `_asw_logs` event types + cross-ref graph + observability bez B6 | OK (docs/24 sekce 12, event tabulka + cross-ref ASCII graf + query patterns pro operator) |
| 3 decision outcomes (AUTO_INSERT / REVIEW_REQUIRED / STOP) maji explicit entry podminky bez overlapu | OK (docs/24 sekce 8, tabulka "kdy nastava" + akce per outcome + role `reply_needs_manual` + role `unknown_inbound`) |
| Manual block model diferencovan od UNSUBSCRIBE (6 dimenzi: zdroj / scope / CS1 state change / reversible / storage / compliance) | OK (docs/24 sekce 14, 6-dim differentiation table) |
| Sample queue rows (initial / follow_up_1 / follow_up_2) s field-level INHERITED/REGENERATED/STAGE-DERIVED markers | OK (docs/24 sekce 10, 3 plne rows s marker per field + klicove observations) |
| Handoff tabulka pokryva C-04/C-05/C-06/C-07/CS1/CS2/CS3/scheduler/B6/copy-gen/C-09/implementation task (12 radku) | OK (docs/24 sekce 15) |
| Stop conditions (5 primary + 5 composite invariants) + stop_reason enum pokryvaji vsechny realne stop cesty | OK (docs/24 sekce 6, tabulka + 5 invariants + 10-value enum) |
| C-05/C-06/C-07 schema NEJSOU touto SPEC mutovany | OK (manual trace — C-08 jen cte z existing fields; PROPOSED additions jsou extensions ne mutations) |
| Zadny novy canonical CS1 state | OK (docs/24 sekce 9 + acceptance checklist + non-goals) |
| B6 explicit NENI blocker | OK (docs/24 sekce 1 + 8 + 14 + acceptance checklist) |

## Output for Audit

- **Follow-up sequence:** 3 stages (`initial`, `follow_up_1`, `follow_up_2`)
- **Max follow-up count:** 2 (initial se nepocita; total max 3 queue rows per sekvence)
- **Timing offsets:** `follow_up_1` = T+3 business days od `initial.sent_at`; `follow_up_2` = T+7 business days od `follow_up_1.sent_at`
- **Business days:** Po-Pá (So-Ne skip). Stat. svatky mimo v1.0 SPEC.
- **Quiet hours:** 09:00–17:00 Europe/Prague (deterministic posun na nejbl. 09:00 business day pokud mimo).
- **Timing source-of-truth:** **actual** `sent_at` z C-06 `NormalizedSendResponse` (propsany do queue row pri SENT). NE scheduled_at, NE queued_at, NE created_at.
- **Stop conditions (5 primary):** (1) REPLY, (2) UNSUBSCRIBE, (3) BOUNCE, (4) MANUAL_BLOCK, (5) REVIEW_FLAG|UNKNOWN_INBOUND
- **Composite stop invariants (5):** (1) CS1 terminal lookup; (2) C-04 gate re-check s `is_followup=true`; (3) C-07 inbound event store check; (4) manual block flag highest-priority po CS1 terminal; (5) OOO hold pause 14 dni (ne stop)
- **Stop reason enum (10):** `REPLY`, `UNSUBSCRIBE`, `BOUNCE_HARD`, `BOUNCE_SOFT_ESCALATED`, `MANUAL_BLOCK`, `REVIEW_FLAG_PENDING`, `OOO_PAUSE`, `C04_GATE_BLOCK:{reason}`, `MAX_FOLLOWUPS_REACHED`, `PARENT_NOT_SENT`
- **Decision outcomes (3):** `AUTO_INSERT` / `REVIEW_REQUIRED` / `STOP`
- **Pregenerate-vs-regenerate (14 fields):**
  - **IMMUTABLE sekvencne (inherited):** `recipient_email`, `sender_identity`, `preview_url`, `personalization_json`, `send_channel`, `preview_url_version`
  - **REGENERATED per stage:** `subject` (v1.0: `Re: {initial.subject}`), `body`, `body.plain`, `body.html`, CTA
  - **STAGE-DERIVED:** `thread_hint`, `send_mode` (follow-upy vzdy SCHEDULED), `scheduled_at`, `priority` (100/90/80), `idempotency_key`
- **5-vrstva tvrda separace identity:**
  - (1) Follow-up stage — pozice v sekvenci, kde zije: `_asw_outbound_queue.sequence_stage` (PROPOSED), hodnoty initial/follow_up_1/follow_up_2, meni: C-08 engine
  - (2) Lifecycle state — canonical CS1 stav leadu, kde zije: `LEADS.lifecycle_state`, 18 states, meni: C-05 queue worker + C-07 ingest + operator
  - (3) Queue status — stav konkretniho queue rowu, kde zije: `_asw_outbound_queue.send_status`, 5 hodnot QUEUED/SENDING/SENT/FAILED/CANCELLED, meni: C-05 queue worker
  - (4) Inbound event — fakt co prislo, kde zije: `_asw_inbound_events.event_type`, 5 hodnot REPLY/BOUNCE/UNSUBSCRIBE/UNKNOWN_INBOUND/COMPLAINT, meni: C-07 ingest
  - (5) Review / manual signal — flag ze operator musi zasahnout, kde zije: LEADS flagy (`reply_needs_manual`, `followup_manual_block`, `followup_review_required`), meni: C-07 auto-set + operator
- **Zakazane kolapse (5):** stage ≠ lifecycle, stage ≠ queue status, queue status ≠ lifecycle, inbound event ≠ lifecycle, review flag ≠ lifecycle
- **Triple-redundant reply guard:** (a) CS1 state check, (b) C-04 gate re-check B3 `TERMINAL_STATE_REPLIED`, (c) C-07 inbound event store `event_type=REPLY` check → garantuje acceptance criteria #4 "lead s reply uz nikdy nedostane follow-up"
- **Idempotency pattern:** `followup:{lead_id}:{sequence_root_queue_id}:{sequence_stage}` — stage-scope (ne obsahovy hash, protoze copy template se muze evolve mezi engine runs)
- **Idempotency invariants (4):** unique per stage×sekvence×lead, safe-to-run-twice, race-safe, ne-rollback-safe (CANCELLED stage pocita jako "pokus byl ucinen")
- **5 sample lead timelines:**
  - (1) **Silent happy path:** initial SENT → 04-22 follow_up_1 SENT → 05-05 follow_up_2 SENT → 05-12+ `followup_skip` stop_reason=MAX_FOLLOWUPS_REACHED. 3 queue rows SENT, lead v "cold / no-response".
  - (2) **Reply → stop:** initial SENT → lead odpovi → C-07 `reply_event` (POSITIVE) → CS1 T20 `EMAIL_SENT → REPLIED` → engine skip, `_asw_logs` event `followup_skip` s `stop_reason=REPLY`, `detected_from_event_id=E-001`. Tier 1 stop. Operator obdrzi signal v LEADS `email_reply_type=REPLY`.
  - (3) **Bounce → stop:** initial SENT → hard bounce DSN 5.1.1 → C-07 `bounce_event` (`bounce_class=HARD`) → CS1 T21 `EMAIL_SENT → BOUNCED` → engine skip, `_asw_logs` event `followup_skip` s `stop_reason=BOUNCE_HARD`. Tier 2 address stop — sirsi dopad na dalsi sekvence pres PROPOSED C-04 `ADDRESS_BOUNCED` (LEADS `bounced_addresses` array + per-adresa blokace).
  - (4) **Unsubscribe → stop:** initial SENT → follow_up_1 SENT → lead odpovi "unsubscribe" → C-07 `unsubscribe_event` (via REPLY_BODY_INTENT) → CS1 T22 `* → UNSUBSCRIBED` → engine skip, `_asw_logs` event `followup_skip` s `stop_reason=UNSUBSCRIBE`. Tier 3 lead stop — vsechna outreach zakazana napric kanaly. LEADS `unsubscribed=true`, `unsubscribed_at`, `unsubscribe_source=REPLY_BODY_INTENT`.
  - (5) **Unknown-inbound → review → manual_stop:** initial SENT → lead odpovi s nejednoznacnym textem (UNCLASSIFIED) → C-07 `reply_event` (`reply_class=UNCLASSIFIED`) + auto-set `reply_needs_manual=TRUE` → engine pri next run route na REVIEW_REQUIRED, `_asw_logs` event `followup_review_required`. Sekvence pauzovana. Operator po 2 dnech rozhodne `stop_followup` (manual action v Google Sheets nebo B6) → engine route na STOP, `_asw_logs` event `followup_manual_stop`. Volitelne po 30 dnech pending review auto-terminate `followup_stale_review_abandoned`.
- **Sample queue rows field-level markers (per stage):**
  - initial: `sequence_stage=initial`, `parent_queue_id=null`, `sequence_root_queue_id=self`, `sequence_position=1`, priority=100, thread_hint=null
  - follow_up_1: INHERITED recipient+preview_url+personalization+sender_identity+channel; REGENERATED subject+body+CTA; STAGE-DERIVED thread_hint+scheduled_at+priority=90+idempotency_key; STAGE-OVERRIDE send_mode=SCHEDULED; parent_queue_id=initial.id, sequence_root_queue_id=initial.id, sequence_position=2
  - follow_up_2: stejne pattern, parent=follow_up_1, sequence_position=3, priority=80
- **Engine `_asw_logs` event types (8 PROPOSED):** `followup_insert`, `followup_skip`, `followup_review_required`, `followup_pause_ooo`, `followup_manual_stop`, `followup_stale_review_abandoned`, `followup_engine_run_summary`, `followup_auto_block_soft_bounce`
- **Cross-ref graph:** LEADS.lead_id ↔ `_asw_outbound_queue.lead_id` (3 rows sekvence, `sequence_root_queue_id` shared) ↔ `_asw_inbound_events.lead_id` (0..N events, `outreach_queue_id` → konkretni parent row) ↔ `_asw_logs.lead_id` (followup events, `detected_from_event_id` → inbound event) ↔ `_asw_dead_letters` (engine run fail)
- **Manual block 6-dim differentiation od UNSUBSCRIBE:**
  - Zdroj: operator explicit vs lead action
  - Scope: follow-up automation only vs entire outreach all channels
  - CS1 state change: zadny vs `* → UNSUBSCRIBED` (T22)
  - Reversible: ano operator unset vs operator manual re-consent but default binding
  - Storage: `LEADS.followup_manual_block` vs `LEADS.unsubscribed` + `unsubscribed_at` + `unsubscribe_source`
  - Compliance: internal policy vs GDPR / CAN-SPAM legal
- **CS3 alignment:** LockService per engine run + failure_class (ENGINE_TIMEOUT→TRANSIENT, C04_GATE_FAIL→PERMANENT, PARENT_NOT_SENT→TRANSIENT, MAX_RETRIES_EXCEEDED→PERMANENT) + dead-letter append-only
- **CS2 alignment:** engine je novy CS2 step `followup_engine_run` (batch daily + optional reactive po inbound event), run summary do `_asw_logs.followup_engine_run_summary`
- **C-04 alignment:** engine re-vola gate s `is_followup=true` context (PROPOSED extension) → bypass B16 `ALREADY_SENT`; respektuje B3/B4/B5/B7/B8 + PROPOSED `ADDRESS_BOUNCED`
- **C-05 alignment:** engine je C-05 producer (stejne jako C-04 pro initial); PROPOSED schema extensions (4 fields: `sequence_stage`, `parent_queue_id`, `sequence_root_queue_id`, `sequence_position` + `created_from_followup_engine_run`) jsou dodatky ne mutace
- **C-06 alignment:** engine populuje `SendRequest.thread_hint` (C-06 forward-compat); cte `NormalizedSendResponse.sent_at/provider_message_id/provider_thread_id`
- **C-07 alignment:** engine cte `_asw_inbound_events` pro stop detection; respektuje 3-tier stop model (Tier 1 follow-up/sekvence per thread, Tier 2 per email address, Tier 3 lead-wide); OOO → pauze ne stop
- **B6 vztah:** NENI blocker — operator muze manage v interim primo v Sheets bunce; B6 budouci task agreguje do per-lead view
- **VERIFIED IN REPO labely:** `_asw_outbound_queue` schema z C-05 merged PR #28 (19 fields pouzivanych C-08); `SendRequest.thread_hint` z C-06 merged PR #29; `_asw_inbound_events` z C-07 merged PR #30; CS1 canonical states EMAIL_SENT/REPLIED/BOUNCED/UNSUBSCRIBED + T20/T21/T22; C-04 block reasons B3/B4/B5/B7/B8/B16; CS2 `_asw_logs` run history; CS3 `_asw_dead_letters` + LockService
- **INFERRED labely:** T+3 / T+7 business days = industry-standard B2B outreach cadence; `Re: {initial.subject}` = Gmail-standard RFC 5322 reply convention; quiet hours 09:00–17:00 Europe/Prague = standard business hours; holiday calendar = mimo v1.0 ops config; copy templates per stage = vystup copy-gen tasku
- **PROPOSED FOR C-08 labely:** 4 C-05 schema extensions (`sequence_stage`, `parent_queue_id`, `sequence_root_queue_id`, `sequence_position`) + `created_from_followup_engine_run`, 3 LEADS flag extensions (`followup_manual_block`, `followup_review_required`, `last_operator_action`), C-04 `is_followup` context parameter, 8 `_asw_logs` event types, 10-value stop_reason enum, decision outcome enum (3 values), idempotency pattern, engine batch run artifact `FU-RUN-*`, priority defaults per stage (100/90/80), CS3 failure_class mapping (4 classes), auto-terminate thresholds (OOO 14d, review stale 30d, soft-bounce 3), 8 Script Properties, payload version "1.0"

## Known Limits

- SPEC neimplementuje engine runtime / scheduler / cron trigger / copy-gen — pouze definuje kontrakt. Implementace je budouci task (`runFollowupEngine()` + Config.gs + sheet schema migration).
- PROPOSED C-05 schema extensions (`sequence_stage`, `parent_queue_id`, `sequence_root_queue_id`, `sequence_position`) nejsou v ramci C-08 pridany do `_asw_outbound_queue`. Implementacni task je musi pridat spolu s materializaci engine.
- PROPOSED LEADS flag extensions (`followup_manual_block`, `followup_review_required`, `last_operator_action`) nejsou v ramci C-08 pridany do LEADS. Implementacni task je musi pridat spolu s materializaci engine.
- C-04 `is_followup` context parameter je PROPOSED extension, do materializace pouziva operator interim pres C-04 fallback logic (degraded, vyzaduje manual handling pri FALSE follow-upu).
- Holiday calendar je **mimo v1.0 SPEC** — engine v interim drzi jen Po-Pá filter. Stat. svatky se ignoruji. Ops config rozhodne o zdroji (Script Property JSON / external API / Google Calendar).
- Timezone je **single Europe/Prague** v v1.0. Timezone-per-lead personalizace je budouci task (nevyzadovano pro ceske lead base).
- **Stage copy templates** (follow_up_1 / follow_up_2 subject + body) jsou mimo C-08 scope — vystup budouciho copy-gen tasku. V1.0 default subject = `"Re: " + initial.subject`; body musi dodat implementacni task s inline template.
- OOO pause = **14 kalendarnich dni** (PROPOSED, konfigurovatelne pres Script Property). Industry variance 7-21 dni; implementator muze upravit.
- Review stale abandonment threshold = **30 dni** (PROPOSED). Pokud operator neresolvuje review flag 30 dni, engine auto-terminuje sekvenci + zapise `followup_stale_review_abandoned`. Konkretni threshold = ops rozhodnuti.
- Soft-bounce escalation threshold = **3 consecutive bounces** (aligned s C-07 PROPOSED). Implementator muze parametrizovat.
- Priority derivation (initial=100, follow_up_1=90, follow_up_2=80) je **PROPOSED default**. Queue worker sam prioritu pouziva pouze pro ordering (nizsi cislo = vyssi priorita). Konkretni hodnoty doladi implementacni task.
- Manual block flag (`followup_manual_block`) NEzastavuje existing QUEUED rows. Pokud block prijde mezi insert a worker claim, queue row se stale posle. Operator musi eksplicitne volat C-05 CANCELLED transition pro existing row. Two-step je per C-05 queue invariant (queue nema transaction safety across lead-level flags).
- Reversibilita manual block: operator muze flag zpet nastavit FALSE a sekvence pokracuje. Ale triple-guard (CS1 terminal / C-04 gate / inbound events) nezavisly — pokud lead byl v mezitimu REPLIED / BOUNCED / UNSUBSCRIBED, engine stale skipne.
- Engine je **daily batch** v v1.0. Pokud operator chce okamzity follow-up trigger (napr. po manual action), musi manually volat engine (budouci task). Reactive post-inbound-event trigger je PROPOSED optimalizace v v2.0.
- Copy regenerate rule `subject = "Re: " + initial.subject` predpoklada initial subject nema jiz `Re:` prefix. Pokud by ho mel, follow_up_1 by dostal `Re: Re:` (nezvykle, ale technicky valid). Implementator muze pridat de-duplication.
- `preview_url` inherited z initial — pokud operator chce zmenit preview napric sekvenci (napr. v1.1 preview renderingu), musi vytvorit **novou sekvenci** (novy initial, novy sequence_root_queue_id). Mid-sequence preview_url update je zakazany SPEC (zajistuje konzistenci leadu).
- `personalization_json` je **byte-identical snapshot z initial**. Pokud LEADS data se mezitim meni (napr. company_name update), follow-up pouzije **stary snapshot** — audit-favored, ale lead vidi potencialne outdated personalizaci. Implementator muze pridat opt-in refresh policy.
- Engine v v1.0 nezohlednuje operator preference pro **response window extension** (napr. "tento lead dej 7 dni na reply misto 3"). Per-lead timing override je PROPOSED for v2.0.
- Sekvence je **mono-channel email v1.0**. Multi-channel (SMS, phone, LinkedIn) je budouci task (pokud vubec).

## Next Dependency

- **Implementacni task C-08 runtime** — vytvori `runFollowupEngine()` v Apps Script, pridava PROPOSED C-05 queue schema extensions + PROPOSED LEADS flag extensions do `apps-script/Config.gs`, implementuje timing computation (business days + quiet hours), stop detection logiku (C-04 gate re-call + C-07 inbound events scan + CS1 terminal check), implementuje 3-decision outcome routing (AUTO_INSERT / REVIEW_REQUIRED / STOP), pridava 8 `_asw_logs` event types, implementuje cron / trigger runtime (daily batch), inline copy templates pro follow_up_1 + follow_up_2 (default basic text).
- **C-04 extension task** — pridava `is_followup` context parameter do gate signature. Konzumuje C-08 SPEC.
- **C-05 extension task** — pridava 4+1 PROPOSED fields do `_asw_outbound_queue` schema (`sequence_stage`, `parent_queue_id`, `sequence_root_queue_id`, `sequence_position`, `created_from_followup_engine_run`). Volitelne cislovano jako C-05.1 nebo soucast C-08 runtime.
- **Follow-up copy-gen task** — definuje stage-specific copy templates (subject + body markdown pro follow_up_1 + follow_up_2). V1.0 inline template (no AI), v2.0 AI-enhanced personalizaci.
- **B6 operator reply-handling UI** — implementuje frontend pro follow-up management: per-lead queue sekvence view, `_asw_logs` follow-up events, LEADS flag resolution buttons. Konzumuje `followup_manual_block` / `followup_review_required` / `reply_needs_manual` read/write contract.
- **C-07 extension task** (PROPOSED `ADDRESS_BOUNCED` C-04 block reason + `unsubscribed` / `bounced_addresses` LEADS flags) — materializuje C-07 PROPOSED artefakty. C-08 engine je downstream konzument.
- **C-09 Suppression list / Exception queue** — centralizovana suppression management. C-08 respektuje pres C-04 B7/B8 existing logic.
- **CS2 workflow orchestrator implementacni task** — pridava `followup_engine_run` jako novy step do CS2 orchestrator. C-08 je SPEC vstup.
- **CS3 engine dead-letter + lock pattern task** — implementuje LockService + dead-letter pro engine runs. C-08 je SPEC vstup.
- **Holiday calendar config task** (optional) — pridava state holiday filter do engine timing computation. V1.0 mimo scope.
- **Reactive engine trigger task** (v2.0) — aktivuje engine bezprostredne po C-07 inbound event (misto jen daily batch). Pricipem zrychluje response time pro bounce / unsubscribe stop. Napr. post-ingest hook podobny A-06/A-07 pattern.

Do te doby zustava follow-up engine SPEC-only. Stavajici runtime nema follow-up automation — po initial sendu se lead nikdy nedostane automaticky `follow_up_1` ani `follow_up_2`. Operator muze rucne poslat dalsi email v threadu (Gmail reply), ale engine / sekvence / stop conditions / idempotency / audit jsou mimo. C-08 SPEC formalizuje, jak bude tento procesni vakuum naplnen — 3-stage sekvenci s deterministickym timing, 5 stop condition kategorii, triple-redundant reply guard, sample row design pro budouci queue rozsireni a handoff graph na 11 navazujicich tasku. C-08 SPEC je prerekvizita pro jakoukoli implementaci follow-up automation a current state zachovava runtime stability aniz by pridal technicky dluh.
