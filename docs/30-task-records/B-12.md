# Task Record: B-12

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | B-12 |
| **Title** | Phase 2 hotfix — brief phone/email aliases for marketing web compat |
| **Owner** | Stream B |
| **Status** | done |
| **Date** | 2026-04-27 |
| **Stream** | B |

## Scope

Phase 2 launch hotfix po prvnim e2e produkcnim run (lead "ALVITO s.r.o. PLYNOSERVIS"). Marketing web `autosmartweb.cz/preview/<slug>` vracel **HTTP 500** na vsechny CRM-generovane preview slugs. Test slug `test-bauhaus-praha` fungoval (200) — mock data v `preview-reader.ts` mela spravne field names.

**Root cause — schema mismatch:**
- CRM Apps Script `buildPreviewBrief_` (`apps-script/PreviewPipeline.gs:634-653`) vracel brief s `contact_phone` + `contact_email` (legacy CRM frontend names).
- Marketing web `ClientBrief` interface (`Spookybro55/ASW-MARKETING-WEB:src/templates/core/types.ts`) vyžaduje `phone` + `email`.
- Render: `<EmergencyProfessional>` template volá `telHref(brief.phone)` v `Header`, `Hero`, `Services`, `Pricing`, `Locations`, `Contact`, `Footer`, `MobileStickyCta` — `brief.phone === undefined` → `undefined.replace(/\s/g, "")` → **TypeError** → SSR throw → 500.

Fix C (hybrid) — implementovan dual-side:
- **PR #73 (CRM Apps Script source-side):** `buildPreviewBrief_` přidá `phone:` a `email:` aliasy vedle existujicich `contact_phone:` a `contact_email:`. Newly-written `_previews` rows budou mit oba klice.
- **ASW-MARKETING-WEB#2 (read-time fallback, companion PR):** `preview-reader.ts:getPreviewBySlug` mapuje `rawBrief.contact_phone → brief.phone` (a email/website) pri čteni z Sheet. Existing rows zapsane pred B-12 (vc. ALVITO + 8 dalsich pilot leadů) renderuji okamzite po Vercel auto-deploy.

## Code Changes

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| apps-script/PreviewPipeline.gs | modified | `buildPreviewBrief_` return object: pridat 2 aliasy `phone: phone, email: email` vedle stavajicich `contact_phone: phone, contact_email: email`. Backward compatible — CRM frontend dale cte contact_phone/contact_email z drawer. |

**Companion PR (jiny repo, mimo tento task record scope):**
- ASW-MARKETING-WEB `src/lib/sheets/preview-reader.ts`: read-time fallback mapping pro existing rows.

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/30-task-records/B-12.md | new | Tento task record. |

## Contracts Changed

- **`buildPreviewBrief_` output schema:** rozsiren o `phone` + `email` klice (additive). Existujici `contact_phone` + `contact_email` zachovany pro CRM frontend backward compat.
- **`_previews.brief_json` content:** newly-written rows obsahuji oba alias sady. Old rows (alvito + 8 pilot leadů) maji jen `contact_phone`/`contact_email`, ale companion read-time fallback je funkčni.
- **Marketing web template ClientBrief:** beze zmeny — read-time mapping v `preview-reader.ts` zajisti dostupnost `brief.phone` + `brief.email`.

## Tests

| Test | Vysledek |
|------|----------|
| `npx tsc --noEmit` (CRM frontend) | OK |
| `npm run build` (CRM frontend) | OK |
| Live curl: `https://autosmartweb.cz/preview/alvito-s-r-o-plynoservis-praha` | OK — **200 OK** (predtim 500) |
| Live curl: `https://autosmartweb.cz/preview/test-bauhaus-praha` | OK — 200 (regression intact) |

## Output for Audit

- PR #73 squash merge `3d202aa` (CRM source-side fix).
- Companion ASW-MARKETING-WEB#2 squash merge `5a4e6b3` (read-time fallback).
- Apps Script TEST `clasp push` — 19 souboru pushnuto v 02:50:30.
- Apps Script Pilot deployment redeploy: `AKfycbzPeG_sHbeKLv9a5Z-SmKsRGhcY32k-MIp0DvxlYONpzZVb4gvfQz4nqR0X18w2AS_Kpw @8` ("phase2 hotfix: phone/email brief aliases"). URL netknuta.
- Vercel auto-deploy z `5a4e6b3` < 60s, novy deployment `dpl_DhGkUBpRTRrygsWepr847SJBWtks`.
- Smoke test ALVITO: 500 → 200 (Phase 2 v1.0 e2e PASS, tag `phase2-v1.0` set on `3d202aa`).

## Known Limits

- **Existing pilot rows:** ALVITO + 8 dalsich leadů zapsanych pred B-12 maji `contact_phone`/`contact_email` v `_previews.brief_json`. Funguji díky read-time fallback v marketing webu, NE source-side aliases. Re-generate v CRM (klik "Vygenerovat preview") prepise rows na nove schema.
- **Companion PR v jinem repo:** ASW-MARKETING-WEB#2 je mergnut samostatne — pokud by se tento (autosmartweby) repo restoroval samostatne na pre-B-12 main, marketing web dale renderuje OK díky read-time fallback. Asymetricka rezilience.
- **`website_url` alias** v read-time fallback zachycuje budouci možny field rename (ASW-MARKETING-WEB#2 čte `rawBrief.website_url`), ale CRM brief tento field nikdy nezapisuje. Dead code branch — kept defensive.

## Next Dependency

| Task | Co potrebuje z B-12 |
|------|---------------------|
| Phase 2 v1.0 launch | Tag `phase2-v1.0` na `3d202aa` (PR #73 merge commit). |
| Backlog | Schema sjednoceni — single brief schema napric CRM (drawer) + marketing web (template), bez aliasu. Ev. introdukce `PreviewBrief` v shared NPM package nebo TypeScript shared types. |
| Backlog | Defensive render v marketing webu (`telHref(phone || "")`) jako belt-and-suspenders pro budouci schema drift. |
