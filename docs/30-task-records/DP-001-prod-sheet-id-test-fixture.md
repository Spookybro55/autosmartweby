# Task Record: DP-001

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | DP-001 |
| **Title** | Replace hardcoded PROD Sheet ID in `preview-render-endpoint.test.ts` with fake fixture constant |
| **Owner** | Stream B |
| **Status** | code-complete |
| **Date** | 2026-04-30 |
| **Stream** | B |
| **Agent Role** | docs-guardian |
| **Track** | A |
| **Plan** | - |
| **Autonomous run** | yes |

## Scope

DP-001 (P0) per `docs/audits/FINDINGS.md:30`: the B-04 unit-test fixture `scripts/tests/preview-render-endpoint.test.ts` shipped with the full plain-text PROD Sheet ID `1RBcLZkn3AruiqaQdJ7PHIxvCcoO5SC9Qnlw_NiLnpYc` baked into the `buildValidPayload()` helper. The handler under test never calls the Sheets API in this suite — it only serializes and validates the payload — so any non-empty string works. Keeping a real PROD ID in tracked test code added a fifth file path to the SEC-001 PROD-ID exposure surface for zero functional reason.

**This task addresses ONLY the DP-001 narrow scope** — the single test-file fixture replacement. SEC-001 in `docs/audits/FINDINGS.md:51` consolidates the broader cleanup across **five other tracked locations** (`apps-script/Config.gs:14`, `apps-script/EnvConfig.gs:13,18,28`, `docs/09-project-control-tower.md:66`, `docs/20-current-state.md:45`, `docs/30-task-records/B6.md:118,179`) plus the open question of git-history rewrite. **SEC-001 stays Open** and will be resolved by Sebastián separately.

## Code Changes

| Soubor | Typ změny | Popis |
|--------|-----------|-------|
| `scripts/tests/preview-render-endpoint.test.ts` | modified | (1) Added `TEST_SHEET_ID_FAKE_FOR_FIXTURES_ONLY` module-level constant with explanatory comment under `// Fixtures` header. (2) Replaced the hardcoded PROD value at the `buildValidPayload()` `spreadsheet_id` field with the new constant. Net diff: +6 / -1 LOC. |

## Docs Updated

| Dokument | Typ změny | Proč |
|----------|-----------|------|
| `docs/audits/FINDINGS.md` | modified | DP-001 row Status flipped from `Open` to `**Resolved** at HEAD` per docs-guardian convention (precedent: `cleanup-and-sec-016.md`). Evidence cell strikes through stale path; recommendation cell unchanged. Cross-references SEC-001 as still-Open partial blocker. |
| `docs/30-task-records/DP-001-prod-sheet-id-test-fixture.md` | new | This task record. Filename matches branch tail per `scripts/agent/validate-task-record.mjs` derivation rules; the `Task ID` metadata field stays `DP-001` to match the FINDINGS row. |
| `docs/agents/QUEUE.md` | modified | Auto-regenerated by `node scripts/agent/triage.mjs` after the FINDINGS status change — DP-001 drops out of the `<!-- triage:ready-start -->` ready table (now Resolved, not Open). |
| `docs/11-change-log.md` | regenerated | Auto from task records (`node scripts/docs/build-changelog.mjs`). |
| `docs/29-task-registry.md` | regenerated | Auto from task records (`node scripts/docs/build-task-registry.mjs`). |

**Stream B mandatory canonical docs** (`docs/20`, `docs/22`, `docs/26`, `docs/27` per `docs/13-doc-update-rules.md`) are **NOT updated** in this task. Rationale per docs-guardian SKILL §2 step 3 ("Update minimally — preserve voice... Don't add new sections unless task explicitly creates new functionality / artifact"): this is pure test-fixture secret-hygiene with zero behavioural, architectural, infra, or offer-generation impact. Adding a "DP-001 fixture rename" line to `docs/22` / `docs/27` would be docs noise. Precedent: `cleanup-and-sec-016.md` (similar SEC reconciliation, no Stream B canonical-doc updates).

## Contracts Changed

- **No code contract changes.** The test still POSTs the same payload shape to `/api/preview/render`; only the literal value of the `spreadsheet_id` field has been swapped to a fake. Handler validation paths exercised by all 16 b04 test cases remain identical.
- **No public-API change.** No new exports, no signature change, no behavioural change in any production code path (only test code touched).

## Tests

| Test | Výsledek |
|------|----------|
| `npm run test:b04` | OK — 16 pass / 0 fail / 0 skip / duration 197.95 ms |
| `node scripts/docs/check-doc-sync.mjs` | OK — see "Output for Audit" |
| `node scripts/agent/validate-task-record.mjs DP-001` | OK — see "Output for Audit" |
| `npx tsc --noEmit` (crm-frontend) | N/A — no `crm-frontend/src/` files touched. Only `scripts/tests/*.test.ts` which is checked by Node's runtime via `--experimental-strip-types` in test:b04. |
| `npm run build` (crm-frontend) | N/A — no `crm-frontend/src/` files touched, no env / config / route change. |

## Output for Audit

After this PR is merged:

- `scripts/tests/preview-render-endpoint.test.ts` no longer contains the PROD Sheet ID literal `1RBcLZkn3AruiqaQdJ7PHIxvCcoO5SC9Qnlw_NiLnpYc`. `grep -n 1RBcLZkn` against the file at HEAD returns 0 hits.
- `docs/audits/FINDINGS.md` row DP-001 shows `**Resolved** at HEAD (verified 2026-04-30)` with the partial-fix caveat about SEC-001.
- The b04 unit-test suite (`npm run test:b04`) continues to pass 16/16; the fixture refactor is purely cosmetic to the handler under test.
- **PROD ID exposure in repo at HEAD reduced from 6 tracked files to 5** (per SEC-001 inventory). Remaining 5: `apps-script/Config.gs:14`, `apps-script/EnvConfig.gs:13,18,28`, `docs/09-project-control-tower.md:66`, `docs/20-current-state.md:45`, `docs/30-task-records/B6.md:118,179`. **Git history of `preview-render-endpoint.test.ts` still contains the PROD ID** — this is acknowledged and left for SEC-001's `git filter-repo` decision (out of scope for Track A).

## Known Limits

- **Partial fix only.** SEC-001 stays Open. The broader 5+ file PROD-ID cleanup, the optional Sheet-ID rotation in Google + Vercel envs, and any history-rewrite decision (`git filter-repo` vs migrate-to-new-Sheet-ID) are explicitly **out of scope** for this Track A run and will be handled by Sebastián separately.
- **History exposure persists.** The PROD ID is in `git log -p scripts/tests/preview-render-endpoint.test.ts` for every commit prior to this one. Removing it from HEAD does not remove it from history. Per SEC-001 recommendation, the proper mitigation is either (a) `git filter-repo` to rewrite history, or (b) migrate to a new PROD Sheet ID and treat the leaked ID as compromised. Both are operator-level decisions.
- **No regression test added.** I did not add a CI grep-rule like "fail PR if `1RBc` appears anywhere in `scripts/tests/`". That's a defensive add-on that belongs with SEC-001 / SEC-022 (broader secret-scan coverage), not this narrow fix. CC-QA-* style coverage is deferred.
- **Stream B mandatory docs untouched** (see Docs Updated rationale). If a future canonical-doc audit demands every DP-* fix touch `docs/22`/`docs/27`, this record's reasoning becomes the precedent to revisit.

## Next Dependency

| Task | Co potřebuje z DP-001 |
|------|----------------------|
| **SEC-001** PROD Sheet ID consolidation | Independent. DP-001 reduced the file count from 6 → 5 but does not unblock SEC-001; the remaining files + history rewrite remain SEC-001's scope. |
| **SEC-022** branch protection / required CI checks (incl. secret scan) | Independent. A future gitleaks-style CI gate would have caught DP-001 at PR time; once added, it would prevent re-introduction. |
| **DP-004** `PreviewPipeline.gs` PROD ID in webhook payload | Independent. Different file, different layer (Apps Script runtime). DP-001 fix establishes the "fake fixture" pattern that DP-004 cannot reuse (DP-004 needs real env-resolved value via `getSpreadsheetId_()`, not a fake). |

## DoD Checklist

> Required for agent-driven tasks (Track A or Track B with `Agent Role` ≠ `human`). Optional for human tasks.

### Code Done

- [x] `npx tsc --noEmit` (frontend, if touched): N/A — no frontend src files touched
- [x] `npm run build` (frontend, if touched): N/A — no frontend src files touched
- [x] No secrets in diff — PROD Sheet ID is **removed** from the test file; the fake `TEST_SHEET_ID_FAKE_FOR_FIXTURES_ONLY` is intentionally non-sensitive
- [x] No regressions in existing tests — `npm run test:b04` 16/16 pass

### Documentation Done

- [x] Affected docs identified per `docs/13-doc-update-rules.md` stream mapping — Stream B mandatory canonical docs reviewed; none semantically affected (rationale in Docs Updated)
- [x] Affected docs updated — `FINDINGS.md` (DP-001 row), task record, generated files
- [x] `docs/11-change-log.md` regenerated (`build-changelog.mjs`)
- [x] `docs/29-task-registry.md` regenerated (`build-task-registry.mjs`)
- [x] Control tower (`docs/09-project-control-tower.md`) checked — DP-001 not tracked there, no update needed (the doc itself is on the SEC-001 cleanup list)
- [x] Route mapa (`docs/12-route-and-surface-map.md`) checked — N/A, no route change

### Test Done

- [x] Tests pass — `npm run test:b04` 16/16 pass
- [x] `npm run build` verified — N/A (no frontend code touched)
- [x] `node scripts/docs/check-doc-sync.mjs`: 0 fail

### Agent Done (Track A only)

- [x] Diff size < 500 LOC (Track A hard limit) — actual diff far under limit
- [x] Secret scan clean — `grep -n 1RBcLZkn scripts/tests/preview-render-endpoint.test.ts` returns 0; no other secrets introduced
- [x] Self-review pass — re-read full diff with fresh eyes; 0 issues found
- [x] Cross-role review pass — Tech Lead read whole diff before PR open; sign-off in RUN-LOG
- [x] `docs/agents/QUEUE.md` updated — regenerated via `node scripts/agent/triage.mjs`; DP-001 drops out of ready table (status flipped to Resolved in FINDINGS)
- [x] `docs/agents/RUN-LOG.md` appended with `complete` step
