# Task Record: B-13

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | B-13 |
| **Title** | Email template schema + CRUD + runtime wiring (T1+T2+T3 of 13-task email templating + analytics project) |
| **Owner** | Stream B |
| **Status** | RUNTIME_WIRED |
| **Date** | 2026-04-28 |
| **Stream** | B |

## Scope

Foundation pro multi-task projekt: nahradit hardcoded `composeDraft_` editovatelnym template systemem s versioning + analytikou per template+segment.

T1 je jen schema migration — zadna business logika, zadne doPost akce, zadny frontend. To prijde v T2-T13.

Co T1 dodava:
- Novy hidden sheet `_email_templates` (16 sloupcu) s 5 placeholder radky pro default template keys: `no-website`, `weak-website`, `has-website`, `follow-up-1`, `follow-up-2` (status='empty', version=0).
- 4 nove `EXTENSION_COLUMNS` v LEADS pro per-lead template tracking: `email_template_key`, `email_template_version`, `email_template_id`, `email_segment_at_send`.
- Idempotentni setup funkce `setupEmailTemplates()` volana z menu — wrapper, ktery (1) zavola `setupPreviewExtension` aby pribyly 4 LEADS sloupce, (2) zavola `ensureEmailTemplatesSheet_` pro vytvoreni hidden listu, (3) zavola `bootstrapEmptyTemplates_` pro nasem 5 placeholder radku.

T1 NEMENI `composeDraft_`, `buildEmailDrafts`, `OutboundEmail.gs`, `WebAppEndpoint.gs`, `PreviewStore.gs` ani frontend. Po clasp push + spusteni `setupEmailTemplates` v editoru je sheet pripraveny pro CRUD operace v T2.

**T2 update (commit pridany ve stejnem PR):** CRUD vrstva nad `_email_templates` sheetem. 10 novych funkci v `EmailTemplateStore.gs` (file: 195 -> 585 radku). Drafts + Publish flow:
- `saveTemplateDraft_(key, subject, body, name, description)` — upsert draft (1-per-key invariant, overwrite-on-update, parent_template_id zachycen z aktualniho active).
- `publishTemplate_(key, commitMessage)` — promotuje draft na novy active (povinny commit message ≥ 5 znaku, blokuje publish prazdneho subject/body, archivuje predchozi active, maze empty placeholder, version+1).
- `discardTemplateDraft_(key)` — smaze draft row (no-op kdyz neexistuje).
- Read API: `loadActiveTemplate_`, `getTemplateDraft_`, `listAllTemplates_`, `listTemplateHistory_`.
- Helpers: `buildTemplateRowMap_`, `rowToTemplate_`, `extractPlaceholders_`.
- Mutace chranene `LockService.getScriptLock()` (5s timeout, try/finally release).
- Zadny in-memory cache — vse ze Sheet.

T2 stale NEMENI `composeDraft_`, `WebAppEndpoint.gs` ani frontend — to je T3 a T5.

**T3 update (commit pridany ve stejnem PR):** runtime wiring. `composeDraft_` v `PreviewPipeline.gs` ted dispatchuje pres template store. 3 nove funkce v `EmailTemplateStore.gs`:
- `renderTemplate_(template, leadData)` — placeholder substitution `{name}` → value, unknown → empty, case-insensitive. Computed convenience tokens: `{greeting}` (`Dobrý den[, jméno]`), `{firm_ref}` (`{business_name}` nebo `vaši firmu`), `{contact_name_comma}` (`, {contact_name}` nebo `''`).
- `buildPlaceholderValues_(ld)` — interni helper, mapuje rd na placeholder dict. Skupiny: LEAD (business_name, contact_name, city, area, service_type, segment, pain_point), PREVIEW (preview_url), SENDER (sender_name, sender_email), COMPUTED (greeting, firm_ref, contact_name_comma).
- `chooseEmailTemplate_(rd)` — auto-route via `resolveWebsiteState_`: NO_WEBSITE → `no-website`, WEAK_WEBSITE → `weak-website`, HAS_WEBSITE → `has-website`. CONFLICT/UNKNOWN/jakekoliv jine → `no-website` (safest default). Nikdy nethrowuje.

`composeDraft_(rd)` v `PreviewPipeline.gs`:
- Stary kod prejmenovan na `composeDraftFallback_` (pure rename, telo nezmeneno).
- Novy `composeDraft_` (~73 radku) volá `chooseEmailTemplate_` → `loadActiveTemplate_` → `renderTemplate_` v try/catch. Pri chybe (vc. `No active template for key:`) fallback na `composeDraftFallback_` + `aswLog INFO`.
- Vraci 6-field shape `{subject, body, template_key, template_version, template_id, segment_at_send}`. Pri fallback path jsou template_*  prazdne (analytics oznaci jako "untemplated").
- Sender identity resolvovana inline z `rd.assignee_email` přes `ASSIGNEE_NAMES` map (s defensive `typeof !== 'undefined'` checky pro test contexty), fallback na `DEFAULT_REPLY_TO_*`.

Caller capture pattern (4× `hr.colOrNull` + `hr.set`) zaveden ve vsech 4 LEADS write-back cestach: `buildEmailDrafts`, `processPreviewQueue` (via `artifacts.draft`), `refreshProcessedPreviewCopy`, `processPreviewForLead_` (writes[] array). Spec zminila jen prvni dva, ale ostatni dva jsou stejnou code-path (composeDraft + LEADS row write), takze stejny princip aplikovan vsude pro consistency v analytics.

T3 NEMENI Config.gs, Menu.gs, OutboundEmail.gs, WebAppEndpoint.gs, frontend. Bez `_email_templates` content (T4 ho bootstrapuje) vsechny drafty jdou pres fallback path — ZADNY behavioural change v pilot az do T4.

## Code Changes

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| apps-script/Config.gs | modified | +42/-1 — novy B-13 blok pod `PREVIEW_SHEET_NAME`: `EMAIL_TEMPLATES_SHEET_NAME='_email_templates'`, `EMAIL_TEMPLATES_SHEET_HEADERS` (16 sloupcu — template_id, template_key, version, name, description, subject_template, body_template, placeholders_used, status, commit_message, created_at, created_by, activated_at, activated_by, archived_at, parent_template_id), `EMAIL_TEMPLATE_DEFAULT_KEYS` (5 keys). Na konci `EXTENSION_COLUMNS` pridany 4 sloupce: email_template_key, email_template_version, email_template_id, email_segment_at_send. |
| apps-script/EmailTemplateStore.gs | new (T1) | 195 radku. `generateTemplateId_` (`ASW-TPL-{ts_base36}-{rand4}` mirror `generateLeadId_`). `ensureEmailTemplatesSheet_` (idempotent: create / repair-headers / no-op, hidden sheet, frozen header row, mirror `PreviewStore.gs:ensurePreviewSheet_`). `bootstrapEmptyTemplates_` (idempotent seed: skipne keys ktere jiz maji jakoukoliv row, vraci pocet pridanych radku, status='empty', version=0, created_by z `Session.getActiveUser().getEmail()` s fallbackem na 'system'). `setupEmailTemplates` (public menu entry — vola `setupPreviewExtension` + `ensureEmailTemplatesSheet_` + `bootstrapEmptyTemplates_`, vraci safeAlert summary). |
| apps-script/EmailTemplateStore.gs | modified (T2) | +389 radku (file now 585). Added 10 CRUD functions: `buildTemplateRowMap_`, `rowToTemplate_`, `extractPlaceholders_`, `listAllTemplates_`, `loadActiveTemplate_`, `getTemplateDraft_`, `listTemplateHistory_`, `saveTemplateDraft_`, `discardTemplateDraft_`, `publishTemplate_`. Mutating ops (`saveTemplateDraft_`, `discardTemplateDraft_`, `publishTemplate_`) wrapped in `LockService.getScriptLock()` with 5s timeout + try/finally. Drafts+Publish flow: 1 draft per `template_key` (overwrite-on-update), publish requires commit_message ≥ 5 chars + non-empty subject/body, archives previous active and deletes empty placeholder, version monotonically increases. No cache — all reads live from sheet. |
| apps-script/EmailTemplateStore.gs | modified (T3) | +129 radku. Added 3 functions: `renderTemplate_(template, leadData)` — substitutes `{placeholder}` tokens (case-insensitive, unknown→empty, supports computed convenience tokens `greeting`, `firm_ref`, `contact_name_comma`); `buildPlaceholderValues_(ld)` internal helper flattening lead data into the placeholder map (LEAD/PREVIEW/SENDER/COMPUTED groups); `chooseEmailTemplate_(leadData)` auto-routes via `resolveWebsiteState_` → `no-website` / `weak-website` / `has-website` (CONFLICT/UNKNOWN fallback to `no-website`, never throws). |
| apps-script/PreviewPipeline.gs | modified (T3) | +140/-1. (a) Renamed legacy `composeDraft_` → `composeDraftFallback_` — body unchanged, returns `{subject, body}`. (b) New `composeDraft_(rd)` dispatcher: `chooseEmailTemplate_` → `loadActiveTemplate_` → `renderTemplate_`, returns 6-field shape `{subject, body, template_key, template_version, template_id, segment_at_send}`. On `loadActiveTemplate_` throw → fallback path with empty metadata + `aswLog INFO`. Sender identity resolved inline via `ASSIGNEE_NAMES[rd.assignee_email]` (defensive `typeof !== 'undefined'` checks) with `DEFAULT_REPLY_TO_*` fallback. (c) Metadata capture (4× `colOrNull` + `hr.set` per site) added at all 4 LEADS write-back paths: `buildEmailDrafts:712`, `processPreviewQueue:971` (via `artifacts.draft`), `refreshProcessedPreviewCopy:1280`, `processPreviewForLead_:1905` (writes[] array — graceful via existing `colOrNull` filter at write loop). |
| apps-script/Menu.gs | modified | +1 — `.addItem('Setup email templates', 'setupEmailTemplates')` mezi `'Setup preview extension'` a `'Ensure lead IDs'` (adjacency reflects internal `setupEmailTemplates -> setupPreviewExtension` call). |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/30-task-records/B-13.md | new | Tento task record. |
| docs/11-change-log.md | regenerated | Auto z task records (`build-changelog.mjs`). |
| docs/29-task-registry.md | regenerated | Auto z task records (`build-task-registry.mjs`). |

## Contracts Changed

- **LEADS schema:** 4 nove sloupce (`email_template_key`, `email_template_version`, `email_template_id`, `email_segment_at_send`) pridane pres `setupPreviewExtension` (additive, idempotent migration). Existujici kod neovlivnen — sloupce jsou prazdne dokud T3 nezacne `composeDraft_` zapisovat.
- **Hidden sheet `_email_templates`:** novy, 16 sloupcu, schema definovane v `EMAIL_TEMPLATES_SHEET_HEADERS` (Config.gs). Single source of truth pro template content + versioning.
- **Template ID format:** `ASW-TPL-{ts_base36}-{rand4}` — mirror `generateLeadId_` patternu, immutable identifier per row.
- **Zadne Apps Script Web App API zmeny** — to je scope T5 (doPost actions for template CRUD).

## Tests

| Test | Vysledek |
|------|----------|
| Code review (manual) | OK — zadne references na neexistujici funkce, schema konzistentni, idempotentni cesta verifikovana proti `PreviewStore.gs` patternu |
| Apps Script editor syntax check | not yet — clasp push az po PR merge per CLAUDE.md |
| Idempotence smoke (setupEmailTemplates 2x) | not yet runtime-tested — soucasti T1 deploy verification |
| T2 manual code review | OK — 10 CRUD functions appended cleanly, LockService pattern verified, version-bump + archive + placeholder-delete sequence in `publishTemplate_` walked through against bootstrap state |
| T3 manual code review | OK — `composeDraft_` rename to `composeDraftFallback_` is a pure rename (verified by diff: only the function name + JSDoc preamble changed, body identical). New dispatcher returns 6-field shape; all 3 direct call sites + 1 indirect (`computePreviewArtifacts_` → consumed by `processPreviewQueue` and `processPreviewForLead_`) capture all 4 metadata cols via `hr.colOrNull` guards. With `_email_templates` still bootstrap-empty post-T1, every draft path goes through fallback — no behavioural change in pilot until T4 publishes `no-website` v1. |
| `npm run docs:check` | viz output v PR description |

## Output for Audit

T1 schema-only — zadny behavioural change pred runtime (`composeDraft_` nezmenen). Po `clasp push` + spusteni `Setup email templates` z menu v Apps Script editoru ocekavany state:
- `_email_templates` sheet existuje, hidden, 16 sloupcu, 5 radku (status='empty', version=0, template_key in EMAIL_TEMPLATE_DEFAULT_KEYS)
- LEADS ma 4 nove sloupce na konci (`email_template_key` ... `email_segment_at_send`), vsechny prazdne
- Druhe spusteni `setupEmailTemplates` musi nic nepridat (idempotence)
- `_asw_logs` ma INFO entries: `ensureEmailTemplatesSheet_ Created hidden sheet`, `bootstrapEmptyTemplates_ Seeded 5 empty template placeholders`, `setupEmailTemplates ...` summary

## Known Limits / Decisions

- `generateTemplateId_` pattern `ASW-TPL-{ts_base36}-{rand4}` mirror `generateLeadId_`, zadny collision check (akceptovany risk pri low-volume <100 publikaci/den; collisions by se navic projevily pri T2 publish flow, ne v T1 bootstrap kde id generuje hned po sobe a `Math.random` rand4 prefix zarucuje rozliseni).
- `created_by` fallback `'system'` kdyz `Session.getActiveUser().getEmail()` vrati prazdny string (time-driven context kompatibilita; T1 bootstrap se vola z menu takze normalne se naplni emailem operatora).
- Load order komentar `4/5` v souborovem headeru je informacni — Apps Script alphabetizuje runtime load (E < H < M < P), takze realne `EmailTemplateStore.gs` se nacita mezi `Config.gs` a `Helpers.gs`. Komentar 4/5 vs 4/8 v `PreviewStore.gs` neresi konflikt — oba jsou jen popisne.
- T1 nepouzil `setupPreviewExtension` ze `setupEmailTemplates` jako transakcni jednotku — pokud `ensureEmailTemplatesSheet_` selze, LEADS uz pribyly. Akceptovano: oba kroky idempotentni a re-run je bezpecny.

## Next Dependency

| Task | Co potrebuje z B-13 |
|------|---------------------|
| T2 (template store CRUD) | DONE in this PR — see commit history. |
| T3 (composeDraft_ refactor + render) | DONE in this PR — see commit history. |
| T4 (bootstrap no-website v1 from hardcoded text) | Migration script v Apps Script editoru: spusti `saveTemplateDraft_('no-website', subject, body, name, desc)` s nakopirovanym textem z `composeDraftFallback_` NO_WEBSITE varianty (uz v {placeholder} forme), pak `publishTemplate_('no-website', 'Bootstrap from hardcoded NO_WEBSITE branch')`. Po prvnim publish prestane fallback path stalat — drafty pojedou pres render. |
| T5 (doPost actions for template CRUD) | T2 funkce (`loadActiveTemplate_`, `saveTemplateDraft_`, `publishTemplate_`, `discardTemplateDraft_`, `listTemplateHistory_`, `listAllTemplates_`) zabaleny do `WebAppEndpoint.gs` doPost handlers + frontend writer fasada v `crm-frontend`. |
| T6 (admin UI Templates page) | T2 + T5 — UI pro editing + publish workflow s commit-message confirm dialog. |
