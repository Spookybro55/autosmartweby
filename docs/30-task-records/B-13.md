# Task Record: B-13

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | B-13 |
| **Title** | Email template schema + CRUD + runtime wiring + bootstrap + backend + API layer + UI listing + editor (T1+T2+T3+T3.5+T4+T5+T6+T7+T8 of 13-task email templating + analytics project) |
| **Owner** | Stream B |
| **Status** | UI_EDITOR_DONE |
| **Date** | 2026-04-28 |
| **Stream** | B |

## Scope

Foundation pro multi-task projekt: nahradit hardcoded `composeDraft_` editovatelnym template systemem s versioning + analytikou per template+segment.

T1 je jen schema migration — zadna business logika, zadne doPost akce, zadny frontend. To prijde v T2-T13.

Co T1 dodava:
- Novy hidden sheet `_email_templates` (16 sloupcu) s 5 placeholder radky pro default template keys: `no-website`, `weak-website`, `has-website`, `follow-up-1`, `follow-up-2` (status='empty', version=0).
- 4 nove `EXTENSION_COLUMNS` v LEADS pro per-lead template tracking: `email_template_key`, `email_template_version`, `email_template_id`, `email_segment_at_send`.
- Idempotentni setup funkce `setupEmailTemplates()` volana z menu — wrapper, ktery (1) zavola `setupPreviewExtension` aby pribyly 4 LEADS sloupce, (2) zavola `ensureEmailTemplatesSheet_` pro vytvoreni hidden listu, (3) zavola `bootstrapEmptyTemplates_` pro nasem 5 placeholder radku.

T1 NEMENI `composeDraft_`, `buildEmailDrafts`, `OutboundEmail.gs`, `WebAppEndpoint.gs`, `PreviewStore.gs` ani frontend. Po clasp push + spusteni `setupEmailTemplates` v editoru je sheet pripraveny pro CRUD operace v T2.

**T2 update (commit pridany ve stejnem PR):** CRUD vrstva nad `_email_templates` sheetem. 10 novych funkci v `EmailTemplateStore.gs` (file: 195 -> 585 radku). Drafts + Publish flow:
- `saveTemplateDraft_(key, subject, body, name, description)` — upsert draft (1-per-key invariant, overwrite-on-update, parent_template_id zachycen z aktualniho active).
- `publishTemplate_(key, commitMessage)` — promotuje draft na novy active (povinny commit message ≥ 5 znaku, blokuje publish prazdneho subject/body, archivuje predchozi active, maze empty placeholder, version+1).
- `discardTemplateDraft_(key)` — smaze draft row (no-op kdyz neexistuje).
- Read API: `loadActiveTemplate_`, `getTemplateDraft_`, `listAllTemplates_`, `listTemplateHistory_`.
- Helpers: `buildTemplateRowMap_`, `rowToTemplate_`, `extractPlaceholders_`.
- Mutace chranene `LockService.getScriptLock()` (5s timeout, try/finally release).
- Zadny in-memory cache — vse ze Sheet.

T2 stale NEMENI `composeDraft_`, `WebAppEndpoint.gs` ani frontend — to je T3 a T5.

**T3 update (commit pridany ve stejnem PR):** runtime wiring. `composeDraft_` v `PreviewPipeline.gs` ted dispatchuje pres template store. 3 nove funkce v `EmailTemplateStore.gs`:
- `renderTemplate_(template, leadData)` — placeholder substitution `{name}` → value, unknown → empty, case-insensitive. Computed convenience tokens: `{greeting}` (`Dobrý den[, jméno]`), `{firm_ref}` (`{business_name}` nebo `vaši firmu`), `{contact_name_comma}` (`, {contact_name}` nebo `''`).
- `buildPlaceholderValues_(ld)` — interni helper, mapuje rd na placeholder dict. Skupiny: LEAD (business_name, contact_name, city, area, service_type, segment, pain_point), PREVIEW (preview_url), SENDER (sender_name, sender_email), COMPUTED (greeting, firm_ref, contact_name_comma).
- `chooseEmailTemplate_(rd)` — auto-route via `resolveWebsiteState_`: NO_WEBSITE → `no-website`, WEAK_WEBSITE → `weak-website`, HAS_WEBSITE → `has-website`. CONFLICT/UNKNOWN/jakekoliv jine → `no-website` (safest default). Nikdy nethrowuje.

`composeDraft_(rd)` v `PreviewPipeline.gs`:
- Stary kod prejmenovan na `composeDraftFallback_` (pure rename, telo nezmeneno).
- Novy `composeDraft_` (~73 radku) volá `chooseEmailTemplate_` → `loadActiveTemplate_` → `renderTemplate_` v try/catch. Pri chybe (vc. `No active template for key:`) fallback na `composeDraftFallback_` + `aswLog INFO`.
- Vraci 6-field shape `{subject, body, template_key, template_version, template_id, segment_at_send}`. Pri fallback path jsou template_*  prazdne (analytics oznaci jako "untemplated").
- Sender identity resolvovana inline z `rd.assignee_email` přes `ASSIGNEE_NAMES` map (s defensive `typeof !== 'undefined'` checky pro test contexty), fallback na `DEFAULT_REPLY_TO_*`.

Caller capture pattern (4× `hr.colOrNull` + `hr.set`) zaveden ve vsech 4 LEADS write-back cestach: `buildEmailDrafts`, `processPreviewQueue` (via `artifacts.draft`), `refreshProcessedPreviewCopy`, `processPreviewForLead_` (writes[] array). Spec zminila jen prvni dva, ale ostatni dva jsou stejnou code-path (composeDraft + LEADS row write), takze stejny princip aplikovan vsude pro consistency v analytics.

T3 NEMENI Config.gs, Menu.gs, OutboundEmail.gs, WebAppEndpoint.gs, frontend. Bez `_email_templates` content (T4 ho bootstrapuje) vsechny drafty jdou pres fallback path — ZADNY behavioural change v pilot az do T4.

**T3.5+T4 update (commits ve stejnem PR):** dokoncena cesta od schematu k publikovane prvni sablone.

T3.5 race fix:
- `composeDraft_` v `computePreviewArtifacts_` se volal PRED tim, nez byl `preview_url` v `rd`. `{preview_url}` placeholder by tedy renderoval prazdne.
- Fix: `computePreviewArtifacts_` nyni resolvuje `previewUrl = resolvePreviewUrl_(slug, false)` a in-place zapise do `rd.preview_url` pred `composeDraft_(rd)`. Guarded `if (!rd.preview_url)` aby se neclobberovaly existujici hodnoty.
- Ostatni 2 LEADS-side call sites (`buildEmailDrafts:712`, `refreshProcessedPreviewCopy:1398`) cetly `hr.row(row)` ktery uz obsahuje LEADS row data vc. preview_url, takze tam fix neni potreba.

T4a — assignee profile extension + legacy migration:
- `ASSIGNEE_NAMES` zmena ze stareho `email→name` literal mapy na IIFE-derived map z noveho `ASSIGNEE_PROFILES`. Domena konsolidovana na `autosmartweb.cz`: 4 stare emaily (`sfridrich@unipong.cz`, `sebastian@autosmartweb.cz`, `tomas@autosmartweb.cz`, `jan.bezemek@autosmartweb.cz`) → 3 nove (`s.fridrich@autosmartweb.cz`, `t.maixner@autosmartweb.cz`, `j.bezemek@autosmartweb.cz`).
- `ASSIGNEE_PROFILES` ma `{name, role, phone, email_display, web}` per assignee. `DEFAULT_ASSIGNEE_PROFILE` pro empty/unknown fallback.
- `getAssigneeProfile_(email)` always returns valid profile object.
- `LEGACY_ASSIGNEE_EMAIL_MAP` map starych klicu na nove + `migrateLegacyAssigneeEmails_` funkce: scanuje LEADS `assignee_email` column, rewritne legacy keys, prazdne cells nikdy nemodifikuje, LockService 10s, vraci pocet upravenych radku.
- `composeDraft_` v `PreviewPipeline.gs` swap z inline `ASSIGNEE_NAMES[email]` na `getAssigneeProfile_`. Augmented objekt nese kompletni sender block (`sender_name`, `sender_role`, `sender_phone`, `sender_email`, `sender_email_display`, `sender_web`).
- `buildPlaceholderValues_` rozsireno z 13 na 18 placeholders: pridany sender_role/phone/email_display/web + `service_type_humanized` (defensive `typeof humanizeServiceType_ === 'function'` guard, try/catch fallback na raw service_type).

T4b — first published template:
- `bootstrapNoWebsiteV1()`: idempotentni publish prvni `no-website` v1 sablony s aprovovanym textem (Phase 2 launch v1.0). Subject: `Dotaz k vašemu webu {business_name}`. Body obsahuje `{service_type_humanized}` / `{city}` / `{preview_url}` / signaturu se sender_*.
- `migrateAndBootstrap`: convenience wrapper migrace → `setupEmailTemplates` → `bootstrapNoWebsiteV1`. Single-click cutover z menu.

Po spusteni `migrateAndBootstrap` v Apps Script editoru:
- 3 rows v TEST sheetu maji `tomas@autosmartweb.cz` -> remapnuto na `t.maixner@autosmartweb.cz` (per-mapping count: tomas: 3).
- `_email_templates` rozšireno o 4 LEADS sloupce (idempotent, run #2 bude no-op).
- `no-website` v1 publikovana s template_id `ASW-TPL-...`, status='active'. Empty placeholder `no-website` row smazana per `publishTemplate_` step 4.
- Od te chvile `composeDraft_` pro NO_WEBSITE leady prestane padat do fallbacku → vsechny novy drafty jsou template-rendered + maji `email_template_*` metadata.

T4 NEMENI `OutboundEmail.gs:resolveSenderIdentity_` (per spec). `DEFAULT_REPLY_TO_*` zustavaji na legacy `sebastian@autosmartweb.cz` — viz Known Limits.

**T5 update (commit ve stejnem PR):** backend doPost endpoints + live analytics aggregation. ZADNY behavioural change v existujicim flow — pure additive surface area pro frontend (T6+).

3 nove funkce v `EmailTemplateStore.gs`:
- `getTemplateAnalytics_()` — single-pass LEADS scan, groupuje podle `(template_key, version)` + per-segment breakdown. Counts: `sent` (rows kde `email_sync_status` ∈ {SENT, REPLIED, LINKED} a non-empty `email_template_id` — fallback drafts excluded), `replied` (subset s `email_reply_type === 'REPLY'`), `won` (subset s `status === 'WON'`). Vraci aktivni-template zero-state entries i kdyz 0 sends, aby UI ukazalo 0/0/0 misto skryti. Stable sort: key ASC, version DESC. Range fetch optimalizovan na min/max needed columns (~50ms / 800 rows ocekavano).
- `_emptyAnalyticsForAllActiveTemplates_()` — interni helper pro pre-migration pripad.
- `regenerateDraftForLead_(leadId, templateKeyOverride)` — LockService 5s, lookup row pres existing `findRowByLeadId_`, primy `getRange()` row read (`readSingleRow_` neexistuje v codebase), spusti `composeDraft_(rd)`, zapise 6 cells. Required cells (`email_subject_draft`, `email_body_draft`) pres `hr.col` (throws on missing), metadata cells pres `hr.colOrNull` guard. `templateKeyOverride` param prijima ale ignoruje — placeholder pro T9 manual override path (vyzaduje composeDraft_ refactor s template injection).

9 novych doPost handlers v `WebAppEndpoint.gs`:
- Read: `listTemplates`, `getTemplate`, `getTemplateDraft`, `getTemplateHistory` (vsechny strip `_rowNum` z return objektu).
- Write: `saveTemplateDraft` (subject ≤ 500, body ≤ 50000 chars validation, returns `subject_too_long` / `body_too_long`), `discardTemplateDraft`, `publishTemplate` (mapuje 3 publish-gate failures: `commit_message_too_short`, `no_draft`, `empty_draft_content` — frontend mapuje na localized cz hlasky).
- Analytics: `getTemplateAnalytics`.
- Operator: `regenerateDraft` (T9 dependency — frontend zatim posila `templateKey` ale handler ho ignoruje az do T9 wiring).

`{ ok: true/false, error: '<reason>' }` shape matches Phase 2 KROK 4-6 (`getPreview`, `sendEmail`, `generatePreview`). Token verification handled upstream v `doPost` `FRONTEND_API_SECRET` check — handlers don't repeat. Existing 5 actions (`updateLead`, `assignLead`, `getPreview`, `generatePreview`, `sendEmail`) untouched.

T5 NEMENI Config.gs, Menu.gs, PreviewPipeline.gs, OutboundEmail.gs, frontend.

**T6 update (commit ve stejnem PR):** Next.js API layer + writer wrappers. Pure proxy vrstva — zadna business logika, jen request validation, AS dispatch, error mapping. Po T6 mohou frontend pages (T7-T11) volat tyto routy primo bez znalosti AS payload shape.

Novy soubor `crm-frontend/src/types/templates.ts`:
- 4 main interfaces: `EmailTemplate` (16 fields mirror AS sheet schema), `TemplateAnalyticsTotals`, `TemplateAnalyticsEntry`, `RegenerateDraftResult`.
- `TemplateStatus` union type.
- `DEFAULT_TEMPLATE_KEYS` const tuple — sync s `EMAIL_TEMPLATE_DEFAULT_KEYS` v `apps-script/Config.gs`.
- `TEMPLATE_KEY_LABELS` map ('Bez webu', 'Slabý web', 'Má web', 'Follow-up 1/2') pro UI fallback display jmena kdyz template je empty.

`crm-frontend/src/lib/google/apps-script-writer.ts` (+317 radku):
- 9 new exported async wrappers s Result interfaces — same pattern jako existing `generatePreview` / `sendEmail`: `SHEET_CONFIG.APPS_SCRIPT_URL` POST s `process.env.APPS_SCRIPT_SECRET` token, AS `{ ok }` response shape mapped to `{ success }`.
- Existujici wrappers (`updateLeadFields`, `generatePreview`, `sendEmail`) untouched.

7 new Next.js API routes (vsechny pouzivaji Next 16 async `params: Promise<...>` pattern dle docs in `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md`):

| Route | Methods | Notes |
|-------|---------|-------|
| `/api/templates` | GET | List all templates. 502 on AS upstream failure. |
| `/api/templates/[key]` | GET | Active template. 404 on `no_active_template`. |
| `/api/templates/[key]/draft` | GET / PUT / DELETE | Per-key draft CRUD. PUT validates `subject ≤ 500`, `body ≤ 50000`. |
| `/api/templates/[key]/history` | GET | All versions for key. |
| `/api/templates/[key]/publish` | POST | Body `{ commitMessage }`. Front-loads `commit_message_too_short` validation. AS publish-gate failures (`no_draft`, `empty_draft_content`) → 400. |
| `/api/analytics/templates` | GET | Live aggregation per (key, version) + by_segment. |
| `/api/leads/[id]/regenerate-draft` | POST | Optional `{ templateKey }` (T9 placeholder). 404 on `lead_not_found`. |

Error mapping konvence: 400 = validation (chybi key, JSON parse fail, length over limit), 404 = not found (`no_active_template`, `lead_not_found`), 502 = AS upstream `{ ok: false }`, 500 = unexpected catch.

T6 NEMENI Apps Script files, frontend pages, ostatni komponenty.

**T7 update (commit ve stejnem PR):** prvni UI stranka — landing pro spravu sablon. URL `/settings/templates`. Listing only — editor je T8.

Novy soubor `crm-frontend/src/app/settings/page.tsx` (server component, prerendered):
- Top-level `/settings` landing s 2 cards: "Šablony emailů" -> `/settings/templates`, "Analýza šablon" -> `/analytics/templates`.
- Lucide icons (Mail, BarChart3). `metadata.title` set.

Novy soubor `crm-frontend/src/app/settings/templates/page.tsx` (client component):
- Fetch z `/api/templates` v `useEffect`. Loading state -> `TemplatesPageSkeleton`. Error state -> destruktivni rounded box s "Zkusit znovu" retry tlacitkem + `sonner` toast.
- `groupByKey` helper — buckets pres `template_key` na `{ active, draft, empty }`. Filtruje `archived` (patri do history view T8).
- Pre-bootstrap state: pred prvnim spustenim `bootstrapEmptyTemplates_` `/api/templates` muze vratit `[]`. Page presto renderuje 5 placeholder cards (po jedne za kazdy klic v `DEFAULT_TEMPLATE_KEYS`) se statusom `empty`. Klik vede na `/settings/templates/[key]` (T8 — zatim 404).
- Stable sort: `DEFAULT_TEMPLATE_KEYS` order first (no-website, weak-website, has-website, follow-up-1, follow-up-2), pak custom keys alphabetical.

Novy soubor `crm-frontend/src/components/templates/template-card.tsx`:
- `StatusBadge` 3 variants: active (green pill `aktivní · v{n}`), draft (amber pill s ✎ icon), empty (gray pill s outline circle). Archived varianta neni — archivovane verze se v listingu nezobrazuji.
- Shows `name` v uvozovkach kdyz active. Italic "— připraveno k vytvoření —" kdyz empty.
- "Rozpracovaná verze" amber pill below name kdyz oba `active` + `draft` existuji (operator je v polovine editovani v2).
- Activated_at byline format: "{authorShort}, {day}. {month}." (e.g. `s.fridrich, 28. 4.`).
- CTA: "Upravit" (kdyz je active) nebo "Vytvořit" (kdyz empty/no active), oba s ArrowRight icon ktery se posune na hover.

Novy soubor `crm-frontend/src/components/templates/templates-page-skeleton.tsx`:
- 5 skeleton cards mimicking actual layout (used existing `<Skeleton>` shadcn primitive z `src/components/ui/skeleton.tsx`).

`crm-frontend/src/components/layout/sidebar.tsx` modified (+2 radky): pridana navigation entry "Nastavení" -> `/settings` (Settings lucide icon) na konec `navigation` array. Nav infrastruktura uz existuje — pridani je trivial.

T7 NEMENI: API routes (T6), apps-script-writer (T6), AS files, lead drawer, ostatni komponenty. Editor (T8 sub-task) zatim neni — klik na card v T7 vede na neexistujici cestu, planovane.

**T8 update (commit ve stejnem PR):** plne funkcni editor `/settings/templates/[key]`. Largest frontend task v projektu.

Novy klient renderer `crm-frontend/src/lib/templates/render-preview.ts`:
- Mirror of AS `EmailTemplateStore.gs:renderTemplate_` + `buildPlaceholderValues_`. Sync requirement — drift mezi tymto a AS = visible bug.
- 18 known placeholders (LEAD/PREVIEW/SENDER/COMPUTED). Unknown → renderuje prázdné, vrací jejich seznam pro warning UI.
- `humanizeServiceType` ma 12-entry Czech approximation map (instalatér → instalatérské služby etc.). Best-effort — AS-side puvodni `humanizeServiceType_` v `PreviewPipeline.gs` je canonical, frontend je informativni preview.

`SAMPLE_LEADS` fixture (`sample-leads.ts`): 3 leady s ruznymi profily (s/bez contact_name, s/bez area, s/bez pain_point) aby editor preview umel ukazat vsechny code paths. Sender block hard-coded na Sebastiana — T9 swap na real leads dropdown.

5 novych komponent v `crm-frontend/src/components/templates/`:

| Komponenta | Role |
|------------|------|
| `template-editor.tsx` | Hlavni client komponenta, ~280 LOC. Split-pane editor + preview, dirty tracking proti baseline snapshotu, beforeunload warning, paralelni fetch active+draft, AS error code → cz toast mapping. |
| `template-preview-pane.tsx` | Lead dropdown + email-style preview card (Komu/Předmět/Tělo). useMemo na render. |
| `placeholder-legend.tsx` | Collapsible panel s 18 placeholdery, click-to-copy `{name}`, warning ribbon kdyz template pouziva unknown tokens. |
| `publish-dialog.tsx` | shadcn Dialog s commit message Textarea (≥ 5 chars required), AS error code mapping, Loader2 spinner. |
| `history-drawer.tsx` | shadcn Sheet (right, max-w-xl) s GET `/api/templates/[key]/history`, expandable rows pro detail subject + body, status badges. Refactored na `useCallback`+`useEffect` pattern aby uspokojil React 19 lint rule (`react-you-might-not-need-an-effect`). |

Page route `crm-frontend/src/app/settings/templates/[key]/page.tsx` (server component, dynamic) — async params unwrap, `generateMetadata` for cz title, renders `<TemplateEditor>`.

User flow:
1. `/settings/templates` (T7) — operator klikne card -> `/settings/templates/no-website` (T8).
2. Editor nacita active + draft paralelne. Pokud draft existuje, zacina v editoru s draftem (rozpracovano), jinak s active content.
3. Operator edituje. `dirty` flag se vypocita realtime, "Neuložené změny" badge svítí, beforeunload guard aktivni.
4. "Uložit draft" -> PUT (`saveTemplateDraft_`). Baseline se posune na nove ulozeny stav.
5. "Zahodit změny" -> kdyz draft existuje, confirm() + DELETE (`discardTemplateDraft_`); jinak jen reset na active.
6. "Publikovat..." -> Dialog s commit message ≥ 5 chars -> POST (`publishTemplate_`). Po success: baseline reset na new active, draft cleared.
7. "Historie" -> Sheet s GET history. Expand row pro inline preview (read-only).

Live preview: na kazdou zmenu editoru se subject + body re-rendituji s vybranym sample leadem. PlaceholderLegend warning ukaze unknown tokens v real-time.

T8 NEMENI: API routes (T6), apps-script-writer (T6), AS files, lead drawer (T9 scope), listing page (T7).

## Code Changes

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| apps-script/Config.gs | modified | +42/-1 — novy B-13 blok pod `PREVIEW_SHEET_NAME`: `EMAIL_TEMPLATES_SHEET_NAME='_email_templates'`, `EMAIL_TEMPLATES_SHEET_HEADERS` (16 sloupcu — template_id, template_key, version, name, description, subject_template, body_template, placeholders_used, status, commit_message, created_at, created_by, activated_at, activated_by, archived_at, parent_template_id), `EMAIL_TEMPLATE_DEFAULT_KEYS` (5 keys). Na konci `EXTENSION_COLUMNS` pridany 4 sloupce: email_template_key, email_template_version, email_template_id, email_segment_at_send. |
| apps-script/EmailTemplateStore.gs | new (T1) | 195 radku. `generateTemplateId_` (`ASW-TPL-{ts_base36}-{rand4}` mirror `generateLeadId_`). `ensureEmailTemplatesSheet_` (idempotent: create / repair-headers / no-op, hidden sheet, frozen header row, mirror `PreviewStore.gs:ensurePreviewSheet_`). `bootstrapEmptyTemplates_` (idempotent seed: skipne keys ktere jiz maji jakoukoliv row, vraci pocet pridanych radku, status='empty', version=0, created_by z `Session.getActiveUser().getEmail()` s fallbackem na 'system'). `setupEmailTemplates` (public menu entry — vola `setupPreviewExtension` + `ensureEmailTemplatesSheet_` + `bootstrapEmptyTemplates_`, vraci safeAlert summary). |
| apps-script/EmailTemplateStore.gs | modified (T2) | +389 radku (file now 585). Added 10 CRUD functions: `buildTemplateRowMap_`, `rowToTemplate_`, `extractPlaceholders_`, `listAllTemplates_`, `loadActiveTemplate_`, `getTemplateDraft_`, `listTemplateHistory_`, `saveTemplateDraft_`, `discardTemplateDraft_`, `publishTemplate_`. Mutating ops (`saveTemplateDraft_`, `discardTemplateDraft_`, `publishTemplate_`) wrapped in `LockService.getScriptLock()` with 5s timeout + try/finally. Drafts+Publish flow: 1 draft per `template_key` (overwrite-on-update), publish requires commit_message ≥ 5 chars + non-empty subject/body, archives previous active and deletes empty placeholder, version monotonically increases. No cache — all reads live from sheet. |
| apps-script/EmailTemplateStore.gs | modified (T3) | +129 radku. Added 3 functions: `renderTemplate_(template, leadData)` — substitutes `{placeholder}` tokens (case-insensitive, unknown→empty, supports computed convenience tokens `greeting`, `firm_ref`, `contact_name_comma`); `buildPlaceholderValues_(ld)` internal helper flattening lead data into the placeholder map (LEAD/PREVIEW/SENDER/COMPUTED groups); `chooseEmailTemplate_(leadData)` auto-routes via `resolveWebsiteState_` → `no-website` / `weak-website` / `has-website` (CONFLICT/UNKNOWN fallback to `no-website`, never throws). |
| apps-script/PreviewPipeline.gs | modified (T3) | +140/-1. (a) Renamed legacy `composeDraft_` → `composeDraftFallback_` — body unchanged, returns `{subject, body}`. (b) New `composeDraft_(rd)` dispatcher: `chooseEmailTemplate_` → `loadActiveTemplate_` → `renderTemplate_`, returns 6-field shape `{subject, body, template_key, template_version, template_id, segment_at_send}`. On `loadActiveTemplate_` throw → fallback path with empty metadata + `aswLog INFO`. (c) Metadata capture (4× `colOrNull` + `hr.set` per site) added at all 4 LEADS write-back paths: `buildEmailDrafts:712`, `processPreviewQueue:971` (via `artifacts.draft`), `refreshProcessedPreviewCopy:1280`, `processPreviewForLead_:1905` (writes[] array — graceful via existing `colOrNull` filter at write loop). |
| apps-script/Config.gs | modified (T4) | +114/-19. New `LEGACY_ASSIGNEE_EMAIL_MAP` (4 entries: `sfridrich@unipong.cz` / `sebastian@autosmartweb.cz` → `s.fridrich@autosmartweb.cz`; `tomas@…` → `t.maixner@…`; `jan.bezemek@…` → `j.bezemek@…`). New `ASSIGNEE_PROFILES` (3 canonical autosmartweb.cz keys with `{name, role, phone, email_display, web}` per assignee). New `DEFAULT_ASSIGNEE_PROFILE` for empty/unknown fallback. `ASSIGNEE_NAMES` is now an IIFE-derived `email→name` map for back-compat with `resolveSenderIdentity_` and `ALLOWED_USERS = Object.keys(ASSIGNEE_NAMES)`. New helper `getAssigneeProfile_(email)` always returns a valid profile (never null). `DEFAULT_REPLY_TO_EMAIL`/`DEFAULT_REPLY_TO_NAME` left untouched per "NETKAT resolveSenderIdentity_" rule — see Known Limits. |
| apps-script/EmailTemplateStore.gs | modified (T4) | +219/-39. Extended `buildPlaceholderValues_` from 13→18 placeholders: added `sender_role`, `sender_phone`, `sender_email_display`, `sender_web`, `service_type_humanized` (defensive `typeof humanizeServiceType_ === 'function'` check + try/catch). Added 3 new functions: `bootstrapNoWebsiteV1` (idempotent: no-op if `loadActiveTemplate_('no-website')` already exists; otherwise `saveTemplateDraft_` + `publishTemplate_` with approved subject `Dotaz k vašemu webu {business_name}` + body containing `{service_type_humanized}` / `{city}` / `{preview_url}` / `{sender_name}` / `{sender_role}` / `{sender_web}` / `{sender_phone}`); `migrateLegacyAssigneeEmails_` (LockService 10s, scans `assignee_email` column via `hr.colOrNull`, rewrites legacy keys to canonical, never touches empty cells, logs per-mapping count, returns total); `migrateAndBootstrap` (one-click: migrate → setupEmailTemplates → bootstrapNoWebsiteV1 with try/catch around bootstrap for idempotent re-run safety). |
| apps-script/PreviewPipeline.gs | modified (T3.5+T4) | +50/-12. (T3.5 race fix) `computePreviewArtifacts_` now resolves `previewUrl = resolvePreviewUrl_(slug, false)` and assigns to `rd.preview_url` BEFORE calling `composeDraft_(rd)` — guards in-place write with `if (!rd.preview_url)` so existing values aren't clobbered. Fixes `{preview_url}` placeholder rendering empty in `processPreviewQueue` and `processPreviewForLead_` paths where the LEADS row hasn't been written yet. (T4 sender refactor) `composeDraft_` swapped from inline `ASSIGNEE_NAMES[email]` lookup to `getAssigneeProfile_` (defensive `typeof === 'function'` check). Augmented object now carries `sender_role`/`sender_phone`/`sender_email_display`/`sender_web` alongside name+email. |
| apps-script/Menu.gs | modified (T4) | +2. Two new items between "Setup email templates" and "Ensure lead IDs": "Migrate legacy assignees → bootstrap" (calls `migrateAndBootstrap` — recommended one-click cutover) and "Bootstrap no-website v1 (only)" (calls `bootstrapNoWebsiteV1` directly, for re-running just the publish step). |
| apps-script/EmailTemplateStore.gs | modified (T5) | +269 radku. 3 new functions (file now 23 functions total, not the 22 the T5 spec acceptance criteria stated — see Decisions). `getTemplateAnalytics_()`: live aggregation, single-pass scan of LEADS, groups by `(template_key, version)` + per-segment breakdown, counts `sent` (rows with `email_sync_status` ∈ {SENT, REPLIED, LINKED} and non-empty `email_template_id`), `replied` (subset where `email_reply_type === 'REPLY'`), `won` (subset where `status === 'WON'`). Excludes fallback drafts (empty `template_id`). Returns active-template zero-state entries even with 0 sends so UI shows 0/0/0. Stable sort by key ASC, version DESC. Range fetch optimized to min/max needed columns. `_emptyAnalyticsForAllActiveTemplates_()`: internal helper used when migration hasn't run. `regenerateDraftForLead_(leadId, templateKeyOverride)`: LockService-protected (5s), looks up row via existing `findRowByLeadId_(sheet, leadIdCol, lid)`, reads row directly via `getRange().getValues()[0]` (no `readSingleRow_` helper exists in codebase — flagged), runs `composeDraft_(rd)`, writes 6 cells back (subject + body required via `hr.col`; 4 metadata cells via `hr.colOrNull` for graceful pre-migration). T5 accepts `templateKeyOverride` param but ignores it — placeholder for T9 frontend dropdown override path. |
| apps-script/WebAppEndpoint.gs | modified (T5) | +227 radku. 9 new dispatch entries in `doPost` (between `sendEmail` and the unknown-action fallback). 9 new handler functions appended after `handleAssignLead_`: `handleListTemplates_`, `handleGetTemplate_`, `handleGetTemplateDraft_`, `handleGetTemplateHistory_`, `handleSaveTemplateDraft_`, `handleDiscardTemplateDraft_`, `handlePublishTemplate_`, `handleGetTemplateAnalytics_`, `handleRegenerateDraft_`. All use `{ ok: true/false, error: ... }` shape from Phase 2 KROK 4-6. All strip `_rowNum` from return objects. `handleSaveTemplateDraft_` validates `subject ≤ 500` chars, `body ≤ 50000` chars (returns `subject_too_long` / `body_too_long`). `handlePublishTemplate_` maps 3 publish-gate failures: `commit_message_too_short`, `no_draft`, `empty_draft_content`. `handleGetTemplate_` distinguishes `no_active_template` from generic errors. Token verification handled upstream in existing `doPost` `FRONTEND_API_SECRET` check — handlers don't repeat. |
| crm-frontend/src/types/templates.ts | new (T6) | 79 radku. Canonical TS types: `TemplateStatus` ('empty' \| 'draft' \| 'active' \| 'archived'), `EmailTemplate` (16 fields mirror `EMAIL_TEMPLATES_SHEET_HEADERS`), `TemplateAnalyticsTotals`, `TemplateAnalyticsEntry`, `RegenerateDraftResult`. Plus `DEFAULT_TEMPLATE_KEYS` const tuple (kept in sync with `EMAIL_TEMPLATE_DEFAULT_KEYS` in `apps-script/Config.gs`) and `TEMPLATE_KEY_LABELS` map for UI fallback display names ('Bez webu', 'Slabý web', etc.). |
| crm-frontend/src/lib/google/apps-script-writer.ts | modified (T6) | +317 radku. 9 new exported async wrappers + 9 Result interfaces: `listTemplates`, `getTemplate(key)`, `getTemplateDraft(key)`, `getTemplateHistory(key)`, `saveTemplateDraft(key, opts)`, `discardTemplateDraft(key)`, `publishTemplate(key, commitMessage)`, `getTemplateAnalytics`, `regenerateDraft(leadId, templateKey?)`. Same pattern as existing `generatePreview` / `sendEmail`: `SHEET_CONFIG.APPS_SCRIPT_URL` POST with `process.env.APPS_SCRIPT_SECRET` token, `{ ok }` AS response shape mapped to `{ success }`. Existing wrappers untouched. |
| crm-frontend/src/app/api/templates/route.ts | new (T6) | GET — proxies to `listTemplates` writer. 502 on AS upstream failure, 500 on unexpected. |
| crm-frontend/src/app/api/templates/[key]/route.ts | new (T6) | GET active template by key. 400 on missing key, 404 on `no_active_template`, 502 on AS, 500 on unexpected. Uses Next 16 async params (`{ params }: { params: Promise<{ key: string }> }`). |
| crm-frontend/src/app/api/templates/[key]/draft/route.ts | new (T6) | GET / PUT / DELETE for the single per-key draft. PUT validates `subject ≤ 500` and `body ≤ 50000` (matches `handleSaveTemplateDraft_` AS-side gate). 400 on JSON parse / length / `unknown_key`, 502 on other AS errors. |
| crm-frontend/src/app/api/templates/[key]/history/route.ts | new (T6) | GET — proxies to `getTemplateHistory`. Returns `{ history: [...] }`. |
| crm-frontend/src/app/api/templates/[key]/publish/route.ts | new (T6) | POST. Body `{ commitMessage }` required. Front-loads `commit_message_too_short` validation locally (≥ 5 chars) before AS call to fail fast. AS-side publish-gate failures (`no_draft`, `empty_draft_content`, `commit_message_too_short`) map to 400. |
| crm-frontend/src/app/api/analytics/templates/route.ts | new (T6) | GET — proxies to `getTemplateAnalytics`. Returns `{ analytics: [...] }` array of `TemplateAnalyticsEntry`. |
| crm-frontend/src/app/api/leads/[id]/regenerate-draft/route.ts | new (T6) | POST. Optional `{ templateKey }` body (placeholder for T9 manual override). 400 on `id.length < 3`, 404 on `lead_not_found`, 502 on other AS errors. |
| crm-frontend/src/app/settings/page.tsx | new (T7) | Settings landing page. 2 cards (Šablony emailů → `/settings/templates`, Analýza šablon → `/analytics/templates`). Lucide icons (Mail, BarChart3). Server component, statically prerendered. |
| crm-frontend/src/app/settings/templates/page.tsx | new (T7) | Listing page. Client component (`'use client'`). Fetches `/api/templates`, calls `groupByKey` to bucket templates by `template_key` → `{ active, draft, empty }`, seeds 5 placeholder groups from `DEFAULT_TEMPLATE_KEYS` so the page renders before bootstrap runs (cards show `status='empty'`). Stable order: default keys first, then any custom keys alphabetical. Loading state via `TemplatesPageSkeleton`, error state with "Zkusit znovu" retry button + `sonner` toast on fetch failure. Card click navigates to `/settings/templates/[key]` (T8). |
| crm-frontend/src/components/templates/template-card.tsx | new (T7) | Reusable card with `StatusBadge` (active = green pill `aktivní · v{n}`, draft = amber pill with pencil, empty = gray pill). Shows name in quotes when active, italic "připraveno k vytvoření" when empty. "Rozpracovaná verze" pill below when both active+draft coexist. CTA "Upravit" if active else "Vytvořit". Uses `font-mono` for the raw `template_key`. |
| crm-frontend/src/components/templates/templates-page-skeleton.tsx | new (T7) | 5 skeleton cards mimicking `TemplateCard` layout. Uses existing `<Skeleton>` shadcn primitive. |
| crm-frontend/src/components/layout/sidebar.tsx | modified (T7) | +2 lines. Added "Nastavení" nav item (Settings icon) at end of `navigation` array. No styling change — reuses existing nav item template (active state, hover, collapsed mode). |
| crm-frontend/src/lib/templates/sample-leads.ts | new (T8) | 3 sample lead fixtures (ALVITO instalatér Praha 9, Elektroservis Krátký Brno bez contact_name, Stěhování Praha-Sever) covering full T3 placeholder set. Sender block hard-coded to Sebastián's signature. T9 will swap to real qualified-leads dropdown. |
| crm-frontend/src/lib/templates/render-preview.ts | new (T8) | Client-side mirror of `apps-script/EmailTemplateStore.gs:renderTemplate_` + `buildPlaceholderValues_`. `KNOWN_PLACEHOLDERS` set for the 18 supported tokens. `humanizeServiceType` Czech approximation map (instalatér → instalatérské služby, etc., 12 entries). `renderPreview(input)` returns `{ subject, body, unknownPlaceholders }`. **Drift between this and AS = visible bug** — kept in sync manually for T8; T12 should add a snapshot test. Plus `PLACEHOLDER_LEGEND` array (18 entries with description + example) for the editor sidebar UI. |
| crm-frontend/src/components/templates/placeholder-legend.tsx | new (T8) | Collapsible (closed by default) panel listing 18 placeholders with click-to-copy `{name}` (writes to clipboard, shows Check icon for 1.2s). Top warning ribbon when `unknownPlaceholders` non-empty (template uses tokens not in known set → render as empty). |
| crm-frontend/src/components/templates/template-preview-pane.tsx | new (T8) | Right side of split-pane. Lead dropdown (3 sample leads), email-style preview card (Komu / Předmět / Tělo), `pre`-formatted body for whitespace preservation. `useMemo`s on render to avoid unnecessary work. |
| crm-frontend/src/components/templates/publish-dialog.tsx | new (T8) | shadcn Dialog. Required commit message ≥ 5 chars Textarea (publish button disabled until met). Maps AS error codes (`commit_message_too_short`, `no_draft`, `empty_draft_content`) to localized Czech messages. Loader2 spinner during POST. |
| crm-frontend/src/components/templates/history-drawer.tsx | new (T8) | shadcn Sheet (right side, max-w-xl). Fetches `/api/templates/[key]/history` on open via `useCallback`-wrapped `fetchHistory` + `useEffect` (avoids `react-you-might-not-need-an-effect` lint error from synchronous setState pattern). HistoryEntry sub-component with status badges (active/draft/archived/empty), expand-to-reveal subject + body in monospace. Czech locale date format. |
| crm-frontend/src/components/templates/template-editor.tsx | new (T8) | Main client component (~280 LOC). Split-pane layout (editor left, preview right, stack on mobile). Loads `/api/templates/[key]` (active) + `/api/templates/[key]/draft` in parallel, prefers draft if exists. Editor state vs `baseline` snapshot for `dirty` detection. Save Draft (PUT), Discard (DELETE with `confirm()` if existing draft), Publish (opens dialog). `beforeunload` listener warns on navigation with unsaved changes. AS error codes mapped to Czech toasts. After publish, baseline resets to new active and draft cleared. |
| crm-frontend/src/app/settings/templates/[key]/page.tsx | new (T8) | Server component shell using Next 16 async `params: Promise<{ key: string }>`. `generateMetadata` builds Czech page title (`{label} — Šablona | Autosmartweby CRM`). Renders `<TemplateEditor templateKey={key} />`. |
| apps-script/Menu.gs | modified | +1 — `.addItem('Setup email templates', 'setupEmailTemplates')` mezi `'Setup preview extension'` a `'Ensure lead IDs'` (adjacency reflects internal `setupEmailTemplates -> setupPreviewExtension` call). |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/30-task-records/B-13.md | new | Tento task record. |
| docs/11-change-log.md | regenerated | Auto z task records (`build-changelog.mjs`). |
| docs/29-task-registry.md | regenerated | Auto z task records (`build-task-registry.mjs`). |

## Contracts Changed

- **LEADS schema:** 4 nove sloupce (`email_template_key`, `email_template_version`, `email_template_id`, `email_segment_at_send`) pridane pres `setupPreviewExtension` (additive, idempotent migration). Existujici kod neovlivnen — sloupce jsou prazdne dokud T3 nezacne `composeDraft_` zapisovat.
- **Hidden sheet `_email_templates`:** novy, 16 sloupcu, schema definovane v `EMAIL_TEMPLATES_SHEET_HEADERS` (Config.gs). Single source of truth pro template content + versioning.
- **Template ID format:** `ASW-TPL-{ts_base36}-{rand4}` — mirror `generateLeadId_` patternu, immutable identifier per row.
- **Apps Script Web App (T5):** 9 additive doPost actions (`listTemplates`, `getTemplate`, `getTemplateDraft`, `getTemplateHistory`, `saveTemplateDraft`, `discardTemplateDraft`, `publishTemplate`, `getTemplateAnalytics`, `regenerateDraft`). Existing actions (`updateLead`, `assignLead`, `getPreview`, `generatePreview`, `sendEmail`) untouched. All use `{ ok: true/false }` shape from Phase 2 KROK 4-6 contract.
- **Next.js API (T6):** 7 additive routes proxying to AS Web App: `GET /api/templates`, `GET /api/templates/[key]`, `GET|PUT|DELETE /api/templates/[key]/draft`, `GET /api/templates/[key]/history`, `POST /api/templates/[key]/publish`, `GET /api/analytics/templates`, `POST /api/leads/[id]/regenerate-draft`. Existing routes (`/api/leads`, `/api/leads/[id]/*`, `/api/preview/render`, `/api/auth/*`, `/api/stats`) untouched. Build verified: 18 routes total (11 pre-existing + 7 new).

## Tests

| Test | Vysledek |
|------|----------|
| Code review (manual) | OK — zadne references na neexistujici funkce, schema konzistentni, idempotentni cesta verifikovana proti `PreviewStore.gs` patternu |
| Apps Script editor syntax check | not yet — clasp push az po PR merge per CLAUDE.md |
| Idempotence smoke (setupEmailTemplates 2x) | not yet runtime-tested — soucasti T1 deploy verification |
| T2 manual code review | OK — 10 CRUD functions appended cleanly, LockService pattern verified, version-bump + archive + placeholder-delete sequence in `publishTemplate_` walked through against bootstrap state |
| T3 manual code review | OK — `composeDraft_` rename to `composeDraftFallback_` is a pure rename (verified by diff: only the function name + JSDoc preamble changed, body identical). New dispatcher returns 6-field shape; all 3 direct call sites + 1 indirect (`computePreviewArtifacts_` → consumed by `processPreviewQueue` and `processPreviewForLead_`) capture all 4 metadata cols via `hr.colOrNull` guards. With `_email_templates` still bootstrap-empty post-T1, every draft path goes through fallback — no behavioural change in pilot until T4 publishes `no-website` v1. |
| T3.5+T4 manual code review | OK — Config.gs ordering verified: `LEGACY_ASSIGNEE_EMAIL_MAP` (311) → `ASSIGNEE_PROFILES` (337) → `DEFAULT_ASSIGNEE_PROFILE` (363) → derived `ASSIGNEE_NAMES` IIFE (374) → `getAssigneeProfile_` (392) → `DEFAULT_REPLY_TO_*` (400-401) → `ALLOWED_USERS = Object.keys(ASSIGNEE_NAMES)` (407). 20 functions in EmailTemplateStore.gs (4 T1 + 10 T2 + 3 T3 + 3 T4). PreviewPipeline.gs: zero remaining direct references to `ASSIGNEE_NAMES` in `composeDraft_` (`grep` confirms only mentions are comment + `getAssigneeProfile_` typeof guard). Race fix only adds preview_url to rd when previously empty (idempotent re-run safe). Migration script tested against TEST sheet inspection: 3 rows have `tomas@autosmartweb.cz` (1 mapping match) — actual blast on first run. All 4 historical legacy emails covered in `LEGACY_ASSIGNEE_EMAIL_MAP` per pre-flight inspection (797 empty + 3 tomas + 0 others). |
| T5 manual code review | OK — 14 dispatch lines (5 existing + 9 new), 14 handle* functions (5 existing + 9 new), all return `{ ok: true/false }` shape. EmailTemplateStore.gs now 23 functions (acceptance said 22 — see Decisions; `_emptyAnalyticsForAllActiveTemplates_` was in the spec body but not counted). `getTemplateAnalytics_` walks LEADS once, range fetch bounded to min/max needed columns, sort stable. `regenerateDraftForLead_` reuses existing `findRowByLeadId_(sheet, leadIdCol, lid)` (signature confirmed in Helpers.gs:241) and direct `getRange()` for row read since `readSingleRow_` doesn't exist in codebase. Required cols write via `hr.col` (throws on missing); metadata cols via `hr.colOrNull` guard. |
| T6 build + lint + B-stream tests | OK — `npm run lint`: 0 errors, 13 pre-existing warnings (none from new files). `npm run build`: 11.6s compile + TypeScript pass + 18 routes total (11 existing + 7 new). All 7 new routes registered as ƒ (dynamic). `npm run test:b03`: 13/13 pass. `npm run test:b04`: 16/16 pass. `npm run test:b05`: 42/42 pass. Zero regressions on existing endpoints. |
| T7 build + lint + B-stream tests | OK — `npm run lint`: 0 errors, 13 pre-existing warnings (none from new files). `npm run build`: 10.6s compile + TypeScript pass + 20 routes total (18 + 2 new: `/settings` and `/settings/templates`, both ○ statically prerendered — listing fetches client-side). `npm run test:b03`: 13/13 pass. `npm run test:b04`: 16/16 pass. `npm run test:b05`: 42/42 pass. |
| T8 build + lint + B-stream tests | OK after one fix — initial lint flagged `react-you-might-not-need-an-effect` "Calling setState synchronously within an effect can trigger cascading renders" in `history-drawer.tsx`; refactored to `useCallback`-wrapped `fetchHistory` + thin `useEffect` (matches existing codebase pattern in `lead-detail-drawer.tsx:249`). Final state: 0 errors, 13 pre-existing warnings. `npm run build`: 21 routes (20 + new `/settings/templates/[key]` ƒ). `test:b03` 13/13, `test:b04` 16/16, `test:b05` 42/42. |
| `npm run docs:check` | viz output v PR description |

## Output for Audit

T1 schema-only — zadny behavioural change pred runtime (`composeDraft_` nezmenen). Po `clasp push` + spusteni `Setup email templates` z menu v Apps Script editoru ocekavany state:
- `_email_templates` sheet existuje, hidden, 16 sloupcu, 5 radku (status='empty', version=0, template_key in EMAIL_TEMPLATE_DEFAULT_KEYS)
- LEADS ma 4 nove sloupce na konci (`email_template_key` ... `email_segment_at_send`), vsechny prazdne
- Druhe spusteni `setupEmailTemplates` musi nic nepridat (idempotence)
- `_asw_logs` ma INFO entries: `ensureEmailTemplatesSheet_ Created hidden sheet`, `bootstrapEmptyTemplates_ Seeded 5 empty template placeholders`, `setupEmailTemplates ...` summary

## Known Limits / Decisions

- `generateTemplateId_` pattern `ASW-TPL-{ts_base36}-{rand4}` mirror `generateLeadId_`, zadny collision check (akceptovany risk pri low-volume <100 publikaci/den; collisions by se navic projevily pri T2 publish flow, ne v T1 bootstrap kde id generuje hned po sobe a `Math.random` rand4 prefix zarucuje rozliseni).
- `created_by` fallback `'system'` kdyz `Session.getActiveUser().getEmail()` vrati prazdny string (time-driven context kompatibilita; T1 bootstrap se vola z menu takze normalne se naplni emailem operatora).
- Load order komentar `4/5` v souborovem headeru je informacni — Apps Script alphabetizuje runtime load (E < H < M < P), takze realne `EmailTemplateStore.gs` se nacita mezi `Config.gs` a `Helpers.gs`. Komentar 4/5 vs 4/8 v `PreviewStore.gs` neresi konflikt — oba jsou jen popisne.
- T1 nepouzil `setupPreviewExtension` ze `setupEmailTemplates` jako transakcni jednotku — pokud `ensureEmailTemplatesSheet_` selze, LEADS uz pribyly. Akceptovano: oba kroky idempotentni a re-run je bezpecny.
- **T4 residual:** `DEFAULT_REPLY_TO_EMAIL = 'sebastian@autosmartweb.cz'` a `DEFAULT_REPLY_TO_NAME = 'Sebastián Fridrich'` zustavaji na pre-T4 legacy hodnotach, protoze spec explicitne rekla `resolveSenderIdentity_ NETKAT` a tyto konstanty cte prave ta funkce. Dusledek: pri Reply-To pro UNASSIGNED leady (`assignee_email = ''`) se pouzije starsi domena `sebastian@autosmartweb.cz` namisto noveho kanonickeho `s.fridrich@autosmartweb.cz`. V praxi nizke riziko (frontend obvykle prirazuje assignee pred sendem). Backlog: doreseni v naslednem PR ktery sjednoti `DEFAULT_REPLY_TO_*` s `DEFAULT_ASSIGNEE_PROFILE` a aktualizuje `resolveSenderIdentity_` aby cetla `getAssigneeProfile_` misto `ASSIGNEE_NAMES + DEFAULT_REPLY_TO_*`.
- **T4 idempotence pozor:** `migrateLegacyAssigneeEmails_` re-run je bezpecny (zadne legacy keys uz neexistuji po prvnim spusteni). Ale `bootstrapNoWebsiteV1` po prvnim publish zustane no-op — re-publish vyzaduje rucne pres frontend UI (T6) nebo `discardTemplateDraft_` + `saveTemplateDraft_` + `publishTemplate_` z editoru.

## Next Dependency

| Task | Co potrebuje z B-13 |
|------|---------------------|
| T2 (template store CRUD) | DONE in this PR — see commit history. |
| T3 (composeDraft_ refactor + render) | DONE in this PR — see commit history. |
| T4 (bootstrap no-website v1 from approved copy) | DONE in this PR — see commit history. Run `migrateAndBootstrap` from menu after deploy. |
| T5 (doPost actions for template CRUD + analytics) | DONE in this PR — see commit history. |
| T6 (Next.js API layer + writer wrappers) | DONE in this PR — see commit history. |
| T7 (Settings landing + templates listing) | DONE in this PR — see commit history. |
| T8 (Template editor — `/settings/templates/[key]`) | DONE in this PR — see commit history. |
| T9 (Lead drawer template override dropdown) | Needs `regenerateDraft` to honor `templateKey` param (currently AS-side `regenerateDraftForLead_` accepts but ignores). Requires `composeDraft_` refactor to accept template injection. |
| T10 (Template history view) | Renders `/api/templates/[key]/history` — timeline UI for all versions including archived. |
| T11 (Analytics dashboard `/analytics/templates`) | Visualizes `/api/analytics/templates` — sent / replied / won per (key, version) + drill-down per segment. |
