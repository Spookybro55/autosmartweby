# Task Record: DP-003

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | DP-003 |
| **Title** | Make `scripts/clasp-deploy.sh` PROD swap atomic via `trap` on EXIT/INT/TERM |
| **Owner** | Stream B |
| **Status** | code-complete |
| **Date** | 2026-04-30 |
| **Stream** | B |
| **Agent Role** | bug-hunter |
| **Track** | A |
| **Plan** | - |
| **Autonomous run** | yes |

## Scope

DP-003 (P1) per `docs/audits/FINDINGS.md:32`: the prod deploy block in `scripts/clasp-deploy.sh` swapped `.clasp.json` from TEST to PROD config, ran `clasp push`, then restored TEST — but the restore was a sequential-execution best-effort. If the script was interrupted between the swap (cp PROD → ACTIVE) and the explicit restore (cp BACKUP → ACTIVE) — Ctrl+C, kill, terminal panic, OOM — `.clasp.json` would linger in PROD state. A subsequent innocent `./scripts/clasp-deploy.sh test` would then `clasp push` to PROD silently (TEST-mode skips the confirmation prompt; `set -e` doesn't help with signal aborts). This is the worst-case "TEST push goes to PROD" data-corruption scenario flagged by GOTCHA-001 (clasp swap risk).

This run implements the recommendation directly from the FINDINGS row: **register a bash `trap` before the swap so the restore fires regardless of how execution exits.** The trap handler is idempotent — checks if `.clasp.json.bak` exists before restoring, so the explicit happy-path restore + the trap fire on EXIT compose without double-cp. Out of scope: alternative recommendations (`clasp push -P <prod-config>` and `clasp-deploy.sh test` hash-check) — those would be DP-003 *deluxe*, not the named fix.

## Code Changes

| Soubor | Typ změny | Popis |
|--------|-----------|-------|
| `scripts/clasp-deploy.sh` | modified | (1) Added `restore_clasp_config()` function (top-level, lines 21-31) — idempotent restore wired to trap. (2) Added test-mode early-return guard (lines 36-38) — `CLASP_DEPLOY_TEST_MODE=1 source clasp-deploy.sh` loads helpers without running deploy logic, used by regression test. (3) In prod case block: registered `trap restore_clasp_config EXIT INT TERM` **before** the cp swap (line 88). (4) Replaced inline cp-restore at the post-push happy path with a call to `restore_clasp_config` (line 99) — idempotent with the trap. (5) Comments updated to explain DP-003 trap rationale. Net diff: +24 / -5 LOC. |
| `scripts/tests/clasp-deploy-trap.test.mjs` | new | Node `node:test` regression test (3 cases). (a) **Abort mid-deploy:** swap → set trap → `exit 42` → assert `.clasp.json` restored to TEST content + `.clasp.json.bak` cleaned up. (b) **Idempotence:** swap → set trap → explicit `restore_clasp_config` → exit 0 → trap fires on EXIT → assert no double-restore corruption. (c) **Test-mode guard:** sourcing with `CLASP_DEPLOY_TEST_MODE=1` exposes helpers without running deploy logic. Test discovers `clasp-deploy.sh` via `import.meta.url` so it's portable. ~110 LOC. |
| `package.json` | modified | Added `"test:clasp-deploy-trap": "node --test scripts/tests/clasp-deploy-trap.test.mjs"` script. +1 LOC. |

## Docs Updated

| Dokument | Typ změny | Proč |
|----------|-----------|------|
| `docs/audits/FINDINGS.md` | modified | DP-003 row Status flipped from `Open` to `**Resolved** at HEAD (verified 2026-04-30)`. Evidence cell strikes through stale line range; recommendation cell unchanged. Notes which of the three FINDINGS recommendations was implemented (trap) and explicitly tags the other two as out-of-scope. |
| `docs/30-task-records/DP-003-clasp-deploy-trap-restore.md` | new | This task record. Filename matches branch tail per `scripts/agent/validate-task-record.mjs`; the `Task ID` metadata field is `DP-003` to match the FINDINGS row. |
| `docs/agents/QUEUE.md` | modified | Auto-regenerated by `node scripts/agent/triage.mjs` after the FINDINGS status change. DP-003 drops out of the `<!-- triage:ready-start -->` ready table. |
| `docs/agents/RUN-LOG.md` | modified | Continued from DP-001 closure entry. Added DP-003 trace: `claim → classify → dispatch → fix → test → fix → self-review → cross-review → dod-check`. Plus QFH-0005 `block` step (logged separately, not blocking DP-003). |
| `docs/agents/QUESTIONS-FOR-HUMAN.md` | modified | Added QFH-0005 (post-merge closure routine SKILL gap) per Sebastián's instruction this session. Bundled into this PR (option (e.1) from QFH-0005 itself). |
| `docs/11-change-log.md` | regenerated | Auto from task records. |
| `docs/29-task-registry.md` | regenerated | Auto from task records. |

**Stream B mandatory canonical docs** (`docs/20`, `docs/22`, `docs/26`, `docs/27` per `docs/13-doc-update-rules.md`) are **NOT updated** in this task — same rationale as DP-001 (`docs/30-task-records/DP-001-prod-sheet-id-test-fixture.md`). Pre-fix grep verified neither `docs/22-technical-architecture.md` nor `docs/27-infrastructure-storage.md` references `clasp-deploy.sh` swap behavior, so no stale content needs reconciling. The fix preserves the script's external behavior (TEST-only deploy still TEST-only; PROD deploy still requires confirmation) and only adds atomicity, which is internal robustness — no documented contract changes. Updating `apps-script/README.md` deploy section is DP-012 / DP-013 scope, not DP-003.

## Contracts Changed

- **No external behavior change.** Same CLI: `./scripts/clasp-deploy.sh test` and `./scripts/clasp-deploy.sh prod`. Same prompts, same exit codes for happy-path and push-failure. The trap only changes interrupt behavior (Ctrl+C / kill / panic): `.clasp.json` is now atomically restored.
- **New test-mode source contract:** `CLASP_DEPLOY_TEST_MODE=1 source scripts/clasp-deploy.sh` loads helper functions and constants without running deploy logic. Returns `0` (or `exit 0` if invoked rather than sourced — defensive). Intended for tests only; not a public-facing contract.
- **No new env vars consumed in production code path.** `CLASP_DEPLOY_TEST_MODE` is test-only; production invocation is unchanged.

## Tests

| Test | Výsledek |
|------|----------|
| `npm run test:clasp-deploy-trap` | **OK — 3 pass / 0 fail / 0 skip / 573.69 ms** |
| → case 1: trap restores after `exit 42` mid-deploy | OK (239.6 ms) — `.clasp.json` matches TEST content; `.clasp.json.bak` removed |
| → case 2: idempotent (explicit restore + trap fire) | OK (202.9 ms) — no double-cp corruption |
| → case 3: test-mode source does not run deploy logic | OK (70.8 ms) — function defined, no `Usage:` output |
| `bash -n scripts/clasp-deploy.sh` (syntax check) | OK |
| `node scripts/docs/check-doc-sync.mjs` | OK — 43 pass / 0 warn / 0 fail |
| `node scripts/agent/validate-task-record.mjs --pr-branch agent/bug-hunter/DP-003-clasp-deploy-trap-restore` | OK — PASSED |
| `npx tsc --noEmit` (crm-frontend) | N/A — no frontend src files touched |
| `npm run build` (crm-frontend) | N/A — no frontend src files touched |

**Test contract verification (Bug Hunter SKILL §3 step 4):** without the fix, the test would fail at: (a) the `type restore_clasp_config` check (function not defined → exit 99), AND (b) test-mode guard absence (sourcing would run deploy logic and print `Usage:`). With the fix all three cases pass. The trap-restore behavior under `exit 42` exercises the actual bash trap semantics against the actual `restore_clasp_config` definition in the script, so a regression that removes the trap line OR breaks the function will fail case 1.

## Output for Audit

After this PR is merged:

- `scripts/clasp-deploy.sh` prod deploy is atomic w.r.t. interrupts. Ctrl+C / kill / panic between swap and restore can no longer leave `.clasp.json` in PROD state.
- `scripts/tests/clasp-deploy-trap.test.mjs` is part of the test corpus; future PRs that touch `clasp-deploy.sh` get caught at PR-time if they regress the trap.
- `docs/audits/FINDINGS.md` row DP-003 shows **Resolved** with verification timestamp + the trap implementation summary. Two alternative recommendations from the original FINDINGS row are explicitly tagged out-of-scope so future readers know the partial vs full mitigation surface.
- `package.json` `test:clasp-deploy-trap` script gives operators a one-command verification before any future prod deploy refactor.

## Known Limits

- **Trap fires on EXIT, not all signal classes.** `trap ... EXIT INT TERM` covers the main interrupt scenarios (Ctrl+C, kill -TERM, kill default = TERM, normal exit). It does **not** fire on `kill -9` (SIGKILL — process gets immediately killed, no userland code runs) or hardware/OS panic / power loss. For those scenarios the only mitigation is `clasp-deploy.sh test` adding a hash-check against `git show HEAD:apps-script/.clasp.json` before push (one of the original FINDINGS recommendations) — explicitly **out of scope** for this fix.
- **No PROD-deploy hash check.** A user could still manually edit `.clasp.json` to PROD config and run `./scripts/clasp-deploy.sh test`. The trap doesn't help there (no swap happened, no backup created). Defense-in-depth via hash check is a separate task.
- **Alternative `clasp push -P <prod-config>` not adopted.** Per FINDINGS recommendation, clasp 2.x supports `-P` flag to point at a different config without file swap. That would eliminate the swap entirely and is structurally safer than the trap approach. **Not adopted here** because: (a) it's a bigger refactor (rewrites the prod deploy flow), (b) requires confirming the installed clasp version supports `-P` (CI doesn't pin clasp version), (c) the trap is the FINDINGS-row's first-listed recommendation and matches the minimal-fix bias of Track A. If Sebastián wants the `-P` rewrite later, open a follow-up DP-003-deluxe.
- **No real-clasp integration test.** The regression test mocks the swap and abort but doesn't exercise an actual `clasp push`. Doing so would require credentials and a live Apps Script project — unsafe for CI. The test verifies the bash-level invariant (atomicity) which is the actual risk surface.
- **No update to `apps-script/README.md` deploy section.** That doc is stale per DP-012 / DP-013 (separate FINDINGS rows). This PR doesn't touch it to avoid scope creep.

## Next Dependency

| Task | Co potřebuje z DP-003 |
|------|----------------------|
| **DP-008** (clasp deploy versioning) | Independent. Adding `clasp deploy --description <sha>` after `clasp push` is a separate concern; the trap protects whatever `clasp push` flavor is in use. |
| **DP-012 / DP-013** (`apps-script/README.md` deploy section refresh) | Independent. README rewrite would document the trap as part of "how prod deploys are made safe" once it's part of the canonical narrative. |
| **DP-002 / SEC-002** (clasp.json gitignore + scriptId rotation) | Independent. The trap protects `.clasp.json` swap regardless of where the file is sourced from. |
| **DP-003-deluxe** (hypothetical follow-up) | If Sebastián decides the trap isn't enough (e.g., wants `clasp push -P` rewrite or pre-push hash-check), open new task. This PR is the named-FINDINGS fix; doesn't preclude further hardening. |

## DoD Checklist

> Required for agent-driven tasks (Track A or Track B with `Agent Role` ≠ `human`). Optional for human tasks.

### Code Done

- [x] `npx tsc --noEmit` (frontend, if touched): N/A — no frontend src files touched
- [x] `npm run build` (frontend, if touched): N/A — no frontend src files touched
- [x] No secrets in diff — fake script IDs in test (`FAKE_TEST_SCRIPT_ID`, `FAKE_PROD_SCRIPT_ID`) explicitly non-sensitive; no real script IDs / Sheet IDs / tokens
- [x] No regressions — `clasp-deploy.sh -n` syntax check OK; new test 3/3; no other tests touch this script

### Documentation Done

- [x] Affected docs identified per `docs/13-doc-update-rules.md` stream mapping — Stream B mandatory canonical docs reviewed; pre-fix grep confirmed no stale references; documented rationale in `## Docs Updated`
- [x] Affected docs updated — `FINDINGS.md` (DP-003 row), `RUN-LOG.md`, `QUESTIONS-FOR-HUMAN.md` (QFH-0005), `QUEUE.md` (auto-regen), task record
- [x] `docs/11-change-log.md` regenerated (`build-changelog.mjs`)
- [x] `docs/29-task-registry.md` regenerated (`build-task-registry.mjs`)
- [x] Control tower (`docs/09-project-control-tower.md`) checked — DP-003 not tracked there
- [x] Route mapa (`docs/12-route-and-surface-map.md`) checked — N/A, no route change

### Test Done

- [x] Tests pass — `npm run test:clasp-deploy-trap` 3/3 pass
- [x] `npm run build` verified — N/A (no frontend code touched)
- [x] `node scripts/docs/check-doc-sync.mjs`: 0 fail

### Agent Done (Track A only)

- [x] Diff size < 500 LOC (Track A hard limit) — code +25 LOC, test ~110 LOC, all under limit
- [x] Secret scan clean — `grep -nE '\b1[A-Za-z0-9_-]{40,}\b' scripts/clasp-deploy.sh scripts/tests/clasp-deploy-trap.test.mjs` → 0 hits; `FAKE_*_SCRIPT_ID` in test are obvious non-sensitive markers
- [x] Self-review pass — re-read full diff with fresh eyes; 0 issues found
- [x] Cross-role review pass — Tech Lead read whole diff before PR open; sign-off in RUN-LOG
- [x] `docs/agents/QUEUE.md` updated — auto-regen via `triage.mjs`; DP-003 drops out of ready table
- [x] `docs/agents/RUN-LOG.md` appended — full trace + DP-001 `complete` bundled per QFH-0005 (d.2) policy
- [x] **No `apps-script/.clasp.json` change** — verified via `git diff` (only `scripts/clasp-deploy.sh` is touched, which orchestrates `.clasp.json` swap but doesn't edit `.clasp.json` content)
- [x] No `.env*` change
- [x] No `docs/archive/` change
- [x] Branch convention: `agent/bug-hunter/DP-003-clasp-deploy-trap-restore` ✓
