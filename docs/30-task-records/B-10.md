# Task Record: B-10

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | B-10 |
| **Title** | Phase 2 KROK 5 — auto trigger lands READY_FOR_REVIEW directly |
| **Owner** | Stream B |
| **Status** | done |
| **Date** | 2026-04-26 |
| **Stream** | B |

## Scope

Phase 2 KROK 5 narovnava `processPreviewQueue` (15-min cron) tak, aby leady BEZ preview_slug skoncily end-to-end na `READY_FOR_REVIEW` v ramci jednoho run. Templaty jsou staticke na `autosmartweb.cz`, neexistuje runtime webhook → KROK 5 obchazi B-04 webhook block, zapisuje do `_previews` (Sheets-backed storage z KROK 2) + LEADS row a posuva preview_stage rovnou na READY_FOR_REVIEW.

Webhook code (B-05 cesta) zachovan s DEPRECATED bannerem — B-05 testy stale beze zmeny (42/42 PASS) protoze beziu izolovane unit-test mode.

Sjednoceni s KROK 4 (B-09): novy helper `resolvePreviewUrl_(slug, allowEmpty)` — single source of truth pro preview URL resolution (honors `PUBLIC_BASE_URL` Script Property pro staging override). Replace 3 inline duplicates v `persistPreviewArtifacts_`, `processPreviewForLead_`, novy queue write.

KROK 5 NEMENI B-04 endpoint, NEMENI B-01 brief, NEMENI B-05 webhook block (jen deprecation banner v komentari).

## Code Changes

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| apps-script/PreviewPipeline.gs | modified | `processPreviewQueue` (cca radky 938-1010): step 2 brief lands `preview_stage = READY_FOR_REVIEW` (ne `BRIEF_READY`). Po `_previews` upsert pridan write-back: `preview_url + preview_generated_at + clear preview_error` na LEADS row. Webhook block preserved netknuty + DEPRECATED banner. `eligibleStages` unchanged (`brief_ready` legacy migration path). Novy helper `resolvePreviewUrl_(slug, allowEmpty)` — honors `PUBLIC_BASE_URL` SP, defaults `https://autosmartweb.cz/preview/<slug>`. `processPreviewForLead_` (KROK 4) refactor na `resolvePreviewUrl_(slug, false)` — sjednoceno. |
| crm-frontend/src/app/api/preview/render/route.ts | modified | Header doc DEPRECATED banner + odůvodnění (route zachovana pro backward compat — validation + cache invalidate, ale Phase 2 ji nepouziva). |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/30-task-records/B-10.md | new | Tento task record. |

## Contracts Changed

- **`processPreviewQueue` lifecycle:** zmena state transition po brief build z `BRIEF_READY` na `READY_FOR_REVIEW` — operator videti preview ihned po cron run, bez webhook prostredniku.
- **`PUBLIC_BASE_URL` Script Property:** novy optional config — pokud set, override default `autosmartweb.cz` (pro staging). Bez SP → default zustava `https://autosmartweb.cz/preview/<slug>`.
- **B-05 webhook contract:** beze zmeny — DEPRECATED ale nadale platny (`ENABLE_WEBHOOK=true` deployments dál fungují).
- **Legacy `BRIEF_READY` rows:** v `eligibleStages` zachovany jako migration path — cron je posune na `READY_FOR_REVIEW`.

## Tests

| Test | Vysledek |
|------|----------|
| `npm run lint` | OK (0 errors, 14 pre-existing warnings) |
| `npx tsc --noEmit` | OK |
| `npm run build` | OK — 15 routes |
| `npm run test:b03` | OK — 13/13 PASS |
| `npm run test:b04` | OK — 16/16 PASS |
| `npm run test:b05` | OK — **42/42 PASS** (webhook block nezmenen, izolovane unit testy intakt) |
| `npm run docs:check` | OK |

## Output for Audit

- PR #71 (recovery for original #69, ktery byl auto-closed po `--delete-branch` na PR #68 base) squash merge `48c18ed`.
- 15-min cron pick up qualified lead bez preview_slug → po 1 run preview_stage=READY_FOR_REVIEW + preview_url + `_previews` row.
- Legacy lead s preview_stage=BRIEF_READY → cron lift forward na READY_FOR_REVIEW (migration path).
- Lead s preview_stage=APPROVED → cron skip (nadale mimo eligibleStages).
- B-05 testy zustanou izolovane PASS — webhook deprecation neporusi unit test contracts.

## Known Limits

- Webhook block (`ENABLE_WEBHOOK=true` cesta) zachovan — duplicita kodu pro backward compat. Backlog: po sunset B-05 webhook v produkci, smazat block + deprecate B-05 testy.
- `BRIEF_READY` legacy stage v `eligibleStages` — pokud se objevi pre-Phase 2 data v tomto stavu, budou znovu zpracovana cronem. V produkci `DRY_RUN=true` byl default, takze takova data prakticky nevznikla.
- `PUBLIC_BASE_URL` SP neni dokumentovany v PILOT-ENV-VARS.md — staging override je chytrejsi pattern, ale momentalne nikde nepouzity.
- Manual smoke test deferred do KROK 9 joint deploy cycle (per Sebastian's deploy budget).

## Next Dependency

| Task | Co potrebuje z B-10 |
|------|---------------------|
| B-11 (KROK 6 email z CRM) | `READY_FOR_REVIEW` jako gate pre frontend send (alignment s KROK 4/5 flow). |
| Backlog | Sunset B-05 webhook block (mazat ENABLE_WEBHOOK cestu po stable Phase 2 produkci). Document `PUBLIC_BASE_URL` SP. |
