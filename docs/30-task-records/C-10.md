# Task Record: C-10

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | C-10 |
| **Title** | Automation performance report — SPEC-only kontrakt pro funnel / operational / quality metriky cele automatizace |
| **Owner** | Claude |
| **Status** | done |
| **Date** | 2026-04-21 |
| **Stream** | C |
| **Dependencies** | **A-09** (ingest quality report — jediny existujici report artefakt, `_ingest_reports` 41-field schema, bottleneck detection algoritmus, `summary_status` enum, per-`source_job_id` grain, fail_reason_breakdown pattern — C-10 rozsiruje report pattern na cely funnel mimo ingest sub-stage). **CS1** (lead lifecycle state machine — 18 canonical stavu je zdroj funnel dim A stageu F2-F10; CS1 terminal states #6 DISQUALIFIED / #15 REPLIED / #16 BOUNCED / #17 UNSUBSCRIBED jsou zdroj outcome dim C — ortogonalni dimenze nad funnelem). **CS2** (workflow orchestrator — `cs2_run_id` je klic pro G3 per-run grain, post-CS2-run hook je primarni trigger point). **CS3** (reliability & idempotency — `_asw_dead_letters` schema + `failure_class` enum zdroj dead_letter_* operational metrik; retry matice zdroj retry_count / retry_success_rate; LockService pattern dedeny pro budouci runtime). **C-04** (sendability gate — outcomes AUTO_SEND_ALLOWED / MANUAL_REVIEW_REQUIRED / SEND_BLOCKED zdroj manual_review_required_count / compliance_hard_stop metrik + blocking_reason_breakdown; gate latency pres `_asw_logs` timestamps). **C-05** (outbound queue — `_asw_outbound_queue` 32 fields + `status` enum jsou zdroj queue dim B (5 statusu QUEUED/SENDING/SENT/FAILED/CANCELLED — ORTOGONALNI dimenze nad funnelem, NE funnel stages) + queue_latency_avg_ms + send_success_rate dim B interna). **C-06** (provider abstraction — `NormalizedSendResponse` + `NormalizedSendErrorClass` zdroj provider_status breakdown + send_latency metrik). **C-07** (inbound event ingest — `_asw_inbound_events` zdroj outcome dim C events (reply / bounce / unsubscribe — ORTOGONALNI dimenze nad funnelem) + cross-dim rates bounce_yield / reply_yield / unsubscribe_yield (B→C) + quality aliasy bounce_rate / reply_rate / unsubscribe_rate + `reply_class` / `bounce_class` breakdowns). **C-08** (follow-up engine — 3-stage sequence zdroj followup_yield_rate + per-stage sent counts + stop_reason breakdown). **C-09** (exception queue — `_asw_exceptions` 24 fields zdroj review_queue_load / review_sla_compliance_rate / exception_rate metrik + exception_type breakdown + retry_chain_depth). Puvodni task brief uvadel `A-09, C-01 az C-09`, ale v repo source-of-truth `C-01.md` / `C-02.md` / `C-03.md` NEEXISTUJI (`C2.md`/`C3.md`/`C4.md` jsou governance hardening — unrelated; `CS1`/`CS2`/`CS3` jsou foundational SPEC tasky). **Dependency narrowing na `A-09, CS1, CS2, CS3, C-04..C-09`** je explicitne dokumentovano v docs/24 sekce uvod C-10 + tento task record, odsouhlaseno jako jediny set tasku, ktery dava smysl jako upstream metric-source prerekvizity. |

## Scope

Formalizuje **authoritative specifikaci** pro reportovaci vrstvu nad celou automatizaci Autosmartweby — od INGEST (A-01..A-10) pres PREVIEW/OUTREACH (B-01..B-05, C-04..C-06) po INBOUND/LIFECYCLE (C-07, CS1) a RELIABILITY (CS3, C-09). Predmetem SPEC je **jak automatizace cislem odpovi na otazky** typu "kolik leadu dnes proslo do BRIEF_READY?", "jaka je reply rate za minuly tyden?", "kde se zastavuje funnel?", "splnujeme SLA pro review queue?", "kolik exceptions vznikli na 1000 poslanych mailu?".

Scope je **SPEC-only** — neimplementuje runtime report worker, cron trigger, `buildPerformanceReport_()` helper, `_asw_perf_reports` sheet creation, Config.gs zapisy, frontend dashboard UI, alerting integraci (email/Slack notifications), BI export, ani `apps-script/PerfReport.gs` kod. Vsechny nove artefakty jsou oznacene **PROPOSED FOR C-10** a budou materializovany implementacnim taskem.

Task dodava:
- **Dependency narrowing `A-09, C-01 az C-09` → `A-09, CS1, CS2, CS3, C-04..C-09`:** explicitni dokumentace proc (C-01/C-02/C-03 neexistuji, C2/C3/C4 jsou governance unrelated, CS1/CS2/CS3 jsou foundational SPEC prerekvizity). Uvod docs/24 C-10 sekce + tento task record.
- **Reporting grain taxonomy:** 5 grains (G1 per-job pro ingest; G2 per-day pro operational rhythms; G3 per-run pro CS2 orchestrator visibility; G4 per-stage pro funnel analysis; G5 per-segment pro business slice) s invariants "jedna zprava = jeden grain" a "grains jsou ortogonalni ne nested".
- **3-dimension reporting taxonomy (tvrda separace):** **Dim A — Funnel** (10 canonical progression stages F1-F10 mapovane na CS1 az #12 OUTREACH_READY; invariant monotonic progression bez back-edge). **Dim B — Queue** (5 operational statusu QUEUED/SENDING/SENT/FAILED/CANCELLED per C-05 — ORTOGONALNI nad queue radkem, NEJSOU funnel stages). **Dim C — Outcome** (4 terminalni outcomes DISQUALIFIED/REPLIED/BOUNCED/UNSUBSCRIBED mapovane na CS1 terminaly resp. C-07 event_type — ortogonalni end-of-lifecycle metrics, NEJSOU funnel stages). Tvrde oddeleni invariantem: funnel stage ≠ lifecycle state ≠ queue status ≠ outcome ≠ review flag ≠ alert state. Zakazane kolapsy: NIKDY "email_queued funnel stage" (= queue_queued_count v dim B); NIKDY "replied funnel stage" (= outcome_replied_count v dim C); NIKDY mixing queue statusu / outcomes do funnel taxonomy.
- **Funnel metrics (dim A):** 10 stage counts (F1..F10) + 9 funnel-internal conversion rates (conv_f1_to_f2 .. conv_f9_to_f10) + `funnel_yield_to_outreach_ready` (F10/F1) + drop-off counts (worst-stage by rate + worst-absolute-count).
- **Queue metrics (dim B):** queue_queued/sending/sent/failed/cancelled_count + queue_status_breakdown_json + send_success_rate (dim B interna: sent/(sent+failed)) + queue_latency_avg_ms.
- **Outcome metrics (dim C):** outcome_disqualified_count + outcome_replied_count + outcome_bounced_count + outcome_unsubscribed_count + reply_class/bounce_class/unsubscribe_source breakdowns.
- **Cross-dimension rates (Blok D):** send_yield (A→B: queue_sent / f10_outreach_ready), reply_yield / bounce_yield / unsubscribe_yield (B→C: outcome_* / queue_sent_count), delivery_yield (A+B: (sent − bounced) / f10_outreach_ready).
- **Review load (Blok E, ortogonalni):** blocked_by_sendability_count + review_queue_load_count + manual_review_entered_count + sequence_followup_reach_count.
- **Operational metrics:** processing time stats (avg_ms / p50_ms / p95_ms / max_ms per step pres `_asw_logs` consecutive timestamps), fail_rate (fail / total per step), retry_count + retry_success_rate (CS3 retry matrix), dead_letter_count + dead_letter_rate (CS3 `_asw_dead_letters`), review_queue_load (C-09 open exceptions snapshot), review_sla_compliance_rate (resolved_on_time / total_resolved per SLA tier), stale_pending_count (P4 > 14 dni pending), queue_latency_avg_ms (C-05 queued_at → sent_at), exception_rate (C-09 new exceptions per 1000 leadu per period).
- **Quality metrics:** bounce_rate (C-07 BOUNCE events / sent), hard_bounce_rate (`bounce_class=HARD` / sent), soft_bounce_rate (`bounce_class=SOFT` / sent), reply_rate (C-07 REPLY / sent), positive_reply_rate (`reply_class=POSITIVE` / REPLY), unclear_reply_rate (`reply_class=UNCLASSIFIED` / REPLY), unsubscribe_rate (C-07 UNSUBSCRIBE / sent), followup_yield_rate (C-08 follow-up stages generated response vs initial-only), preview_approval_rate (PREVIEW_READY_FOR_REVIEW → APPROVED / total reviewed), exception_rate_per_stage (C-09 exceptions / stage throughput), compliance_hard_stop_rate (C-04 `compliance_hard_stop` + B7/B8 blocked / total evaluated).
- **Metric definition contract template:** kazda metrika ma kontrakt (`metric_id`, `name`, `grain`, `unit`, `formula`, `source_fields[]`, `null_handling`, `min_sample_size`, `value_range`, `baseline_ref`, `interpretation`, `alert_enabled`, `warning_threshold`, `critical_threshold`, `threshold_type`) — sekce 8 + 2 plne priklady (bounce_rate, review_queue_load).
- **Alert threshold model:** WARNING / CRITICAL severity, 3 threshold typy (ABS absolute, REL relative vs baseline, COMBO kombinovany), min_sample invariant (pod min_sample = `summary_status=INCOMPLETE` misto alert), baseline reference pattern (previous_day / previous_week / rolling_7d / rolling_30d / absolute_number).
- **Bottleneck detection 3-lens algoritmus:** funnel lens (najit stage F_n kde conv_rate(F_n → F_{n+1}) < baseline - threshold; weight by absolute drop-off count), latency lens (najit step s p95_ms > baseline * multiplier; weight by throughput), review lens (najit priority tier s SLA compliance < threshold; weight by open count). Priority tiebreaker: funnel > latency > review pri equal severity; alphabetical stage_id pri equal within lens.
- **Report schema `_asw_perf_reports`:** 67-field sheet (append-only) v blokove strukture podle dimenze: metadata (8) + Blok A funnel/dim A (14: 10 stage counts + 9 conv rates + funnel_yield + drop-off 3) + Blok B queue/dim B (8: 5 statusy + breakdown_json + send_success_rate + queue_latency) + Blok C outcome/dim C (4: disqualified/replied/bounced/unsubscribed counts) + Blok D cross-dim rates (5: send_yield / reply_yield / bounce_yield / unsubscribe_yield / delivery_yield) + Blok E review load (4: blocked/review_queue/manual_review_entered/sequence_followup_reach) + operational (9) + quality aliases (8) + synthetic summary (7). Per-field VERIFIED / INFERRED / PROPOSED label.
- **Sample report:** full realisticky G2 per-day report pro 2026-04-19 s 5 jobs, AT_RISK status, 4 warnings + 3 criticals + bottleneck detekce (primary lens=funnel, stage F5→F6 preview approval rate 62% vs baseline 85%).
- **Comparison rules per grain:** G1 job-vs-job same portal/segment; G2 day-vs-day (weekday-matching DOW grouping); G3 run-vs-run same step; G4 stage-vs-stage cross-day rolling; G5 segment-vs-segment within same day. Outlier handling: >3 stddev excluded pres `PERF_REPORT_OUTLIER_EXCLUDE_STDDEV` Script Property.
- **Auditability:** 5 `_asw_logs` event types (`performance_report_started` / `performance_report_generated` / `performance_report_failed` / `performance_alert_threshold_crossed` / `performance_bottleneck_detected`) + cross-ref graph (`_ingest_reports` A-09 ↔ `_asw_perf_reports` C-10 ↔ source tables LEADS/queue/inbound_events/exceptions/dead_letters ↔ `_asw_logs`) + observability query patterns bez dashboard UI.
- **Known limitations:** per-layer dependency completeness flags (`data_completeness_flags_json` structure) — ze pokud C-05/C-06/C-07/C-09 nejsou runtime, report ma null values + `INCOMPLETE` summary_status + data_completeness_flags_json audit trail (proc null).
- **Handoff tabulka (12 radku):** A-09 / CS1 / CS2 / CS3 / C-04 / C-05 / C-06 / C-07 / C-08 / C-09 / future dashboard / future alerting / future BI-export / future runtime-worker. Per-row "jak C-10 konzumuje" + "jak C-10 prispiva".
- **Non-goals (18 polozek):** explicit — runtime worker, cron, dashboard UI, alerting integration, BI export, novy canonical CS1 state, mutace existing schema, frontend charts, real-time metrics, per-operator attribution, cost metrics, SEO/marketing KPIs, CS3 retry matrix mutace, A/B testing framework, predictive analytics, AI-based anomaly detection, per-recipient drill-down, multi-tenant reporting.
- **Acceptance checklist (23 polozek):** vsechny checked.
- **PROPOSED vs INFERRED vs VERIFIED label summary** (sekce 19).

**A-09 kompatibilita:**
- A-09 `_ingest_reports` je NEZAVISLE report artefakt (ingest-only, per-job grain). C-10 `_asw_perf_reports` je **rozsireny** report pokryvajici cely funnel (vsech 14 F-stages, operational + quality). A-09 je **upstream zdroj** pro F1-F4 (raw / imported / normalization_error / duplicate counts); C-10 konzumuje `duplicate_count`, `raw_count`, `imported_count`, `bottleneck_stage` z posledniho A-09 zaznamu matchujiciho `source_job_id`.
- Report pattern (summary_status enum, append-only, full JSON payload do `_asw_logs`, derived rates, fail_reason_breakdown) je **dedden** z A-09 a rozsiren. A-09 G1 per-job je de facto **subset** C-10 G1 per-job (ingest section).
- C-10 **nemutuje** A-09 `_ingest_reports` schema. Zadne pridavani sloupcu. A-09 zustava authoritative pro ingest-only reports.

**CS1 kompatibilita:**
- CS1 18-state lifecycle je **zdroj pravdy** pro funnel dim A stages F2-F10 (IMPORTED → WEB_CHECKED → QUALIFIED → IN_PIPELINE → PREVIEW_PENDING → PREVIEW_READY_FOR_REVIEW → BRIEF_READY → DRAFT_READY → … → OUTREACH_READY #12). Funnel dim A konci na F10=OUTREACH_READY; queue a outcome jsou separatni dimenze.
- CS1 terminal stavy DISQUALIFIED (#6), REPLIED (#15), BOUNCED (#16), UNSUBSCRIBED (#17) jsou zdroj **outcome dim C** (separatni dimenze od funnel dim A, NE drop-off "stages"). CS1 review stavy REVIEW_REQUIRED (#7), PREVIEW_READY_FOR_REVIEW (#10), FAILED (#18) jsou **review flags** (separatni dimenze v Bloku E).
- C-10 NIKDY neemituje novy canonical CS1 state. Nemutuje `lifecycle_state` field, nemutuje CS1 transitions T1-T24.
- Funnel stages F1-F10 jsou **reporting abstrakce** nad CS1 + A-09 ingest — zadny vlastni runtime zapis. Queue dim B cte C-05 `_asw_outbound_queue.status`; outcome dim C cte CS1 terminal state + C-07 `_asw_inbound_events.event_type`.

**CS2 kompatibilita:**
- CS2 `cs2_run_id` je **klic** pro G3 per-run grain. C-10 G3 reports filtrovane per `cs2_run_id` na `_asw_logs`.
- PROPOSED CS2 step: `performance_report_generator` (nereactive post-run hook + optional daily cron + manual menu trigger).
- C-10 nemutuje CS2 step taxonomy ani event model. Jen konzumuje `cs2_run_id` + `_asw_logs` timestamps.

**CS3 kompatibilita:**
- CS3 `_asw_dead_letters` je **primarni zdroj** pro `dead_letter_count` + `dead_letter_rate` + per-step `failure_class` breakdown operational metrics.
- CS3 `failure_class` enum (TRANSIENT / PERMANENT / AMBIGUOUS) driv per-step fail classification v C-10 reports.
- CS3 retry matrix zdroj `retry_count` + `retry_success_rate`.
- CS3 LockService pattern **dedden** pro budouci C-10 runtime worker (lock per-grain report generation race prevention).
- C-10 nemutuje CS3 dead_letter schema. Jen cte append-only.

**C-04 kompatibilita:**
- C-04 gate outcomes jsou zdroj `manual_review_required_count` + `compliance_hard_stop_rate` + `send_blocked_count` + `blocking_reason_breakdown_json` (21 blocking reason codes).
- C-04 gate latency (pres `_asw_logs` timestamps gate_evaluated event) driv operational metrics.
- PROPOSED C-04 extension: zadne. C-10 cte `sendability_outcome` + `blocking_reasons[]` per-lead.

**C-05 kompatibilita:**
- C-05 `_asw_outbound_queue.status` je zdroj **queue dim B** (celych 5 statusu QUEUED/SENDING/SENT/FAILED/CANCELLED) + queue_status_breakdown_json + send_success_rate (dim B interna) + queue_latency_avg_ms.
- C-05 5 queue statusu jsou **ortogonalni dimenze nad queue radkem** per C-05 invariant — NEJSOU funnel stages, NEJSOU lifecycle states. Mapovani do funnel dim A NEEXISTUJE (NEMAPOVAT queue_sent_count na jakoukoli F-stage).
- C-10 nemutuje `_asw_outbound_queue` schema.

**C-06 kompatibilita:**
- C-06 `NormalizedSendResponse` + `NormalizedProviderStatus` + `NormalizedSendErrorClass` jsou zdroj provider_status_breakdown + send_latency_stats.
- C-10 cte `provider_send_duration_ms` + `normalized_status` + `normalized_error_class` per queue row.

**C-07 kompatibilita:**
- C-07 `_asw_inbound_events.event_type` je zdroj **outcome dim C** (outcome_replied/bounced/unsubscribed counts) + `reply_class` breakdown (POSITIVE / NEGATIVE / UNCLASSIFIED) + `bounce_class` breakdown (HARD / SOFT).
- Cross-dim rates reply_yield / bounce_yield / unsubscribe_yield (B→C) + quality aliasy reply_rate / bounce_rate / unsubscribe_rate / positive_reply_rate / unclear_reply_rate / hard_bounce_rate jsou **derivaty** s denominatorem `queue_sent_count` z C-05 (NE funnel F-stage count).
- Outcome dim C NEJSOU funnel stages. Mapovani z outcome na F-stage NEEXISTUJE.
- C-10 nemutuje `_asw_inbound_events` schema.

**C-08 kompatibilita:**
- C-08 3-stage follow-up sekvence (initial / follow_up_1 / follow_up_2) je zdroj followup_yield_rate (% sequences kde stage > 1 vygeneroval response) + per-stage sent counts + stop_reason breakdown (10-value enum).
- `sequence_stage` dimenze zachycena v per-stage sent / reply counts.

**C-09 kompatibilita:**
- C-09 `_asw_exceptions` je zdroj review_queue_load + review_sla_compliance_rate + exception_rate + exception_type_breakdown (10 types) + retry_chain_depth_breakdown.
- C-09 4-tier priority (P1/P2/P3/P4) + SLA targets jsou zdroj per-priority SLA compliance metrics.
- exception_rate_per_stage = C-09 exceptions / C-10 stage throughput (stage-scoped attribution).

**Future dashboard task vztah:**
- Dashboard **konzumuje** `_asw_perf_reports` schema (read-only). C-10 SPEC je **vstupni kontrakt** pro dashboard task. Dashboard NENI blocker pro C-10.

**Future alerting task vztah:**
- Alerting konzumuje `alert_summary_json.triggered[]` field + 5 PROPOSED `_asw_logs` event types. Notification channel (email/Slack) mimo C-10 scope.

**Future BI export task vztah:**
- BI export mirror `_asw_perf_reports` schema do external data warehouse. C-10 poskytuje stable schema contract.

Task NEDODAVA:
- Runtime report worker / cron / scheduler / `buildPerformanceReport_()` helper v Apps Script
- `_asw_perf_reports` sheet creation
- `apps-script/PerfReport.gs` soubor
- `apps-script/Config.gs` zapisy (PERF_REPORT_* Script Properties)
- 5 `_asw_logs` event type emissions
- Dashboard / UI / charts
- Alerting / notification system (email alerts, Slack integration)
- BI export / data warehouse mirror
- Per-operator attribution / workload balancing
- Cost metrics / token usage / API billing tracking
- SEO / marketing KPIs / conversion funnel (downstream WON/LOST)
- CS3 retry matrix mutace
- A/B testing framework
- Predictive analytics / forecasting
- AI-based anomaly detection
- Per-recipient drill-down
- Multi-tenant reporting
- Frontend deployment / CDN
- Retention / archive policy pro `_asw_perf_reports`
- Backfill cron pro historicka data
- Real-time streaming metrics (> daily grain mimo scope v1.0)
- CI/CD integration (napr. alert na PR merge blok)
- A-09 `_ingest_reports` schema mutace
- Novy canonical CS1 state

## Code Changes

Zadne. Task je pure specifikace.

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| — | — | Zadne code changes |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/24-automation-workflows.md | modified | Primarni spec location — nova sekce "Automation performance report — C-10 (funnel / operational / quality metrics)" (19 podsekci) pridana za C-09 sekci. Obsahuje: ucel + ASCII architekturu diagram (INGEST → PREVIEW/OUTREACH → INBOUND/LIFECYCLE → RELIABILITY → C-10 report layer), dependency narrowing `A-09, C-01..C-09` → `A-09, CS1, CS2, CS3, C-04..C-09` explicit explanation, boundary + non-goals, 5 reporting grains (G1 per-job / G2 per-day / G3 per-run / G4 per-stage / G5 per-segment), **3-dimension reporting model (dim A funnel F1-F10 / dim B queue 5 statusu QUEUED/SENDING/SENT/FAILED/CANCELLED / dim C outcome 4 terminaly DISQUALIFIED/REPLIED/BOUNCED/UNSUBSCRIBED)** mapovany na CS1 + A-09 + C-05 + C-07 + C-08 s tvrdou separaci invariantem, funnel metrics (10 counts + 9 funnel-internal conv rates + funnel_yield_to_outreach_ready), queue metrics (5 counts + send_success_rate + queue_latency), outcome metrics (4 counts + breakdowns), cross-dim rates (send_yield / reply_yield / bounce_yield / unsubscribe_yield / delivery_yield), review load (blocked/review_queue/manual_review_entered/sequence_followup_reach), operational metrics, quality aliasy (denominator=queue_sent_count), metric contract template + 2 plne priklady (bounce_rate, review_queue_load), alert thresholds (WARNING/CRITICAL severity, ABS/REL/COMBO types, min_sample invariant, threshold table + send_success_rate / send_yield / funnel_yield_to_outreach_ready / delivery_yield rows), bottleneck detection 3-lens algoritmus (funnel 9 rates + latency + review + priority tiebreaker; cross-dim rates mapped to alert thresholds, ne funnel lens), `_asw_perf_reports` 67-field blokove strukturovany schema s VERIFIED/INFERRED/PROPOSED label per field, plny realisticky sample report s dim A funnel block + QUEUE SNAPSHOT block + OUTCOMES block + cross-dim rates block, comparison rules per grain, cross-ref graph + 5 PROPOSED `_asw_logs` event types, known limitations (dim B / dim C runtime dependency mapping), handoff tabulka, non-goals, acceptance checklist (updated for 10 + 5 + 4 enum layout), PROPOSED/INFERRED/VERIFIED label summary (funnel stage enum 10 + queue status enum 5 + outcome type enum 4) vcetne Script Properties. |
| docs/20-current-state.md | modified | Nova polozka v sekci "Specifikace" — odkaz na C-10 spec v docs/24 s vyctem 5 reporting grains + 3-dimension reporting model (funnel dim A F1-F10 / queue dim B 5 statusu / outcome dim C 4 terminaly) + funnel/queue/outcome/cross-dim/review taxonomy + `_asw_perf_reports` 67-field blokove strukturovany schema + alert threshold model + bottleneck detection 3-lens algoritmus + 5 `_asw_logs` event types + dependency narrowing + A-09/CS1/CS2/CS3/C-04..C-09 alignment + scope disclosure. Update "Posledni aktualizace" na `2026-04-21 (C-10)`. |

## Contracts Changed

**SPEC level — ano, runtime level — ne.**

- Novy **performance report** kontrakt je formalizovan v docs/24: 5 reporting grains, 3-dimension reporting model (funnel dim A F1-F10 / queue dim B 5 statusu / outcome dim C 4 terminaly), funnel metrics (10 counts + 9 funnel-internal conv rates + funnel_yield + drop-offs), queue metrics (5 counts + send_success_rate + queue_latency), outcome metrics (4 counts + breakdowns), cross-dim rates (send_yield A→B / reply_yield+bounce_yield+unsubscribe_yield B→C / delivery_yield A+B), review load Blok E, operational metrics, quality aliasy s denominator=queue_sent_count, metric definition contract template, alert threshold model (WARNING/CRITICAL × ABS/REL/COMBO), bottleneck detection 3-lens algoritmus, `_asw_perf_reports` 67-field blokove strukturovany schema, 5 `_asw_logs` event types, comparison rules per grain, data completeness flags pattern.
- PROPOSED artefakty jsou v SPEC oznacene jako **PROPOSED FOR C-10**:
  - **`_asw_perf_reports` sheet schema** (67 fields v blokove strukture dim A/B/C + cross-dim D + review load E + ops + quality + summary, append-only)
  - **`perf_report_id` format** `perf-{grain}-{unit_id}-{ts14}-{uuid8}`
  - **`grain` enum** (5 values: `G1_PER_JOB` / `G2_PER_DAY` / `G3_PER_RUN` / `G4_PER_STAGE` / `G5_PER_SEGMENT`)
  - **`summary_status` enum** (5 values: `OK` / `DEGRADED` / `AT_RISK` / `CRITICAL` / `INCOMPLETE`)
  - **Funnel stage enum (dim A)** (10 values: F1..F10 canonical names az po OUTREACH_READY)
  - **Queue status enum (dim B)** (5 values: `QUEUED` / `SENDING` / `SENT` / `FAILED` / `CANCELLED` — sourced z C-05 PROPOSED)
  - **Outcome type enum (dim C)** (4 values: `DISQUALIFIED` / `REPLIED` / `BOUNCED` / `UNSUBSCRIBED` — sourced z CS1 terminals + C-07 event_type)
  - **Alert threshold contract** (WARNING / CRITICAL severity; ABS / REL / COMBO types; min_sample invariant)
  - **Bottleneck detection 3-lens algoritmus** (funnel + latency + review + priority tiebreaker)
  - **`alert_summary_json` structure** (`warning_count` / `critical_count` / `triggered[]` / `suppressed[]`)
  - **`bottleneck_summary_json` structure** (`funnel` / `latency` / `review` / `primary`)
  - **`data_completeness_flags_json` structure** (boolean flags per dependency layer: ingest / cs1 / cs2 / cs3 / c04 / c05 / c06 / c07 / c08 / c09)
  - **`comparison_baseline_ref` lookup contract** (previous_day / previous_week / rolling_7d / rolling_30d / absolute_number)
  - **`PERF_REPORT_COLUMNS` constant** (PROPOSED `apps-script/PerfReport.gs`, 67 fields)
  - **`_asw_logs` event types (5):** `performance_report_started`, `performance_report_generated`, `performance_report_failed`, `performance_alert_threshold_crossed`, `performance_bottleneck_detected`
  - **Future runtime worker kontrakt:** `buildPerformanceReport_(grain, unit_id, opts)` pure function; `writePerfReport_(sheet, report)` append-only writer; `generatePerformanceReport(grain, unit_id)` public menu entry
  - **Trigger points:** post-CS2-run hook (G3 per-run) + daily scheduled trigger (G2 per-day) + manual menu (G1 per-job / G5 per-segment on-demand)
  - **Script Properties:** `PERF_REPORT_P95_MULTIPLIER=2.0`, `PERF_REPORT_MIN_SAMPLE_BOUNCE=30`, `PERF_REPORT_MIN_SAMPLE_REPLY=50`, `PERF_REPORT_BASELINE_WINDOW_DAYS=7`, `PERF_REPORT_DOW_GROUPING=true`, `PERF_REPORT_OUTLIER_EXCLUDE_STDDEV=3.0`
  - **Threshold defaults** (per sekce 9 — PROPOSED tuneable): bounce_rate WARNING > 5% / CRITICAL > 10%; hard_bounce_rate WARNING > 2% / CRITICAL > 5%; reply_rate WARNING < 3% / CRITICAL < 1% (REL baseline); unsubscribe_rate WARNING > 0.5% / CRITICAL > 1%; review_sla_compliance_rate WARNING < 80% / CRITICAL < 60%; queue_latency_avg_ms WARNING > 5000 / CRITICAL > 15000; conv_rate(F5→F6) preview_approval WARNING < 75% / CRITICAL < 60%.
  - **Tyto budou materializovany implementacnim taskem** (sheet creation + `apps-script/PerfReport.gs` + Config.gs zapisy + 5 `_asw_logs` event type emissions + runtime trigger install).
- Zadny existujici datovy / behavioralni kontrakt (A-01 az A-10, B-01 az B-05, BX1, C-04, C-05, C-06, C-07, C-08, C-09, CS1, CS2, CS3) neni touto SPEC mutovan. C-10 jen **cte** z `_ingest_reports` (A-09) + LEADS (CS1) + `_asw_logs` (CS2) + `_asw_dead_letters` (CS3) + `_asw_outbound_queue` (C-05) + queue `NormalizedSendResponse` fields (C-06) + `_asw_inbound_events` (C-07) + `_asw_exceptions` (C-09). Zadne zapisy do existing sheets. PROPOSED `_asw_perf_reports` je **dodatecny** sheet, ne mutace existing.
- **Dependency narrowing `A-09, C-01 az C-09` → `A-09, CS1, CS2, CS3, C-04..C-09`** je formalni SPEC-level rozhodnuti (ne code change). Dokumentovano v docs/24 uvod C-10 sekce + tento task record.

## Tests

| Test | Vysledek |
|------|----------|
| check-doc-sync | OK (TBD po final run — expected 43 pass / 0 warn / 0 fail, 29 task records) |
| tsc --noEmit | N/A (doc-only task) |
| npm run build | N/A (doc-only task) |
| 5 reporting grains pokryvaji cely reporting prostor | OK (docs/24 sekce 3: G1 per-job / G2 per-day / G3 per-run / G4 per-stage / G5 per-segment; invariant "jedna zprava = jeden grain"; invariant "grains jsou ortogonalni ne nested") |
| 3-dimension reporting model pokryva cely lifecycle | OK (docs/24 sekce 4: dim A funnel F1 raw_scraped → F2 normalized → F3 deduped_imported → F4 web_checked → F5 qualified → F6 brief_ready → F7 preview_generating → F8 preview_ready_for_review → F9 preview_approved → F10 outreach_ready; dim B queue 5 statusu QUEUED/SENDING/SENT/FAILED/CANCELLED; dim C outcome 4 terminaly DISQUALIFIED/REPLIED/BOUNCED/UNSUBSCRIBED mapovane na CS1 terminal stavy + C-07 event_type; dim A konci na OUTREACH_READY (CS1 #12), queue a outcome jsou separatni dimenze) |
| Funnel stage ≠ lifecycle state ≠ queue status ≠ outcome ≠ review flag ≠ alert state | OK (docs/24 sekce 4 invariants + zakazane kolapsy — NIKDY "email_queued funnel stage" (= queue_queued_count v dim B); NIKDY "replied funnel stage" (= outcome_replied_count v dim C); CS1 terminals DISQUALIFIED/REPLIED/BOUNCED/UNSUBSCRIBED jsou outcome dim C ne funnel stages; queue statusy QUEUED/SENDING/SENT/FAILED/CANCELLED jsou dim B ortogonalni dimenze ne funnel stages; REVIEW_REQUIRED/PREVIEW_READY_FOR_REVIEW/FAILED jsou review flags v Bloku E; alert states WARNING/CRITICAL jsou report-level ne per-lead stav) |
| Funnel metrics pokryvaji count + funnel-internal conv_rate + drop-off; queue/outcome/cross-dim separatni | OK (docs/24 sekce 5 bloky A/B/C/D/E: Blok A 10 stage counts + 9 funnel-internal conv rates (conv_f1_to_f2 .. conv_f9_to_f10) + funnel_yield_to_outreach_ready + drop-off; Blok B 5 queue status counts + send_success_rate + queue_latency; Blok C 4 outcome counts + breakdowns; Blok D cross-dim rates send_yield/reply_yield/bounce_yield/unsubscribe_yield/delivery_yield; Blok E review load) |
| Operational metrics pokryvaji latency + fail + retry + dead_letter + review SLA + queue + exception | OK (docs/24 sekce 6: avg/p50/p95/max processing time per step, fail_rate, retry_count + retry_success_rate, dead_letter_count + dead_letter_rate, review_queue_load, review_sla_compliance_rate per tier, stale_pending_count, queue_latency_avg_ms, exception_rate per 1000 leadu) |
| Quality metrics pokryvaji bounce/reply/unsub/followup/preview-approval/compliance | OK (docs/24 sekce 7: bounce_rate / hard_bounce_rate / soft_bounce_rate / reply_rate / positive_reply_rate / unclear_reply_rate / unsubscribe_rate / followup_yield_rate / preview_approval_rate / exception_rate_per_stage / compliance_hard_stop_rate) |
| Metric definition contract template + priklady | OK (docs/24 sekce 8: template 15 poli (metric_id/name/grain/unit/formula/source_fields[]/null_handling/min_sample_size/value_range/baseline_ref/interpretation/alert_enabled/warning_threshold/critical_threshold/threshold_type) + 2 plne priklady (bounce_rate COMBO, review_queue_load ABS)) |
| Alert threshold model s WARNING/CRITICAL + ABS/REL/COMBO + min_sample invariant | OK (docs/24 sekce 9: WARNING/CRITICAL severity + ABS absolute / REL relative vs baseline / COMBO kombinovany + min_sample invariant (pod = INCOMPLETE ne alert) + baseline ref pattern (previous_day / previous_week / rolling_7d / rolling_30d / absolute_number) + threshold table) |
| Bottleneck detection 3-lens algoritmus | OK (docs/24 sekce 10: funnel lens (stage s conv_rate < baseline - threshold, weight by drop-off count) + latency lens (step s p95_ms > baseline * multiplier, weight by throughput) + review lens (priority tier s SLA compliance < threshold, weight by open count) + priority tiebreaker (funnel > latency > review, alphabetical stage_id within lens)) |
| `_asw_perf_reports` 67-field blokove strukturovany schema | OK (docs/24 sekce 11: blokove strukturovane — metadata (8) + Blok A funnel/dim A (14) + Blok B queue/dim B (8) + Blok C outcome/dim C (4) + Blok D cross-dim rates (5) + Blok E review load (4) + operational (9) + quality aliasy (8) + synthetic summary (7); per-field VERIFIED/INFERRED/PROPOSED label; F11-F14 stara numerace eliminovana) |
| Sample report s realistickymi hodnotami | OK (docs/24 sekce 12: G2 per-day 2026-04-19, 5 jobs, summary_status=AT_RISK, 4 warnings + 3 criticals, bottleneck primary=funnel F5→F6 preview_approval_rate 62% vs baseline 85%) |
| Comparison rules per grain | OK (docs/24 sekce 13: G1 job-vs-job same portal/segment, G2 day-vs-day weekday-matching, G3 run-vs-run same step, G4 stage-vs-stage rolling, G5 segment-vs-segment within day; outlier handling >3 stddev) |
| Auditability (5 `_asw_logs` event types + cross-ref graph) | OK (docs/24 sekce 14: `performance_report_started` / `performance_report_generated` / `performance_report_failed` / `performance_alert_threshold_crossed` / `performance_bottleneck_detected` + cross-ref graph + observability query patterns bez dashboard UI) |
| Known limitations (dependency completeness flags) | OK (docs/24 sekce 15: tabulka mapujici dependencies (CS2 run, C-05 runtime, C-06 runtime, C-07 runtime, C-09 runtime) na INFERRED status + data_completeness_flags_json structure) |
| Handoff tabulka pokryva A-09/CS1/CS2/CS3/C-04..C-09 + future tasks | OK (docs/24 sekce 16: 12 radku s per-row "jak konzumuje" + "jak prispiva" pro A-09 / CS1 / CS2 / CS3 / C-04 / C-05 / C-06 / C-07 / C-08 / C-09 / future dashboard / future alerting / future BI export / future runtime worker) |
| SPEC-only — zadne runtime zmeny, zadne Config.gs zapisy | OK (docs/24 sekce 17 explicit non-goals, 18 polozek) |
| Dependency narrowing `C-01..C-09` → `CS1..CS3, C-04..C-09` explicit | OK (docs/24 uvod C-10 sekce + tento task record Dependencies + Scope) |
| Metric invariants: funnel monotonic, grains ortogonalni, metriky auditovatelne | OK (docs/24 sekce 4 + sekce 3 + sekce 8 contract template) |
| Acceptance checklist 23 polozek | OK (docs/24 sekce 18 vsechny checked) |
| Oddeleni: funnel stage vs lifecycle state vs queue milestone vs outcome metric vs alert state | OK (docs/24 sekce 4 tabulka + sekce 11 per-field labels explicit) |
| INCOMPLETE status fallback pro missing dependency layers | OK (docs/24 sekce 15 + summary_status enum + data_completeness_flags_json pattern — pokud C-05/C-06/C-07/C-09 nejsou runtime, null values + INCOMPLETE + flags audit trail) |

## Output for Audit

- **Report sheet:** `_asw_perf_reports` (PROPOSED, append-only, 67 fields v blokove strukture)
- **Reporting grains (5):** G1 per-job / G2 per-day / G3 per-run / G4 per-stage / G5 per-segment
- **3-dimension reporting model (hard separace):**
  - **Dim A — Funnel (10 canonical progression stages):** F1 raw_scraped / F2 normalized / F3 deduped_imported / F4 web_checked / F5 qualified / F6 brief_ready / F7 preview_generating / F8 preview_ready_for_review / F9 preview_approved / F10 outreach_ready
  - **Dim B — Queue (5 operational statusu per C-05):** QUEUED / SENDING / SENT / FAILED / CANCELLED (ortogonalni dimenze, NEJSOU funnel stages)
  - **Dim C — Outcome (4 terminalni outcomy):** DISQUALIFIED / REPLIED / BOUNCED / UNSUBSCRIBED (mapovane na CS1 terminaly #6/#15/#16/#17 resp. C-07 event_type; NEJSOU funnel stages)
- **Funnel metrics (dim A):** 10 stage counts + 9 funnel-internal conv rates (conv_f1_to_f2 .. conv_f9_to_f10) + `funnel_yield_to_outreach_ready` + drop-off (worst-stage + worst-absolute)
- **Queue metrics (dim B):** 5 status counts + queue_status_breakdown_json + send_success_rate (dim B interna) + queue_latency_avg_ms
- **Outcome metrics (dim C):** 4 outcome counts + reply_class/bounce_class/unsubscribe_source breakdowns
- **Cross-dim rates (Blok D):** send_yield (A→B) + reply_yield + bounce_yield + unsubscribe_yield (B→C) + delivery_yield (A+B end-to-end)
- **Review load (Blok E):** blocked_by_sendability_count + review_queue_load_count + manual_review_entered_count + sequence_followup_reach_count
- **Operational metric families (10+):** processing_time (avg/p50/p95/max per step), fail_rate, retry_count, retry_success_rate, dead_letter_count, dead_letter_rate, review_queue_load, review_sla_compliance_rate (per tier), stale_pending_count, queue_latency_avg_ms, exception_rate
- **Quality metric families (11):** bounce_rate, hard_bounce_rate, soft_bounce_rate, reply_rate, positive_reply_rate, unclear_reply_rate, unsubscribe_rate, followup_yield_rate, preview_approval_rate, exception_rate_per_stage, compliance_hard_stop_rate
- **Summary status (5):** OK / DEGRADED / AT_RISK / CRITICAL / INCOMPLETE
- **Alert severity (2):** WARNING / CRITICAL
- **Alert threshold types (3):** ABS absolute / REL relative vs baseline / COMBO kombinovany
- **Baseline reference types (5):** previous_day / previous_week / rolling_7d / rolling_30d / absolute_number
- **Bottleneck detection lenses (3):** funnel + latency + review; priority tiebreaker funnel > latency > review
- **`_asw_logs` event types (5):** `performance_report_started` / `performance_report_generated` / `performance_report_failed` / `performance_alert_threshold_crossed` / `performance_bottleneck_detected`
- **Min sample invariant:** pod min_sample_size threshold → `summary_status=INCOMPLETE` misto alert trigger (prevence false positives z noisy small samples)
- **DOW grouping:** `PERF_REPORT_DOW_GROUPING=true` → weekday-matching comparison (po-vs-po, út-vs-út atd.); prevence vikendovych outlieru v day-vs-day comparison
- **Outlier handling:** values > 3 stddev od rolling mean excluded z comparison calculations (`PERF_REPORT_OUTLIER_EXCLUDE_STDDEV=3.0`)
- **Retry chain depth tracking:** C-09 retry_reference_exception_id chain depth 1-3 kategorizovano v exception_rate breakdown
- **Dependency narrowing:** puvodni task brief → `A-09, C-01 az C-09`. Repo audit → `C-01.md` / `C-02.md` / `C-03.md` NEEXISTUJI. `C2.md` / `C3.md` / `C4.md` jsou governance hardening (unrelated). `CS1` / `CS2` / `CS3` jsou foundational SPEC. **Dependency narrowing na `A-09, CS1, CS2, CS3, C-04..C-09`** (10 tasku total jako upstream metric-source prerekvizity). Dokumentovano v docs/24 uvod C-10 sekce + tento task record Dependencies + Scope.
- **Cross-ref graph:** `_ingest_reports` (A-09, G1) ↔ `_asw_perf_reports` (C-10, G1-G5) ↔ LEADS.lead_id (CS1) ↔ `_asw_outbound_queue.outreach_queue_id` (C-05) ↔ `_asw_inbound_events.inbound_event_id` (C-07) ↔ `_asw_exceptions.exception_id` (C-09) ↔ `_asw_dead_letters.dead_letter_id` (CS3) ↔ `_asw_logs.cs2_run_id` (CS2)
- **Handoff tabulka (12 radku):** A-09 (upstream ingest report pattern + zdroj F1-F4 funnel dim A) / CS1 (zdroj F2-F10 funnel dim A + terminal #6/#15/#16/#17 jako outcome dim C) / CS2 (cs2_run_id pro G3 + post-run hook trigger) / CS3 (_asw_dead_letters zdroj op metrics + LockService pattern dedeny) / C-04 (gate outcomes + blocking_reason_breakdown) / C-05 (queue dim B 5 statusu + send_success_rate + queue_latency) / C-06 (NormalizedSendResponse provider_status + send_latency) / C-07 (outcome dim C events: replied/bounced/unsubscribed + reply/bounce/unsub rates + cross-dim yields) / C-08 (3-stage sekvence zdroj followup_yield + stop_reason breakdown) / C-09 (_asw_exceptions zdroj review_queue_load + SLA compliance + exception_rate) / future dashboard (konzumuje stable schema) / future alerting (konzumuje alert_summary_json.triggered[] + 5 log events) / future BI export (mirror schema) / future runtime worker (implementuje buildPerformanceReport_ + writePerfReport_ + generatePerformanceReport)
- **VERIFIED IN REPO labely:** `_ingest_reports` (A-09 merged, 41 fields, bottleneck detection pattern), `_asw_logs` append-only pattern (CS2), CS1 18-state lifecycle (docs/21 line 156-173), CS3 `_asw_dead_letters` schema + `failure_class` enum, C-04 gate outcomes + 21 blocking reasons, C-05 `_asw_outbound_queue` 32-field schema + 5 statusu, C-06 `NormalizedSendResponse` 17 fields + 7-value `NormalizedProviderStatus` enum + 8-value `NormalizedSendErrorClass`, C-07 `_asw_inbound_events` schema + `reply_class` / `bounce_class` / `unsubscribe_source` enumy, C-08 3-stage sequence + 10-value stop_reason enum, C-09 `_asw_exceptions` 24-field schema + 4-tier priority + SLA targets + 5-status model
- **INFERRED labely:** p50/p95/max latency z consecutive `_asw_logs` timestamps (bez explicit start/end pair), compliance_hard_stop_rate (zavisi na PROPOSED C-04 `sendability_outcome` field materialization), followup_yield_rate (zavisi na C-07 + C-08 runtime), conversion rate healthy ranges (baseline values), threshold defaults (industry B2B email benchmarks), DOW grouping benefit, outlier 3-stddev exclusion threshold, bottleneck 3-lens weighting formulas
- **PROPOSED FOR C-10 labely:** `_asw_perf_reports` sheet (67 fields v blokove strukture), `perf_report_id` format, `grain` enum (5 values), `summary_status` enum (5 values), funnel stage enum dim A (10 values F1-F10), queue status enum dim B (5 values QUEUED/SENDING/SENT/FAILED/CANCELLED), outcome type enum dim C (4 values DISQUALIFIED/REPLIED/BOUNCED/UNSUBSCRIBED), alert threshold contract (WARNING/CRITICAL × ABS/REL/COMBO), bottleneck detection 3-lens algoritmus, `alert_summary_json` structure, `bottleneck_summary_json` structure, `data_completeness_flags_json` structure (10 dependency flags), `comparison_baseline_ref` lookup contract, `PERF_REPORT_COLUMNS` constant, 5 `_asw_logs` event types, 6 Script Properties (PERF_REPORT_P95_MULTIPLIER / MIN_SAMPLE_BOUNCE / MIN_SAMPLE_REPLY / BASELINE_WINDOW_DAYS / DOW_GROUPING / OUTLIER_EXCLUDE_STDDEV), threshold defaults per metric, future runtime worker contract, trigger points

## Known Limits

- SPEC neimplementuje engine runtime / cron / scheduler / `buildPerformanceReport_()` helper / `writePerfReport_()` writer / `generatePerformanceReport()` menu entry — pouze definuje kontrakt. Implementace je budouci task (sheet creation + `apps-script/PerfReport.gs` + Config.gs zapisy + trigger install + 5 `_asw_logs` event emissions).
- PROPOSED `_asw_perf_reports` sheet neni v ramci C-10 vytvoren. Implementacni task ho musi pridat.
- PROPOSED enumy (`grain`, `summary_status`, funnel stage dim A F1-F10, queue status dim B 5 values, outcome type dim C 4 values, alert severity, threshold type, baseline ref type) nejsou v ramci C-10 zapsany do `apps-script/Config.gs`. Implementacni task je musi pridat.
- PROPOSED Script Properties (`PERF_REPORT_*`, 6 klicu) nejsou v ramci C-10 nastaveny. Implementacni task je musi nastavit.
- `data_completeness_flags_json` struktura znamena, ze **do materializace C-05 / C-06 / C-07 / C-09 runtime** ma C-10 reporty null values pro operational/quality metriky zavisle na tech vrstvach + `summary_status=INCOMPLETE`. Report zustava validni, ale s omezenym obsahem (degraded gracefully).
- Dashboard UI je **mimo C-10 scope**. V interim operator/admin cte reporty primo z `_asw_perf_reports` sheet bunky (Google Sheets query / filter). Degraded UX (bez charts, bez time-series vizualizace, bez drill-down).
- Alerting integration (email / Slack notifications) je **mimo C-10 scope**. V interim operator musi aktivne kontrolovat `_asw_perf_reports` sheet + `alert_summary_json.triggered[]` array. Budouci alerting task konzumuje `performance_alert_threshold_crossed` event.
- BI export / external data warehouse mirror je **mimo C-10 scope**. Budouci task mirror stable `_asw_perf_reports` schema.
- Per-operator attribution / workload balancing je **mimo C-10 scope** (ops concern). C-10 report je system-wide, ne per-operator.
- Cost metrics (token usage, API billing, serper quota, Gmail API limits) jsou **mimo C-10 v1.0** (nezavisle v2.0 task).
- SEO / marketing KPIs (traffic, page views, conversion funnel po WON/LOST) jsou **mimo scope** (Autosmartweby je outreach automation ne marketing analytics).
- AI-based anomaly detection / predictive analytics / forecasting jsou **mimo v1.0 SPEC** (v2.0 mozna add pres ML model).
- A/B testing framework (variant A vs variant B conversion) je **mimo scope** (prilis komplexni feature).
- Multi-tenant reporting (per-organization scoping) je **mimo v1.0** (single-tenant).
- Real-time streaming metrics (sub-daily grain) jsou **mimo scope** (daily G2 + per-run G3 dostacujici pro current use case).
- Retention / archive policy pro `_asw_perf_reports` je **dokumentovana jen principe** (append-only, no delete). Detail policy = ops rozhodnuti + budouci task.
- Backfill cron pro historicka data (pre-C-10 epochy) je **mimo v1.0** — report starts fresh at C-10 implementation date.
- CI/CD integration (napr. alert na PR merge blok) je **mimo scope** (separate workflow, mozna future task).
- p50 / p95 / max latency jsou **INFERRED z consecutive `_asw_logs` timestamps** — pokud step nema explicit start / end event pair, derived approximation s mozne nepresnosti (dokumentovano v sekci 6).
- Threshold defaults (bounce_rate > 5% WARNING, > 10% CRITICAL; reply_rate < 3% WARNING < 1% CRITICAL atd.) jsou **PROPOSED industry benchmarks**. Ops/business musi finalizovat per-threshold per Script Property override.
- Baseline reference pattern (previous_day / previous_week / rolling_7d / rolling_30d / absolute_number) je **PROPOSED contract** — konkretne baseline_ref per metrika je v sekci 8 contract template ale lze rucne override.
- DOW grouping je **PROPOSED** pres `PERF_REPORT_DOW_GROUPING=true` Script Property. Vypnuti = naivni day-vs-day comparison (zahrne vikend-vs-weekday noise).
- Outlier exclusion threshold 3-stddev je **PROPOSED default** pres `PERF_REPORT_OUTLIER_EXCLUDE_STDDEV=3.0`. Kratsi threshold (napr. 2.0) = vice excluded values = mene robust median; delsi (napr. 4.0) = mene excluded = vice noise.
- min_sample thresholds (bounce 30, reply 50) jsou **PROPOSED defaults**. Ops musi kalibrovat na realistic daily volume.
- Compatibility matrix bottleneck priority tiebreaker (funnel > latency > review) je **PROPOSED** — ops muze override pres Script Property (pokud latency issue je business-critical).
- Audit immutability `_asw_perf_reports` rows po append je **dokumentovana jako invariant** ale **Google Sheets nema native immutability** — implementacni task musi pridat edit-protection (data validation rule + protected range).
- Dependency completeness flags jsou **PROPOSED struktura** — implementacni task musi pridat per-flag sanity check logic (napr. pokud `c05_runtime=false` a report spousti queue dim B metriky → warning v `data_completeness_flags_json.warnings[]`).

## Next Dependency

- **Implementacni task C-10 runtime** — vytvori `_asw_perf_reports` sheet s 67 fields v blokove strukture, implementuje `buildPerformanceReport_(grain, unit_id, opts)` pure function, `writePerfReport_(sheet, report)` append-only writer, `generatePerformanceReport(grain, unit_id)` public menu entry, pridava 5 `_asw_logs` event types, pridava PROPOSED enumy do `apps-script/Config.gs`, implementuje bottleneck detection 3-lens algoritmus, min_sample invariant, baseline lookup contract, DOW grouping, outlier exclusion, data completeness flags, audit protection. Trigger install: post-CS2-run hook (G3) + daily cron (G2) + menu entries (G1/G5). Konzumuje SPEC z docs/24 C-10 sekce.
- **Dashboard task (B-TBD)** — implementuje frontend dashboard pro `_asw_perf_reports`. Per-grain view (G1/G2/G3/G4/G5), time-series charts (conv rates, latency trends, error rates), drill-down od report do source leads, alert badge per AT_RISK/CRITICAL row. Konzumuje stable `_asw_perf_reports` schema (read-only data source). C-10 SPEC je **vstupni kontrakt**.
- **Alerting integration task (C-TBD)** — implementuje notification delivery (email / Slack) pri `performance_alert_threshold_crossed` events. Per-severity routing (WARNING → daily digest; CRITICAL → realtime). Konzumuje `alert_summary_json.triggered[]` + 5 PROPOSED `_asw_logs` event types.
- **BI export task (D-TBD)** — mirror `_asw_perf_reports` schema do external data warehouse (BigQuery / Snowflake / Metabase). Incremental append-only sync pres stable schema contract. Konzumuje `_asw_perf_reports` SPEC.
- **Audit protection task** — Google Sheets data validation + protected ranges pro `_asw_perf_reports` rows. Enforces append-only invariant.
- **Retention / archive policy task** — definuje ops policy pro old report cleanup (napr. > 1 rok → archive sheet). C-10 SPEC jen dokumentuje principle (append-only).
- **C-10 extension task (v2.0)** — pridava pokrocile metriky: AI-based anomaly detection, predictive forecasting (next-day reply_rate prediction), per-operator attribution, A/B testing framework, cost metrics (token/API usage), SEO/marketing integration.
- **A-09 extension task** (optional) — pokud C-10 runtime odhali potrebu novych polí v `_ingest_reports` (napr. per-normalization-error breakdown), extension task pridava A-09 schema extensions. C-10 je **konzument** A-09 data, ne producer mutace.
- **Cost metrics task** (v2.0 future) — per-job cost tracking (Serper API queries, Gmail API quota, OpenAI tokeny pro future AI classification). Konzumuje C-10 runtime infrastruktura.
- **Compliance audit task** (future) — pravidelny audit compliance metrics (unsubscribe propagation, hard bounce suppression, EU GDPR adherence). Konzumuje `compliance_hard_stop_rate` + `unsubscribe_rate` + audit trail.
- **Anomaly detection task** (v2.0 future) — ML-based detekce anomalii v historickych datech. Konzumuje `_asw_perf_reports` time-series jako training data.

Do te doby zustava automation performance **neobservovany sekt** — nemame jeden reporting artefakt, ktery rekne "jak system performuje dnes/tento tyden". A-09 pokryva jen ingest funnel (F1-F4); PREVIEW/OUTREACH/INBOUND/LIFECYCLE/RELIABILITY vrstvy nemaji equivalent. Operator/business nevi bez manualniho checkovania LEADS + queue + inbound events + exceptions, jestli system plati na bounce_rate, reply_rate, review SLA, queue latency. Kriticky: nevidime bottlenecks (kde se zastavuje funnel), nevidime degradace (retry success rate trend), nevidime compliance risks (unsubscribe rate spike). C-10 SPEC formalizuje jak bude tento observability vacuum naplnen — 5 grains, 3-dimension reporting model (funnel dim A F1-F10 / queue dim B 5 statusu / outcome dim C 4 terminaly), funnel + queue + outcome + cross-dim + review + operational + quality metric families, alert threshold model s WARNING/CRITICAL severity + ABS/REL/COMBO types + min_sample invariant, bottleneck detection 3-lens algoritmus, 67-field blokove strukturovany `_asw_perf_reports` schema, 5 `_asw_logs` event types pro full auditability, comparison rules per grain, data completeness flags pro graceful degradation pred C-05..C-09 runtime. C-10 SPEC je prerekvizita pro jakoukoli implementaci dashboard / alerting / BI export a current state zachovava runtime stability aniz by pridal technicky dluh. Dependency narrowing `A-09, C-01..C-09` → `A-09, CS1, CS2, CS3, C-04..C-09` je formalni SPEC-level rozhodnuti zajistujici, ze implementacni task zna spravnou upstream metric-source taxonomy (10 source tasku, ne neexistujici C-01/C-02/C-03).
