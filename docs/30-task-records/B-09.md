# Task Record: B-09

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | B-09 |
| **Title** | Phase 2 KROK 4 — manual "Vygenerovat preview" button v CRM |
| **Owner** | Stream B |
| **Status** | done |
| **Date** | 2026-04-26 |
| **Stream** | B |

## Scope

Phase 2 KROK 4 doplnuje operatorske manualni vygenerovani preview pro 1 lead z CRM lead detail draweru. Flow: kliknuti "Vygenerovat preview" → frontend POST `/api/leads/[id]/generate-preview` → Apps Script doPost akce `generatePreview` → `processPreviewForLead_(leadId)` → `_previews` row + LEADS write-back → response s `previewUrl`. Frontend drawer ukaze "Preview hotov" + odkaz "Otevrit".

Reuse existujicich Phase 2 KROK 2 primitiv: `upsertPreviewRecord_` (Sheets-backed `_previews` storage), `buildPreviewBrief_` (18-pole brief). Refactor: extrakce `computePreviewArtifacts_(rd)` (pure helper sdileny `processPreviewQueue` + manual flow) a `persistPreviewArtifacts_(artifacts, leadId)` wrapper.

KROK 4 NEMENI B-04 webhook endpoint, NEMENI B-01 `PreviewBrief` shape, nemeni B-05 testy (regression PASS).

## Code Changes

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| apps-script/PreviewPipeline.gs | modified | Refactor: extrakce `computePreviewArtifacts_(rd)` (pure, no IO) a `persistPreviewArtifacts_(artifacts, leadId)` (upsert + URL resolver). `processPreviewQueue` pouziva nove helpers. Nova funkce `processPreviewForLead_(leadId)` — eligibility check (qualified + !dedupe), compute, persist, single-row LEADS write-back, draft conditional pri `send_allowed='true'`, lead_stage upgrade pri QUALIFIED. |
| apps-script/WebAppEndpoint.gs | modified | Nova doPost akce `generatePreview` → `handleGeneratePreview_({leadId, token})` wrapuje `processPreviewForLead_` v try/catch, surface `{ok:false, error}` na vsech chybach. |
| crm-frontend/src/lib/google/apps-script-writer.ts | modified | Nova `generatePreview(leadId): Promise<GeneratePreviewResult>` POST do APPS_SCRIPT_URL. |
| crm-frontend/src/app/api/leads/[id]/generate-preview/route.ts | new | Next.js route s error mapping: `not_qualified`/`dedupe_blocked` → 400, `lead_not_found` → 404, AS ok=false → 502, exception → 500. Validation: subject ≤ 200, body ≤ 20000. Mock-mode vetev volá `generateMockPreview`. |
| crm-frontend/src/lib/mock/mock-service.ts | modified | Nova `generateMockPreview(leadId)`: validace `qualifiedForPreview === true`, slug build, mutate mock state (previewUrl, previewHeadline, previewStage='READY_FOR_REVIEW'). |
| crm-frontend/src/components/leads/lead-detail-drawer.tsx | modified | Preview sekce reworked: state machine `idle / generating / success / error`, reset na drawer close. Tlacitko logic: !qualified → disabled + Tooltip; qualified + previewUrl → "Zobrazit preview" link + outline "Regenerovat"; qualified + !previewUrl → primary "Vygenerovat preview"; generating → spinner. Po success: `fetchLead(leadId)` + green checkmark + toast. Local Lead interface rozsiren o `qualifiedForPreview, previewStage`. |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/30-task-records/B-09.md | new | Tento task record. |

## Contracts Changed

- **Apps Script Web App contract:** additive — nova doPost akce `generatePreview`. Existujici akce (getPreview, updateLead, render webhook) beze zmeny.
- **Frontend Lead model:** rozsireny o `qualifiedForPreview, previewStage` v draweru (`sheet-to-domain.ts` uz mapoval).
- **B-04 endpoint contract:** beze zmeny.
- **`processPreviewQueue`:** vnitrne refactored, externe funkcni-shodny — B-05 42/42 PASS.

## Tests

| Test | Vysledek |
|------|----------|
| `npm run lint` | OK (0 errors, 14 pre-existing warnings) |
| `npx tsc --noEmit` | OK |
| `npm run build` | OK — 15 routes (new `/api/leads/[id]/generate-preview`) |
| `npm run test:b03` | OK — 13/13 PASS |
| `npm run test:b04` | OK — 16/16 PASS |
| `npm run test:b05` | OK — 42/42 PASS (queue + webhook regression intact) |
| `npm run docs:check` | OK |

## Output for Audit

- PR #68 squash merge `c76b5a9`.
- Operator klik v CRM lead detail draweru → 5–15 s → `_previews` row vznikne s slug + brief_json + preview_url, LEADS row preview_stage=READY_FOR_REVIEW, preview_url, preview_slug.
- "Zobrazit preview" link vede na `autosmartweb.cz/preview/<slug>` (marketing web rendruje EmergencyProfessional template).
- Non-qualified leads majiu disabled button + Tooltip "Lead neni kvalifikovany".

## Known Limits

- Frontend send má mírnější gate (Q1 decision, viz B-11) než Sheet path: NE `assertSendability_`, jen `qualified + READY_FOR_REVIEW + drafts + email`. Drift backlog.
- `_previews` sheet vznikne lazy pri prvnim `upsertPreviewRecord_` volani (idempotent `ensurePreviewSheet_`). Zadny explicit pre-setup neni nutny.
- Tooltip pro non-qualified neukaze konkretni `qualification_reason` (Q8 decision, backlog).
- Server-side rate limit pro "Vygenerovat preview" neni implementovan — Apps Script LockService 5s na slug urovni je dostatecny pro single-operator pace.

## Next Dependency

| Task | Co potrebuje z B-09 |
|------|---------------------|
| B-10 (KROK 5 auto trigger) | `computePreviewArtifacts_` + `persistPreviewArtifacts_` helpers, alignment processPreviewQueue eligibility check. |
| B-11 (KROK 6 email send) | Drawer state pattern, Lead interface fields, Apps Script doPost dispatch konvence. |
| Backlog | Sjednotit frontend gate vs Sheet APPROVE gate. Expose qualification_reason v tooltip. Server-side rate limit. |
