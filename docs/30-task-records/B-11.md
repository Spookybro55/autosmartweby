# Task Record: B-11

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | B-11 |
| **Title** | Phase 2 KROK 6 — email z CRM (UI editor + Odeslat button) |
| **Owner** | Stream B |
| **Status** | done |
| **Date** | 2026-04-26 |
| **Stream** | B |

## Scope

Phase 2 KROK 6 doplnuje operatorske posilani emailu primo z CRM lead detail draweru. Operator vidi pre-generated email draft (z KROK 4/5), edituje subject/body inline, klikne "Odeslat" → confirm Dialog → email letiu klientovi pres existing pilot send primitives.

Reuse pilot KROK 4 primitives bez modifikace: `resolveSenderIdentity_` (Reply-To assignee map), `sendGmailMessage_` (GmailApp send), `persistOutboundMetadata_` (LEADS write-back).

**Drift uznán (Q1 decision):** frontend send má mírnější gate než Sheet path:
- **Sheet path** (`executeCrmOutbound_` operator menu): `assertSendability_` requires `review_decision === 'APPROVE'`.
- **Frontend path** (`sendEmailForLead_` z drawer Odeslat): jen `qualified + preview_stage=READY_FOR_REVIEW + drafts + email valid`.

Argument: KROK 4/5 generuji preview do `READY_FOR_REVIEW` ale `review_decision=''` (B-06 review queue se aktivuje jen pri Sheet edit). Pokud by frontend send vyzadoval APPROVE, kazdý CRM-only workflow by byl zablokovany. Operator klik "Odeslat" + Dialog confirm IS the approval. Sheet path keeps APPROVE pro operatory kteri preferuji Sheet review queue.

**Backlog:** sjednotit gates jakmile CRM-only workflow stabilizuje (likely: drop B-06 review queue + assertSendability_, frontend confirm dialog becomes single source of approval).

KROK 6 NEMENI Sheet outbound flow, NEMENI pilot send primitives, NEMENI B-04 contract.

## Code Changes

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| apps-script/Config.gs | modified | EXTENSION_COLUMNS append `email_body_last` (po `email_subject_last`). Idempotent migration via `setupPreviewExtension`. |
| apps-script/OutboundEmail.gs | modified | `persistOutboundMetadata_` snapshot `payload.body` → `email_body_last` (s `colOrNull` guard pro graceful pre-migration). Nova `sendEmailForLead_(leadId, opts)`: findRowByLeadId_, apply subject/body overrides do email_*_draft pred validaci, validate (qualified + READY_FOR_REVIEW + drafts + email), resolveSenderIdentity_, sendGmailMessage_, persistOutboundMetadata_('SEND') → email_subject_last + email_body_last + last_email_sent_at + outreach_stage='CONTACTED' + email_sync_status='SENT'. Throws: `lead_not_found / not_qualified / preview_not_ready / empty_drafts / invalid_email / send_failed: ...`. |
| apps-script/WebAppEndpoint.gs | modified | Nova doPost akce `sendEmail` → `handleSendEmail_` + dispatch. |
| crm-frontend/src/lib/google/apps-script-writer.ts | modified | Nova `sendEmail(leadId, opts): Promise<SendEmailResult>` POST do AS. |
| crm-frontend/src/lib/mock/mock-service.ts | modified | Nova `sendMockEmail(leadId, opts)` — validation mirror, override persist, outreach_stage upgrade, lastEmailSentAt mutation. |
| crm-frontend/src/app/api/leads/[id]/send-email/route.ts | new | Next.js route: not_qualified/preview_not_ready/empty_drafts/invalid_email → 400, lead_not_found → 404, AS ok=false (send_failed) → 502, exception → 500. Body validation: subject ≤ 200, body ≤ 20000. |
| crm-frontend/src/components/leads/lead-detail-drawer.tsx | modified | "E-mail" sekce. Local state `emailSubject + emailBody` (re-seed z lead.emailSubjectDraft/BodyDraft na fetchLead). State machine `idle / sending / success / error`. Editable Input/Textarea, disabled v sending. Disabled reasons most-specific first: !qualified → "Lead neni kvalifikovany"; previewStage !== READY_FOR_REVIEW → "Preview jeste neni pripraveny..."; empty → "Predmet/telo prazdne"; invalid email → "Neni validni adresa". Disabled button wrapped v Tooltip. AlreadySent warning (Q6): amber banner + button label "Odeslat znovu". Klik "Odeslat" → shadcn `<Dialog>` confirm s recipient + Reply-To + full subject (no truncate per Q4). Po success: fetchLead + toast + onSaved(). |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/30-task-records/B-11.md | new | Tento task record. |

## Contracts Changed

- **Apps Script Web App:** additive doPost akce `sendEmail`. Existujici akce beze zmeny.
- **LEADS schema:** novy sloupec `email_body_last` (snapshot odeslaneho body). `email_subject_last` jiz existoval. Idempotent migration v `setupPreviewExtension`.
- **`persistOutboundMetadata_`:** body snapshot pridan, but graceful pre-migration via `colOrNull` guard — funguje i bez sloupce.
- **Pilot send primitives:** beze zmeny — `resolveSenderIdentity_`, `sendGmailMessage_` reused as-is.

## Tests

| Test | Vysledek |
|------|----------|
| `npm run lint` | OK (0 errors, 13 pre-existing warnings — down from 14) |
| `npx tsc --noEmit` | OK |
| `npm run build` | OK — **16 routes** (new `/api/leads/[id]/send-email`) |
| `npm run test:b03` | OK — 13/13 PASS |
| `npm run test:b04` | OK — 16/16 PASS |
| `npm run test:b05` | OK — **42/42 PASS** (pilot send primitives untouched) |
| `npm run docs:check` | OK |

## Output for Audit

- PR #72 (recovery for original #70, ktery byl OPEN ale base byl `phase2/05-auto-trigger` — neslo cleanly mergnout po squash KROK 4/5) squash merge `bdd6af5`.
- Operator klik "Odeslat" v lead drawer → Dialog confirm → email odesilan z deployer Google účtu (master), Reply-To na assignee per ASSIGNEE_NAMES.
- LEADS row update: outreach_stage=CONTACTED, last_email_sent_at, email_subject_last + email_body_last snapshot, email_sync_status=SENT, last_contact_at.
- Drawer refresh po success: amber "Odeslano {date}" warning + button label "Odeslat znovu".
- Negative cases: lead bez drafts → button disabled + Tooltip "Predmet/telo prazdne". Lead bez preview → "Preview neni pripraveny".

## Known Limits

- **Drift dual-gate:** Sheet path APPROVE vs frontend READY_FOR_REVIEW. Documented v Scope. Backlog sjednotit po stable Phase 2 produkci.
- **Náhled** (preview body before Dialog send) — SKIP per Q5 decision. Operator vidi body v Textarea predeplenem, Dialog ukaže recipient + subject + Reply-To. Backlog: separate "Preview" modal pred confirm.
- **Re-send guard** (Q6): warn-only, ne hard block. Operator muze poslat email opakovane. Server-side guard backlog.
- **Setup migration:** `setupPreviewExtension` musi byt zavolano manualne v Apps Script editoru pred prvnim KROK 6 send (jinak `email_body_last` sloupec chybi → snapshot fails graceful s `colOrNull`, ale data nejsou zachycena).

## Next Dependency

| Task | Co potrebuje z B-11 |
|------|---------------------|
| B-12 (hotfix brief aliases) | Phase 2 e2e in production — odhalil schema mismatch CRM brief vs marketing web template. |
| Backlog | Sjednoceni dual-gate. Náhled modal pred Dialog send. Server-side re-send guard. Outbound retry/idempotency (per CS3-reliability-spec). |
