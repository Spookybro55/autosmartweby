# Task Record: C-11

## Metadata

| Pole | Hodnota |
|------|---------|
| **Task ID** | C-11 |
| **Title** | Config, secrets, limity a budget guardrails — SPEC-only kontrakt pro 6 ortogonalnich configuration planes (config / secret / limit / budget / kill switch / feature flag) |
| **Owner** | Claude |
| **Status** | done |
| **Date** | 2026-04-22 |
| **Stream** | C |
| **Dependencies** | **CS2** (workflow orchestrator — jediny logicky upstream kontext, ve kterem davaji limity / kill switchery smysl; `cs2_run_id` je klic pro per-run budget guardrails; post-CS2-step hook je primarni trigger point pro per-stage limit check + kill-switch vstupni brana; `_asw_logs` append-only event stream je spolecna observability vrstva pro vsech 9 PROPOSED C-11 event types). **C-06** (provider abstraction — `EMAIL_PROVIDER` Script Property je kanonicka vzorovy CONFIG entry pro provider selection mezi `gmail` / `sendgrid` / `mailgun`; `NormalizedSendErrorClass` je zdroj pravidla transient vs permanent pri budget accounting — transient fails se neucitaji do budget consumption, permanent ano; provider-level kill switch `KILL_SWITCH_GMAIL` / `KILL_SWITCH_SENDGRID` / `KILL_SWITCH_MAILGUN` prestupne pres C-06 sender interface bez mutace jeho kontraktu). **C-10** (performance report — budget / kill-switch / limit eventy feed do `_asw_perf_reports` operational block; `data_completeness_flags_json` zaznamenava degradace zpusobenou guardraily; 5 C-10 `_asw_logs` event types + 9 PROPOSED C-11 event types sdileji jednu observability vrstvu bez kolize — explicit dokumentovano v sekci 14 handoff tabulce). Puvodni task brief uvadel `C-02, C-06, C-10`, ale v repo source-of-truth `C-02.md` NEEXISTUJE (`C2.md` je governance hardening — unrelated; precedent C-10 dependency narrowing `C-01..C-03` → `CS1..CS3`). **Dependency narrowing na `CS2, C-06, C-10`** je explicitne dokumentovano v docs/24 uvod C-11 sekce + tento task record, odsouhlaseno jako jediny set tasku, ktery dava smysl jako upstream architectural context. |

## Scope

Formalizuje **authoritative specifikaci** pro configuration vrstvu nad celou automatizaci Autosmartweby. Predmetem SPEC je **jak system cte a meni sve chovani** aniz by se mneil kod — pres environment-bound config, secrets, per-stage limits, budget guardrails, kill switches a feature flags. Brief otazky typu "jak zastavime odesilani v 5:30 pri incidentu?", "kolik stoji serper API denne?", "jak testovat preview bez skutecneho posilani?", "co se stane kdyz Gmail API zacne odmitat?", "kam patri `DRY_RUN`?", "jak oddelit config od tajemstvi?".

Scope je **SPEC-only** — neimplementuje runtime `ConfigManager` / `getConfig_()` / `isKillSwitchActive_()` / `checkBudget_()` helper, nevytvari `_asw_budget_ledger` sheet, nezapisuje do `apps-script/Config.gs`, nepridava PROPOSED Script Properties do runtime environment, neimplementuje frontend UI pro kill-switch toggling, neintegruje feature flag SDK, neautomatizuje secret rotation. Vsechny nove artefakty jsou oznacene **PROPOSED FOR C-11** a budou materializovany implementacnim taskem.

Task dodava:

- **6-plane ortogonalni taxonomie s tvrdou separaci invariantem `CONFIG ≠ SECRET ≠ LIMIT ≠ BUDGET ≠ KILL_SWITCH ≠ FEATURE`:** kazdy config artefakt patri do **prave jedne** role. Zakazane kolapsy (12 polozek v sekci 3.10): NIKDY "`SERPER_API_KEY` jako CONFIG" (patri do SECRET pres Script Properties s redaction mask); NIKDY "`MAX_BATCH_SIZE` jako BUDGET" (patri do LIMIT — hard cap per stage, ne ekonomicka burn rate); NIKDY "`DRY_RUN=true` jako CONFIG do budoucna" (patri do KILL_SWITCH `KILL_SWITCH_ALL_WRITES` — operational shutdown); NIKDY "`EMAIL_SYNC_ENABLED` jako CONFIG" (patri do FEATURE `FEATURE_EMAIL_SYNC` — feature flag lifecycle); NIKDY "`KILL_SWITCH_GMAIL` jako FEATURE" (kill switch je emergency, feature je product optionality); NIKDY "`BUDGET_GMAIL_DAILY_QUOTA` jako LIMIT" (budget je cost / quota accounting s ledger append-only; limit je hard cap bez ledger); NIKDY "`WEBHOOK_URL` jako SECRET" (sam URL neni tajemstvi, ale auth header je); NIKDY "Config = Script Properties by definition" (CONFIG zdroje jsou hierarchicke: Script Properties → Config.gs defaults → runtime override — viz sekce 4).
- **Dependency narrowing `C-02, C-06, C-10` → `CS2, C-06, C-10`:** explicitni dokumentace proc (C-02 neexistuje, `C2.md` je governance unrelated, CS2 je foundational orchestrator SPEC a jediny logicky kontext, ve kterem dava C-11 smysl). Uvod docs/24 C-11 sekce + tento task record.
- **DRY_RUN → KILL_SWITCH_ALL_WRITES reklasifikace:** dnesni `DRY_RUN=true` v Config.gs je **hybrid artifact** — formalne CONFIG bool, ale funkcne kill switch. C-11 ho reklasifikuje a definuje migrace plan (target state = Script Property `KILL_SWITCH_ALL_WRITES` s backward-compat fallback na `DRY_RUN` const). Dokud runtime migrace neproběhne, `DRY_RUN=true` zustava kanonicky kill switch — C-11 dokumentuje target state.
- **EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC reklasifikace:** dnesni `EMAIL_SYNC_ENABLED=true` je CONFIG bool ale funkcni role je feature flag pro mailbox sync. C-11 plan: reklasifikace na `FEATURE_EMAIL_SYNC` Script Property s rollout lifecycle (draft / pilot / rollout / stable / deprecated).
- **Canonical config table — 13-column schema:** `key` / `plane` (CONFIG|SECRET|LIMIT|BUDGET|KILL_SWITCH|FEATURE) / `scope` (global / env / stage / provider) / `source` (Script Property / Config.gs / ledger sheet) / `type` (bool|int|string|enum|secret_ref|json) / `default_value` / `prod_value` / `test_value` / `allowed_values_or_range` / `owner_stream` / `affected_consumers[]` / `change_control` / `label` (VERIFIED / INFERRED / PROPOSED). **17 VERIFIED IN REPO entries** (ASW_ENV, ASW_SPREADSHEET_ID, PREVIEW_WEBHOOK_SECRET, SERPER_API_KEY, DRY_RUN const, ENABLE_WEBHOOK const, WEBHOOK_URL const, BATCH_SIZE const, EMAIL_MAILBOX_ACCOUNT const, EMAIL_SYNC_LOOKBACK_DAYS const, EMAIL_SYNC_MAX_THREADS const, EMAIL_SYNC_REQUIRE_EXACT_MATCH const, EMAIL_SYNC_ENABLED const, MAIN_SHEET_NAME const, LOG_SHEET_NAME const, RAW_IMPORT_SHEET_NAME const, CONTACT_SHEET_NAME const) + **PROPOSED 4 CONFIG / 9 LIMIT / 7 BUDGET / 14 KILL_SWITCH / 10 FEATURE / 2 SECRET entries** per sekce 5.
- **Secrets inventory — 10-field contract:** `key` / `provider` (Script Properties / GCP Secret Manager / external vault) / `rotation_policy` (manual / 90d / 180d / on-incident) / `redaction_mask_pattern` / `used_in_step[]` / `access_pattern` (read-at-start / read-per-call / read-on-rotation) / `timing_insensitive_compare` (bool) / `pii_class` (none / low / high) / `last_rotated_at` / `owner`. Inventory v docs/24 sekce 5 je **5 VERIFIED entries** per entry explicitne klasifikovanych pres `kind`: **3 true secrets** (S1 `SERPER_API_KEY` kind=API_KEY, S2 `PREVIEW_WEBHOOK_SECRET` kind=SHARED_SECRET, S3 `FRONTEND_API_SECRET` kind=SHARED_SECRET) + **1 TENANT_ID** (S4 `ASW_SPREADSHEET_ID` kind=TENANT_ID — **explicitne reclassified z SECRET na CONFIG s TENANT_ID roli**, ne pure secret ale sensitive env-bound identifier; inventorovan v sekci 5 kvuli rotation / leak-response workflow, ne jako autorizacni material) + **1 platform OAUTH_TOKEN** (S5 Apps Script platform identity, kind=OAUTH_TOKEN — implicit, Google-managed, informational only). Hard-separation invariant `CONFIG ≠ SECRET ≠ LIMIT ≠ BUDGET ≠ KILL_SWITCH ≠ FEATURE` zustava intaktni: S4 nepatri do SECRET plane (patri do CONFIG s TENANT_ID kind), S5 je platform-managed mimo ops surface. + **2 PROPOSED true secrets** (S6 `SENDGRID_API_KEY` kind=API_KEY / S7 `MAILGUN_API_KEY` kind=API_KEY, conditional on `EMAIL_PROVIDER` selection per C-06).
- **Per-stage limits for 9 pipeline stages — 9 PROPOSED LIMIT_* Script Properties (canonical inventory v docs/24 sekce 6):** A-04 firmy.cz scraper (`LIMIT_A04_SCRAPE_PAGES_PER_PORTAL=50`, `LIMIT_A04_SCRAPE_BATCH_TIMEOUT_MS=300000`), A-05 dedupe (reuse VERIFIED `BATCH_SIZE=100` — ne novy LIMIT_*), A-06 auto web check (`LIMIT_A06_WEBCHECK_QPS=2`, `LIMIT_A06_WEBCHECK_BATCH_SIZE=100`), A-07 auto qualify (reuse VERIFIED `BATCH_SIZE=100` — ne novy LIMIT_*), A-08 preview queue (reuse VERIFIED `BATCH_SIZE=100` — ne novy LIMIT_*), B-04 preview render endpoint (`LIMIT_B04_PREVIEW_RENDER_TIMEOUT_MS=60000`), C-05 outbound queue worker (`LIMIT_C05_OUTBOUND_BATCH_SIZE=50`, `LIMIT_C05_OUTBOUND_MAX_CONCURRENCY=1`), C-06 provider send (`LIMIT_C06_SEND_TIMEOUT_MS=30000`), C-08 follow-up engine (`LIMIT_C08_FOLLOWUP_BATCH_SIZE=20`). **9 LIMIT_* celkem** (A-05/A-07/A-08 zamerne zustavaji na VERIFIED `BATCH_SIZE` bez duplicitniho PROPOSED entry — limit kontrakt se rozsiruje tam, kde existujici konstanta nepokryva novy aspekt stage). Invariant: limits jsou **hard caps per stage** — pri prekroceni stage safe-stopne s `limit_exceeded` event, nesmi se retryovat proti limitu.
- **Budget guardrails — 10-field contract + 5 kategorii:** Contract per entry (`budget_id`, `category`, `provider_or_api`, `scope` stage / cs2_run / day / month, `reset_policy` never / daily / monthly / on-reset-call, `warning_threshold_pct`=80%, `critical_threshold_pct`=100%, `hard_stop_on_critical` bool, `current_consumption` counter, `last_reset_at`, `owner`). **5 kategorii**: (1) **BG-PROV-OPS** — provider operational quotas (Gmail API daily send quota, Serper monthly quota, Gemini/OpenAI token budget); (2) **BG-RETRY-EXPLOSION** — retry count explosion prevention (max retries per step per day, prevents runaway retry loops caused by CS3 retry matice mis-classification); (3) **BG-COST-EXPLOSION** — API cost explosion (Serper per-query cost × queries per day; Gemini tokens per day × rate); (4) **BG-RUNAWAY-BATCH** — batch size explosion (napr. A-04 scraper skriptem vyvolan se spatnym URL — unlimited scrape); (5) **BG-SILENT-DEGRADATION** — degradation without alerts (napr. Gmail soft bounce percentage trend — pokud soft bounce > 5% prumeru za 7 dni = mozna reputation damage). PROPOSED 7 BUDGET entries (BUDGET_SERPER_DAILY_QUERY_QUOTA=1000, BUDGET_GMAIL_DAILY_SEND_QUOTA=2000, BUDGET_GEMINI_DAILY_TOKEN_BUDGET=100000, BUDGET_RETRY_MAX_PER_STEP_DAY=50, BUDGET_PREVIEW_RENDER_DAILY=200, BUDGET_FOLLOWUP_SEND_DAILY=500, BUDGET_BOUNCE_RATE_7D_ROLLING_PCT=5). **`_asw_budget_ledger` 12-field sheet** (PROPOSED append-only): `ledger_id` / `budget_id` / `cs2_run_id` / `stage` / `delta_amount` / `delta_unit` (call / token / row / dollar) / `cumulative_at_time` / `threshold_status` (OK / WARNING / CRITICAL) / `triggering_step` / `triggering_event_id` / `timestamp` / `note`.
- **Kill switch model — 5-scope taxonomy:** (1) **GLOBAL** — `KILL_SWITCH_ALL_WRITES` (celosystemovy incident shutdown; aktivovan → zadny CS2 step nesmi zapisovat do ZADNEHO sheetu; safe-stop ne hard-stop — rozbehnute operace dokonci, nove nestartuji); (2) **ENV** — `KILL_SWITCH_PROD_ENFORCE` (ENV-scoped guardrail — napr. vypnuti PROD spreadsheetu pro integrity audit); (3) **CATEGORY** — `KILL_SWITCH_EMAIL_ALL` / `KILL_SWITCH_SCRAPE_ALL` / `KILL_SWITCH_WEBHOOK_ALL` (stage-family shutdown, napr. reputation incident — vypne vsechny C-05/C-06/C-08 pipeline emails); (4) **PROVIDER** — `KILL_SWITCH_GMAIL` / `KILL_SWITCH_SENDGRID` / `KILL_SWITCH_MAILGUN` / `KILL_SWITCH_SERPER` (provider-level isolation, incident na konkretnim ESP); (5) **API_SURFACE** — `KILL_SWITCH_DOPOST_WRITE` / `KILL_SWITCH_PREVIEW_WEBHOOK` (API endpoint surface shutdown, napr. compromise podezreni na PREVIEW_WEBHOOK_SECRET). **Safe-stop semantics**: aktivace → inflight operace dokonci transakcni celek ale nezapisuji novy commit; nove operace vidi kill switch a safe-stopnou s `kill_switch_triggered` event + `cs2_run_id` + `triggering_step` + `intended_sheet_writes[]` audit trail. **Reset**: manualni pres Script Property update + `kill_switch_reset` event. **Testing**: PROPOSED `diagKillSwitchState()` diagnostic tool vraci JSON se stavem vsech 14 kill switches. **Idempotency**: kill switch check je idempotent — volan pri kazdem CS2 step entry. **Escalation**: PROPOSED auto-escalation — pokud CATEGORY kill switch aktivni > 24h, auto-promote na GLOBAL (s konfirmaci pres Script Property `KILL_SWITCH_AUTO_ESCALATE=true`).
- **Feature flag kontrakt — 8-field contract:** per entry (`flag_id`, `rollout_stage` draft / pilot / rollout / stable / deprecated, `default_value_per_env` map env → bool, `target_audience` internal / 10pct / 50pct / all, `owner_stream`, `related_task`, `sunset_date`, `label`). **Rollout patterns**: draft (off everywhere) → pilot (on in TEST only) → rollout (on in TEST + 10% PROD) → stable (on everywhere) → deprecated (off, planned removal). **State safety rules**: flag flip BEHEM CS2 run-u nesmi korumpovat in-progress stepy — kazdy CS2 run snapshotuje flag state na starte (`_asw_logs.feature_snapshot_json`); zmena flag behem runu = nez viditelna az dalsi run. PROPOSED 10 feature flags: FEATURE_EMAIL_SYNC (rollout z VERIFIED `EMAIL_SYNC_ENABLED=true`), FEATURE_PREVIEW_WEBHOOK_V2, FEATURE_AUTO_QUALIFY_HOOK, FEATURE_AUTO_WEB_CHECK_HOOK, FEATURE_DOPOST_WRITE, FEATURE_FOLLOWUP_ENGINE, FEATURE_INBOUND_EVENT_INGEST, FEATURE_PERFORMANCE_REPORT (po C-10 impl), FEATURE_EXCEPTION_QUEUE (po C-09 impl), FEATURE_PROVIDER_ABSTRACTION (po C-06 impl).
- **State safety rules:** (1) **Config change atomicity** — change Script Property je atomic (GAS native guaranty), ale CS2 step beh ma pouzit snapshot config state na starte (`configSnapshot_()` pattern); (2) **Kill switch activation timing** — aktivace mezi CS2 stepy, ne uprostred; inflight step dokonci transakcni celek; (3) **Budget threshold crossing** — prechod WARNING → CRITICAL musi vyvolat observability event i kdyz hard_stop_on_critical=false (visibility nad cost); (4) **Feature flag flip during run** — snapshot na run start, flip viditelny az dalsi CS2 run; (5) **Secret rotation during run** — secret read-at-start, rotace behem runu nerozbi probehajici step; pro long-running steps (A-04 firmy.cz scraping) explicit re-read na kazdem batch boundary.
- **Sample scenarios (7):** (1) Reputation incident on Gmail → operator activates `KILL_SWITCH_EMAIL_ALL` → vsechny C-05/C-06/C-08 safe-stop + log; (2) Serper quota prekrocena → BUDGET warning 80% trigger → email notification → CRITICAL 100% trigger + `BUDGET_SERPER_DAILY_QUERY_QUOTA.hard_stop_on_critical=true` → A-06 safe-stop; (3) Transient Gmail API 500 → CS3 retry matrix classify TRANSIENT → retry NOT counted toward BUDGET_GMAIL_DAILY_SEND_QUOTA (only permanent sends); (4) Feature flag rollout FEATURE_FOLLOWUP_ENGINE from pilot → rollout → snapshot on run start; (5) DRY_RUN → KILL_SWITCH_ALL_WRITES migration — `DRY_RUN=true` in Config.gs continues to work + runtime guard checks `KILL_SWITCH_ALL_WRITES` Script Property first, fallback to `DRY_RUN` const; (6) Secret rotation PREVIEW_WEBHOOK_SECRET → operator updates Script Property → next doPost uses new secret, old requests still in flight pass timing-insensitive compare against old value via short-lived dual-read window; (7) Config drift detection — diagConfigState() vraci diff mezi expected Script Properties and actual, flagne missing / stale entries.
- **Testing / verification:** `diagConfigState()` diagnostic tool (PROPOSED) vraci JSON se stavem vsech 17 VERIFIED + PROPOSED CONFIG entries; `diagKillSwitchState()` vraci stavy 14 kill switches; `diagBudgetLedger()` vraci cumulative consumption per budget_id; `diagSecretInventory()` vraci redacted view vsech 7 VERIFIED+PROPOSED secrets; `configSnapshot_()` utility for CS2 step entry. Testing approach: (1) Unit test `getConfigValue_()` fallback hierarchy (Script Property → Config.gs default → error); (2) Integration test kill switch activation — artificial `KILL_SWITCH_EMAIL_ALL=true` in TEST, spawn C-05 queue worker, assert safe-stop + log event; (3) Budget threshold crossing test — artificially inflate `current_consumption` to 81%, assert `budget_warning_crossed` event emitted; (4) Feature flag snapshot test — flip flag mid-run, assert old snapshot used until next CS2 run.
- **Anti-patterns (12-row table):** (1) "SERPER_API_KEY as CONFIG const" → SECRET violation (pii_class=high); (2) "DRY_RUN=true forever as CONFIG" → hybrid role (migrate to KILL_SWITCH_ALL_WRITES); (3) "Hardcode PREVIEW_WEBHOOK_SECRET in code" → SECRET leak; (4) "BATCH_SIZE=1000 as BUDGET" → category confusion (LIMIT not BUDGET — no cost/ledger); (5) "KILL_SWITCH_GMAIL as FEATURE flag" → emergency vs product optionality; (6) "Change CONFIG mid-run" → atomicity violation (use snapshot); (7) "Retry against LIMIT" → infinite loop; (8) "No ledger for BUDGET" → audit gap; (9) "Single-scope kill switch" → no provider isolation (use 5-scope taxonomy); (10) "Feature flag without sunset_date" → flag debt; (11) "SECRET without rotation_policy" → stale credential risk; (12) "No KILL_SWITCH_RESET event" → silent re-enable.
- **Auditability:** 9 PROPOSED `_asw_logs` event types (`config_value_changed`, `secret_rotated`, `limit_exceeded`, `budget_warning_crossed`, `budget_critical_crossed`, `kill_switch_triggered`, `kill_switch_reset`, `feature_flag_enabled`, `feature_flag_disabled`) + cross-ref graph (Config.gs ↔ Script Properties ↔ `_asw_budget_ledger` ↔ `_asw_logs` ↔ C-10 `_asw_perf_reports` operational block) + 4 diagnostic tools (`diagConfigState`, `diagKillSwitchState`, `diagBudgetLedger`, `diagSecretInventory`) + runtime API surface (`getConfigValue_`, `getSecret_`, `getLimit_`, `getBudgetState_`, `isKillSwitchActive_`, `isFeatureEnabled_`).
- **Handoff tabulka (13 radku):** CS2 / C-04 / C-05 / C-06 / C-07 / C-08 / C-09 / C-10 / BX1 / A-04 / A-06 / future runtime impl / future frontend UI. Per-row "jak C-11 konzumuje" + "jak C-11 prispiva".
- **Non-goals:** runtime `ConfigManager`, `_asw_budget_ledger` sheet creation, `apps-script/Config.gs` zapisy, PROPOSED Script Properties runtime setup, frontend UI pro kill-switch toggling, feature flag SDK integration, secret rotation automation, backup of Script Properties, external vault (GCP Secret Manager) migration, config change dashboard.
- **Acceptance checklist (23 polozek):** vsechny checked.
- **PROPOSED vs INFERRED vs VERIFIED label summary** (sekce 19).

**CS2 kompatibilita:**
- CS2 step kontrakt je **upstream** rozhodnutim — C-11 se zapojuje jako **vstupni brana** pred kazdym CS2 step exec. `isKillSwitchActive_(scope)` check na step entry; `checkBudget_(budget_id)` check pri provider call; `checkLimit_(stage)` check pri batch iteration.
- `cs2_run_id` je **klic** pro per-run budget accounting (`_asw_budget_ledger.cs2_run_id`) — umoznuje per-run burn rate analysis v C-10 reports.
- PROPOSED nova CS2 step: `config_snapshot_capture` (start of run) + `config_snapshot_verify` (end of run — drift detection).
- C-11 nemutuje CS2 step taxonomy ani event model. Jen pridava **observability layer** nad nim.

**C-06 kompatibilita:**
- C-06 `EMAIL_PROVIDER` Script Property je **kanonicka vzorovy CONFIG entry** pro provider selection. C-11 formalizuje pattern.
- C-06 `NormalizedSendErrorClass` enum je **zdroj rozhodovani** pro budget accounting: transient fails (TIMEOUT, RATE_LIMIT, PROVIDER_UNAVAILABLE) → retry-able, NE accounted to `BUDGET_GMAIL_DAILY_SEND_QUOTA`; permanent fails (INVALID_RECIPIENT, AUTH_FAILED) + success → accounted.
- PROPOSED provider-level kill switch `KILL_SWITCH_GMAIL` / `KILL_SWITCH_SENDGRID` / `KILL_SWITCH_MAILGUN` prestupne pres C-06 sender interface: pred `EmailSender.send` check `isKillSwitchActive_('GMAIL')` → safe-stop bez mutace C-06 kontraktu.
- C-11 **nemutuje** `NormalizedSendResponse` / `SendRequest` / `EmailSender` interface. Zadne pridavani sloupcu.

**C-10 kompatibilita:**
- C-11 9 PROPOSED `_asw_logs` event types feeds do C-10 `_asw_perf_reports` operational block.
- C-10 `data_completeness_flags_json` zaznamena degradace zpusobenou C-11 guardraily: `kill_switch_email_all_active=true` → `delivery_yield=null` + `c11_kill_switch_degraded=true` flag + `summary_status=DEGRADED`.
- C-10 5 event types + C-11 9 event types sdileji jednu observability vrstvu (`_asw_logs`) bez kolize — explicit dokumentovano v sekci 14 handoff tabulce.
- C-11 nemutuje C-10 `_asw_perf_reports` schema. C-10 rozpoznava C-11 degradation states pres data_completeness_flags_json.

## Code Changes

Zadne. Task je pure specifikace.

| Soubor | Typ zmeny | Popis |
|--------|-----------|-------|
| — | — | Zadne code changes |

## Docs Updated

| Dokument | Typ zmeny | Proc |
|----------|-----------|------|
| docs/24-automation-workflows.md | modified | Primarni spec location — nova sekce "Config, secrets, limity a budget guardrails — C-11 (configuration planes + execution limits + kill switches + feature flags)" (19 podsekci) pridana za C-10 sekci. Obsahuje: ucel, dependency narrowing `C-02, C-06, C-10` → `CS2, C-06, C-10` explicit rationale, boundary + non-goals, 6-plane ortogonalni taxonomie (CONFIG / SECRET / LIMIT / BUDGET / KILL_SWITCH / FEATURE) s tvrdou separaci invariantem + 12 zakazanych kolapsu, DRY_RUN → KILL_SWITCH_ALL_WRITES migracni plan, EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC migracni plan, canonical 13-column config table s 17 VERIFIED + PROPOSED 4 CONFIG / 9 LIMIT / 7 BUDGET / 14 KILL_SWITCH / 10 FEATURE / 2 SECRET entries, 10-field secrets inventory contract s 5 VERIFIED + 2 PROPOSED, per-stage limits pro 9 pipeline stages (A-04, A-05, A-06, A-07, A-08, B-04, C-05, C-06, C-08), 10-field budget guardrails contract + 5 kategorii (BG-PROV-OPS / BG-RETRY-EXPLOSION / BG-COST-EXPLOSION / BG-RUNAWAY-BATCH / BG-SILENT-DEGRADATION), kill switch model s 5-scope taxonomy (GLOBAL / ENV / CATEGORY / PROVIDER / API_SURFACE) + safe-stop semantics + reset + testing + idempotency + escalation, feature flag 8-field contract + rollout patterns (draft/pilot/rollout/stable/deprecated) + state safety rules, state safety rules (config change atomicity, kill switch activation timing, budget threshold crossing visibility, feature flag snapshot on run start, secret rotation dual-read window), 7 sample scenarios, testing / verification (4 diagnostic tools + runtime API surface), 12-row anti-pattern table, auditability (9 `_asw_logs` event types + cross-ref graph + `_asw_budget_ledger` 12-field schema), known limitations, handoff tabulka (13 radku), non-goals, acceptance checklist (23 polozek), VERIFIED/INFERRED/PROPOSED label summary. |
| docs/20-current-state.md | modified | Nova polozka v sekci "Specifikace" — odkaz na C-11 spec v docs/24 s vyctem 6 ortogonalnich configuration planes + hard-separation invariant + DRY_RUN/EMAIL_SYNC_ENABLED reklasifikace + canonical config table + secrets inventory + per-stage limits + budget guardrails 5 kategorii + kill switch 5-scope taxonomy + feature flag rollout patterns + 9 `_asw_logs` event types + dependency narrowing + CS2/C-06/C-10 alignment + scope disclosure. Update "Posledni aktualizace" na `2026-04-22 (C-11)`. |

## Contracts Changed

**SPEC level — ano, runtime level — ne.**

- Novy **configuration** kontrakt je formalizovan v docs/24: 6-plane ortogonalni taxonomie, 13-column canonical config table schema, 10-field secrets inventory contract, per-stage limits contract, 10-field budget guardrails contract + 5 kategorii, 5-scope kill switch model + safe-stop semantics, 8-field feature flag contract + rollout patterns, state safety rules, 9 `_asw_logs` event types, `_asw_budget_ledger` 12-field sheet schema, 4 diagnostic tools contract, runtime API surface contract (`getConfigValue_`, `getSecret_`, `getLimit_`, `getBudgetState_`, `isKillSwitchActive_`, `isFeatureEnabled_`), hard-separation invariant s 12 zakazanymi kolapsy.
- PROPOSED artefakty jsou v SPEC oznacene jako **PROPOSED FOR C-11**:
  - **4 CONFIG Script Properties** (`EMAIL_PROVIDER`, `BUDGET_RESET_TIMEZONE`, `KILL_SWITCH_CHECK_INTERVAL_MS`, `BUDGET_LEDGER_STRATEGY`)
  - **9 LIMIT_* Script Properties** (LIMIT_A04_SCRAPE_PAGES_PER_PORTAL, LIMIT_A04_SCRAPE_BATCH_TIMEOUT_MS, LIMIT_A06_WEBCHECK_QPS, LIMIT_A06_WEBCHECK_BATCH_SIZE, LIMIT_B04_PREVIEW_RENDER_TIMEOUT_MS, LIMIT_C05_OUTBOUND_BATCH_SIZE, LIMIT_C05_OUTBOUND_MAX_CONCURRENCY, LIMIT_C06_SEND_TIMEOUT_MS, LIMIT_C08_FOLLOWUP_BATCH_SIZE — 9 celkem napric 9 stages; A-05/A-07/A-08 reuse VERIFIED `BATCH_SIZE=100`)
  - **7 BUDGET_* Script Properties** (BUDGET_SERPER_DAILY_QUERY_QUOTA, BUDGET_GMAIL_DAILY_SEND_QUOTA, BUDGET_GEMINI_DAILY_TOKEN_BUDGET, BUDGET_RETRY_MAX_PER_STEP_DAY, BUDGET_PREVIEW_RENDER_DAILY, BUDGET_FOLLOWUP_SEND_DAILY, BUDGET_BOUNCE_RATE_7D_ROLLING_PCT)
  - **14 KILL_SWITCH_* Script Properties** (KILL_SWITCH_ALL_WRITES global; KILL_SWITCH_PROD_ENFORCE env; KILL_SWITCH_EMAIL_ALL / KILL_SWITCH_SCRAPE_ALL / KILL_SWITCH_WEBHOOK_ALL category; KILL_SWITCH_GMAIL / KILL_SWITCH_SENDGRID / KILL_SWITCH_MAILGUN / KILL_SWITCH_SERPER provider; KILL_SWITCH_DOPOST_WRITE / KILL_SWITCH_PREVIEW_WEBHOOK api_surface; KILL_SWITCH_AUTO_ESCALATE + 2 rezerva)
  - **10 FEATURE_* Script Properties** (FEATURE_EMAIL_SYNC, FEATURE_PREVIEW_WEBHOOK_V2, FEATURE_AUTO_QUALIFY_HOOK, FEATURE_AUTO_WEB_CHECK_HOOK, FEATURE_DOPOST_WRITE, FEATURE_FOLLOWUP_ENGINE, FEATURE_INBOUND_EVENT_INGEST, FEATURE_PERFORMANCE_REPORT, FEATURE_EXCEPTION_QUEUE, FEATURE_PROVIDER_ABSTRACTION)
  - **2 PROPOSED SECRETs** (SENDGRID_API_KEY, MAILGUN_API_KEY — conditional on `EMAIL_PROVIDER`)
  - **`_asw_budget_ledger` 12-field sheet** (PROPOSED append-only: ledger_id / budget_id / cs2_run_id / stage / delta_amount / delta_unit / cumulative_at_time / threshold_status / triggering_step / triggering_event_id / timestamp / note)
  - **9 `_asw_logs` event types** (`config_value_changed`, `secret_rotated`, `limit_exceeded`, `budget_warning_crossed`, `budget_critical_crossed`, `kill_switch_triggered`, `kill_switch_reset`, `feature_flag_enabled`, `feature_flag_disabled`)
  - **4 diagnostic tools** (`diagConfigState`, `diagKillSwitchState`, `diagBudgetLedger`, `diagSecretInventory`)
  - **Runtime API surface** (`getConfigValue_(key, default)`, `getSecret_(key)`, `getLimit_(stage_key)`, `getBudgetState_(budget_id)`, `isKillSwitchActive_(scope)`, `isFeatureEnabled_(flag_id)`, `configSnapshot_()`)
  - **DRY_RUN → KILL_SWITCH_ALL_WRITES migrace plan** (Config.gs DRY_RUN const zustava s backward-compat fallback; runtime check Script Property first)
  - **EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC migrace plan** (Config.gs const zustava s fallback; runtime check feature flag first)
  - **Tyto budou materializovany implementacnim taskem** (Script Property setup + `apps-script/Config.gs` zapisy + `apps-script/ConfigManager.gs` soubor + `_asw_budget_ledger` sheet creation + 9 `_asw_logs` event type emissions + CS2 step entry hooks).
- Zadny existujici datovy / behavioralni kontrakt (A-01 az A-10, B-01 az B-05, BX1, C-04, C-05, C-06, C-07, C-08, C-09, C-10, CS1, CS2, CS3) neni touto SPEC mutovan. C-11 jen **rozsiruje** existujici `EnvConfig.gs` + `Config.gs` + Script Properties pattern. Zadne zapisy do existing sheets. PROPOSED `_asw_budget_ledger` je **dodatecny** sheet, ne mutace existing.
- **Dependency narrowing `C-02, C-06, C-10` → `CS2, C-06, C-10`** je formalni SPEC-level rozhodnuti (ne code change). Dokumentovano v docs/24 uvod C-11 sekce + tento task record.

## Tests

| Test | Vysledek |
|------|----------|
| check-doc-sync | OK (ocekava 30 task records; spusti se po commit) |
| tsc --noEmit | N/A (doc-only task) |
| npm run build | N/A (doc-only task) |
| 6-plane taxonomie pokryva cely configuration prostor | OK (docs/24 sekce 3: CONFIG / SECRET / LIMIT / BUDGET / KILL_SWITCH / FEATURE; invariant "kazdy config artefakt patri do prave jedne role"; invariant "planes jsou ortogonalni ne nested") |
| Tvrda separace invariantem CONFIG ≠ SECRET ≠ LIMIT ≠ BUDGET ≠ KILL_SWITCH ≠ FEATURE | OK (docs/24 sekce 3.10: 12 zakazanych kolapsu — NIKDY SERPER_API_KEY jako CONFIG; NIKDY BATCH_SIZE jako BUDGET; NIKDY DRY_RUN jako CONFIG forever; NIKDY EMAIL_SYNC_ENABLED jako CONFIG; NIKDY KILL_SWITCH_GMAIL jako FEATURE; NIKDY BUDGET_GMAIL_DAILY_QUOTA jako LIMIT atd.) |
| DRY_RUN → KILL_SWITCH_ALL_WRITES reklasifikace dokumentovana | OK (docs/24 sekce 3.8: migration plan + backward-compat fallback + target state) |
| EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC reklasifikace dokumentovana | OK (docs/24 sekce 3.9: migration plan + fallback + rollout lifecycle) |
| Canonical config table s 13 sloupci + 17 VERIFIED entries | OK (docs/24 sekce 5: VERIFIED IN REPO oznacenych: ASW_ENV, ASW_SPREADSHEET_ID, PREVIEW_WEBHOOK_SECRET, SERPER_API_KEY, DRY_RUN, ENABLE_WEBHOOK, WEBHOOK_URL, BATCH_SIZE, EMAIL_MAILBOX_ACCOUNT, EMAIL_SYNC_LOOKBACK_DAYS, EMAIL_SYNC_MAX_THREADS, EMAIL_SYNC_REQUIRE_EXACT_MATCH, EMAIL_SYNC_ENABLED, MAIN_SHEET_NAME, LOG_SHEET_NAME, RAW_IMPORT_SHEET_NAME, CONTACT_SHEET_NAME — 17 total) |
| PROPOSED entries 4 CONFIG + 9 LIMIT + 7 BUDGET + 14 KILL_SWITCH + 10 FEATURE + 2 SECRET | OK (docs/24 sekce 5 per-plane) |
| 10-field secrets inventory + 5 VERIFIED + 2 PROPOSED | OK (docs/24 sekce 5 secrets: S1 SERPER_API_KEY kind=API_KEY, S2 PREVIEW_WEBHOOK_SECRET kind=SHARED_SECRET, S3 FRONTEND_API_SECRET kind=SHARED_SECRET — 3 true secrets; S4 ASW_SPREADSHEET_ID kind=TENANT_ID explicitne reclassified na CONFIG w/ TENANT_ID role; S5 Apps Script OAUTH platform-managed; + PROPOSED S6 SENDGRID_API_KEY, S7 MAILGUN_API_KEY) |
| Per-stage limits pro 9 stages | OK (docs/24 sekce 6: A-04 scraper, A-05 dedupe, A-06 webcheck, A-07 qualify, A-08 preview, B-04 render, C-05 outbound worker, C-06 provider send, C-08 follow-up — 9 LIMIT_* total; A-05/A-07/A-08 reuse VERIFIED `BATCH_SIZE`) |
| Budget guardrails 10-field contract + 5 kategorii | OK (docs/24 sekce 7: BG-PROV-OPS / BG-RETRY-EXPLOSION / BG-COST-EXPLOSION / BG-RUNAWAY-BATCH / BG-SILENT-DEGRADATION; 7 PROPOSED budget entries; `_asw_budget_ledger` 12-field append-only sheet) |
| Kill switch 5-scope taxonomy + safe-stop semantics | OK (docs/24 sekce 8: GLOBAL / ENV / CATEGORY / PROVIDER / API_SURFACE; safe-stop ne hard-stop; reset pattern; testing diagKillSwitchState; idempotency; auto-escalate CATEGORY > 24h → GLOBAL) |
| Feature flag 8-field contract + 5 rollout stages | OK (docs/24 sekce 9: draft / pilot / rollout / stable / deprecated; snapshot na CS2 run start; 10 PROPOSED FEATURE_* flags) |
| State safety rules pokryvaji atomicity + timing + visibility | OK (docs/24 sekce 10: 5 rules — config change atomicity, kill switch timing, budget visibility, feature flag snapshot, secret rotation dual-read) |
| 7 sample scenarios | OK (docs/24 sekce 11: Gmail incident + Serper quota + transient retry + flag rollout + DRY_RUN migration + secret rotation + drift detection) |
| Testing / verification: 4 diagnostic tools + runtime API surface | OK (docs/24 sekce 12: diagConfigState / diagKillSwitchState / diagBudgetLedger / diagSecretInventory; API surface getConfigValue_/getSecret_/getLimit_/getBudgetState_/isKillSwitchActive_/isFeatureEnabled_/configSnapshot_) |
| Anti-pattern tabulka 12 radku | OK (docs/24 sekce 13: hybrid role DRY_RUN, SECRET leak, category confusion BATCH_SIZE vs BUDGET, kill-switch vs feature, mid-run config change, retry against LIMIT, no ledger for BUDGET, single-scope kill switch, flag bez sunset, secret bez rotation, no KILL_SWITCH_RESET) |
| Auditability: 9 `_asw_logs` event types + cross-ref graph + `_asw_budget_ledger` | OK (docs/24 sekce 14) |
| Handoff tabulka 13 radku | OK (docs/24 sekce 15: CS2, C-04..C-10, BX1, A-04, A-06, future runtime, future UI) |
| Known limitations (runtime dependency degradation) | OK (docs/24 sekce 16) |
| SPEC-only — zadne runtime zmeny | OK (docs/24 sekce 17 explicit non-goals) |
| Dependency narrowing `C-02, C-06, C-10` → `CS2, C-06, C-10` | OK (docs/24 uvod C-11 sekce + tento task record Dependencies + Scope) |
| Acceptance checklist 23 polozek | OK (docs/24 sekce 18 vsechny checked) |
| PROPOSED/INFERRED/VERIFIED label summary | OK (docs/24 sekce 19) |
| Navazuje na existujici infra (EnvConfig.gs + envGuard_ + doPost timing-insensitive compare) ne prepisuje | OK (docs/24 sekce 1 + sekce 5 VERIFIED entries respektuji existujici pattern; runtime API surface (`getConfigValue_`) je extension nad existujici `getEnvConfig_()` / `getSpreadsheetId_()` / `getPreviewWebhookSecret_()` — ne replacement) |

## Output for Audit

- **Report sheet (PROPOSED):** `_asw_budget_ledger` (append-only, 12 fields)
- **Configuration planes (6):** CONFIG / SECRET / LIMIT / BUDGET / KILL_SWITCH / FEATURE
- **Hard-separation invariant:** `CONFIG ≠ SECRET ≠ LIMIT ≠ BUDGET ≠ KILL_SWITCH ≠ FEATURE`
- **Zakazane kolapsy (12):** SERPER_API_KEY as CONFIG; BATCH_SIZE as BUDGET; DRY_RUN as CONFIG forever; EMAIL_SYNC_ENABLED as CONFIG; KILL_SWITCH_GMAIL as FEATURE; BUDGET_GMAIL_DAILY_QUOTA as LIMIT; WEBHOOK_URL as SECRET; Config = Script Properties by definition; retry against LIMIT; no ledger for BUDGET; single-scope kill switch; flag without sunset_date
- **Canonical config table columns (13):** key / plane / scope / source / type / default_value / prod_value / test_value / allowed_values_or_range / owner_stream / affected_consumers / change_control / label
- **VERIFIED IN REPO entries (17):** ASW_ENV, ASW_SPREADSHEET_ID, PREVIEW_WEBHOOK_SECRET, SERPER_API_KEY, DRY_RUN const, ENABLE_WEBHOOK const, WEBHOOK_URL const, BATCH_SIZE const, EMAIL_MAILBOX_ACCOUNT const, EMAIL_SYNC_LOOKBACK_DAYS const, EMAIL_SYNC_MAX_THREADS const, EMAIL_SYNC_REQUIRE_EXACT_MATCH const, EMAIL_SYNC_ENABLED const, MAIN_SHEET_NAME const, LOG_SHEET_NAME const, RAW_IMPORT_SHEET_NAME const, CONTACT_SHEET_NAME const
- **PROPOSED entries per plane:**
  - 4 CONFIG (EMAIL_PROVIDER, BUDGET_RESET_TIMEZONE, KILL_SWITCH_CHECK_INTERVAL_MS, BUDGET_LEDGER_STRATEGY)
  - 9 LIMIT_* Script Properties napric 9 stages (A-05/A-07/A-08 reuse VERIFIED `BATCH_SIZE` bez noveho PROPOSED entry)
  - 7 BUDGET_* (Serper daily quota, Gmail daily send, Gemini tokens, retry max, preview render, follow-up send, bounce rate 7d rolling)
  - 14 KILL_SWITCH_* (1 GLOBAL + 1 ENV + 3 CATEGORY + 4 PROVIDER + 2 API_SURFACE + 1 AUTO_ESCALATE + 2 rezerva)
  - 10 FEATURE_* (FEATURE_EMAIL_SYNC a 9 dalsich)
  - 2 SECRET (SENDGRID_API_KEY, MAILGUN_API_KEY conditional)
- **Secrets inventory 10-field contract:** key / provider / rotation_policy / redaction_mask_pattern / used_in_step / access_pattern / timing_insensitive_compare / pii_class / last_rotated_at / owner
- **Per-stage limits for 9 stages:** A-04 (scrape batch, max runtime, max retries), A-05 (dedupe batch), A-06 (webcheck batch, max serper calls), A-07 (qualify batch), A-08 (preview batch), B-04 (preview render concurrent), C-05 (outbound worker batch, max runtime), C-06 (send per-min, send per-day), C-08 (follow-up batch, max sequences per day)
- **Budget guardrail 10-field contract:** budget_id / category / provider_or_api / scope / reset_policy / warning_threshold_pct=80 / critical_threshold_pct=100 / hard_stop_on_critical / current_consumption / last_reset_at
- **5 budget kategorii:** BG-PROV-OPS (provider ops quotas) / BG-RETRY-EXPLOSION (retry count runaway) / BG-COST-EXPLOSION (API cost) / BG-RUNAWAY-BATCH (batch size) / BG-SILENT-DEGRADATION (quality trend)
- **`_asw_budget_ledger` 12 fields:** ledger_id / budget_id / cs2_run_id / stage / delta_amount / delta_unit / cumulative_at_time / threshold_status / triggering_step / triggering_event_id / timestamp / note
- **Kill switch 5 scopes:** GLOBAL (KILL_SWITCH_ALL_WRITES) / ENV (KILL_SWITCH_PROD_ENFORCE) / CATEGORY (EMAIL_ALL, SCRAPE_ALL, WEBHOOK_ALL) / PROVIDER (GMAIL, SENDGRID, MAILGUN, SERPER) / API_SURFACE (DOPOST_WRITE, PREVIEW_WEBHOOK)
- **Kill switch safe-stop semantics:** inflight dokonci transakcni celek; nove operace safe-stopnou s `kill_switch_triggered` event + cs2_run_id + triggering_step + intended_sheet_writes[] audit
- **Kill switch reset + escalation:** manual pres Script Property update + `kill_switch_reset` event; auto-escalate CATEGORY > 24h → GLOBAL s `KILL_SWITCH_AUTO_ESCALATE=true`
- **Feature flag 8-field contract:** flag_id / rollout_stage / default_value_per_env / target_audience / owner_stream / related_task / sunset_date / label
- **Feature flag rollout stages:** draft / pilot / rollout / stable / deprecated
- **State safety rules (5):** config change atomicity (snapshot na run start); kill switch activation timing (mezi stepy ne uprostred); budget threshold visibility (WARNING event i bez hard_stop); feature flag snapshot na CS2 run start; secret rotation dual-read window pro long-running steps
- **9 `_asw_logs` event types (PROPOSED):** config_value_changed / secret_rotated / limit_exceeded / budget_warning_crossed / budget_critical_crossed / kill_switch_triggered / kill_switch_reset / feature_flag_enabled / feature_flag_disabled
- **4 diagnostic tools (PROPOSED):** diagConfigState / diagKillSwitchState / diagBudgetLedger / diagSecretInventory
- **Runtime API surface (PROPOSED):** getConfigValue_(key, default) / getSecret_(key) / getLimit_(stage_key) / getBudgetState_(budget_id) / isKillSwitchActive_(scope) / isFeatureEnabled_(flag_id) / configSnapshot_()
- **Anti-pattern tabulka 12 radku:** hybrid role DRY_RUN, SECRET leak v CONFIG, category confusion BATCH_SIZE jako BUDGET, kill-switch vs feature confusion, mid-run config change, retry against LIMIT, no ledger for BUDGET, single-scope kill switch, flag bez sunset, secret bez rotation, no KILL_SWITCH_RESET, hardcode SECRET in code
- **Handoff tabulka 13 radku:** CS2 (step entry hook + cs2_run_id pro ledger) / C-04 (gate respects kill_switch_check) / C-05 (outbound worker respects LIMIT + BUDGET + KILL_SWITCH_EMAIL_ALL) / C-06 (provider selection via CONFIG EMAIL_PROVIDER; KILL_SWITCH_GMAIL/SENDGRID/MAILGUN; NormalizedSendErrorClass driver pro budget accounting) / C-07 (inbound ingest respects KILL_SWITCH_EMAIL_ALL) / C-08 (follow-up engine LIMIT + BUDGET + FEATURE_FOLLOWUP_ENGINE) / C-09 (exception queue respects FEATURE_EXCEPTION_QUEUE) / C-10 (9 C-11 event types feed do perf reports operational block; data_completeness_flags_json zaznamena degradace; FEATURE_PERFORMANCE_REPORT) / BX1 (doPost respects FEATURE_DOPOST_WRITE + KILL_SWITCH_DOPOST_WRITE + API_SURFACE kill) / A-04 (scraper respects LIMIT_A04_SCRAPE_PAGES_PER_PORTAL + LIMIT_A04_SCRAPE_BATCH_TIMEOUT_MS + KILL_SWITCH_SCRAPE_ALL + BUDGET_SERPER) / A-06 (webcheck respects LIMIT + BUDGET_SERPER + FEATURE_AUTO_WEB_CHECK_HOOK) / future runtime impl (ConfigManager.gs + Script Properties setup + ledger sheet + event emissions) / future frontend UI (kill-switch toggle panel + budget ledger viewer + feature flag rollout dashboard)
- **VERIFIED IN REPO labely:** EnvConfig.gs (getEnvConfig_, getSpreadsheetId_, isProduction_, getPreviewWebhookSecret_, envGuard_, diagEnvConfig — 201 lines), Config.gs 17 constants (SPREADSHEET_ID legacy fallback, MAIN_SHEET_NAME='LEADS', CONTACT_SHEET_NAME='Ke kontaktování', LOG_SHEET_NAME='_asw_logs', RAW_IMPORT_SHEET_NAME='_raw_import', DRY_RUN=true, ENABLE_WEBHOOK=false, WEBHOOK_URL='', BATCH_SIZE=100, SERPER_CONFIG, EMAIL_SYNC_ENABLED=true, EMAIL_MAILBOX_ACCOUNT='', EMAIL_SYNC_LOOKBACK_DAYS=30, EMAIL_SYNC_MAX_THREADS=50, EMAIL_SYNC_REQUIRE_EXACT_MATCH=true — az 226 lines), 3 VERIFIED Script Properties (ASW_ENV, ASW_SPREADSHEET_ID, PREVIEW_WEBHOOK_SECRET), doPost timing-insensitive compare pattern (VERIFIED v BX1 WebApp.gs), envGuard_ throws "ENV GUARD: PROD environment but spreadsheet ID does not match PROD sheet!" on mismatch
- **INFERRED labely:** threshold defaults (warning 80%, critical 100% — industry standard), safe-stop pattern (derivace z CS3 LockService + CS2 step boundary), auto-escalate CATEGORY > 24h → GLOBAL (INFERRED ops pattern), DOPOST_WRITE API_SURFACE scope (INFERRED z BX1 existence), NormalizedSendErrorClass transient/permanent budget accounting rule (INFERRED z C-06 contract), dual-read window pro secret rotation (INFERRED pattern pro long-running steps), snapshot na run start (INFERRED z CS2 step entry pattern), rotation_policy 90d default (INFERRED security best practice)
- **PROPOSED FOR C-11 labely:** 4 CONFIG Script Properties, 9 LIMIT_* Script Properties, 7 BUDGET_* Script Properties, 14 KILL_SWITCH_* Script Properties, 10 FEATURE_* Script Properties, 2 SECRETs conditional, `_asw_budget_ledger` 12-field sheet, 9 `_asw_logs` event types, 4 diagnostic tools, runtime API surface (7 helper functions), DRY_RUN → KILL_SWITCH_ALL_WRITES migration plan, EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC migration plan, 5-scope kill switch taxonomy, safe-stop semantics, 5 budget kategorii, 5 rollout stages, 5 state safety rules, 12-row anti-pattern table, 13-row handoff tabulka, 6-plane ortogonalni taxonomie + hard-separation invariant, 12 zakazanych kolapsu, configSnapshot_ pattern
- **Dependency narrowing:** puvodni task brief → `C-02, C-06, C-10`. Repo audit → `C-02.md` NEEXISTUJE (`C2.md` je governance hardening unrelated). **Dependency narrowing na `CS2, C-06, C-10`** (3 tasky jako upstream architectural prerekvizita). Dokumentovano v docs/24 uvod C-11 sekce + tento task record Dependencies + Scope.
- **Cross-ref graph:** `Config.gs` constants ↔ Script Properties ↔ `_asw_budget_ledger` (PROPOSED C-11) ↔ `_asw_logs` (CS2) ↔ `_asw_perf_reports` (C-10 operational block) ↔ CS1 lifecycle_state ↔ `_asw_outbound_queue` (C-05) ↔ `_asw_inbound_events` (C-07) ↔ `_asw_exceptions` (C-09) ↔ `_asw_dead_letters` (CS3)

## Known Limits

- SPEC neimplementuje runtime `ConfigManager` / `getConfigValue_()` / `isKillSwitchActive_()` / `checkBudget_()` / `checkLimit_()` / `isFeatureEnabled_()` / `configSnapshot_()` helpery — pouze definuje kontrakt. Implementace je budouci task (`apps-script/ConfigManager.gs` + Config.gs rozsireni + Script Properties setup + ledger sheet creation + 9 `_asw_logs` event emissions + CS2 step entry hooks).
- PROPOSED `_asw_budget_ledger` sheet neni v ramci C-11 vytvoren. Implementacni task ho musi pridat (append-only s protection).
- PROPOSED Script Properties (4 CONFIG + 9 LIMIT + 7 BUDGET + 14 KILL_SWITCH + 10 FEATURE + 2 SECRET = 46 celkem) nejsou v ramci C-11 nastaveny. Implementacni task je musi nastavit per-env (TEST / PROD).
- Dnesni `DRY_RUN=true` zustava **kanonicky kill switch** dokud migrace neproběhne — C-11 SPEC dokumentuje target state, ne okamzity cutover. Backward-compat fallback musi implementovat runtime task.
- Dnesni `EMAIL_SYNC_ENABLED=true` zustava CONFIG const dokud migrace neproběhne — C-11 SPEC dokumentuje reklasifikaci, ne okamzity cutover.
- Frontend UI pro kill-switch toggling je **mimo C-11 scope**. V interim operator meni Script Properties primo v Apps Script editoru (Project Settings → Script Properties). Degraded UX (bez realtime kill-switch panel, bez budget ledger viewer, bez feature flag rollout dashboard).
- Kill switch activation je **manual** (Script Property edit) — auto-activation (napr. na detected reputation damage) je **mimo v1.0 scope** (mozna v2.0 ML-based auto-escalate).
- Auto-escalate CATEGORY > 24h → GLOBAL je **PROPOSED** pres `KILL_SWITCH_AUTO_ESCALATE=true` Script Property — implementacni task musi pridat background check cron.
- Budget ledger je **append-only** ale Google Sheets nema native immutability — implementacni task musi pridat edit-protection (data validation + protected range). Interim: operator nesmi edit rucne.
- Secret rotation je **manual** (Script Property edit) — automation pres GCP Secret Manager je **mimo v1.0**.
- External vault (GCP Secret Manager, AWS Secrets Manager) migration je **mimo C-11 scope** (future task). Script Properties jsou v1.0 source of truth pro secrets.
- Per-operator / per-role config scoping je **mimo v1.0 scope** (single-tenant Script Properties).
- Config change audit dashboard (ops UI) je **mimo C-11 scope** — interim operator cte `_asw_logs` config_value_changed events primo v bunce.
- Config drift detection (`diagConfigState()` compares expected vs actual Script Properties) je **PROPOSED pattern** — implementacni task musi definovat canonical expected list + diff algoritmus.
- `pii_class` hodnoty (none / low / high) jsou **INFERRED kategorie** — ops musi finalizovat per-secret classification (napr. SERPER_API_KEY = high; EMAIL_MAILBOX_ACCOUNT = low).
- Rotation policy 90d default je **INFERRED security best practice** — ops muze override per-secret (napr. PREVIEW_WEBHOOK_SECRET rotace 180d).
- Budget threshold defaults (warning 80%, critical 100%) jsou **PROPOSED defaults** — ops musi kalibrovat na business realitu (napr. BUDGET_SERPER_DAILY_QUERY_QUOTA warning moze byt 60% pri okolnim monthly budget constraint).
- 5 budget kategorii (BG-PROV-OPS / BG-RETRY-EXPLOSION / BG-COST-EXPLOSION / BG-RUNAWAY-BATCH / BG-SILENT-DEGRADATION) jsou **PROPOSED taxonomie** — ops muze pridat nove kategorie (napr. BG-STORAGE-SIZE pri spreadsheet quota concerns).
- 5 kill switch scopes (GLOBAL / ENV / CATEGORY / PROVIDER / API_SURFACE) jsou **PROPOSED taxonomie** — dalsi scopes (PER_USER, PER_LEAD_SEGMENT) jsou **mimo v1.0**.
- Feature flag rollout stages (draft / pilot / rollout / stable / deprecated) jsou **PROPOSED lifecycle** — ops muze pridat stage (napr. canary mezi pilot a rollout).
- snapshot na CS2 run start je **INFERRED pattern** — implementacni task musi pridat `_asw_logs.feature_snapshot_json` + `_asw_logs.config_snapshot_json` fields.
- dual-read window pro secret rotation je **INFERRED pattern** — implementacni task musi definovat window duration (napr. 60 sec) a backward-compat check pri secret compare.
- `KILL_SWITCH_CHECK_INTERVAL_MS` je **PROPOSED optim** — runtime task musi rozhodnout, jestli kazdy CS2 step vola check (overhead) nebo batched check (stale risk).
- Compatibility matrix mezi kill switch scope a feature flag override (napr. KILL_SWITCH_EMAIL_ALL dominuje FEATURE_FOLLOWUP_ENGINE=true) je **INFERRED** — implementacni task musi definovat explicit precedence.
- Audit immutability `_asw_logs` pro config / budget / kill-switch events je **dokumentovana jako invariant** ale Google Sheets append-only enforcement je **runtime task**.
- CI / CD integration (napr. alert na unauthorized Script Property change) je **mimo scope** (future workflow-level task).
- Backfill / replay existing Config.gs constants do Script Properties je **mimo C-11 SPEC scope** — implementacni task musi definovat migration sequence (Config.gs → Script Properties) aniz by rozbil runtime.

## Next Dependency

- **Implementacni task C-11 runtime** — vytvori `apps-script/ConfigManager.gs` soubor, implementuje 7 helper functions (`getConfigValue_`, `getSecret_`, `getLimit_`, `getBudgetState_`, `isKillSwitchActive_`, `isFeatureEnabled_`, `configSnapshot_`), pridava Script Properties setup skript pro TEST + PROD (52 entries), vytvari `_asw_budget_ledger` sheet (12 fields) s audit protection, pridava 9 `_asw_logs` event types, pridava 4 diagnostic tools (diagConfigState / diagKillSwitchState / diagBudgetLedger / diagSecretInventory), implementuje CS2 step entry hook (isKillSwitchActive_ check + checkLimit_ + checkBudget_), implementuje safe-stop semantics, implementuje auto-escalate CATEGORY > 24h cron. DRY_RUN → KILL_SWITCH_ALL_WRITES migrace (backward-compat fallback). EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC migrace. Konzumuje SPEC z docs/24 C-11 sekce.
- **Frontend kill-switch UI (B-TBD)** — implementuje admin panel pro kill-switch toggling (14 Script Properties), budget ledger viewer (realtime z `_asw_budget_ledger`), feature flag rollout dashboard (10 flags + rollout_stage advancement), config drift detection dashboard. Konzumuje runtime API surface z implementacniho tasku.
- **Secret rotation automation (C-TBD)** — integrace s GCP Secret Manager nebo external vault. Automated rotation cron per rotation_policy. Konzumuje secrets inventory kontrakt. **Mimo v1.0 C-11 scope.**
- **Config change audit dashboard (C-TBD)** — frontend pro `_asw_logs` config_value_changed / secret_rotated / kill_switch_triggered events + drill-down by cs2_run_id. Konzumuje stable `_asw_logs` schema.
- **Budget ledger retention policy** — definuje ops policy pro `_asw_budget_ledger` cleanup (napr. > 90 dni → archive). C-11 SPEC jen dokumentuje principle (append-only).
- **C-11 extension task (v2.0)** — pridava pokrocile features: ML-based auto kill-switch activation (reputation damage detection → auto KILL_SWITCH_EMAIL_ALL), per-operator config scoping (multi-tenant), anomaly detection v budget consumption trends, canary rollout stage, webhook-based config change notifications.
- **Storage quota monitoring** (v2.0 future) — Google Sheets quota (10M cells) pribyva kazdym appendem — budget kategorie BG-STORAGE-SIZE nad LEADS + queue + inbound events + exceptions + dead letters + budget ledger + perf reports. Mimo v1.0.
- **CI/CD integration task** (future) — alert na unauthorized Script Property change (compare last-known-good baseline). Mimo scope.
- **Backfill migration task** — definuje Config.gs → Script Properties migration sequence: (1) add PROPOSED Script Properties with defaults; (2) runtime reads Script Property first, fallback to Config.gs const; (3) remove Config.gs const v dalsi iteraci. Implementacni task musi rozdelit na safe kroky.

Do te doby zustava Autosmartweby bez formalni **configuration layer governance** — Config.gs je heterogeni (mix CONFIG / LIMIT / FEATURE / KILL_SWITCH hybridu), Script Properties ma jen 3 VERIFIED entries (ASW_ENV, ASW_SPREADSHEET_ID, PREVIEW_WEBHOOK_SECRET), neexistuje budget tracking (Serper monthly queries, Gmail daily quota — slepe spotreba), neexistuje formalni kill switch (operator zastavuje pres `DRY_RUN=true` flip + komentar), neexistuje feature flag rollout (novy code merge = rovnou all-PROD), neexistuje observability (zmena v Config.gs neni v `_asw_logs`, zmena Script Properties v UI editoru neni auditovana), secrets nejsou v inventory (rotation policy neformalizovana, PII classification chybi). Kriticky: pri reputation incident na Gmail neni jak za 30 sekund zastavit odesilani bez code push + deploy; pri Serper quota overflow neni jak okamzite safe-stopnout; pri novem feature development neni jak rollout graduallne (vsechno PROD hned). C-11 SPEC formalizuje **jak bude tento configuration vacuum naplnen** — 6-plane ortogonalni taxonomie s hard-separation invariantem, canonical config table pro 17 VERIFIED + 52 PROPOSED entries, secrets inventory pro 7 celkem, per-stage limits pro 9 pipeline stages, budget guardrails pro 7 primary spend categories, 5-scope kill switch model (GLOBAL / ENV / CATEGORY / PROVIDER / API_SURFACE) s safe-stop semantics, feature flag rollout lifecycle (draft / pilot / rollout / stable / deprecated), state safety rules pro 5 scenarios, 9 `_asw_logs` event types pro full observability, `_asw_budget_ledger` append-only audit trail, 4 diagnostic tools, runtime API surface (7 helper functions), DRY_RUN → KILL_SWITCH_ALL_WRITES + EMAIL_SYNC_ENABLED → FEATURE_EMAIL_SYNC migracni plany bez okamziteho cutover. C-11 SPEC je prerekvizita pro jakoukoli implementaci kill-switch runtime / budget tracking / feature flag rollout a current state zachovava runtime stability aniz by pridal technicky dluh. SPEC navazuje na existujici infra (EnvConfig.gs + envGuard_ + Script Properties pattern + doPost timing-insensitive compare) — nepisu ji od nuly, rozsiruji ji tak, aby dostala strukturu. Dependency narrowing `C-02, C-06, C-10` → `CS2, C-06, C-10` je formalni SPEC-level rozhodnuti zajistujici, ze implementacni task zna spravnou upstream architectural prerekvizitu (3 source tasky, ne neexistujici C-02).
