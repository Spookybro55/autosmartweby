# Audit Findings — centrální seznam

> Všechny nálezy ze všech fází auditu, konsolidované.
> Finding ID formát a severity viz [README.md](README.md).

---

## Findings table

| ID | Doména | Popis | Severity | Evidence | Impact | Suggested Action | Status |
|----|--------|-------|----------|----------|--------|------------------|--------|
| DM-001 | Data Model | Extension columns count drift: docs tvrdí 45, realně 55 v `EXTENSION_COLUMNS` | P2 | `apps-script/Config.gs:68-125` (55 entries) vs `docs/23-data-model.md:35` heading "(45 sloupcu)" | Nováček, který se orientuje podle docs, bude mít chybnou mentální mapu schema; drift narůstá s každým B/A-task append | Aktualizovat `docs/23:35` heading na reálný počet; přidat do docs audit check do CI (`check-doc-sync.mjs` by mohl validovat count); viz [02-data-model.md](domains/02-data-model.md#3-rozpory-merge-a-vs-b) | Open |
| DM-002 | Data Model | Intra-batch T3/T4 dedupe collision ponechává `duplicate_of_lead_id` prázdné (inconsistence s T1/T2 HARD_DUPLICATE pattern) | P2 | `apps-script/DedupeEngine.gs` — intra-batch bucket `REVIEW_INTRA_BATCH_T3/T4` nesettuje `duplicate_of_lead_id` field; T1/T2 HARD_DUPLICATE flow nastavuje | Operator reviewing duplicate candidate nemá pointer k matching řádku; manual resolution pomalejší; potenciálně chybí audit trail | Dokumentovat v `docs/contracts/dedupe-decision.md` proč T3/T4 intra-batch empty (design decision); nebo kód doplnit o duplicate_of_lead_id pro intra-batch matches; viz [02-data-model.md#5](domains/02-data-model.md#5-bílá-místa-v-dokumentaci-kód-dělá-docs-nezmiňuje) | Open |
| DM-003 | Data Model | `branch_key` je v EXTENSION_COLUMNS, zapisováno při qualify, ale **nigde runtime nečteno** (dead field) | P3 | `apps-script/PreviewPipeline.gs:292` (write); `apps-script/Config.gs:71` (column def); grep `hr.get.*branch_key` / `row\['branch_key'\]` v runtime code → 0 match (mimo A-05 test harness) | Dead field zabírá sloupec v LEADS, mate reviewera/nového developera; možný future scope (multi-branch companies), ale neexistuje plán | Dokumentovat záměr (future use) v `docs/23` nebo smazat z `EXTENSION_COLUMNS` a writes; viz [02-data-model.md#5](domains/02-data-model.md#5-bílá-místa-v-dokumentaci-kód-dělá-docs-nezmiňuje) | Open |
| DM-004 | Data Model | LEGACY_COL header validation na každém write-back call je křehká — column insert v LEADS blokuje write-back | P2 | `apps-script/ContactSheet.gs:728-742` (validateLegacyColHeaders_ per `onContactSheetEdit`); `apps-script/Config.gs:58-65` (LEGACY_COL_HEADERS) | Pokud operator / admin přidá sloupec do LEADS sheetu (insert col), shift pozic way-above LEGACY_COL[20] nezpůsobí problém, ale insert mezi 1-20 blokuje celý write-back — silent failure s cell-level notou, ne user-friendly | Docs warning v `docs/23` ohledně "nevkládat sloupce mezi 1-20 LEGACY_COL"; zvážit migration HeaderResolver-first pro všechny legacy reads; viz [02-data-model.md#5](domains/02-data-model.md#5-bílá-místa-v-dokumentaci-kód-dělá-docs-nezmiňuje) | Open |
| DM-005 | Data Model | Preview snapshot fields (`preview_headline`, `preview_subheadline`, `preview_cta`) jsou kopie z `preview_brief_json`, drift při re-edit `preview_brief_json` bez re-sync kopií | P3 | `apps-script/PreviewPipeline.gs` (webhook response parse → writes all 4 atomically); následná úprava `preview_brief_json` (manual edit nebo re-run) nezpůsobí automatický re-sync snapshot copies | Operator vidí Ke kontaktování hyperlink na starý headline v sloupci 4 (Preview), ale v brief_json už je nová verze — potential confusion | Dokumentovat jako known limit v `docs/23:119-135` (preview snapshot columns); nebo dodat manual "re-sync snapshot copies" menu button; viz [02-data-model.md#5](domains/02-data-model.md#5-bílá-místa-v-dokumentaci-kód-dělá-docs-nezmiňuje) | Open |
| DM-006 | Data Model | `lead_stage='NEW'` je overloaded — pokrývá CS1 sub-states RAW_IMPORTED → WEB_CHECKED (4 lifecycle states) | P3 | `docs/21-business-process.md:70-174` (CS1 18 states); `apps-script/Config.gs:169-177` (LEAD_STAGES má jen 6 hodnot); `PreviewPipeline.gs` `evaluateQualification_` transition z NEW přímo na QUALIFIED/DISQUALIFIED/REVIEW | Technical debt documented v CS1 spec; audit trail granularity chybí pro pre-qualification states (nemůžu z lead_stage poznat, jestli lead má web check nebo ne) | Buď rozšířit `LEAD_STAGES` na full CS1 alignment, nebo explicit "known debt" komentář v Config.gs; viz [02-data-model.md#3](domains/02-data-model.md#3-rozpory-merge-a-vs-b) | Open |
| AS-001 | Apps Script | 4+ funkcí bez LockService (processPreviewQueue, qualifyLeads, buildEmailDrafts, setupPreviewExtension) — concurrent-run risk | P1 | `apps-script/PreviewPipeline.gs:885,245,676,14` (žádný `LockService.getScriptLock()` v těchto fcích); grep `tryLock` ukazuje jen AutoQualifyHook/AutoWebCheckHook/ContactSheet/WebAppEndpoint | Při překryvu 15-min triggeru s manuální invokací z menu může dojít ke duplicate webhook calls, duplicate state transitions (QUALIFIED/DISQUALIFIED race), ztrátě dat. 15-min cadence + worst-case single-call > 20 s × batch 100 = timeout + restart možný | Přidat `LockService.getScriptLock().tryLock(5000)` wrapper do všech public entry-point funkcí; viz [03-apps-script.md#sekce-2](domains/03-apps-script.md#sekce-2-robustnost-a-failure-modes-agent-b-failure-mode-analyst) | Open |
| AS-002 | Apps Script | Pipeline nemá 6-min timeout guard (stopwatch + break) — worst case hit consumer limit | P1 | `apps-script/PreviewPipeline.gs:885-1094` (processPreviewQueue bez `Date.now()` check); `qualifyLeads`, `buildEmailDrafts`, `MailboxSync.runMailboxSync_` — žádný stopwatch pattern | Při BATCH_SIZE=100 a webhook volání > 20 s → 2000 s > 6 min hard limit; Apps Script konzole: "Exceeded maximum execution time"; pipeline padá mid-batch, část řádků v `GENERATING` stavu bez návratu | Stopwatch pattern `var startMs = Date.now(); ... if (Date.now()-startMs > 5*60*1000) { aswLog_('WARN','timeout'); break; }` v hlavních cyklech; viz [03-apps-script.md#sekce-2](domains/03-apps-script.md#sekce-2-robustnost-a-failure-modes-agent-b-failure-mode-analyst) | Open |
| AS-003 | Apps Script | `OutboundEmail.sendCrmEmail` neověřuje globální `DRY_RUN=true` — odešle mail i v test módu | P1 | `apps-script/OutboundEmail.gs:38,47-275` (executeCrmOutbound_, sendGmailMessage_) — žádný `if (DRY_RUN) return` check; `apps-script/Config.gs:24` (`var DRY_RUN = true;`) | Pokud operátor klikne "Odeslat e-mail" v menu zatímco DRY_RUN=true v Configu, Gmail skutečně odešle. Byl-li Config nastaven pro TEST, mail půjde přes účet deployera. User-visible confirm dialog existuje, ale DRY_RUN guard chybí | Přidat `if (DRY_RUN && mode === 'SEND') { return ui.alert('DRY_RUN je aktivní — skutečný send zablokován'); }` na začátku `executeCrmOutbound_`; viz [03-apps-script.md#sekce-2](domains/03-apps-script.md#sekce-2-robustnost-a-failure-modes-agent-b-failure-mode-analyst) | Open |
| AS-004 | Apps Script | `apps-script/.clasp.json.prod` commitnutý do main — PROD scriptId (first 4: `1fnL`) v public source | P1 | `git ls-tree -r HEAD` ukazuje `100644 blob 9b96ff52... apps-script/.clasp.json.prod`; `.gitignore` neobsahuje `.clasp.json.prod` | PROD scriptId je identifikátor na Apps Script projekt; s write access přes OAuth kdokoli s project membership může deploy. Bez write access je to info-disclosure, ale zjednodušuje cílený útok (hacker ví, jaký projekt cílit phishingem na projekt-ownera) | `.clasp.json.prod` dát do `.gitignore`, vytvořit `.clasp.json.prod.example` s placeholderem. Doc pro team o pattern "přepni scriptId před `clasp push` do PROD"; viz [03-apps-script.md#sekce-3](domains/03-apps-script.md#sekce-3-security-agent-c-security-auditor) | Open |
| AS-005 | Apps Script | `WebAppEndpoint.doPost` token compare není timing-safe (`payload.token !== secret`) | P1 | `apps-script/WebAppEndpoint.gs:15` — `if (!secret || payload.token !== secret)` | Teoreticky timing-attack leak z délky common-prefix match. V Apps Script runtime via Google frontend je prakticky transport-level fixed-time, ale best practice porušeno. Kombinace s ANYONE_ANONYMOUS access v appsscript.json → žádný rate limit na straně AS | Implementovat HMAC-based compare: `sha256(token) vs sha256(secret)` a compare jako hex string (constant-time hash); nebo přemigrovat token do HTTP header + gateway auth; viz [03-apps-script.md#sekce-3](domains/03-apps-script.md#sekce-3-security-agent-c-security-auditor) | Open |
| AS-006 | Apps Script | `Config.gs:14` SPREADSHEET_ID hardcoded jako "legacy PROD fallback" — PROD ID `1RBc...` v source | P2 | `apps-script/Config.gs:10-14` (komment "Do not use in runtime", ale hodnota commitnutá); runtime čte `getSpreadsheetId_()` z EnvConfig; legacy fallback by mohl být v případě chybějících Script Properties | PROD Sheet ID je data-sensitive identifier; kdo má read access na repo + získal OAuth user impersonation, může číst/editovat PROD data. Komment říká "legacy fallback", ale real risk = budoucí edit někoho kdo si nevšimne a použije v novém kódu | Odstranit konstantu úplně; pokud fallback nutný, `var SPREADSHEET_ID = '';` a error-out v getSpreadsheetId_ když Script Properties chybí; viz [03-apps-script.md#sekce-3](domains/03-apps-script.md#sekce-3-security-agent-c-security-auditor) | Open |
| AS-007 | Apps Script | Empty catch blok `PreviewPipeline.gs:1071 catch (ignore) {}` — explicitně "ignore" bez komentu proč | P2 | `apps-script/PreviewPipeline.gs:1071` (`} catch (ignore) {}` v processPreviewQueue error handling); ostatní empty catche v ContactSheet.gs jsou commentované "cell note best-effort" | Silent failure bez loggingu; při budoucím debugging se ztrácí stopa. Contextual: je uvnitř processPreviewQueue kde právě byl webhook call, takže `ignore` může maskovat post-webhook cleanup failure | Buď přidat `aswLog_('WARN', 'processPreviewQueue', 'post-webhook cleanup ignored: ' + ignore.message)`, nebo krátký komentář v kódu proč je ignore OK; viz [03-apps-script.md#sekce-2](domains/03-apps-script.md#sekce-2-robustnost-a-failure-modes-agent-b-failure-mode-analyst) | Open |
| AS-008 | Apps Script | B-06 review write není tranzakční (4× setValue bez rollback) — částečný write při selhání uprostřed | P2 | `apps-script/ContactSheet.gs:678-827` (B-06 write-back) — sekvence setValue(review_decision) + setValue(reviewed_at) + setValue(reviewed_by) + setValue(preview_stage); lock chrání concurrent runs ale ne atomicitu zápisů | Pokud první setValue projde a druhý selže (quota, transient error), řádek má `review_decision` ale chybí `reviewed_at`/`reviewed_by` → invalid state. Pipeline dále čte jen `review_decision`, ale audit chybí | Konsolidovat 4× setValue do jednoho `setValues([[...]])` na sousední range, nebo explicitní two-phase write s cleanup on fail; viz [03-apps-script.md#sekce-2](domains/03-apps-script.md#sekce-2-robustnost-a-failure-modes-agent-b-failure-mode-analyst) | Open |
| AS-009 | Apps Script | `installProjectTriggers` nemá cleanup obsolete handlerů (přejmenování fce → trigger duch) | P2 | `apps-script/PreviewPipeline.gs:1345-1424` — idempotent pro **stejné** handler names (hasTime/hasOnOpen atd.), ale žádný `ScriptApp.deleteTrigger()` loop pro jiné handler names | Pokud v budoucnu někdo přejmenuje `processPreviewQueue` → `runPreviewPipeline`, starý trigger zůstane a tík 15 min volá neexistující fci → Stackdriver error spam + zbytečný quota spend. `installProjectTriggers` obvolaný po deploy nesmaže obsolete | Na začátku installProjectTriggers loop přes `ScriptApp.getUserTriggers(ss)` a smazat vše co **není** v known-handlers whitelistu; viz [03-apps-script.md#sekce-2](domains/03-apps-script.md#sekce-2-robustnost-a-failure-modes-agent-b-failure-mode-analyst) | Open |
| AS-010 | Apps Script | Dead diag / pilot kód v menu: `auditCurrentSheetStructure`, `runWebhookPilotTest` | P3 | `apps-script/PreviewPipeline.gs:1263` (auditCurrentSheetStructure — manual diag only); `apps-script/PreviewPipeline.gs:1436` (runWebhookPilotTest — B-07 pilot, post-production redundantní) | UI clutter v menu; operator může omylem spustit a vyvolat webhook call na pilot-only set, potenciálně duplicate preview generation | Buď přesunout do `Menu` → "Debug" submenu, nebo označit jako dev-only (hide když `ASW_ENV=PROD`); viz [03-apps-script.md#sekce-1](domains/03-apps-script.md#sekce-1-funkční-mapa-agent-a-funkční-inventář) | Open |
| AS-011 | Apps Script | Script Properties uložené plaintext (no Vault/KMS) — per-project shared secrets | P3 | `PropertiesService.getScriptProperties()` vzor v EnvConfig.gs, LegacyWebCheck.gs:24,134, WebAppEndpoint.gs:14 — žádné encrypt-at-rest, žádný audit trail | Editor-level access k Apps Script projektu = read-access ke všem tajemstvím. Standard Apps Script runtime (není chyba kódu), ale doc-worthy — tým by měl vědět, kdo má "Editor" role | Doc note v `docs/22-technical-architecture.md` nebo `docs/27-infrastructure-storage.md` o tomto faktu; review ACL Apps Script projektu (manuální krok); viz [MANUAL_CHECKS.md](MANUAL_CHECKS.md) | Open |
| AS-012 | Apps Script | Frontend → AS token chodí v JSON body (`payload.token`) místo HTTP Authorization header | P2 | `apps-script/WebAppEndpoint.gs:12-15` (`payload = JSON.parse(e.postData.contents); ... payload.token`); frontend `crm-frontend/app/api/*` posílá token v body | Token může být zalogován v proxy/middleware logs, request body je logovaný standardně častěji než headers. Google Apps Script request log naopak headers běžně neexposuje, ale upstream proxy ano | Přemigrovat na `Authorization: Bearer <token>` — `e.parameter` Apps Script může číst Authorization; vyžaduje changes na frontend + AS stranu; viz [03-apps-script.md#sekce-3](domains/03-apps-script.md#sekce-3-security-agent-c-security-auditor) | Open |
| FE-001 | Frontend | `SESSION_SECRET` empty fallback (`process.env.NEXTAUTH_SECRET \|\| ''`) — jen warn, nefail-fast → fakeable HMAC pokud env unset | P1 | `crm-frontend/src/app/api/auth/login/route.ts:7,9-11`; `crm-frontend/src/middleware.ts:4` (paralelní pattern) | Při missing env var (deploy bez secrets, lokální dev bez .env) se HMAC podepisuje prázdným klíčem — token-forging je pak triviální. Server jen warnuje a běží dál; útočník dostane platnou session bez znalosti hesla | Fail-fast: `if (!SESSION_SECRET) throw new Error('NEXTAUTH_SECRET required')` na load-time; CI test že env vars jsou nastaveny před deploy; viz [04-frontend.md#sekce-3](domains/04-frontend.md#sekce-3-security-build-agent-c) | Open |
| FE-002 | Frontend | Login `password !== AUTH_PASSWORD` non-constant-time compare → timing attack | P1 | `crm-frontend/src/app/api/auth/login/route.ts:42` (`if (password !== AUTH_PASSWORD)`) | JS `!==` short-circuits na první rozdílný byte. Útočník může zjišťovat heslo char-by-char přes timing analysis (~ms rozdíl per match-prefix), zvláště při vysokorychlostním network access. Combine s žádným rate limitem na `/api/auth/login` = realistic exploit | Použít `crypto.timingSafeEqual(Buffer.from(password), Buffer.from(AUTH_PASSWORD))` po length check; nebo přejít na bcrypt/argon2 hash storage; viz [04-frontend.md#sekce-3](domains/04-frontend.md#sekce-3-security-build-agent-c) | Open |
| FE-003 | Frontend | `next.config.ts` prázdný — chybí CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy | P1 | `crm-frontend/next.config.ts:4-7` (`const nextConfig: NextConfig = { /* config options here */ };`) | Bez CSP může XSS exploit načítat externí scripty; bez X-Frame-Options je app vulnerable na clickjacking (preview/[slug] je public, atraktivní target); bez HSTS první request může jít přes HTTP a být MITM | Přidat `headers()` v next.config s baseline security headers (CSP, HSTS s `max-age=31536000`, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin); viz [04-frontend.md#sekce-3](domains/04-frontend.md#sekce-3-security-build-agent-c) | Open |
| FE-004 | Frontend | Phantom dep `next-auth ^5.0.0-beta.30` v package.json, 0 imports v src/ | P1 | `crm-frontend/package.json:20`; `grep -rn "next-auth\|from 'next-auth'" src/` = 0 výskytů | Beta unreleased package jako prod dep zvyšuje attack surface (transitive deps), supply-chain riziko, npm audit hits, build-time download. Žádný runtime používání = clutter bez benefitu. Také zaměňující při onboardingu ("aha, používáme next-auth") | Odstranit `next-auth` z package.json + `npm uninstall next-auth` + `npm prune`; viz [04-frontend.md#sekce-1](domains/04-frontend.md#sekce-1-struktura-routing-agent-a) | Open |
| FE-005 | Frontend | 4 dead custom hooks (`use-leads`, `use-lead-detail`, `use-lead-update`, `use-dashboard-stats`) — defined, 0 imports | P2 | `crm-frontend/src/hooks/*.ts` (4 souborů); `grep -rn "useLeads\|useLeadDetail\|useLeadUpdate\|useDashboardStats" src/app src/components` = 0 hits | Pages duplikují fetch+useEffect+AbortController pattern inline místo abstrahovat do hooků. Nepřehlédnutelné při onboardingu (vidí "use-leads.ts" a má za to, že je použivaný), maintenance burden | Buď migrovat pages na hooks (a doplnit unit testy), nebo smazat hooks; viz [04-frontend.md#sekce-1](domains/04-frontend.md#sekce-1-struktura-routing-agent-a) | Open |
| FE-006 | Frontend | `LeadDetailDrawer` redefinuje lokální `Lead`/`LeadEditableFields` interfaces (type drift risk) | P2 | `crm-frontend/src/components/leads/lead-detail-drawer.tsx:59-104` (lokální interface deklarace duplikují `domain/lead.ts:6-59`) | Pokud canonical `Lead` v `domain/lead.ts` přidá pole, drawer ho neuvidí (silent type drift). Pokud field ubude, drawer ho stále posílá → server 400 nebo fail. Cross-module type contract není vynucen | Importovat `Lead`/`LeadEditableFields` z `@/lib/domain/lead`; smazat lokální duplikáty; viz [04-frontend.md#sekce-2](domains/04-frontend.md#sekce-2-data-flow-state-agent-b) | Open |
| FE-007 | Frontend | Žádná client-side form validace — jen server-side (round-trip pro každou bad input) | P2 | `crm-frontend/src/components/leads/lead-detail-drawer.tsx` — žádné `if (form.salesNote.length > 5000) return` před submit; server `update/route.ts:7-46` validuje a vrátí 400 | UX: user získá toast "Chyba při ukládání" bez konkretního důvodu, musí experimentálně zkoušet co je špatně. Network round-trip pro každý attempt; pomalé na slow connection | Přidat zod schema pro `LeadEditableFields` na klientu; show inline error message před submit; viz [04-frontend.md#sekce-2](domains/04-frontend.md#sekce-2-data-flow-state-agent-b) | Open |
| FE-008 | Frontend | `<Suspense>` wrap v `LeadsPage` bez `fallback` + `LeadsPageInner` fetchuje sám v useEffect → Suspense never triggers | P2 | `crm-frontend/src/app/leads/page.tsx:117-123` (Suspense bez fallback prop); `LeadsPageInner` `'use client'` + inline fetch | Suspense pattern je no-op zde. Nikoli aktivně škodlivý, ale signalizuje cargo-cult code (developer myslel, že Suspense automaticky catchne useEffect fetch — nezachytí). Revize architektury vyžaduje pochopení vzoru | Buď migrovat na server component s `await fetch()` (RSC pattern, Suspense suspends naturally), nebo Suspense odstranit a doplnit explicit `<Skeleton />` pro loading state; viz [04-frontend.md#sekce-2](domains/04-frontend.md#sekce-2-data-flow-state-agent-b) | Open |
| FE-009 | Frontend | `.gitignore` chybí `.clasprc.json`, `*.pem`, `credentials.json`, `*.key` patterns | P2 | `.gitignore` (root, 60 řádků) — žádný z výše uvedených; INVENTORY.md (Phase 1) také flaglo | Pokud někdo dropne service account JSON keyfile do repa nebo `.clasprc.json` (clasp OAuth refresh token v `~/.clasprc.json` ale taky local), hrozí accidental commit. Public GitHub leak service account = full read/write na connected Google services | Přidat patterns do `.gitignore`: `.clasprc.json`, `*.pem`, `*.key`, `credentials.json`, `service-account.json`; viz [04-frontend.md#sekce-3](domains/04-frontend.md#sekce-3-security-build-agent-c) | Open |
| FE-010 | Frontend | Žádný data cache — dashboard + leads page volá `/api/leads` nezávisle = 2× plný Sheets API read per page transition | P2 | `crm-frontend/src/lib/google/sheets-reader.ts:27-44` (jen header-cache 5min, žádný data cache); pages `dashboard/page.tsx`, `leads/page.tsx` volají `fetch('/api/leads')` v useEffect | Při návštěvě dashboard → leads → dashboard = 3 plné Sheet reads. Quota Sheets API je 60 reads/min/user na default tier; při real-world traffic dosažitelné. Také latence — každá nav = network + Sheets fetch | Použít `unstable_cache` nebo `react.cache()` v sheets-reader; nebo move na SWR/React Query s deduplication; nebo `revalidate: 30` na route handler; viz [04-frontend.md#sekce-2](domains/04-frontend.md#sekce-2-data-flow-state-agent-b) | Open |
| FE-011 | Frontend | `console.warn/error` v production routes — preview slug, raw error details | P3 | `crm-frontend/src/app/api/auth/login/route.ts:10,32`; `api/preview/render/route.ts:103-106`; `api/leads/[id]/update/route.ts:109` | Vercel platform-logs zobrazí console.* výstupy, ale bez structure / log-level filtering. Pro audit / debug investigation je žádný strukturovaný log; PII jako preview slug nebo email v error messages může proniknout do log aggregator | Přepnout na `console.error` strukturovaný (JSON object), ideálně Sentry/Logtail integration; redact PII v error logs; viz [04-frontend.md#sekce-3](domains/04-frontend.md#sekce-3-security-build-agent-c) | Open |
| FE-012 | Frontend | Pages reimplementují fetch+AbortController+useEffect inline (paralel s unused hooks v `src/hooks/`) | P3 | `crm-frontend/src/app/dashboard/page.tsx:25-58`, `leads/page.tsx:143-157`, `follow-ups/page.tsx:132-144` — 3 různé inline copies | Code duplication; bug-fix v jedné komponentě nepropagne do ostatních. Když se mění error UX nebo loading state, je třeba update 3+ míst. Cross-ref FE-005 (dead hooks) — refactoring opportunity | Smazat dead hooks NEBO migrovat pages na hooks; konsoliduj pattern; viz [04-frontend.md#sekce-1](domains/04-frontend.md#sekce-1-struktura-routing-agent-a) | Open |
| FE-013 | Frontend | `input-group.tsx` UI komponenta v shadcn boilerplate, nikdy importovaná | P3 | `crm-frontend/src/components/ui/input-group.tsx`; grep ukazuje 0 imports | Dead code z shadcn scaffold. Maintenance burden (TypeScript check / lint na souboru, který nikdo nepoužívá), ale nevýznamné | Smazat soubor; nebo dokumentovat plán pro budoucí použití; viz [04-frontend.md#sekce-1](domains/04-frontend.md#sekce-1-struktura-routing-agent-a) | Open |
| FE-014 | Frontend | Inline preview store v `api/preview/render` — memory only, lost on restart | P3 | `crm-frontend/src/app/api/preview/render/route.ts:95` (in-memory map upsert); B-04/B-05 design notes | Při Vercel cold-start nebo deploy preview briefs zmizí. UX: ke kontaktování operátor vidí "preview not found". Acknowledged v B-04/B-05/B-06 design, ale nezdokumentováno explicitně v `.env.example` ani README | Doc note v `crm-frontend/README.md` o ephemeral storage; budoucí migrace na Vercel KV / Redis / Sheets-based cache; viz [04-frontend.md#sekce-1](domains/04-frontend.md#sekce-1-struktura-routing-agent-a) | Open |

---

## Severity distribution (progress tracker)

| | P0 | P1 | P2 | P3 | Celkem |
|---|----|----|----|----|--------|
| DM (Data Model) | 0 | 0 | 3 | 3 | 6 |
| AS (Apps Script) | 0 | 5 | 5 | 2 | 12 |
| FE (Frontend) | 0 | 4 | 6 | 4 | 14 |
| IN (Integration) | 0 | 0 | 0 | 0 | 0 |
| DP (Deploy Pipeline) | 0 | 0 | 0 | 0 | 0 |
| SEC (Security) | 0 | 0 | 0 | 0 | 0 |
| FF (Funnel Flow) | 0 | 0 | 0 | 0 | 0 |
| BLD (Buildability) | 0 | 0 | 0 | 0 | 0 |
| DOC (Docs & Onboarding) | 0 | 0 | 0 | 0 | 0 |
| CC-NEW (Newbie) | 0 | 0 | 0 | 0 | 0 |
| CC-OPS (DevOps) | 0 | 0 | 0 | 0 | 0 |
| CC-SEC (Attacker) | 0 | 0 | 0 | 0 | 0 |
| CC-QA (QA) | 0 | 0 | 0 | 0 | 0 |
| **Total** | **0** | **9** | **14** | **9** | **32** |

---

## Notes

- Status `Open` = všechny findings (audit je read-only, nic se neopravuje)
- Evidence je vždy `soubor:řádek` odkaz na konkrétní místo
- Suggested Action je návrh, **neprovádí se** jako součást auditu
- Cross-refs: každý finding odkazuje na detailní analýzu v odpovídajícím `domains/XX-*.md` nebo `cross-check/11*-*.md`
