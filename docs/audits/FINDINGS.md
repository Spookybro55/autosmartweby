# Audit Findings — centrální seznam

> Všechny nálezy ze všech fází auditu, konsolidované.
> Finding ID formát a severity viz [README.md](README.md).

---

## Findings table

| ID | Doména | Popis | Severity | Evidence | Impact | Suggested Action | Status |
|----|--------|-------|----------|----------|--------|------------------|--------|
| IN-001 | Integration | Stale příklad `apps-script-endpoint.gs.example` diverguje od reálného `WebAppEndpoint.gs` — používá `payload.rowNumber` pro lookup a non-normalizovaný identity check; reálný kód lookuje přes `lead_id` a normalizuje identity. | P1 | `crm-frontend/src/lib/google/apps-script-endpoint.gs.example:38-58` vs `apps-script/WebAppEndpoint.gs:30-94` | Onboarding/copy-paste pasti: kdo bude implementovat nový endpoint podle příkladu, dostane jiný (a méně bezpečný) pattern než produkce. Může se stát zdrojem regrese. | Buď příklad smazat, nebo synchronizovat s reálným endpointem. Pokud zachovat, přidat banner "DEPRECATED — see apps-script/WebAppEndpoint.gs". | Open |
| IN-002 | Integration | Frontend writer posílá v payloadu `rowNumber`, backend ho ignoruje (lookup je přes `lead_id` přes `findRowByLeadId_`). Dead field na drátu. | P2 | `crm-frontend/src/lib/google/apps-script-writer.ts:51` (sendá `rowNumber`); `apps-script/WebAppEndpoint.gs:72` (lookup ignoruje rowNumber) | Žádný runtime impact, ale matoucí kontrakt: čtenář kódu si myslí, že `rowNumber` je zdroj identity. Nárůst confidence-degradation pro budoucí refactor. | Odstranit `rowNumber` z payloadu ve writer (FE) **nebo** explicitně použít rowNumber v BE jako fast-path s lead_id fallback. Vybrat jednu strategii a zdokumentovat. | Open |
| IN-003 | Integration | Docs drift: `docs/12-route-and-surface-map.md:27` říká `POST` pro `/api/leads/[id]/update`, ale implementace je `PATCH`. | P2 | `docs/12-route-and-surface-map.md:27` vs `crm-frontend/src/app/api/leads/[id]/update/route.ts:49` | Rozcházející dokumentace zhoršuje onboarding a může vést k chybám při psaní integračních klientů. | Aktualizovat docs/12 na `PATCH`. | Open |
| IN-004 | Integration | Hook `useLeadUpdate` je definovaný se správnou double-submit ochranou a structured error handlingem, ale **nikde se neimportuje**. Lead detail drawer dělá vlastní fetch bez těchto ochran. | P2 | `crm-frontend/src/hooks/use-lead-update.ts:6` (definice); `crm-frontend/src/components/leads/lead-detail-drawer.tsx:250-262` (vlastní fetch); `grep useLeadUpdate` v `crm-frontend/src` najde jen definici | Buď dead code (hook k odstranění), nebo missed integration (drawer postrádá ochranu). Konkrétně: bez double-submit ochrany může dvojklik vyvolat dva paralelní writes (LockService je seserializuje, ale UI nedostane správný feedback). | Buď napojit drawer na `useLeadUpdate`, nebo hook smazat. | Open |
| IN-005 | Integration | `apps-script-writer.ts` defaultuje `success` na **true** pokud pole v response chybí: `return { success: data.success ?? true, error: data.error }`. Pokud backend někdy vrátí jiný shape, FE tiše prochází. | P1 | `crm-frontend/src/lib/google/apps-script-writer.ts:66` | Silent failure: chybný response shape vypadá jako úspěch. Update se neprovede, ale UI zobrazí success toast. Data ze sheetu zůstanou stará bez upozornění. | Změnit na `data.success === true` (explicitní). Přidat unit test pro response shapes. | Open |
| IN-006 | Integration | `lead-detail-drawer.tsx:255` kontroluje pouze `res.ok`, **nečte** `data.error` ani `data.success`. Při chybě zobrazí generický toast `"Chyba při ukládání"` bez konkrétní zprávy. | P2 | `crm-frontend/src/components/leads/lead-detail-drawer.tsx:250-262` | UX: user neví proč selhalo. Operátor neví, jestli má retry, opravit data, kontaktovat dev. Ladění je závislé na server logs. | Přečíst `data.error` z response a propsat do toastu (nebo přepnout na `useLeadUpdate` hook viz IN-004). | Open |
| IN-007 | Integration | Backend identity check (`payload.businessName/city` vs sheet) je **podmíněný** přítomností pole v payloadu. Pokud caller token získá ale identity fields neuvede, zápis projde bez ověření. | P1 | `apps-script/WebAppEndpoint.gs:81-94` `if (bnIdx !== null && payload.businessName)` | Frontend writer dnes vždy posílá businessName/city, takže produkční flow OK. Ale **kontrakt to negarantuje** — kdokoli s tokenem může bypassnout identity check vynecháním pole. Defense-in-depth gap. | Změnit na povinné: pokud `bnIdx` existuje v sheetu, vyžadovat `payload.businessName`. Stejně pro city. Nebo přidat samostatný kontrakt-level required flag. | Open |
| IN-008 | Integration | `outreach_stage` projde dvojitou transformací: FE EN key (`OutreachStageKey`) → `humanizeOutreachStage` → CZ label → wire → backend `reverseHumanizeOutreachStage_` → EN key → write to sheet. Fragile coupling. | P2 | `crm-frontend/src/lib/google/apps-script-writer.ts:29` (humanize) + `apps-script/WebAppEndpoint.gs:101` (reverse). Dictionary v `crm-frontend/src/lib/config.ts:38-45` (`OUTREACH_STAGES`). Backend dictionary nezkoumána ze zdrojáku. | Pokud se FE dictionary a backend dictionary rozejdou (přidání nového stage jen na jedné straně, typo, lokalizační drift), updaty se silently corruptují / zapíšou nesprávnou hodnotu. | Sjednotit na jediný formát na drátu (preferovaně EN key). Backend přijme EN key, mapuje na display label až při čtení sheetu. | Open |
| IN-009 | Integration | FE→GAS `updateLead` `fetch` nemá `signal` s timeoutem, retry policy ani idempotency token. Síťový blip = nejasný stav: backend možná zapsal, FE ukáže fail. | P1 | `crm-frontend/src/lib/google/apps-script-writer.ts:45-58` | Bez timeout: user může čekat minuty (Apps Script default ~6 min). Bez retry: jednorázové síťové chyby = manuální user retry. Bez idempotency: pokud retry implementuje, riziko double-write. | Přidat AbortController s timeout (~15-30 s). Přidat client-side idempotency key (UUID per attempt). Backend zaznamenává `last_seen_idempotency_key` per lead a no-ops opakování. | Open |
| IN-010 | Integration | Backend `WebAppEndpoint.gs:25-27` `catch (err)` vrací `err.message` přímo klientovi. Může leakovat interní detaily (Apps Script quirky messages, parsing chyby s offsety, atd.). | P2 | `apps-script/WebAppEndpoint.gs:25-27` | Information disclosure: přes legitimní call může útočník získat detail o interních chybách. Reálný impact malý (token chrání endpoint), ale defense-in-depth. | Vrátit generické `{success:false, error:'Internal error'}` a logovat detail přes `aswLog_('ERROR', ...)`. | Open |
| IN-011 | Integration | Backend chybové stringy jsou v angličtině (`'Unauthorized'`, `'Lead not found'`, `'Disallowed field'`, `'Identity verification failed'`), frontend route je propaguje 1:1 do české UI. | P2 | `apps-script/WebAppEndpoint.gs:16,23,33,38,50,56,63,69,74,85,92` + `crm-frontend/src/app/api/leads/[id]/update/route.ts:103` | Inkonzistentní UX. User vidí směs češtiny (FE-side validation messages) a angličtiny (backend errors). Při serious chybě (např. identity mismatch) je zpráva nesrozumitelná pro non-tech operátory. | Buď BE odpovídat česky, nebo FE route mapovat error kódy → CZ messages. Preferovaně: BE vrací error code (např. `IDENTITY_MISMATCH`), FE mapuje na lokalizovanou zprávu. | Open |
| IN-012 | Integration | Backend `WebAppEndpoint.gs:41-47` validuje pouze field allowlist, **nevaliduje enum hodnoty**. `next_action` může být libovolný string. Frontend route validuje (`route.ts:27` proti `NEXT_ACTIONS`). | P2 | `apps-script/WebAppEndpoint.gs:41-47` (žádný value check); `crm-frontend/src/app/api/leads/[id]/update/route.ts:16,26-29` (FE check) | Defense-in-depth gap. Pokud token unikne nebo someone bypassne FE route (přímý hit na Apps Script URL), backend zapíše jakýkoli string do `next_action`. Sheet konzistence ohrožena. | Backend validovat hodnotu proti enum (replikovat NEXT_ACTIONS jako `var ALLOWED_NEXT_ACTIONS = ['Oslovit', …]`). | Open |
| IN-013 | Integration | Backend nevaliduje `sales_note.length`. FE route caps 5000 znaků. | P2 | `apps-script/WebAppEndpoint.gs:30-113` (žádný length check) vs `crm-frontend/src/app/api/leads/[id]/update/route.ts:18,42-44` (5000 char cap) | Defense-in-depth gap. Velký `sales_note` může pomalu zapsat (Sheets cell limit ~50k znaků), ale efektivně neomezený a bez limit logiky. Pokud token unikne, lze způsobit slow-write DoS. | Backend přidat `MAX_NOTE_LENGTH = 5000` check. | Open |
| IN-014 | Integration | In-memory preview store v `/api/preview/render` ztrácí stav při restartu/cold-startu. Apps Script už zapsal `preview_url` do LEADS, ale `/preview/<slug>` po restartu vrací 404. | P1 | `crm-frontend/src/app/api/preview/render/route.ts:95` `putPreviewRecord` (in-memory upsert); komentář na řádcích 14-17 explicitně říká "Out of scope (B-06): external storage" | V produkci na Vercel serverless je každá funkce může být cold-startována nezávisle. `preview_url` zapsaná do LEADS přestane fungovat při dalším deployu. Apps Script o tom neví — stará data v sheet, broken URLs, zákazníkům posílá email s mrtvým odkazem. | Persistovat preview store (Vercel KV / Redis / Supabase). Nebo: re-render on-demand z `webhook_payload_json` v LEADS. Tracker ticket pro B-X (post-MVP). | **Resolved** (Phase 2 KROK 2). New Sheets-backed `apps-script/PreviewStore.gs` (`_previews` hidden sheet) is now the canonical store; `crm-frontend/src/lib/preview/preview-store.ts:9` keeps an in-memory cache with explicit "Replaces the B-04 in-memory Map (FF-004 fix): Vercel restarts no longer wipe state." Verified 2026-04-29. Cross-ref FF-004. |
| IN-015 | Integration | `apps-script-writer.ts:60-63` `if (!res.ok)` větev je v praxi mrtvá. Apps Script Web Apps vždy vrací HTTP 200 (i pro chyby — chybu signalizuje JSON body `{success:false}`). | P3 | `crm-frontend/src/lib/google/apps-script-writer.ts:60-63` | Nemá user-facing impact. Jen matoucí pro čtenáře (vypadá to jako pokrytá HTTP error path). Aktivuje se pouze při infra error (DNS, CORS preflight, network outage). | Buď zdokumentovat (`// HTTP errors only fire on infra-level failure; Apps Script always returns 200`), nebo zjednodušit na try/catch + body parse. | Open |
| IN-016 | Integration | Hardcoded SPREADSHEET_ID v `apps-script/Config.gs:14`. Typ: `sheet_id`, první 4 znaky: `1RBc`, délka: 44 znaků (>20 char threshold). | P1 | `apps-script/Config.gs:14` | Cross-domain finding: ownerství patří **Phase 7 SEC**. Hodnota je v public Git history. Vlastní access kontrola závisí na Sheets sharing nastavení (audit nemůže ověřit). Komentář v souboru říká "do not use directly" / "legacy fallback", ale stále se commituje. | Přesunout do Script Property `SPREADSHEET_ID_PROD`, mazat z kódu. Pokud byl ID kdy public exposed, zvážit migraci do nového sheetu. Cross-ref Phase 7. | Open |
| IN-017 | Integration | `updateLead` kontrakt nenese `contract_version`. GAS změna `ALLOWED_FIELDS` se projeví až při volání jako `'Disallowed field: …'`. Žádný preflight ani version handshake. | P2 | `apps-script/WebAppEndpoint.gs:41-47` (allowlist) + `crm-frontend/src/lib/google/apps-script-writer.ts:48-58` (payload bez verze) | Při deploy mismatch (FE nový, GAS starý) FE odešle nový field, dostane back generic error. User-facing 502 bez kontextu. Není to outage, ale ladění je obtížné. | Přidat `contract_version: '1.0'` do payloadu i response. GAS odmítne `{error:'INCOMPATIBLE_VERSION', minimum:'1.0'}` pokud nesedí. | Open |
| IN-018 | Integration | `parseOutreachStage` (`apps-script-writer.ts:82`) silently mapuje neznámé CZ labely na `'NOT_CONTACTED'`. Žádný error, žádný log. | P2 | `crm-frontend/src/lib/google/apps-script-writer.ts:81-83` | Pokud sheet obsahuje legacy / typo hodnotu (např. `'Osloven'` místo `'Osloveno'`), FE zobrazí stage jako "neosloveno" — operátor bude lead znova oslovovat. Tichý data-quality bug. | Vyhodit warning (`console.warn`) nebo `null`/`unknown` enum hodnotu. Přidat data-quality alert do dashboard. | Open |
| DP-001 | Deploy Pipeline | PROD Sheet ID hardcoded v test fixture (test code committed do mainu obsahuje plnou hodnotu produkčního Sheet ID). | P0 | ~~`scripts/tests/preview-render-endpoint.test.ts:56`~~ (typ: sheet_id, prvních 4 znaky `1RBc`, délka 44) — replaced by `TEST_SHEET_ID_FAKE_FOR_FIXTURES_ONLY` constant at line 76 | Plná hodnota PROD ID je v Git history napříč testovým kódem. Odstranění z HEAD by nezahladilo expozici (history persistuje). Test fixture by měl používat fake ID. Cross-domain s Phase 7. | Nahradit za fake ID (např. `'TEST_SHEET_ID_FAKE_FOR_FIXTURES_ONLY'`). Test stejně nevolá Sheets API, payload se jen serializuje a validuje. | **Resolved** at HEAD (verified 2026-04-30) — `scripts/tests/preview-render-endpoint.test.ts` now uses `TEST_SHEET_ID_FAKE_FOR_FIXTURES_ONLY` constant; PROD ID removed from this file. `npm run test:b04` 16/16 pass. **Partial fix** — SEC-001 (broader PROD ID cleanup across `Config.gs`, `EnvConfig.gs`, `docs/09`, `docs/20`, plus git history rewrite) remains Open. See `docs/30-task-records/DP-001-prod-sheet-id-test-fixture.md`. |
| DP-002 | Deploy Pipeline | `apps-script/.clasp.json` a `.clasp.json.prod` committed s plnými scriptIds (TEST `1Sjd…` 58 znaků, PROD `1fnL…` 58 znaků) a parentId (TEST sheet `13fy…` 44 znaků). | P0 | `apps-script/.clasp.json:2,4`; `apps-script/.clasp.json.prod:2` (typ: script_id + sheet_id_parent) | Anyone s clone repa zná deployment targets. Sám scriptId není auth credential, ale v kombinaci s OAuth scope `clasp.projects` na cílovém projektu (které lze získat jako kterýkoli editor projektu) by umožnil `clasp pull` a získání source kódu z runtime. Také atestuje PROD identity pro útočníka, který by chtěl phishingovat owner Apps Scriptu. | Přesunout `.clasp.json` patterny do `.gitignore`, vytvořit `.clasp.json.example` s placeholdery, držet skutečné scriptIds v 1Password / Vercel env / GH Environment secrets a ve `clasp-deploy.sh` materializovat `.clasp.json` z env. Cross-domain s Phase 7. | **In Progress**. `.gitignore` now lists `apps-script/.clasp.json` + `apps-script/.clasp.json.prod`; `apps-script/.clasp.json.example` exists with placeholder scaffold. `git ls-tree HEAD apps-script/.clasp.json` = empty (no longer tracked at HEAD). **Historical exposure remains** in git history — full mitigation would need `git filter-repo` + scriptId rotation. Awaiting operator decision on history rewrite. |
| DP-003 | Deploy Pipeline | `scripts/clasp-deploy.sh:74-87` swap-and-restore není atomic. Pokud shell padne mezi `cp .clasp.json.prod .clasp.json` a `cp .clasp.json.bak .clasp.json` (Ctrl+C, kill, panic), `.clasp.json` zůstane v PROD stavu. Následný `clasp push test` by potichu pushnul do PROD. | P1 | ~~`scripts/clasp-deploy.sh:74-87`~~ — replaced by `trap restore_clasp_config EXIT INT TERM` registered before the swap (line 88) + idempotent `restore_clasp_config` helper (lines 21-31) | Po neúspěšném prod deployu může developer instinktivně spustit `./scripts/clasp-deploy.sh test`, který nevyžaduje confirmation a okamžitě volá `clasp push` — který půjde do PROD. Vážná data-corruption možnost. | Použít `trap` v bashi pro guaranteed restore at EXIT/INT/TERM. Alternativně: nepřepisovat `.clasp.json`, ale použít `clasp push -P <prod-clasp-config>` (clasp 2.x supports). Alternativně: `clasp-deploy.sh test` by měl ověřit hash `.clasp.json` proti `.clasp.json` v `git show HEAD:apps-script/.clasp.json` před pushem. | **Resolved** at HEAD (verified 2026-04-30) — `scripts/clasp-deploy.sh` now registers `trap restore_clasp_config EXIT INT TERM` before the swap; restore is idempotent (no-op if `.clasp.json.bak` already cleaned up by happy-path explicit restore). Regression test `scripts/tests/clasp-deploy-trap.test.mjs` covers (a) abort-mid-deploy → restore, (b) idempotent double-restore, (c) test-mode source guard. `npm run test:clasp-deploy-trap` 3/3 pass. Alternative options (`clasp push -P` / hash-check) explicitly **out of scope** here. See `docs/30-task-records/DP-003-clasp-deploy-trap-restore.md`. |
| DP-004 | Deploy Pipeline | `apps-script/PreviewPipeline.gs:976,1561` posílá `spreadsheet_id: SPREADSHEET_ID` (raw konstanta = PROD ID) v webhook payloadu místo env-resolved `getSpreadsheetId_()`. V TEST runs frontend dostává **PROD** Sheet ID v payloadu, i když operace probíhají na TEST sheetu. | P1 | `apps-script/PreviewPipeline.gs:976`, `:1561`; `apps-script/Config.gs:14` (raw const); `apps-script/EnvConfig.gs:83` (`getSpreadsheetId_` helper) | Drift mezi env-resolved runtime (`getSpreadsheetId_` v `openCrmSpreadsheet_`) a webhook contract. Audit logu na FE straně by ukázal PROD ID i pro TEST runs. Pokud FE někdy začne validovat `spreadsheet_id` proti expected env, TEST runs failnou. | Nahradit `SPREADSHEET_ID` za `getSpreadsheetId_()` na obou call-sitech. | Open |
| DP-005 | Deploy Pipeline | `.github/workflows/docs-governance.yml` je jediný CI workflow. Žádný TypeScript check, ESLint, frontend build, ani backend tests v CI. PR může mergnout s broken kódem. | P1 | `.github/workflows/docs-governance.yml` (jediný workflow); `crm-frontend/package.json:6-9` (`build`, `lint` scripts unused v CI); `package.json:11-14` (`test:b03..b06` unused v CI) | Defense-in-depth gap: docs-governance check chrání jen docs sync. Reviewer musí ručně zkontrolovat, že kód buildí a testy projdou. V realitě se to neděje (viz aktuální drift docs vs realita = symptom missing CI). | Přidat workflow `ci.yml` s steps: `cd crm-frontend && npm ci && npm run lint && npm run build`, `npm run test:b03 && npm run test:b04 && npm run test:b05 && npm run test:b06`. Přidat do `required_status_checks`. | **Resolved** (KROK 7). `.github/workflows/pilot-ci.yml` exists — runs frontend lint + tsc + build + test:b06 plus apps-script logic tests (ingest runtime + a08 preview queue) on every PR + main push. Verified 2026-04-29. **Note (operator action):** still need to add `pilot-ci` to `required_status_checks` in branch protection (currently only `docs-governance` is required) — see DP-010 / SEC-022. |
| DP-006 | Deploy Pipeline | `.gitignore` neobsahuje `.clasprc.json` (clasp credentials file). Pokud user spustí `clasp login` z `apps-script/` cwd, token tam zůstane jako `.clasprc.json` a může být `git add`-nutý. | P1 | `.gitignore` (whole file, no `.clasprc*` pattern); `apps-script/.clasp.json` (sourdire) | Token v `.clasprc.json` je access_token + refresh_token pro Google OAuth. Commit do veřejné historie = full takeover Apps Script projektu (a všeho, k čemu mají scopes). | Přidat do `.gitignore`: `.clasprc.json`, `.clasprc`, `**/.clasprc*`. Cross-domain s Phase 7. | **Resolved**. `.gitignore` line 23: `.clasprc.json`. Verified 2026-04-29. (Broader patterns `*.pem`, `*.key`, `service-account*.json` etc. still missing — see SEC-010.) |
| DP-007 | Deploy Pipeline | Žádný `.claspignore` v repu. Default clasp behavior pushuje vše matchující `scriptExtensions` (`.gs`, `.js`) a `.json`. Žádná explicitní allow/deny lista. | P2 | `apps-script/` (žádný `.claspignore`); `apps-script/.clasp.json:5-13` (extensions) | Pokud někdo do `apps-script/` přidá nový `.json` (např. omylem zkopírovaný service-account JSON, nebo testovací data), clasp ho pushne do Apps Script projektu. | Vytvořit `apps-script/.claspignore` s explicitním allow-listem (nebo deny pro `*.bak`, `*.test.*`, `**/credentials*`, `**/*secret*`). | **Resolved**. `apps-script/.claspignore` exists. Verified 2026-04-29. |
| DP-008 | Deploy Pipeline | `clasp push` v `clasp-deploy.sh:41,82` pushuje pouze HEAD bez vytvoření verze (`clasp deploy`). Apps Script Web App vždy servíruje aktuální HEAD. Rollback = git revert + redeploy. | P2 | `scripts/clasp-deploy.sh:41,82` (žádné `clasp deploy`) | Bez verzí Apps Script: žádný "instant rollback" v Apps Script Console. TTR pro rollback = full deploy cycle (revert + PR + merge + clasp push) ~5-10 min. Při critical PROD chybě je to dlouho. Také ztráta historie deploys (kdy se co nasadilo). | Přidat `clasp deploy --description "<commit_sha>"` po `clasp push`. Owner pak může revert z Apps Script Console. Alternativně: zaznamenat každý PROD deploy do `docs/30-task-records/release-log.md` s commit hash + timestamp. | Open |
| DP-009 | Deploy Pipeline | Vercel deploy je úplně mimo repo. Žádný `vercel.json`, žádný `.vercel/`, žádný GH workflow. Audit nemůže ověřit cílový branch, env vars set, kdo může deploynout, kdy byl poslední deploy. | P1 | `find . -name "vercel*"` = 0 hits (mimo `node_modules`); `.github/workflows/` = pouze `docs-governance.yml` | Deploy state = invisible v Git. Pokud někdo Vercel project odstraní / přepne env vars / změní deployment branch, repo o tom neví. Lepší integrace by zaznamenala deploy hash + timestamp do PR jako check. | Buď přidat `vercel.json` do repa (alespoň `{"git": {"deploymentEnabled": {"main": true}}}` jako attestation), nebo migrovat na GH Action s `vercel deploy` a explicit env. Doc rotation v `docs/27-infrastructure-storage.md` actually reflect Vercel. | Open |
| DP-010 | Deploy Pipeline | Branch protection na `main` má `enforce_admins: false`. Repo admin může bypassnout docs-governance status check i required reviewers. | P1 | `gh api repos/Spookybro55/autosmartweby/branches/main/protection` → `enforce_admins.enabled = false`; `CLAUDE.md:6-12` (governance pravidla) | Governance ze `CLAUDE.md` ("main je chranena branch — direct push zakazan", "kazdy PR musi byt schvalen") je platná **pouze pro non-admin**. Při solo-admin repu (1 majitel) je to soft policy bez technical enforcement. Také: kompromitace admin účtu = bypass všeho. | Zapnout `enforce_admins: true`. Pokud admin potřebuje občasný bypass (emergency hotfix), dělat to vědomě přes dočasný unprotect. | Open |
| DP-011 | Deploy Pipeline | Test scripts `test:b03`–`test:b06` definovány v `package.json:11-14`, ale **nejsou volány v žádném workflow** ani v `clasp-deploy.sh`. Nikdy se neběží automaticky. | P2 | `package.json:11-14` (definice); `.github/workflows/docs-governance.yml` (žádné `npm test`); `scripts/clasp-deploy.sh` (žádné `npm test` před push) | Tests jsou **dead artefakt** — existují, ale nikoho nechrání. Reviewer musí ručně spustit, což se neděje. Drift mezi tests a kódem může být velký. | Přidat do `ci.yml` (per DP-005). | Open |
| DP-012 | Deploy Pipeline | `apps-script/README.md:145-155` Deployment sekce je stale — tvrdí "Pro deploy do produkce: rucne zkopirovat soubory" a "NIKDY nemenit parentId na produkcni ID", ale `scripts/clasp-deploy.sh prod` automatizovaně přepisuje `.clasp.json` na PROD config. | P2 | `apps-script/README.md:145-155` (drift); `scripts/clasp-deploy.sh:74-87` (realita) | Onboarding pasti pro nového developera: bude číst README, dělat manuální copy-paste místo aby použil oficiální skript. Nebo opačně — bude spustit skript v dobré víře, že to README popisuje, ale README říká, že to nikdo nedělá. | Přepsat sekci na: "Deploy do TEST: `./scripts/clasp-deploy.sh test`. Deploy do PROD: `./scripts/clasp-deploy.sh prod` (vyžaduje branch=main, žádné uncommitted, manual confirmation)." | Open |
| DP-013 | Deploy Pipeline | `apps-script/README.md:35-73` popisuje neexistující soubory (`Code.gs`, `Qualify.gs`, `Preview.gs`, `Pipeline.gs`) a chybný počet sloupců ("28 new columns"). Reálný stav: `Menu.gs`, `AutoQualifyHook.gs`, `PreviewPipeline.gs`, atd., a 55+ extension columns. | P2 | `apps-script/README.md:35-73`; `apps-script/` (`ls *.gs`); `apps-script/Config.gs:68-125` (`EXTENSION_COLUMNS`) | Onboarding mismatch: nový dev hledá soubory, které neexistují, dělá audit "kde je qualifyAllLeads?", ztrácí čas. Setup steps "create files (click + → Script)" navíc opomíjejí celý clasp workflow. | Přepsat README na current reality: list files podle `ls`, EXTENSION_COLUMNS odkazem na `Config.gs:68-125`, setup přes `clasp clone <scriptId>` + `clasp pull`. | Open |
| DP-014 | Deploy Pipeline | `docs/27-infrastructure-storage.md` má multi-line drift: tvrdí "Frontend lokalne", "Zadny CI/CD", "Zadny hosting", "OAuth ceka na .env"; chybí 2 env vars (`PREVIEW_WEBHOOK_SECRET`, `PUBLIC_BASE_URL`); obsahuje 2 zombie vars (Google OAuth client IDs nepoužité v aktuálním kódu); last update 2026-04-05 (20 dní stará). | P1 | `docs/27-infrastructure-storage.md:16-19,30-39,46-48,59` | Tento doc je v `docs/27` = part of "kanonicka vrstva 20-29" per `CLAUDE.md`. Stale kanonický doc poškozuje celou governance. Nový dev / new operator dostane chybný obraz infrastruktury. | Aktualizovat doc: Vercel jako frontend hosting, GitHub Actions docs-governance jako CI, doplnit 2 chybějící env vars, odstranit 2 zombie vars (nebo je realizovat). Bump "Posledni aktualizace". Cross-domain s Phase 10 DOC. | Open |
| DP-015 | Deploy Pipeline | `docs/22-technical-architecture.md:46,52,53` má stejný drift jako DP-014: "Zadny CI/CD, zadne testy", "Frontend bezi lokalne", "Zadny hosting nakonfigurovan". | P2 | `docs/22-technical-architecture.md:46,52-53` | Stejný impact jako DP-014, kanonický doc stale. Odděleně klasifikováno protože by se mělo opravit oba zároveň. | Aktualizovat sekci `## Limity` a `## Deployment` per realita. Cross-domain s Phase 10 DOC. | Open |
| DP-016 | Deploy Pipeline | `crm-frontend/README.md` chybí deploy sekce, env list neúplný (chybí `APPS_SCRIPT_SECRET`, `PREVIEW_WEBHOOK_SECRET`, `PUBLIC_BASE_URL`); odkaz `docs/CRM-SYSTEM-MAP.md` ukazuje na archive (`docs/archive/CRM-SYSTEM-MAP.md`); referencuje `use-lead-update` který je dead code (cross-ref IN-004). | P2 | `crm-frontend/README.md:5-39` (no deploy); `:25-31` (env list); `:60` (use-lead-update); `:91` (broken link) | Dev frontends nemůže nasadit aplikaci z README. Žádný onboarding pro deployment chain. | Přidat sekci `## Deployment` s návodem (Vercel, branch, env vars). Doplnit 3 env vars. Opravit broken link. | Open |
| DP-017 | Deploy Pipeline | Apps Script nemá globální version stamp / health endpoint pro frontend. Pouze `PREVIEW_VERSION = 'b04-mvp-1'` v `…/preview/render/route.ts:32` (frontend-side). Žádné backend → frontend version handshake. | P2 | `apps-script/WebAppEndpoint.gs:10-118` (žádný version field v response); `…/api/preview/render/route.ts:32` | Drift mezi deployments invisible. Po PROD push nemá nikdo (operátor, dev, monitoring) způsob jak ověřit, že nová verze běží. Cross-ref IN-017. | Přidat `var ASW_VERSION = '<commit-sha or semver>';` v Apps Script (auto-generated v `clasp-deploy.sh` před push). Vrátit ji v `WebAppEndpoint.gs` doPost response. Frontend ji loguje. Health endpoint `?action=health`. | Open |
| DP-018 | Deploy Pipeline | Žádná dokumentovaná rollback procedura pro Apps Script ani frontend. | P2 | `docs/`, `apps-script/README.md`, `crm-frontend/README.md`, `scripts/` (grep "rollback" = 0 hits ve flowech) | Při PROD incidentu nikdo neví exact steps. Implicitní git revert + redeploy je dlouhý (5-10 min) a může se zaseknout (např. clasp deploy permission lost). Operator panic risk. | Vytvořit `docs/ROLLBACK.md` s kroky pro: AS rollback (revert PR + clasp-deploy.sh prod), FE rollback (Vercel deployments tab → Promote previous), env-vars rollback (Vercel env history). Cross-domain s Phase 10 DOC. | Open |
| DP-019 | Deploy Pipeline | Žádná dokumentovaná secrets rotation procedura pro `FRONTEND_API_SECRET`, `PREVIEW_WEBHOOK_SECRET`, `AUTH_PASSWORD`, `NEXTAUTH_SECRET`, `GOOGLE_PRIVATE_KEY`. | P1 | `docs/`, `scripts/` (grep "rotat" = 0 hits in deploy/ops) | Při kompromitaci secret (např. klíč unikne v logu) tým neví exactly co dělat. Bez overlap-window strategy je rotace = downtime. Cross-ref IN-009 a Phase 7. | Vytvořit `docs/SECRETS-ROTATION.md` s per-secret postupem (kde je uložený, jak rotovat s overlap, kdo má přístup, kdy naposled rotovaný). | Open |
| DP-020 | Deploy Pipeline | Branch protection na `main` nemá `require_last_push_approval`. Re-push po approval bypassuje review. | P2 | `gh api repos/.../branches/main/protection` → `required_pull_request_reviews.require_last_push_approval = false` | V long-running PR může autor po approval pushnout dodatečné commits (rebase, fixup, additional commits) které review never viděl. Zvlášť riskantní pro security-sensitive PR. `dismiss_stale_reviews=true` ji aktuálně zmírňuje (nový commit zruší review), ale to není totéž jako vyžadovat re-approval. | Zapnout `require_last_push_approval: true` v branch protection. | Open |
| DP-021 | Deploy Pipeline | `envGuard_()` funkce existuje (`EnvConfig.gs:119-140`), ale **nikdy automaticky nevolaná** před destruktivními operacemi. Volá se jen ručně přes `diagEnvConfig` (`EnvConfig.gs:194-199`). | P1 | `apps-script/EnvConfig.gs:119-140`; grep `envGuard_` v repu — 0 volání mimo `diagEnvConfig` | Pokud Script Property `ASW_ENV` v PROD projektu je nesprávně `TEST` (nebo ASW_SPREADSHEET_ID ukazuje na PROD sheet zatímco env=TEST), guard by měl throw, ale není v hot path. `processPreviewQueue`, `qualifyAllLeads`, `setupPreviewExtension`, `installContactEditTrigger` všechny zápis do sheetu mohou proběhnout bez guard checku. | Přidat `envGuard_()` na začátek každé funkce, která zapisuje do sheetu (Helpers.gs `openCrmSpreadsheet_` je dobrý choke point). Logovat volání pro audit. Cross-domain s Phase 7. | **Resolved**. `apps-script/Helpers.gs:148` (`openCrmSpreadsheet_`) calls `envGuard_(spreadsheetId)` before `SpreadsheetApp.openById` — the choke-point recommendation. All sheet-write paths go through `openCrmSpreadsheet_`, so the guard now fires on every destructive op. Verified 2026-04-29. Cross-ref SEC-011. |
| SEC-001 | Security | Hardcoded production Sheet ID (`1RBc…`, 44 znaků) v 5+ tracked souborech: `Config.gs:14`, `EnvConfig.gs:13,18,28`, `scripts/tests/preview-render-endpoint.test.ts:56`, `docs/09-project-control-tower.md:66`, `docs/20-current-state.md:45`. Plus TEST Sheet ID `14U9…` v EnvConfig.gs:13,33 a docs/30-task-records/B6.md. Konsoliduje IN-016 a DP-001. | P0 | `apps-script/Config.gs:14`; `apps-script/EnvConfig.gs:13,18,28,33`; `scripts/tests/preview-render-endpoint.test.ts:56`; `docs/09-project-control-tower.md:66`; `docs/20-current-state.md:45`; `docs/30-task-records/B6.md:118,179` | Sheet ID + sharing settings = potenciální data-targeting vektor. Hodnoty v Git history navždy (rotation pouze přes `git filter-repo` se ztrátou commit hashů, nebo migrace na nové sheet IDs). Public repo + scraping vede k extra plus že kdokoli zná data target. | Krátkodobě: přesunout všechny hodnoty do Script Properties / env vars, refactorovat code paths na env-resolved access. Z docs odstranit hodnoty (substituovat za `<PROD_SHEET_ID>` placeholder). Z testu nahradit fake fixture. Dlouhodobě: zvážit migraci na nové Sheet IDs + history cleanup, pokud sheet sharing settings neumí omezit risk. | Open |
| SEC-002 | Security | Apps Script scriptIds (TEST `1Sjd…`, PROD `1fnL…`, oba 58 znaků) a parentId (TEST sheet `13fy…`, 44 znaků) committed v `apps-script/.clasp.json` a `.clasp.json.prod`. Konsoliduje DP-002. | P0 | `apps-script/.clasp.json:2,4`; `apps-script/.clasp.json.prod:2`; `docs/archive/04-git-and-deploy-decisions.md:154,155`; `docs/archive/17-writeback-rollout-checklist.md:23` | scriptId + Apps Script project access (= editor membership v projektu) = kdokoli s membership může `clasp pull` a získat current source. ScriptId sám neuděluje access, ale kombinace s phishing nebo s členstvím v týmu = full source code exfiltration. parentId = TEST sheet — reveals deploy target. | Přesunout `.clasp.json` patterny do `.gitignore`, vytvořit `.clasp.json.example` s placeholdery (`"scriptId": "<YOUR_SCRIPT_ID>"`). Skutečné scriptIds držet v 1Password / Vercel env / GH Environment secrets a v `clasp-deploy.sh` materializovat `.clasp.json` z env. | **In Progress** (mirrors DP-002). Gitignore + example file in place; `.clasp.json` no longer tracked at HEAD. Historical exposure in git history remains. Awaiting operator decision on `git filter-repo` + scriptId rotation. |
| SEC-003 | Security | Apps Script Web App config: `executeAs: USER_DEPLOYING` + `access: ANYONE_ANONYMOUS`. Token (`payload.token`) v JSON body je jediná autorizace. URL je public. Pokud token unikne, kdokoli může volat `updateLead` action s authority deploying-user (read/write na všechny accessible sheets, gmail.modify, send_mail). | P1 | `apps-script/appsscript.json:11-13`; `apps-script/WebAppEndpoint.gs:14-17` (token check) | Token-only auth model bez identity attribution. Pokud Vercel env unikne, attacker má bearer authority po dobu, dokud token nebyl rotován (DP-019: žádný rotation runbook). Authority zahrnuje sheet write + gmail read/modify + mail send (per OAuth scopes SEC-004). | Krátkodobě: dokumentovat hrozební model + rotation runbook (cross-ref SEC-017). Středně: omezit `executeAs` na service account s minimum scopes (vyžaduje refactor — Apps Script má omezení). Dlouhodobě: nahradit shared-secret model za asymmetric / per-user attribution (např. JWT podepsaný frontendem s user identity, ověřovaný v Apps Scriptu). | Open |
| SEC-004 | Security | OAuth scopes overclaim v `apps-script/appsscript.json:7-15`: `gmail.modify` umožňuje read+write **všech** emailů deploying user; `gmail.readonly` je redundant s `.modify`; `spreadsheets` (full) místo `spreadsheets.currentonly`. | P1 | `apps-script/appsscript.json:7-15` | Při kompromitaci tokenu / kódu / Google account dostane attacker access ke všem emailům a sheetům deploying user (ne jen CRM). Pokud deploying user má i osobní gmail, ten je rovněž exposed. | Odstranit `gmail.readonly` (redundant). Zvážit downgrade `gmail.modify` na granular scopes (`gmail.compose` + `gmail.labels` + `gmail.metadata` pro sync) — vyžaduje refactor mailbox sync. Změnit `spreadsheets` na `spreadsheets.currentonly` pokud kód neptřebuje cross-sheet access (verify per call site). | Open |
| SEC-005 | Security | Login `password !== AUTH_PASSWORD` (`crm-frontend/src/app/api/auth/login/route.ts:42`) — plain string compare, **není timing-safe**. Timing oracle umožňuje deduce password length / common prefix přes diferenciální timing analýzu. | P1 | `crm-frontend/src/app/api/auth/login/route.ts:42-44` | Praktické sníženo tím, že password je sdílený single secret (ne per-user), takže timing oracle dává limited info. Ale defense-in-depth gap. Při velkém volume requestů + statistical analysis lze leak length info. | Použít `crypto.timingSafeEqual(Buffer.from(password), Buffer.from(AUTH_PASSWORD))` se zarovnáním délky. | Open |
| SEC-006 | Security | Login route v `crm-frontend/src/app/api/auth/login/route.ts:37-44` má **early return** na invalid email allowlist check (`:39`) — neporovnává password když email není v listu. Časový rozdíl mezi "invalid email" a "valid email + invalid password" = user enumeration oracle. | P1 | `crm-frontend/src/app/api/auth/login/route.ts:37-44` | Útočník zjistí, které emaily jsou v allowlist, přes timing analýzu. V kombinaci s SEC-005 + SEC-007 = practical credential stuffing target list. | Refactor: vždy provádět password compare (timing-safe), i pro invalid email; rozhodnout o success/fail až po dokončení obou checks. Pak vrátit identický error response. | Open |
| SEC-007 | Security | Žádný rate limit / lockout / CAPTCHA na `/api/auth/login`, ani na backend write endpoints (`/api/leads/[id]/update`, `updateLead` v Apps Scriptu). 2FA neimplementováno. | P1 | `crm-frontend/src/app/api/auth/login/route.ts` (žádný rate limit middleware); `crm-frontend/src/middleware.ts` (jen auth check, ne rate limit); `apps-script/WebAppEndpoint.gs:10-118` (žádný per-IP/per-token throttle) | Credential stuffing možný (~86k pokusů/den per IP před Vercel-side rate limits). Při znalosti email allowlistu (SEC-006) cíleně útočit. DoS na write endpoints přes single auth account. | Implementovat per-IP rate limit na `/api/auth/login` (např. 5/min, exponenciální backoff). Per-token rate limit na Apps Script write (např. PropertiesService timestamp-based throttle). 2FA pro PROD admins (cross-ref SEC-022). | Open |
| SEC-008 | Security | Session token (`crm-session` cookie) je `base64url(JSON{email, ts}).hmac` — bez `jti` / version / server-side blocklist. Logout endpoint chybí. Token revocation impossible bez rotace `NEXTAUTH_SECRET` (= invalidate **všech** sessions). | P2 | `crm-frontend/src/app/api/auth/login/route.ts:14-19,46-47`; `crm-frontend/src/middleware.ts:6-45` (verify); žádný `/api/auth/logout` route | Po phishing / device theft / mvtas zachycení tokenu, attacker má bearer authority po 7 dní (maxAge). Server nemůže selektivně revokovat. | Přidat `jti` (random uuid) do payloadu + server-side blocklist (lite KV / cookie blocklist). Implementovat `/api/auth/logout` route, která přidá `jti` do blocklistu. Při verify checkovat blocklist. | Open |
| SEC-009 | Security | `/preview/<slug>` je v `PUBLIC_PATHS` (`crm-frontend/src/middleware.ts:3`) — anonymous přístup. Slugy deterministicky odvozeny z `business_name + city` → guessable. Brief vykreslený na public URL obsahuje plné PII: `contact_name`, `contact_phone`, `contact_email`, `business_name`. | P1 | `crm-frontend/src/middleware.ts:3` (PUBLIC_PATHS); `crm-frontend/src/app/preview/[slug]/page.tsx:1-41`; `crm-frontend/src/components/preview/contact-section.tsx` (renders PII); `crm-frontend/src/lib/domain/preview-contract.ts:507` (slug = business_name + city) | Lead, který nikdy o nabídku nepožádal, má svoje kontaktní údaje na public URL. Konkurence může enumerovat slugy. GDPR čl. 5(1)(a),(b) potenciální spor (zákonnost zpracování + účelové omezení). Indexace search engines pokud preview-store je dostupný v jejich crawl window. | Krátkodobě: přidat `<meta name="robots" content="noindex, nofollow">` na preview layoutu; přidat slug entropy (např. random suffix `-{8 chars}`). Středně: zauthnout preview routes nebo zavést per-link expiring token (signed URL). Dlouhodobě: revisited GDPR posouzení s právníkem (legitimate interest doc). | Open |
| SEC-010 | Security | `.gitignore` chybí: `.clasprc.json`, `*.pem`, `*.key`, `service-account*.json`, `credentials*.json`, `*.p12`, `*.pfx`. Konsoliduje DP-006. | P1 | `.gitignore` (entire file); `git log --all --diff-filter=A --name-only` (nikdy nebyl committed → still risk pro budoucí commits) | Pokud někdo spustí `clasp login` nebo `gcloud auth` z repo dir, credentials file tam zůstane. `git add .` ho zachytí. Plain Google service-account JSON obsahuje private key — full account takeover. | Přidat do `.gitignore`: `.clasprc.json`, `.clasprc`, `**/.clasprc*`, `*.pem`, `*.key`, `*.p12`, `*.pfx`, `*.crt`, `service-account*.json`, `credentials*.json`, `**/google-credentials*.json`, `*.private`, `*-secret*`, `secrets/`, `.secrets/`. | Open |
| SEC-011 | Security | `envGuard_()` definovaná v `apps-script/EnvConfig.gs:119-140`, ale grep ukazuje 0 volání mimo `diagEnvConfig`. Konsoliduje DP-021. | P1 | `apps-script/EnvConfig.gs:119-140` (definition); `apps-script/EnvConfig.gs:194-199` (single call site v diagEnvConfig); `apps-script/Helpers.gs:openCrmSpreadsheet_` (sheet open path bez guard) | Misconfigured Script Property (`ASW_ENV=TEST` v PROD project, nebo `ASW_SPREADSHEET_ID` ukazuje na opačný env) → destruktivní operace proběhne na špatném sheetu. Žádné early failure. | Přidat `envGuard_()` na začátek `openCrmSpreadsheet_()` (single choke point). Při guard fail logovat kritický error a throw. | **Resolved**. Choke-point recommendation implemented at `apps-script/Helpers.gs:148` — `openCrmSpreadsheet_` now calls `envGuard_(spreadsheetId)` before `SpreadsheetApp.openById`. All sheet-write paths go through it. Verified 2026-04-29. Cross-ref DP-021. |
| SEC-012 | Security | `next@16.2.2` má HIGH DoS vulnerability (GHSA-q4gf-8mx6-v5v3, "Next.js has a Denial of Service with Server Components"). Fix v 16.2.4. | P1 | `crm-frontend/package.json:13` (next: "16.2.2"); `npm audit` output | Útočník může zaútočit na server-rendered routes a způsobit DoS celé aplikace. Vercel auto-scaling absorbuje, ale účet platí. | `npm install next@16.2.4` v `crm-frontend/`; verify build a tests. Note: minor version bump, no expected breaking changes. | **Resolved** in `2aecb39` (KROK 2). `crm-frontend/package.json` now declares `"next": "^16.2.4"`. Verified 2026-04-29. |
| SEC-013 | Security | Apps Script Web App URL je v Vercel env (`APPS_SCRIPT_WEB_APP_URL`) — pokud unikne (logs, env exposure, CI artifact), token v body je single point of failure pro write authority. | P2 | `crm-frontend/src/lib/config.ts:7`; `crm-frontend/src/lib/google/apps-script-writer.ts:20` | Defense-in-depth: jediná vrstva (token) místo dvou (URL secrecy + token). | Předpokládat URL je known a chránit pouze tokenem. Posílit token rotation runbook (SEC-017). | Open |
| SEC-014 | Security | Žádný GDPR/PII inventory v repu, žádná erasure path / export path / consent record / privacy policy / data retention policy. Systém zpracovává PII (jméno, telefon, email, IČO). | P1 | `docs/` grep "GDPR\|privacy\|erasure\|consent" — žádné kanonické docs; žádný `/privacy` route ani footer v `crm-frontend/src/app/preview/`; `apps-script/Helpers.gs:307-309` (log retention count-based, ne TTL) | GDPR čl. 5–17 compliance gap. Při právním dotazu / žádosti o vymazání není mechanism. Při data breach / DPA audit nelze poskytnout záznam o zpracování. | Vytvořit `docs/PRIVACY.md` s data inventory, legal basis, retention, erasure path. Implementovat `/preview/<slug>?delete=true` nebo manual deletion runbook. Přidat privacy footer na preview layout. Cross-domain s Phase 10 DOC. | Open |
| SEC-015 | Security | SSRF risk v `validateWebsite_(url)` — `UrlFetchApp.fetch(url)` kde `url` je z Serper search response (nepřímo z user input ze sheet `business_name + city` query). `BLOCKED_HOST_FRAGMENTS` se aplikuje jen na search results, ne před fetch. | P2 | `apps-script/LegacyWebCheck.gs:246-251`; `apps-script/Config.gs:41-46` (BLOCKED_HOST_FRAGMENTS list) | Apps Script egress je omezený Google policy (RFC1918 typically blocked), ale potenciálně reach metadata services or specific cloud endpoints. Limited risk. | Přidat hostname check před `UrlFetchApp.fetch`: blokovat localhost, RFC1918, link-local, .internal/.local TLDs. Whitelisted public TLDs. | Open |
| SEC-016 | Security | `NEXTAUTH_SECRET` fallback na prázdný string `''`: `crm-frontend/src/app/api/auth/login/route.ts:7` a `crm-frontend/src/middleware.ts:4`. Pokud env není set v PROD, HMAC se podepisuje prázdným klíčem → predikovatelný podpis = full auth bypass. | P1 | ~~`crm-frontend/src/app/api/auth/login/route.ts:7-11`~~ ~~`crm-frontend/src/middleware.ts:4`~~ — both call sites now import `SESSION_SECRET` from `@/lib/auth/session-secret` (no fallback) | Pokud Vercel env vars chybí (mistake při deployi, project relocation), aplikace běží s defacto disabled session signing. Anyone může vyrobit valid session cookie. | Změnit oba na `if (!process.env.NEXTAUTH_SECRET) throw new Error('NEXTAUTH_SECRET required')` — fail closed. Pre-deploy check: `npm run build` vyžaduje env. | **Resolved** in `24e3d65` (fix(pilot): NEXTAUTH_SECRET fail-fast). New helper `crm-frontend/src/lib/auth/session-secret.ts` validates min 32 chars + throws; used by `middleware.ts`, `app/api/auth/login/route.ts`, `app/api/auth/me/route.ts`. Verified 2026-04-29: `NEXTAUTH_SECRET= npm run build` fails with the throw message; valid 32+ char secret builds successfully. |
| SEC-017 | Security | Žádný documented secrets rotation procedure pro `FRONTEND_API_SECRET`, `PREVIEW_WEBHOOK_SECRET`, `AUTH_PASSWORD`, `NEXTAUTH_SECRET`, `GOOGLE_PRIVATE_KEY`. Konsoliduje DP-019. | P1 | `docs/`, `scripts/`, `apps-script/README.md`, `crm-frontend/README.md` — grep "rotat" v deploy/ops kontextu = 0 hits | Při kompromitaci secret tým nemá known steps. Bez overlap window strategy = downtime per rotace. Riziko: rotace se neprovádí ze strachu z downtime. | Vytvořit `docs/SECRETS-ROTATION.md` s per-secret postupem (kde uložený, jak rotovat s overlap, kdo má přístup, kdy naposled rotovaný). Cross-domain s Phase 10 DOC. | Open |
| SEC-018 | Security | `PUBLIC_PATHS = ['/login', '/api/auth', '/preview']` v `crm-frontend/src/middleware.ts:3` — `/api/auth` je široký prefix, future routes pod `/api/auth/*` se stanou silently public bez explicit review. | P2 | `crm-frontend/src/middleware.ts:3,51` (`PUBLIC_PATHS.some(p => pathname.startsWith(p))`) | Pokud někdo přidá `/api/auth/admin` nebo `/api/auth/debug`, bude public bez auth check. Convention-based bezpečnost. | Změnit na exact-match list: `['/login', '/api/auth/login', '/preview']` (a explicit add per nová auth route). Code review checklist: nová `/api/auth/*` route = explicit decision o auth. | Open |
| SEC-019 | Security | `_asw_logs` Sheet logs obsahují `lead_id` a chybové zprávy (které mohou obsahovat business_name, např. `'Lead not found: ' + leadId`). Retention pouze count-based (5000 řádků pak prune 1000), žádný TTL ani redaction tier. | P2 | `apps-script/Helpers.gs:296-323`; `apps-script/Helpers.gs:307-309` (rotation logic); error messages `'Lead not found: ' + leadId` v `WebAppEndpoint.gs:74` | PII v logs (lead_id mapuje na konkrétní firmu, error messages mohou obsahovat business_name). Retention nediskutována s GDPR perspective. Sheets read access = log access. | Implementovat redaction tier: pro PII fields logovat hash místo plain. TTL based rotation (delete rows older than N days). Dokumentovat retention policy v `docs/PRIVACY.md` (cross-ref SEC-014). | Open |
| SEC-020 | Security | `hono` (multi-CVE), `postcss` (XSS), `@hono/node-server` (middleware bypass) — všechny moderate. `shadcn` v `dependencies` místo `devDependencies` (`crm-frontend/package.json:24`) zvyšuje production attack surface. | P2 | `crm-frontend/package.json:24`; `crm-frontend/npm audit` output (4 vulnerabilities total: 1 high, 3 moderate); `npm ls hono`: `crm-frontend → shadcn → @modelcontextprotocol/sdk → hono` | hono je dev-only (CLI MCP server), ale ships v production bundle. Reálný attack surface omezený (žádný runtime usage), ale rejected dependency hygiene. | Přesunout `shadcn` do `devDependencies`. Pravidelné `npm audit` v CI (cross-ref DP-005). | Open |
| SEC-021 | Security | Token v JSON body (`crm-frontend/src/lib/google/apps-script-writer.ts:56` `body: JSON.stringify({ ..., token: process.env.APPS_SCRIPT_SECRET })`) místo `Authorization` header. Body content se loguje častěji než header tokens (které jsou typicky redacted v Vercel/proxy logs defaultně). | P2 | `crm-frontend/src/lib/google/apps-script-writer.ts:48-58`; `apps-script/WebAppEndpoint.gs:14` (token check) | Při traceback / log analysis (Vercel network logs, error tracking SaaS, Apps Script execution logs) má větší šanci, že token bude zachycen v plaintextu. Header-based auth + redacted log middleware by snížil expozici. | Refactor na: FE posílá `Authorization: Bearer <token>` header. Apps Script čte z `e.parameter.headers?` — Apps Script Web Apps však nemají přímý přístup k arbitrary headers. Alternativně: posílat token v custom header `X-Asw-Token` které Apps Script umí číst přes `e.parameters` (pokud je předán jako query param) nebo zachovat body ale **explicitně redactovat** v logging middleware. | Open |
| SEC-022 | Security | Branch protection na `main`: `enforce_admins: false` (admin bypass docs-governance + reviewers); `required_signatures: false` (žádné GPG, commit author trivial spoofnutelný). Cross-ref DP-010, DP-020. | P2 | `gh api repos/Spookybro55/autosmartweby/branches/main/protection` → `enforce_admins.enabled = false`, `required_signatures.enabled = false`, `require_last_push_approval = false` | Compromised admin account může pushnout secrets / malicious code mimo review. Spoofed commit author může být použit pro social engineering nebo audit log spoofing. | Zapnout `enforce_admins: true` (nebo zdokumentovat výjimku v `CLAUDE.md`). Zvážit `required_signatures: true` (GPG-signed commits). Zapnout `require_last_push_approval` (cross-ref DP-020). | Open |
| FF-001 | Funnel Flow | Scraper output → `_raw_import` link **chybí**. `scripts/scraper/firmy-cz.mjs` produkuje JSON do file/stdout; žádný kód v `apps-script/` ho neimportuje do `_raw_import` sheetu. `writeRawImportRows_` (RawImportWriter.gs:57) je definovaná ale 0 callerů v repu. | P0 | `scripts/scraper/firmy-cz.mjs` (CLI output: JSON file); `apps-script/RawImportWriter.gs:57` `writeRawImportRows_` definovaná; `grep writeRawImportRows_` v repu = 0 hits mimo definici; `docs/24-automation-workflows.md:131` výslovně "Sheet write path (A-04 neperzistuje do `_raw_import`; je to samostatny downstream krok)" | End-to-end ingest pipeline nesplňuje "auto" claim doc 24:65. Operator musí provést neznámý manual step (paste? Apps Script editor copy?) mezi scraperem a Apps Scriptem. Při scale je to neudržitelné. | Buď: (a) přidat Apps Script `doPost` action `importRaw` který volá `writeRawImportRows_`, scraper končí HTTP POST do webapp; (b) přidat menu item "Import from JSON" který otevře dialog pro paste; (c) připojit scraper na Google Drive folder + Apps Script trigger na nové soubory. Vybrat jednu strategii. | **Resolved** in `ec6445f` (A-11). `writeRawImportRows_` is now called from `apps-script/WebAppEndpoint.gs:690` (`handleIngestScrapedRows_`) which is the GH Actions callback path — strategy (a) chosen. Verified 2026-04-29. |
| FF-002 | Funnel Flow | `processRawImportBatch_` definován (`RawImportWriter.gs:118`), ale **nemá menu item ani trigger**. Spustit ho lze pouze ručně z Apps Script editoru. | P0 | `apps-script/RawImportWriter.gs:118`; `apps-script/Menu.gs` (žádná položka pro raw ingest); `apps-script/PreviewPipeline.gs:installProjectTriggers` (žádný trigger) | End-to-end ingest is **manual-only**. Bez wiring do menu/trigger se nedá zprovoznit jako automatic pipeline. Cross-ref FF-001 — oba kroky chybí. | Přidat menu item v Menu.gs nebo trigger v installProjectTriggers (např. each 30 min processRawImportBatch). | **Resolved**. Menu item `Menu.gs:61` ("🔄 Import raw → LEADS") + 30-min time trigger registered in `PreviewPipeline.gs:1614` (the `installed.push` line explicitly references "FF-002"). Verified 2026-04-29. |
| FF-003 | Funnel Flow | `processPreviewQueue` (15-min cron trigger) **nepoužívá LockService**. Pokud běh trvá > 15 min (latentní webhook + 100-row BATCH_SIZE), další tick startuje paralelně. Oba čtou stejné rows, oba zapisují → last-write-wins, lost updates. | P1 | `apps-script/PreviewPipeline.gs:885` (`processPreviewQueue`, 200+ řádků, žádné `LockService.tryLock`); compared to `AutoQualifyHook.gs:33`, `AutoWebCheckHook.gs:34` které mají lock | Race v cron může přepsat operator's review_decision (cross-ref FF-019). Také webhook duplications. | Wrap celý `processPreviewQueue` body do `LockService.getScriptLock().tryLock(60000)` s try/finally release. Při lockFailed log + early return. | Open |
| FF-004 | Funnel Flow | Funnel-level dopad IN-014: po Vercel deploy / cold-start in-memory preview store je prázdný, ale `preview_url` v LEADS ukazuje na `/preview/<slug>` který vrací 404. Funnel nemá detection ani recovery — `MailboxSync` neví, že email s mrtvým odkazem byl odeslán. | P1 | `crm-frontend/src/lib/preview/preview-store.ts:28` (in-memory Map); `crm-frontend/src/lib/mock/sample-brief-loader.ts:45` (fallback na hardcoded fixtures pouze 5 slugs); funnel-side `apps-script/PreviewPipeline.gs:1026` zapisuje `preview_url` bez health check | Operator pošle email klientovi s preview URL → klient klikne → 404. Lost lead, embarrassed operator. **Critical** pokud send proběhl po Vercel restart. | Krátkodobě: `processPreviewQueue` track `preview_generated_at`, pokud > 24h re-render. Středně: persistent preview-store (Vercel KV / Sheets-backed). Cross-ref IN-014. | **Resolved** (Phase 2 KROK 2). New `apps-script/PreviewStore.gs` is the Sheets-backed `_previews` store; frontend `crm-frontend/src/lib/preview/preview-store.ts:9` explicitly comments "Replaces the B-04 in-memory Map (FF-004 fix): Vercel restarts no longer wipe state." Verified 2026-04-29. |
| FF-005 | Funnel Flow | CHANGES_REQUESTED → `preview_stage=BRIEF_READY` → re-eligible pro `processPreviewQueue` → regenerace identického briefu (deterministic) → READY_FOR_REVIEW se stejným obsahem → operator opět CHANGES_REQUESTED → infinite loop. Operator musí změnit zdrojová data (key_benefits, painPoint atd.) v LEADS, jinak loop. | P2 | `apps-script/ContactSheet.gs:937-938` `decision === 'CHANGES_REQUESTED' → newPreviewStage = BRIEF_READY`; `apps-script/PreviewPipeline.gs:922` `eligibleStages` zahrnuje `'brief_ready'`; brief je deterministicky generovaný z LEADS row data | Operator workflow gap. Operator může nevědět, že CHANGES_REQUESTED bez data change = no-op. Plýtvá webhook calls. | Buď: (a) CHANGES_REQUESTED otevře modal/dialog ke specifikaci čeho — co se má změnit (free text → uložit do `review_note`, pak operator manualně edituje source); (b) auto-flag nutnost edit source dat v cell note; (c) implementovat AI-driven brief regeneration s alternative variants. Krátkodobě: dokumentovat workflow v 21-business-process. | Open |
| FF-006 | Funnel Flow | `OutboundEmail.executeCrmOutbound_` **nekontroluje `review_decision == APPROVE`** ani `send_allowed=true`. Operator může poslat email leadu, který byl REJECT-nutý v review, nebo nemá `send_allowed=true`. | P1 | `apps-script/OutboundEmail.gs:47-98` (executeCrmOutbound_ guards check pouze: valid email, subject/body, identity, won/lost stage); žádný read `review_decision` ani `send_allowed` ani `preview_stage` | Operator může omylem poslat email leadu jehož preview byl odmítnutý (např. obsah mu připadal nevhodný). Komunikace s rejected leadem = brand risk. Cross-ref C-04 Sendability gate (SPEC ONLY, FF-009). | Přidat guard: pokud `review_decision !== 'APPROVE'` → safeAlert + abort. Pokud `send_allowed !== 'TRUE'` → safeAlert + abort. Lift logic do C-04 sendability gate runtime. | **Resolved** (KROK 4). `apps-script/OutboundEmail.gs:54` comments "KROK 4 (FF-006): sendability gate — review_decision MUST be APPROVE"; gate enforced at `:243` (read) and `:279` (`if (reviewDecision !== REVIEW_DECISIONS.APPROVE)` → abort with explicit error). Verified 2026-04-29. |
| FF-007 | Funnel Flow | Double-send guard v `OutboundEmail.gs` je **pouze confirmation prompt** (`OUTBOUND_DOUBLE_SEND_MINUTES`). Operator může opakovaně potvrdit a poslat 2×, 3×... | P2 | `apps-script/OutboundEmail.gs:175-192` (prompt s YES_NO, ne hard block) | Spam riziko — leadu může přijít 5 emailů během 10 min, pokud operator je netrpělivý / klikne omylem na YES. | Hard block s explicit override: druhé odeslání vyžaduje typing of explicit string ("RESEND") nebo musí proběhnout po `OUTBOUND_HARD_LIMIT_MINUTES` (e.g. 60). | Open |
| FF-008 | Funnel Flow | `MailboxSync.syncMailboxMetadata` **nemá trigger**. Reply / bounce / OOO classification proběhne pouze když operator ručně klikne menu. Mezitím lead leží ve stavu CONTACTED bez detected reply weeks. | P1 | `apps-script/MailboxSync.gs:22` (manual function); `apps-script/Menu.gs:67` (menu item only); `apps-script/PreviewPipeline.gs:installProjectTriggers` (žádný `syncMailboxMetadata` trigger) | Critical leady (RESPONDED stage) zůstávají invisible operatorovi. Race s WON/LOST manual updates. | Přidat 30-min trigger pro `syncMailboxMetadata` v `installProjectTriggers`. Zvážit Gmail push notifications jako alternativa (vyžaduje Cloud Pub/Sub + service account). | **Resolved** (FF-008 KROK 4). 1-hour `syncMailboxMetadata` time trigger registered in `apps-script/PreviewPipeline.gs:1624` with explicit `installed.push('Timer: syncMailboxMetadata (1 hour) — FF-008')` label. Verified 2026-04-29. |
| FF-009 | Funnel Flow | C-04 Sendability gate je SPEC-ONLY (per `docs/30-task-records/C-04.md` "SPEC-only autoritativni gate mezi preview a outreach"). Žádný runtime enforcement. | P1 | `docs/30-task-records/C-04.md` (Title říká SPEC-only); grep `sendability` v `apps-script/` = 0 hits | Bez gate runtime: send je gated pouze ad-hoc guards (FF-006). Spec-only artifact bez implementace = false sense of safety pro reviewers, kteří podle docs vidí "gate". | Implementovat C-04 runtime: function `isLeadSendable_(leadId)` která agreguje review_decision, send_allowed, suppression list, bounce/unsubscribe markers. Volat z `executeCrmOutbound_` před send. | Open |
| FF-010 | Funnel Flow | C-05 Outbound queue + send payload kontrakt SPEC-ONLY. Žádná queue, žádný retry / dead-letter. | P2 | `docs/30-task-records/C-05.md` (Title SPEC-only) | Manual operator-driven send je single point of failure. Bez queue + retry = ztráta sendů při Apps Script timeout. | Implementovat C-05 runtime po C-04 gate. | Open |
| FF-011 | Funnel Flow | C-06 Provider abstraction SPEC-ONLY. GmailApp je hardcoded v `OutboundEmail.gs`. | P2 | `docs/30-task-records/C-06.md`; `apps-script/OutboundEmail.gs:233`,`281` (GmailApp.createDraft, GmailApp.sendEmail) | Vendor lock-in. Migrace na ESP (Resend, SendGrid, AWS SES) by vyžadovala rewrite OutboundEmail.gs. Limity: Gmail consumer 100/den, Workspace 1500/den. | Implementovat sender interface dle C-06 (provider switch via Config). | Open |
| FF-012 | Funnel Flow | C-07 Inbound event ingest SPEC-ONLY — žádný webhook listener / push notification. Manuální `syncMailboxMetadata` je single source pro reply/bounce/unsubscribe. | P1 | `docs/30-task-records/C-07.md`; manual MailboxSync flow per FF-008 | Late inbound detection. Unsubscribe requests can sit unread for days = legal compliance risk (CAN-SPAM / GDPR Art. 7(3) right to withdraw consent). | Implementovat C-07 runtime: Gmail Pub/Sub watch + push subscription, nebo IMAP idle (out-of-band Apps Script), nebo polling cron 30-min jako MVP. | Open |
| FF-013 | Funnel Flow | C-08 Follow-up engine SPEC-ONLY — `next_followup_at` operator manually editable, žádný cron picking up overdue follow-ups. | P1 | `docs/30-task-records/C-08.md`; `next_followup_at` v Config.gs `EXTENSION_COLUMNS` ale žádný runtime scheduler | Operator musí ručně checkovat každý den "co je overdue". V realitě → forgotten follow-ups = lost leads. Frontend `/follow-ups` view exists, ale ten pouze zobrazuje (read-only) — žádné automatic action. | Implementovat C-08 runtime: 1× denně cron projde leady kde `next_followup_at <= today` AND not in terminal state, vytvoří task pro operatora (Slack ping / email summary / cell note in Ke kontaktování). | Open |
| FF-014 | Funnel Flow | C-11 Config / kill switch / feature flags SPEC-ONLY. Pouze `DRY_RUN`, `ENABLE_WEBHOOK` jako file-level vars v `Config.gs`. | P2 | `docs/30-task-records/C-11.md`; `apps-script/Config.gs:24-26` (3 vars) | Žádný runtime kill-switch. Pokud bug způsobí, že GAS posílá email loop / corrupts data, jediný stop je: deploy nový `.gs` s flag flipped, OR delete trigger, OR rotate FRONTEND_API_SECRET. Reactive, not preventive. | Implementovat C-11 runtime: read kill-switch z Script Properties (`KILL_SEND=true`, `KILL_PREVIEW=true`, atd.). Při toggle = okamžitý effect. | Open |
| FF-015 | Funnel Flow | CS1 `lifecycle_state` SPEC-ONLY. Runtime má 4 separate state machines (`lead_stage`, `preview_stage`, `outreach_stage`, `email_sync_status`) bez kanonického orchestrátoru. | P1 | `docs/30-task-records/CS1.md` ("Tento PR nezavadi runtime enforcement lifecycle_state"); `apps-script/Config.gs:141-231` (4 separate enums) | Operátor i dev musí cross-check 4 sloupce, aby zjistil "kde je lead". Inconsistencies between sloupci jsou nemožné chytit ručně. CS2 workflow orchestrator depends on CS1 → blokuje cesta k C-04..C-11. | Implementovat lifecycle_state runtime: column add do EXTENSION_COLUMNS, derive function (per CS1 spec), populate v každé funkci, která mění některý ze 4 stávajících stavů. Cross-ref CS2 orchestrator. | Open |
| FF-016 | Funnel Flow | Žádný per-lead audit trail / event log. `_asw_logs` je function-level (logs lead_id v záznamu, ale není to per-lead history view). | P2 | `apps-script/Helpers.gs:296` `aswLog_` (logs do `_asw_logs` sheet, řazeno chronologicky napříč všemi funkcemi); žádná dedikovaná `_lead_events` sheet | Operator nemá "co vše se s tímhle leadem stalo od ingestu" view. Při audit / debug "proč je lead v tomhle stavu" musí grep `_asw_logs` ručně. | Vytvořit `_lead_events` sheet: per-row entry `{lead_id, ts, event_type, actor, before_state, after_state, payload_hash}`. Append from každé state-changing funkce. | Open |
| FF-017 | Funnel Flow | Žádný funnel-health alerting / dashboard. Stuck leady (preview_stage=GENERATING > 1h, FAILED retry > 5×, SENT > 30 dní bez RESPONDED) jsou invisible until manual audit. | P2 | Žádný cron + alert v `apps-script/`; žádný dashboard widget v `crm-frontend/src/app/dashboard/` | Operator nemá detection mechanism pro "funnel je nefunkční". Critical leady mohou vypadnout. | Vytvořit `funnelHealthCheck_()` cron 30-min: scan LEADS, count stuck states; pokud > thresholds → email/Slack alert + log do `_asw_logs` ERROR. Frontend dashboard widget "Funnel health" se 4 KPI bars. | Open |
| FF-018 | Funnel Flow | Frontend nemá review queue. Operator musí scrollovat "Ke kontaktování" v Sheets UI a hledat řádky s `Rozhodnutí ✎` empty + `preview_stage=READY_FOR_REVIEW`. | P2 | `crm-frontend/src/app/` (chybí `/reviews` route); `crm-frontend/src/components/` (chybí ReviewQueue komponenta) | Operator UX gap. Sheets UI je dense, hard-to-filter. Dedicated review page + filtry by zrychlilo work. | Přidat `/reviews` route v `crm-frontend/src/app/reviews/page.tsx` s tabulkou leads where `preview_stage=READY_FOR_REVIEW`, sortovatelné, klik = open preview iframe + accept/reject buttons (volá Apps Script přes existing webapp endpoint). | **Resolved** in `ec6445f` (A-11). `crm-frontend/src/app/scrape/review/page.tsx` ships the dedupe-review queue with `DedupeReviewDialog` (side-by-side compare + APPROVE/REJECT/CHANGES_REQUESTED actions) — chosen route name `/scrape/review` rather than `/reviews` (per A-11 task record). Verified 2026-04-29. |
| FF-019 | Funnel Flow | Concurrency: cron `processPreviewQueue` (no lock) může batch-write přepsat operator's review_decision pokud cron's read snapshot byl před operator edit. | P1 | `apps-script/PreviewPipeline.gs:893` (`readAllData_`), `:1076` (`writeExtensionColumns_` batch write); cron has žádný consistency check že rows nebyly modifikovány v mezičase | Lost operator edits. Operator zase APPROVE-uje, znovu se to přepíše... operator dostane note konfuze. | Buď: (a) `processPreviewQueue` use LockService (FF-003); (b) batch write check timestamp `last_processed_at` před zápisem (optimistic concurrency) — pokud byl změněn, skip overwrite. | Open |
| FF-020 | Funnel Flow | `OutboundEmail.executeCrmOutbound_` bez LockService. 2 paralelní operator menu clicks (theoretically) mohou poslat 2×. | P2 | `apps-script/OutboundEmail.gs:47` (žádný `LockService.tryLock`) | Apps Script script-level locks by zabránily 2 současným executions od různých operátorů. Bez lock + bez idempotency key = duplicate sends možné v concurrent scenario. | Wrap `executeCrmOutbound_` do LockService(10s). Při lockFailed safeAlert. | Open |
| FF-021 | Funnel Flow | `simulateAndWrite()` (PreviewPipeline.gs:1096) "dry run" který fakticky zapisuje do extension columns. Confusing semantics — "dry run" obvykle = no writes. | P2 | `apps-script/PreviewPipeline.gs:1096-1110` (saves DRY_RUN+ENABLE_WEBHOOK, sets DRY_RUN=true ENABLE_WEBHOOK=false, calls qualifyLeads + processPreviewQueue → ZÁPIS do extension columns); `apps-script/Menu.gs:44` "Simulace + zápis (dry run)" — Czech naming hints na semantics ale funkce calls explicitně `processPreviewQueue` který zapisuje | Operator dostane false sense "tahle akce nic nezmění"; ve realitě modifikuje 50+ extension columns napříč rows. | Buď přejmenovat funkci na `simulateAndWriteExtension_` a clearly documentovat, nebo refactorovat na true dry-run (no writes, jen log report). | Open |
| FF-022 | Funnel Flow | `lead_stage = REVIEW` definovaný v `Config.gs:174` ale žádný kód do něj zapisuje. Zombie state. | P2 | `apps-script/Config.gs:174` (`REVIEW: 'REVIEW'`); grep `LEAD_STAGES.REVIEW\|'REVIEW'` v PreviewPipeline.gs evaluateQualification_ = nikdy nenastaví | Confusing pro reviewer kódu. Otázka "kdy lead přejde do REVIEW" = nedefinováno. Buď spec drift (kdysi se uvažovalo, nikdy implementováno) nebo zombie kód. | Buď odstranit z `LEAD_STAGES` enum (cleanup), nebo implementovat: dedupe REVIEW bucket → propagovat do `lead_stage`. | **Resolved**. `LEAD_STAGES.REVIEW` is now written by `apps-script/PreviewPipeline.gs:451` (`stage: LEAD_STAGES.REVIEW`) and counted in `:325` + `apps-script/AutoQualifyHook.gs:141`; consumed by `apps-script/ContactSheet.gs:87` and gated in `:850`. Strategy: implementation chosen (dedupe REVIEW bucket propagated to `lead_stage`). Verified 2026-04-29. |
| BLD-001 | Buildability | `crm-frontend/.env.example` obsahuje **plnou produkční hodnotu** `GOOGLE_SPREADSHEET_ID=1RBc…` (44 znaků) místo placeholderu. Fresh dev po `cp .env.example .env.local` má v lokálním env reálný PROD Sheet ID. Audited at `crm-frontend/.env.example:4 @ fb2372d`. | P0 | `crm-frontend/.env.example:4 @ fb2372d` (typ `sheet_id`, prvních 4 znaky `1RBc`, délka 44); cross-ref SEC-001, DP-001 evidence | Rozšiřuje plochu PROD ID v repu o další tracked file. Onboarding řeší tím, že fresh dev neúmyslně cílí na PROD sheet při lokálním vývoji. | Nahradit `1RBc…` za `your-spreadsheet-id-here` placeholder. Cross-domain s SEC-001 cleanup task. | **Resolved** in `2aecb39` (fix(pilot): env hygiene + next@16.2.4 + fail-fast mock mode (KROK 2)). `.env.example` rewritten with placeholders only; PROD ID removed from this file (still in Config.gs / EnvConfig.gs — see SEC-001). Verified 2026-04-29. |
| BLD-002 | Buildability | Žádný root `README.md` v repu. Fresh dev po `git clone` nemá top-level setup instrukce. Audited at fresh clone @ `fb2372d` — `ls README.md` returns "No such file or directory". | P0 | `git ls-tree origin/main README.md @ fb2372d` = empty; sub-READMEs existují v `crm-frontend/`, `apps-script/`, `scripts/scraper/` ale fresh dev je nenajde bez index | First-impression UX gap. New dev neví: kde začít, jak nainstalovat, jak rozjet, kde jsou docs. CONTRIBUTING.md a CLAUDE.md existují ale ne primary entry point. | Vytvořit root `README.md` s: project description (1 odst), setup instrukce (link na crm-frontend/README a apps-script/README), docs map (link na docs/ a docs/audits/), license. Cross-domain s Phase 10 DOC. | **Resolved**. Root `README.md` exists (`# Autosmartweby` + tagline "Sales CRM nad Google Sheets s Apps Script backend a Next.js frontendem."). Verified 2026-04-29. |
| BLD-003 | Buildability | `crm-frontend/.env.example @ fb2372d` chybí 3 reálně používané env vars: `AUTH_PASSWORD`, `PREVIEW_WEBHOOK_SECRET`, `PUBLIC_BASE_URL`. Bez nich aplikace neběží plně (login fails 503, webhook 500, preview URLs broken). | P0 | `crm-frontend/.env.example @ fb2372d` (10 vars deklarovaných); `crm-frontend/src/app/api/auth/login/route.ts:6` (`AUTH_PASSWORD`); `…/api/preview/render/route.ts:62` (`PREVIEW_WEBHOOK_SECRET`); `:56` (`PUBLIC_BASE_URL`) | Fresh dev po `cp .env.example .env.local` + naplnění values **stále nemůže přihlásit** (login = 503 "Přihlášení není nakonfigurováno" pokud `AUTH_PASSWORD` chybí). Dev je v slepé uličce bez external help. | Doplnit do `.env.example`:\n```\nAUTH_PASSWORD=your-shared-login-password\nPREVIEW_WEBHOOK_SECRET=your-preview-webhook-secret\nPUBLIC_BASE_URL=http://localhost:3000\n``` | **Resolved** in `2aecb39` (KROK 2). `.env.example` now contains `AUTH_PASSWORD`, `PREVIEW_WEBHOOK_SECRET`, `PUBLIC_BASE_URL`, plus 4 more (`ALLOWED_EMAILS`, `APPS_SCRIPT_WEB_APP_URL`, `APPS_SCRIPT_SECRET`, `GOOGLE_PRIVATE_KEY`) per the commit body. Verified 2026-04-29. |
| BLD-004 | Buildability | `crm-frontend/.env.example @ fb2372d` obsahuje 3 zombie env vars: `NEXTAUTH_URL`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET` — nepoužité v `crm-frontend/src/`. Fresh dev předpokládá NextAuth + Google OAuth jsou wired, není tomu tak. | P1 | `crm-frontend/.env.example:11-15 @ fb2372d`; `grep NEXTAUTH_URL\|GOOGLE_CLIENT_ID\|GOOGLE_CLIENT_SECRET crm-frontend/src/` = 0 hits | Misleading onboarding. Dev konfiguruje OAuth setup ve Google Cloud, věří že je potřeba; reálně runtime používá `AUTH_PASSWORD` (BLD-003) + custom HMAC v `middleware.ts`. | Buď odstranit zombies z `.env.example`, nebo implementovat NextAuth + Google OAuth (cross-ref archive task plans v `docs/archive/18-google-auth-*.md`). | **Resolved** in `2aecb39` (KROK 2). `.env.example` rewritten — zombie OAuth vars removed, replaced with the 7 truly-used vars per BLD-003. Verified 2026-04-29. |
| BLD-005 | Buildability | Žádný root `package-lock.json @ fb2372d`. Root `package.json` obsahuje 0 dependencies, scripts používají `node --experimental-strip-types` (TS-strip flag) které se mezi Node verzemi mírně liší. | P2 | `git ls-tree origin/main package-lock.json @ fb2372d` = empty; `package.json @ fb2372d` (žádné dependencies pole) | Test scripts mohou zlobit na různých Node verzích (≥22.6 supports `--experimental-strip-types`, exact behavior se vyvíjí). Bez lockfile = no version pin. | Buď přidat minimální dependencies + lockfile (např. `node:test` + types), nebo přidat `engines.node` constraint. | BLD-019 |
| BLD-006 | Buildability | `crm-frontend/package.json @ fb2372d` nemá `typecheck` script. `npx tsc --noEmit` funguje, ale fresh dev musí znát příkaz. | P2 | `crm-frontend/package.json:5-9 @ fb2372d` (scripts: dev, build, start, lint — žádný typecheck) | Onboarding gap. Build (`next build`) **interně** spustí TS check (PASS v Phase 9), ale standalone TS check je užitečný pro IDE / CI gating. | Přidat `"typecheck": "tsc --noEmit"` do `crm-frontend/package.json` scripts. | — |
| BLD-007 | Buildability | Apps Script vyžaduje `@google/clasp` global install, ale není v žádném `package.json` ani v setup docs. Fresh dev musí znát `npm i -g @google/clasp`. | P1 | Žádný `clasp` v `package.json` (root nebo apps-script); `apps-script/README.md @ fb2372d` (žádný `npm i -g clasp` step); `scripts/clasp-deploy.sh:41,82` (volá `clasp` jako PATH binary) | Fresh dev klone repo, snaží se Apps Script setup, narazí na `clasp: command not found`. Hledá ad-hoc návod, ztrácí čas. | Přidat do `apps-script/README.md` setup section: prerequisites (Node, clasp install command). Případně přidat `clasp` do `package.json` `optionalDependencies` pro auto-install. | DP-013 |
| BLD-008 | Buildability | `apps-script/README.md @ fb2372d` setup steps stále stale per DP-013: lists `Code.gs`, `Qualify.gs`, `Preview.gs`, `Pipeline.gs` (neexistují); říká "28 new columns" (now 55+). | P1 | `apps-script/README.md:35-73 @ fb2372d`; `ls apps-script/*.gs @ fb2372d` (real files: Menu.gs, AutoQualifyHook.gs, AutoWebCheckHook.gs, PreviewPipeline.gs, etc.); `apps-script/Config.gs:68-125 @ fb2372d` (55+ EXTENSION_COLUMNS) | Onboarding mismatch. New dev hledá Code.gs, narazí prázdno; čte 28 cols, real je 55+. Ztracený čas + zmatení. | Přepsat sekci files na current `ls`, EXTENSION_COLUMNS odkazem na Config.gs, setup přes clasp clone. Cross-ref DP-013. | DP-013 |
| BLD-009 | Buildability | Next.js 16 build warns: `middleware` file convention deprecated → use `proxy`. Build PASS ale future Next.js update může break. | P2 | `npm run build` output: `⚠ The "middleware" file convention is deprecated. Please use "proxy" instead.`; `crm-frontend/src/middleware.ts @ fb2372d` (existing file) | Tech debt. Pokud Next.js 17 odstraní `middleware`, aplikace přestane fungovat při auto-update. Žádný immediate impact. | Migrate `src/middleware.ts` → `src/proxy.ts` per https://nextjs.org/docs/messages/middleware-to-proxy. Verify auth check stále funguje. | — |
| BLD-010 | Buildability | Cross-ref SEC-012: `next@16.2.2 @ fb2372d` HIGH DoS CVE v `npm audit --omit=dev` output. Fix v 16.2.4. Persistuje od Phase 7 — ne fixed v main. | P1 | `crm-frontend/package.json:13 @ fb2372d` (`"next": "16.2.2"`); `npm audit --omit=dev` output: GHSA-q4gf-8mx6-v5v3 next | Bezpečnostní gap stále aktivní. Fresh dev nasadí Vercel, dostane production app s known HIGH CVE. | `npm install next@16.2.4` v `crm-frontend/`. Cross-ref SEC-012. | **Resolved** transitively via SEC-012 fix in `2aecb39`. `next@^16.2.4` is now in `package.json`. |
| BLD-011 | Buildability | Build prints warning `[CRM Auth] NEXTAUTH_SECRET is not set — session tokens will not be signed securely.` při static page gen v fresh clone bez `.env.local`. Build succeeds ale signal že env var fallback je SEC risk. | P1 | `npm run build` output (Phase 9 fresh clone, no `.env.local`); cross-ref SEC-016 (NEXTAUTH_SECRET fallback na empty string) | Pokud Vercel deploy s missing env var, runtime auth bypass možný. Build neselže = silent danger. | Build by měl FAIL pokud `NEXTAUTH_SECRET` není set v PROD context. Implementovat SEC-016 fix (throw na missing env). | **Resolved** transitively via SEC-016 fix in `24e3d65`. Verified 2026-04-29: `NEXTAUTH_SECRET= npm run build` now fails with "NEXTAUTH_SECRET is missing or shorter than 32 chars" — no more silent warning. |
| BLD-012 | Buildability | `npm run lint @ fb2372d` produkuje 14 ESLint warnings (unused imports/vars) napříč 6 souborů. Code quality, ne blocker. | P3 | `npm run lint` output: 14 problems / 0 errors / 14 warnings; files: priority-leads-widget.tsx, lead-detail-drawer.tsx (2x), lead-filters.tsx, leads-table.tsx, template-family.ts | Code drift. Žádný immediate impact ale ESLint config eskaluje při tightening rules. | Cleanup nepoužité imports / `_` prefix pro intent-unused vars. CI by měl zapnout `--max-warnings 0` po cleanup. | DP-005 |
| BLD-013 | Buildability | A-stream tests (`test-a05-*` až `test-a09-*` + `test-ingest-runtime`, `test-wave4-pipeline`) **nejsou v `package.json` scripts**. Spuštění vyžaduje `node scripts/test-XXX.mjs` přímo. | P2 | `package.json:6-15 @ fb2372d` (jen `test:b03..b06`); `scripts/test-a*.mjs`, `scripts/test-ingest-*.mjs`, `scripts/test-wave4-*.mjs` (8 souborů bez npm script) | Inconsistent UX. New dev čte package.json scripts, předpokládá že to je full test suite. A-stream tests skipped → drift mezi tested a untested. | Přidat npm scripts pro každý A-stream test (`test:a05-batch`, `test:a05-bc`, `test:a06`, `test:a07`, `test:a08`, `test:a09`, `test:ingest`, `test:wave4`). Plus aggregator `test:all` (cross-ref BLD-014). | — |
| BLD-014 | Buildability | Žádný root `npm test` ani `npm run verify` aggregator. Fresh dev musí ručně volat 4× `test:b03..b06` + 8× A-stream tests. | P2 | `package.json @ fb2372d` (no `test` ani `verify`); cross-ref DP-005 (no CI test gate) | Bez aggregátoru fresh dev může omylem skip některé tests. CI by potřebovalo single command. | Přidat `"test": "npm run test:b03 && npm run test:b04 && npm run test:b05 && npm run test:b06 && npm run test:a05-bc && ..."` (po BLD-013). Dokumentovat v README. | DP-005 |
| BLD-015 | Buildability | CI nespouští žádné testy / build / lint / typecheck (cross-ref DP-005). Fresh dev nemá feedback že jeho PR test-suite passes před review. Tato Phase 9 ověřila všechny tyto checky lokálně PASS — ale nikdo ne-CI je negaranccováno udržuje. | P1 | `.github/workflows/docs-governance.yml @ fb2372d` (jen docs check); cross-ref DP-005 | High drift risk. Bez CI gate může merge break build / typecheck / tests, manifestuje se až při manual local test nebo Vercel deploy fail. | Přidat `ci.yml` workflow per DP-005 doporučení. Cross-ref BLD-014 aggregator. | **Resolved** transitively via DP-005 fix (KROK 7). `.github/workflows/pilot-ci.yml` runs lint + tsc + build + tests on every PR. Operator action remaining: add `pilot-ci` to required_status_checks in branch protection. |
| BLD-016 | Buildability | `crm-frontend/README.md @ fb2372d` postrádá deployment instructions (cross-ref DP-016) a env list je neúplný (chybí `APPS_SCRIPT_SECRET`, `PREVIEW_WEBHOOK_SECRET`, `PUBLIC_BASE_URL`). | P2 | `crm-frontend/README.md:5-39 @ fb2372d` (no deploy section); `:25-31` (env list 7/10 vars) | Onboarding gap. Frontend dev nemůže deploynout z README, env config incomplete. | Cross-ref DP-016 — opravit jako součást toho findingu. | DP-016 |
| BLD-017 | Buildability | Windows + Czech path + OneDrive ("`C:/Users/spook/Nabídka weby`") combo není v Phase 9 verifikováno (audit běží v `/tmp/...`). Theoretical risk: OneDrive sync vs `node_modules` lock contention, paths s diakritikou v error messages. | P2 | Phase 9 protokol běžel v `C:\Users\spook\AppData\Local\Temp\...` (no Czech, no OneDrive sync); user-machine path obsahuje Czech `í` + space + OneDrive layer | Pokud user pracuje v OneDrive working copy, npm install / build mohou narazit na lock issues, atd. (per Phase 5 cleanup, viděny 0-byte artefakty s neobvyklými jmény) | Doporučit v root README: "Pracuj mimo OneDrive (např. `~/projects/autosmartweby/`) pro nejnižší riziko sync issues". | — |
| BLD-018 | Buildability | `crm-frontend/next.config.ts @ fb2372d` je prázdný (`{}` config). Žádné customizace — default Next.js setup. | P3 | `crm-frontend/next.config.ts:3-7 @ fb2372d` (`const nextConfig: NextConfig = {};`) | Není to problém samo o sobě, ale signál že žádné performance / security tweaks nejsou nakonfigurované (např. `images.domains`, security headers, CSP). | Zvážit přidání security headers (CSP, X-Frame-Options) pro `/preview/<slug>` public route (cross-ref SEC-009). | SEC-009 |
| BLD-019 | Buildability | Žádný `.nvmrc`, `.tool-versions`, ani `engines.node` v `package.json` (root nebo frontend) @ fb2372d. Fresh dev s nesprávnou Node verzí může mít subtle bugs (TS-strip flag, ESM behavior). | P2 | `git ls-tree origin/main .nvmrc .tool-versions @ fb2372d` = empty; `package.json @ fb2372d` (no engines); `crm-frontend/package.json @ fb2372d` (no engines) | Test scripts používají `node --experimental-strip-types` který je v Node ≥ 22.6. Fresh dev s Node 18/20 dostane error. | Přidat `.nvmrc` s `lts/*` (nebo specific verze, např. `22.11.0`); přidat `"engines": {"node": ">=22.6"}` do `package.json` (root + frontend). | BLD-005 |
| BLD-020 | Buildability | `crm-frontend/package-lock.json @ fb2372d` je 391 KB. Konzistentní (npm ci PASS), ale velký. | P3 | `ls -la crm-frontend/package-lock.json @ fb2372d` = 391678 B | Žádný immediate impact. Velký lockfile = pomalejší git pull / merge / clone. | Periodic dep cleanup (nepoužívané deps). Žádná immediate akce. | — |
| BLD-021 | Buildability | `scripts/clasp-deploy.sh @ fb2372d` je shell skript (`#!/usr/bin/env bash`) → vyžaduje Bash. Windows-only dev s pouze PowerShell/cmd ho nemůže spustit. | P2 | `scripts/clasp-deploy.sh:1 @ fb2372d` (`#!/usr/bin/env bash`); cross-ref Phase 6 deploy flow | Cross-platform onboarding gap. Windows dev musí mít WSL/Git Bash/Cygwin. | Buď: (a) přidat ekvivalent `clasp-deploy.ps1` PowerShell skript; (b) převést na Node.js skript v `scripts/clasp-deploy.mjs` (cross-platform). | — |
| DOC-001 | Docs & Onboarding | `docs/CRM-SYSTEM-MAP.md` (lives in `docs/archive/`) referenced as canonical v 5 trackovaných souborech. 3 z nich ho označují jako "AKTUALNI" / canonical source. Cross-ref DP-016. | P0 | `docs/01-decision-list.md:111`; `docs/09-project-control-tower.md:229`; `docs/10-documentation-governance.md:9,39`; `docs/29-task-registry.md:38` (generated); `crm-frontend/README.md:90`; všechny @ `26d4802` | Onboarding/governance gap: každý reader sledující "the canonical CRM map" narazí na 404. Decision/governance docs ztrácí důvěryhodnost. Pokud se v archive doc něco změnilo, není sync s "canonical" claim. | Buď: (a) přesunout `docs/archive/CRM-SYSTEM-MAP.md` zpět do `docs/` jako canonical; (b) odstranit reference z 5 souborů + nahradit za nové canonical docs. Decision musí učinit owner. | DP-016 |
| DOC-002 | Docs & Onboarding | `docs/10-documentation-governance.md` referencuje 7 archive docs jako canonical (`CRM-SYSTEM-MAP.md`, `01-audit-consolidation.md`, `06-bug-registry.md`, `06-column-mappings-analysis.md`, `00-folder-inventory.md`, `00-project-map.md`, `02-target-structure.md`, `03-cleanup-executed.md`, `05-monorepo-setup-log.md`) + 1 neexistující soubor `docs/07-test-plan.md`. Governance doc je sám stale → governance pravidla nejsou důvěryhodná. | P0 | `docs/10-documentation-governance.md:9-22 @ 26d4802` (table referencing archive docs as canonical) | Doc governance se nemůže opřít sama o sobě. Repo maintainer čte governance doc, věří jí; ale uvedené "canonical" docs jsou v archive nebo neexistují. Onboarding novému maintainerovi je broken. | Aktualizovat `docs/10` na současný canonical layer `docs/20-29` + `docs/30-task-records/`. Odstranit archive references. Přidat sekci "Audit docs" pro `docs/audits/`. | — |
| DOC-003 | Docs & Onboarding | `docs/09-project-control-tower.md:7 @ 26d4802` má 3 stale claims v jednom řádku: wrong path `node scripts/check-doc-sync.mjs` (real `scripts/docs/check-doc-sync.mjs`), "26 pass" (Phase 9 verified 43 pass), "Validace" tooling instrukce vede k `command not found`. | P1 | `docs/09-project-control-tower.md:7 @ 26d4802` | Operator/maintainer čte control tower (claimed "jediny zdroj pravdy") a dostává wrong commands + wrong counts. Důvěryhodnost source-of-truth ztracena. | Aktualizovat řádek na `node scripts/docs/check-doc-sync.mjs` + current pass count + bump `Datum`. | — |
| DOC-004 | Docs & Onboarding | `docs/09-project-control-tower.md:18 @ 26d4802` claims "20 deduplikovanych problemu (C-1 az L-5), 8 rozhodnuti (D-1 az D-8). Vetsi cast vyresena." + `docs/10-documentation-governance.md:21 @ 26d4802` "Rozhodovaci log (D-1 az D-8)". Reality: `docs/01-decision-list.md @ 26d4802` má 15 decisions D-1..D-15. | P1 | `docs/09-project-control-tower.md:18,20 @ 26d4802`; `docs/10-documentation-governance.md:21 @ 26d4802`; `docs/01-decision-list.md:^| D-` count 15 | Stale references → maintainer ztrácí důvěru v decision list completeness. Možná že D-9..D-15 jsou neevidované v control tower. | Aktualizovat `docs/09:18-21` na current count. Bump "Datum" header. Cross-ref DOC-003. | DOC-003 |
| DOC-005 | Docs & Onboarding | `docs/14-definition-of-done.md:35,78 @ 26d4802` references `node scripts/check-doc-sync.mjs` (deleted path; real `scripts/docs/check-doc-sync.mjs`). DoD validation step → `command not found`. | P1 | `docs/14-definition-of-done.md:35` (`T3 ... node scripts/check-doc-sync.mjs — 0 fail`); `:78` (checklist `node scripts/check-doc-sync.mjs: 0 fail`) | Dev plní DoD checklist, spustí příkaz, dostane error. DoD si myslí "is satisfied" pokud command output není checked. CI je nezávisle ok (workflow používá správnou cestu) ale lokální DoD validace broken. | Update path na `node scripts/docs/check-doc-sync.mjs` (řádky 35, 78). Cross-ref DOC-003. | DOC-003 |
| DOC-006 | Docs & Onboarding | `docs/20-current-state.md:22 @ 26d4802` claims "task records system s 5 zaznamy". Reality 33 (`ls docs/30-task-records/*.md`). Drift mezi current-state doc a current state. | P1 | `docs/20-current-state.md:22 @ 26d4802`; Phase 9/10 fresh clone count: 33 | Self-defeating doc — current state doc nereflektuje current state pro základní metrik. New dev čte 5, vidí 33, neví čemu věřit. | Aktualizovat na `33 zaznamu` (lze auto-derive ze `docs/29-task-registry.md`). Bump "Posledni aktualizace". | — |
| DOC-007 | Docs & Onboarding | `docs/21-business-process.md @ 26d4802` 20 days stale (last update 2026-04-05). Mezi tím merged: CS1 lifecycle spec, CS2 orchestrator, CS3 idempotency, C-04 sendability gate, C-05 outbound queue, C-06 provider abstraction, C-07 inbound, C-08 follow-up, C-09 exception queue, C-10 perf report, C-11 config — žádný neni v doc. | P1 | `docs/21-business-process.md @ 26d4802` (>**Posledni aktualizace:** 2026-04-05); `docs/30-task-records/CS{1,2,3}.md, C-{04..11}.md` (post-2026-04-05 merges) | Operator čte business process spec, vidí pre-CS1 state. Reality: 11 SPEC-only contracts existují which significantly redefine intended business process. Operator confused, dev confused, governance broken. | Update doc s explicit "SPEC-only vs runtime" section linking each C-stream task record. Bump date. | FF-013, FF-015 |
| DOC-008 | Docs & Onboarding | `docs/25-lead-prioritization.md @ 26d4802` 20 days stale. B-06 review layer (review_decision, review_note, reviewed_at, reviewed_by sloupce + REVIEW_DECISIONS enum + APPROVED/REJECTED stage transitions) merged 2026-04-24, doc to neodráží. | P1 | `docs/25-lead-prioritization.md @ 26d4802`; `apps-script/Config.gs:120-124,156-167 @ 26d4802` (B-06 columns); `docs/30-task-records/B6.md @ 26d4802` (B-06 task record) | Lead prioritization rules musí account za review_decision. Doc bez B-06 = stale prioritization → operator může prioritizovat REJECT-nutý lead. | Update sekci pro B-06 review layer. Bump date. | — |
| DOC-009 | Docs & Onboarding | `docs/26-offer-generation.md @ 26d4802` 20 days stale. B-04 preview render endpoint, B-05 webhook auth + slug, B-06 review layer všechny merged ale doc je z pre-B-04. | P1 | `docs/26-offer-generation.md @ 26d4802` (last 2026-04-05); `docs/30-task-records/B4.md, B5.md, B6.md` post-2026-04-05 | Offer/preview generation doc nereflektuje aktuální offer pipeline. Dev/operator se rozhodují podle stale doc. | Update B-04/B-05/B-06 sekce. Bump date. | — |
| DOC-010 | Docs & Onboarding | `docs/28-risks-bottlenecks-scaling.md @ 26d4802` 20 days stale. Listuje 5 closed (R-1..R-3, H-2, C-3) + 4 open risks (H-1/D-7, H-3, M-6, M-7). Phase 5–9 audit identified **104 findings** (P0:9, P1:41, P2:50, P3:4) — **žádný není v risk doc**. | P1 | `docs/28-risks-bottlenecks-scaling.md @ 26d4802`; `docs/audits/FINDINGS.md @ 26d4802` (104 findings) | Risk doc claims to be canonical for risks but is structurally stale. Risk decisions / capacity planning are broken. | Konsolidovat audit findings do `docs/28` jako new top-level risks (nebo link na `docs/audits/FINDINGS.md` + summary). Cross-ref Phase 12 summary plan. | All audits |
| DOC-011 | Docs & Onboarding | `docs/01-decision-list.md:111 @ 26d4802` claims "`crm-frontend/README.md` (prázdný)". Reality 91 řádků (existuje, jen incomplete per DP-016). Decision context stale. | P2 | `docs/01-decision-list.md:111 @ 26d4802`; `crm-frontend/README.md @ 26d4802` (91 lines) | Decision se opírala o "FE README je prázdný" — pokud byl decision triggered touto observation, dnes může být moot. Decisions log proto stale na motivation level. | Aktualizovat decision context nebo close decision pokud byl resolved. | DP-016 |
| DOC-012 | Docs & Onboarding | Onboarding paths missing pro 4 audiences (deploy owner, security/privacy owner, new operator, repo maintainer beyond CLAUDE.md). Pouze new dev má částečnou cestu — a ta je broken (BLD-002). | P1 | Audit Phase 10 sekce D — Onboarding maps; ❌ řešeno pro 4/5 audiences | Žádný well-defined path pro role mimo "developer". Operator je odkázán na "by example", deploy owner musí objevit `clasp-deploy.sh` sám, security owner nemá threat model. | Vytvořit per-audience onboarding docs (cross-ref DOC-017/018/019/020/021/022). | BLD-002, DOC-022 |
| DOC-013 | Docs & Onboarding | `docs/22-technical-architecture.md` (DP-015) a `docs/27-infrastructure-storage.md` (DP-014) jsou cross-domain s deploy pipeline phase. Phase 10 zdůrazňuje impact na DOC governance. | P2 | viz DP-014, DP-015 evidence | Stale canonical docs mean any doc-driven onboarding/decision je broken. | Cross-ref DP-014 + DP-015 cleanup. | DP-014, DP-015 |
| DOC-014 | Docs & Onboarding | Source-of-truth ambiguity: `docs/20-current-state.md:1` ("Kanonicky"), `docs/09-project-control-tower.md:7` ("jediny operacni zdroj pravdy"), `docs/10-documentation-governance.md:21` ("Control tower → jediny zdroj pravdy"), `docs/22-technical-architecture.md:1` ("Kanonicky"). Multiple "the source of truth" without clear ownership map. | P2 | All 4 docs claim canonical/source-of-truth status @ 26d4802 | New dev/operator: kterému doc věřit při konfliktu? Reality všechny 4 mají overlap a žádný jasný ownership. | Vytvořit single source-of-truth matrix (např. v `docs/10-documentation-governance.md` aktualizovat sekci "Co aktualizovat pro kazdy typ zmeny"). Definovat per-topic single owner. | — |
| DOC-015 | Docs & Onboarding | Konsoliduje DP-016 + DOC-001: `crm-frontend/README.md:90 @ 26d4802` `[docs/CRM-SYSTEM-MAP.md](../docs/CRM-SYSTEM-MAP.md)` link → real soubor je v `docs/archive/CRM-SYSTEM-MAP.md`. | P2 | `crm-frontend/README.md:90 @ 26d4802` | Frontend dev klikne na link, dostane 404 v GitHub UI. Onboarding break point. | Update link na `../docs/archive/CRM-SYSTEM-MAP.md` (s disclaimer "archive") nebo nahradit za `../docs/22-technical-architecture.md`. Cross-ref DP-016. | DP-016, DOC-001 |
| DOC-016 | Docs & Onboarding | Generated `docs/29-task-registry.md:38 @ 26d4802` references C3 task with `docs/CRM-SYSTEM-MAP.md` v "files changed" — generated z `docs/30-task-records/C3.md` historicky. Generated artifact je faithful k task record (correct), ale propaguje broken reference do canonical generated doc. | P3 | `docs/29-task-registry.md:38 @ 26d4802`; `docs/30-task-records/C3.md @ 26d4802` (historical) | Generated docs perpetuate historical references. Není to bug, ale signal že task records s broken refs = source of truth. | Buď: (a) accept jako historical record (no action); (b) post-process v `build-task-registry.mjs` aby zveřejnil warning o broken references. | DOC-001 |
| DOC-017 | Docs & Onboarding | Žádný root `README.md @ 26d4802`. Konsoliduje BLD-002 z DOC perspective: bez root README není entry point pro žádnou audience. | P0 | `git ls-tree origin/main README.md` = empty | First-impression UX gap. New dev/operator/maintainer nemá index. CONTRIBUTING.md a CLAUDE.md jsou specific concerns, ne entry point. | Vytvořit root README.md s: (a) project overview (1 odst), (b) component map (apps-script/, crm-frontend/, scripts/, docs/), (c) onboarding links per audience (dev, operator, maintainer, deploy, security), (d) docs index (`docs/audits/README.md` + `docs/20-current-state.md` + canonical layer). | **Resolved** transitively via BLD-002. Root `README.md` exists with overview tagline. Per-audience link map and full docs index are partial — operator may want to expand. |
| DOC-018 | Docs & Onboarding | Žádný `docs/PRIVACY.md` / GDPR/PII inventory @ 26d4802. Cross-ref SEC-014 — Phase 7 identified gap, Phase 10 konfirmuje DOC-level missing artifact. | P1 | `find docs/ -iname "PRIVACY*" -o -iname "GDPR*"` = empty (kromě audits/FINDINGS); `grep "GDPR\|privacy" docs/*.md` = pouze archive nebo task records | Bez privacy doc: žádný legal basis claim, žádná erasure path, žádná retention policy, žádný DPO contact, žádný consent record. GDPR Art. 5–17 compliance gap. | Vytvořit `docs/PRIVACY.md` s data inventory, legal basis, retention, erasure path, ROPA. Cross-ref SEC-014. | SEC-014 |
| DOC-019 | Docs & Onboarding | Žádný `docs/ROLLBACK.md @ 26d4802`. Cross-ref DP-018 — Phase 6 identified gap, Phase 10 owner = deploy owner audience. | P1 | `find docs/ -iname "ROLLBACK*"` = empty; `grep "rollback" docs/*.md` = pouze v audits | Při PROD incident deploy owner nezná exact steps (revert + clasp push + Vercel rollback + env restore). TTR risk + panic risk. | Vytvořit `docs/ROLLBACK.md` per DP-018 doporučení. | DP-018 |
| DOC-020 | Docs & Onboarding | Žádný `docs/SECRETS-ROTATION.md @ 26d4802`. Cross-ref SEC-017 + DP-019 — multi-phase gap. | P1 | `find docs/ -iname "*ROTATION*" -o -iname "*SECRETS*"` = empty; `grep "rotation\|rotat" docs/*.md` = pouze v audits/change-log | Bez rotation runbook tým nerotuje secrets ze strachu z downtime. Pokud secret unikne (cf. SEC-021 token v body) tým neví exact steps. | Vytvořit `docs/SECRETS-ROTATION.md` per SEC-017 doporučení. | SEC-017, DP-019 |
| DOC-021 | Docs & Onboarding | Žádný `docs/DEPLOY.md @ 26d4802` jako canonical entry point. Deploy info rozprostřena 4× (`apps-script/README.md:145-155`, `crm-frontend/README.md`, `docs/27-infrastructure-storage.md:50-59`, `docs/22-technical-architecture.md:49-53`) s drift mezi nimi. | P1 | scattered docs above; cross-ref DP-014, DP-015, DP-016, BLD-008 | Deploy owner musí pochopit deploy z 4 zdrojů které se navzájem rozcházejí. Risk: missed step, wrong order, wrong env. | Vytvořit `docs/DEPLOY.md` jako canonical s: pre-deploy checklist, AS deploy steps, FE deploy steps, post-deploy validation, rollback link (DOC-019), rotation link (DOC-020). | DP-014, DP-015, DP-016 |
| DOC-022 | Docs & Onboarding | Žádný `docs/OPERATOR-GUIDE.md @ 26d4802`. New operator nemá runbook pro: review queue ("Ke kontaktování"), decision dropdowns (APPROVE/REJECT/CHANGES_REQUESTED), Gmail draft/send menu, mailbox sync, lead lifecycle interpretation. | P1 | `find docs/ -iname "*OPERATOR*"` = empty; per Phase 10 onboarding map D.2 | Operator se učí "by example" od jiného operatora. Risk: misunderstand send (FF-006), missed review queue (FF-018), forgotten follow-up (FF-013). | Vytvořit `docs/OPERATOR-GUIDE.md` s: overview "Ke kontaktování", review workflow, send workflow, lifecycle states (cross-ref DOC-007 update of `21-business-process`), troubleshooting. | DP-016, FF-018 |
| DOC-023 | Docs & Onboarding | `docs/github-collaboration-setup.md` je mimo numbered tier `20-29`. Hygiene: buď přesunout do `27-infrastructure-storage.md` (governance setup je infra) nebo přejmenovat na `15-github-collaboration-setup.md` v "pre-canonical" tier `01-19`. | P3 | `docs/github-collaboration-setup.md @ 26d4802` (existuje); `docs/13-doc-update-rules.md` (žádná zmínka) | Discoverability: dev hledá GitHub setup, neví zda v `27-infrastructure` nebo separate. Convention drift. | Decide naming convention: numbered tier vs free-named. Aktualizovat `docs/13-doc-update-rules.md` s "free-named docs allowed pro on-boarding setup; canonical = 20-29". | — |
| DOC-024 | Docs & Onboarding | `docs/audits/INVENTORY.md @ 26d4802` je STUB (Phase 1 Inventory wasn't fully populated). Audit framework setupoval Phase 1 sekce ale obsah se nikdy nedoplnil. | P3 | `docs/audits/INVENTORY.md @ 26d4802` (sekce A–F označené `_(vyplňuje Fáze 1)_`); `docs/audits/README.md:18` claim "Faktický soupis repa (co tam je)" | Audit framework claim "Phase 1 INVENTORY done" v README ale soubor je stub. Phase 1 byl ve skutečnosti pouze framework setup, ne plný inventory. | Buď: (a) doplnit Phase 1 inventory retroactively (run grep / wc -l / loc analysis); (b) update `docs/audits/README.md` aby reflectoval že Phase 1 je framework + INVENTORY je stub-by-design. | — |
| DOC-025 | Docs & Onboarding | Apps Script side nemá `apps-script/AGENTS.md` ani `apps-script/CLAUDE.md @ 26d4802`. Frontend má oba. Onboarding gap pro Apps Script-specific contributors. | P3 | `find apps-script/ -iname "AGENTS.md" -o -iname "CLAUDE.md"` = empty; `crm-frontend/AGENTS.md, crm-frontend/CLAUDE.md` exist | Apps Script kód má specific rules (V8 runtime, no npm, clasp workflow) které frontend AGENTS.md neneguje. New AS contributor je bez per-component instructions. | Vytvořit `apps-script/AGENTS.md` s warnings: "no Node runtime", "use clasp", "test via scripts/test-aXX.mjs", "deploy via scripts/clasp-deploy.sh". | — |
| CC-NB-001 | CC — Newbie | Mock mode silent fallback bez UI/log indikátoru. Pokud newbie nemá Google creds, app spadne do mock mode (`isMockMode()`); newbie netuší zda vidí mock vs PROD data. V kombinaci s BLD-001 (PROD Sheet ID v env) může newbie omylem přepnout na PROD aniž si toho všimne. | P1 | `crm-frontend/src/lib/mock/mock-service.ts:isMockMode @ 61129bc`; `crm-frontend/src/app/api/leads/route.ts:6` (`isMockMode()` check); BLD-001 evidence | Newbie misinterprets data origin. Risk: editace v "mock UI" myslí že nic se neděje; reality může být PROD pokud env vars jsou populated. | Přidat banner / toast / page header "MOCK MODE ACTIVE" když `isMockMode()=true`. Plus log warning v server-side console při startup. | BLD-001, BLD-003 |
| CC-NB-002 | CC — Newbie | `crm-frontend/AGENTS.md` warning "this is NOT the Next.js you know" je matoucí pro lidského newbie. Soubor je psán pro AI agenty ale newbie to neví; čte ho jako "Next.js v tomhle repu má neznámé breaking changes" a stráví hodiny. | P2 | `crm-frontend/AGENTS.md @ 61129bc` (3 řádky text + import); `crm-frontend/CLAUDE.md @ 61129bc` (`@AGENTS.md` import) | Onboarding friction. Newbie hledá custom Next.js fork, customizace, override behavior — nic z toho. Reality: AGENTS.md je AI hint pro Claude/Cursor že má číst Next.js v16 docs, ne v14. | Buď: (a) přidat header "FOR AI AGENTS — humans see crm-frontend/README.md"; (b) přesunout do `.claude/agents.md` a out-of-scope pro humans. | — |
| CC-NB-003 | CC — Newbie | Žádný "what is this project" v 30 sekundách. Root nemá README, `docs/20-current-state.md` má 5+ KB hutné "souhrn" text. Newbie potřebuje 1-věta description + 30-sec orientation, ne page-long status. | P2 | `git ls-tree origin/main README.md` = empty; `docs/20-current-state.md @ 61129bc` (>200 řádků densely-written context) | First-impression overhead. Newbie čte 5 minut a stále neví "co projekt dělá v 1 větě". | Root README.md s 1-věta tagline + 5-bullet overview (cross-ref DOC-017). | DOC-017 |
| CC-NB-004 | CC — Newbie | `CLAUDE.md` v rootu newbie zmate. Newbie čte CLAUDE.md jako "rules pro me" (developer onboarding); ale obsah je psán pro AI agenta (Claude Code). Mix CLAUDE.md (AI rules) + AGENTS.md (frontend AI rules) + CONTRIBUTING.md (human rules) bez jasného "for AI agents only" labelu. | P2 | `CLAUDE.md @ 61129bc` (Claude Code instructions, ne human dev guide); `CONTRIBUTING.md @ 61129bc` (human rules) | Onboarding ambiguity — newbie aplikuje AI rules na sebe (např. "needituj generated docs" rules ano, ale "use TodoWrite tool" no). | Přejmenovat `CLAUDE.md` na `.claude/CLAUDE.md` (per Claude Code convention) nebo přidat header "**FOR AI AGENTS — humans see CONTRIBUTING.md**". | — |
| CC-NB-005 | CC — Newbie | Žádný `docs/CHANGELOG.md` ani `docs/RELEASE-NOTES.md` v human-readable form. `docs/11-change-log.md` je generated z task records — stručný per-task (32 entries) ale ne release-summary. | P3 | `docs/11-change-log.md @ 61129bc` (generated, per-task entries); `git tag` (žádné release tags v repu) | Newbie hledá "what's new in last release" — nic. Pro project bez tagged releases je to OK, ale po go-live je gap. | Po go-live zavést semver tags + `docs/RELEASE-NOTES.md` (manual + script). | — |
| CC-OPS-001 | CC — DevOps | Žádný `docs/INCIDENT-RESPONSE.md` ani escalation path. Při PROD incidentu (token leak, sheet corruption, AS cron looping) operator/dev nezná: kdo eskalovat, jak rozeznat severity, jak pause services, jak zafixovat blast radius. Cross-domain s DOC-019 (rollback) a DOC-020 (rotation) ale incident response je separate document (rollback je technická akce, IR je orchestrování). | P1 | `find docs/ -iname "*INCIDENT*"` = empty; cross-ref DOC-019 (rollback) + DOC-020 (rotation) | Při PROD incident chaos. TTR roste s každým "kdo má rozhodnout" gateway. Critical pro go-live. | Vytvořit `docs/INCIDENT-RESPONSE.md` s: severity ladder (P0/P1/P2), escalation chain, on-call contact, common scenarios (token leak, sheet corruption, runaway cron), pause-and-rollback flowchart. | DOC-019, DOC-020 |
| CC-OPS-002 | CC — DevOps | Žádná documented secrets rotation cadence + entropy baseline. I pokud DOC-020 vznikne s "how to rotate", tým musí znát: kdy rotovat (90 dní? per incident?), entropy minima (16 chars? 32 random bytes?), kdo je accountable. Bez cadence = secrets nikdy nerotují → token theft persistuje navždy. | P1 | `find docs/ -iname "*ROTATION*" -o -iname "*SECRETS*"` = empty; cross-ref SEC-017, DP-019 | Operational gap. Rotation runbook (DOC-020) řeší "how", ale "when" + "by whom" + "to what entropy" je separate. | Doplnit do `docs/SECRETS-ROTATION.md` per-secret cadence + entropy table + accountability matrix. | DP-019, SEC-017 |
| CC-OPS-003 | CC — DevOps | Žádný "deploy log" / release notes per Apps Script deploy. `clasp push prod` je manual operator action, žádný auto-záznam (kdo, kdy, jaký SHA). Apps Script Console má execution history ale bez commit linkage. Při incident analysis "kdy se to rozbilo" = manuální cross-ref `git log` × Apps Script Console timestamps. | P2 | `scripts/clasp-deploy.sh @ 61129bc` (no logging step); Apps Script Console (no per-deploy commit metadata) | Incident forensics gap. "Did the bug land in this deploy?" cannot be answered without manual investigation. | `clasp-deploy.sh` post-deploy: append entry do `docs/30-task-records/release-log.md` (commit SHA, timestamp, target=test/prod, operator email). Plus `clasp deploy --description "<commit-sha>"` (cross-ref DP-008). | DP-008 |
| CC-OPS-004 | CC — DevOps | Žádný post-deploy verification step v `clasp-deploy.sh`. Po `clasp push` skript končí. Žádný smoke test (např. ping `doPost` endpoint, ověřit version stamp, ověřit web app dostupnost). | P1 | `scripts/clasp-deploy.sh @ 61129bc` (po `clasp push` end) | Operator deploys → no automated verification → silent failure (e.g. clasp pushed broken code, runtime crashes invisibly until next ingestion). | Add `clasp-deploy.sh` post-step: `curl $WEBHOOK_URL?action=health` (vyžaduje DP-017 health endpoint) + verify HTTP 200 + version stamp matches commit. | DP-008, DP-017 |
| CC-OPS-005 | CC — DevOps | Žádné BUDGET / quota monitoring v repu. Apps Script má quotas (UrlFetchApp 20k/day, executions 30 simultaneous, mail 100-1500/day). Žádný monitor "jsme na 80% denního limitu". Při překročení = silent service degradation pro všechny operatory. C-11 SPEC-only kontrakt zmiňuje budgets, runtime žádné. | P2 | C-11 task record (SPEC-only); žádný runtime budget check v `apps-script/`; FF-014 cross-ref | Quota exhaustion = invisible until incident. Critical pro scale (cross-ref CC-QA-008 load test). | Implement minimal quota monitor: `_asw_logs` summary count / day vs hardcoded thresholds; alert via aswLog WARN if > 70%. Long-term: C-11 runtime. | FF-014 |
| CC-OPS-006 | CC — DevOps | Žádný "deployable units" inventory. DevOps potřebuje znát: co je deployovatelný unit, co se mění spolu, co se mění samostatně. Reality: AS + FE + Sheets + Vercel env + Script Properties = 5 deploy surfaces se cross-dependencies (per FF-001 ingest, FF-004 preview store), žádný inventory v jednom místě. Deploy owner musí mít mapu v hlavě. | P2 | scattered docs (apps-script/README, crm-frontend/README, docs/27-infrastructure-storage); žádný unifying doc | Deploy owner risk: forget to update one of 5 surfaces. E.g., update Apps Script, forget to update Vercel env → mismatch. | `docs/DEPLOY.md` (DOC-021) musí explicitly listuje 5 deploy surfaces + jejich versioning relationships + "if you change X, also update Y". | DP-009, DOC-021 |
| CC-OPS-007 | CC — DevOps | Žádný runtime kill switch. Při incident lze stopnout funkčnost pouze: (a) clasp push verzí s DRY_RUN=true (5-10 min); (b) delete Apps Script triggers (manual GUI); (c) rotate FRONTEND_API_SECRET (breaks frontend write-back). Žádný runtime toggle "stop sending emails NOW". C-11 SPEC-only zmiňuje kill switches. | P2 | `apps-script/Config.gs:24-26 @ 61129bc` (file-level vars `DRY_RUN`, `ENABLE_WEBHOOK`); FF-014 cross-ref | Operational gap. Critical pro incident: cannot pause sends instantly. | Implement Script Property-based kill switches (`KILL_SEND=true`, `KILL_PREVIEW=true`, `KILL_INGEST=true`) checked at start of each function. | FF-014 |
| CC-SEC-001 | CC — Attacker | Attack chain SEC-005 + SEC-006 + SEC-007 = practical credential stuffing pipeline. Individually každý je P1; dohromady tvoří full-blown attack: timing → email enumeration → password brute-force → 7-day session. Žádný kombinovaný threat-model document v repu vyhodnocuje řetězení. Single-fix priority: rate limiting (SEC-007) zlomí všechny tři. | P1 | Chain: `crm-frontend/src/app/api/auth/login/route.ts:37-44 @ 61129bc` (timing oracle); `:42` (plain compare); whole file (no rate limit) | High-impact attack chain. Single-shared-AUTH_PASSWORD = single point of compromise. | Rate limiting (per-IP exponential backoff) + timing-safe compare + 2FA. Cross-ref SEC-007 fix priority. | SEC-005, SEC-006, SEC-007 |
| CC-SEC-002 | CC — Attacker | Public preview URL guessable + indexable + no consent. Slug = `business-city` deterministically (preview-contract.ts:507). Žádný `<meta name="robots" content="noindex">`. Když Google crawler trefí window kdy preview-store je dostupný, indexuje + cache navždy. SEC-009 gap rozšířen o crawler caching = lead data exposed even after operator deletes preview. Žádný takedown procedure. | P1 | `crm-frontend/src/lib/domain/preview-contract.ts:507 @ 61129bc` (slug derivation); `crm-frontend/src/app/preview/[slug]/page.tsx @ 61129bc` (no robots meta); `crm-frontend/src/middleware.ts:3 @ 61129bc` (PUBLIC_PATHS includes `/preview`) | Search engine cache amplifies SEC-009. Lead nikdy o preview nepožádal, jeho contact info je v Google cache navždy. GDPR čl. 17 (right to erasure) impossible to satisfy. | Krátkodobě: `<meta name="robots" content="noindex,nofollow">` + `X-Robots-Tag` header. Středně: random suffix v slug + auth na preview. Dlouhodobě: SEC-009 fix + crawler takedown procedure. | SEC-009 |
| CC-SEC-003 | CC — Attacker | Ex-employee 7-day session window + no per-user attribution. Po revoke Google Workspace access ex-employee má valid `crm-session` cookie po 7 dní (SEC-008). Plus Apps Script Web App accepts shared `FRONTEND_API_SECRET` token bez per-user identity (SEC-003) — i pokud session expired, ex-employee s lokální `.env.local` (BLD-001) může přímo volat Apps Script webapp endpoint. | P2 | `crm-frontend/src/middleware.ts:84-91 @ 61129bc` (7-day maxAge check, no server blocklist); SEC-003 evidence; SEC-008 evidence; BLD-001 evidence | Insider threat post-offboarding. 7+ days authority + permanent webapp endpoint access. | Implement session blocklist (SEC-008 fix) + offboarding checklist must rotate `FRONTEND_API_SECRET` (DOC-020). | SEC-003, SEC-008, BLD-001 |
| CC-SEC-004 | CC — Attacker | Sheet ID + sharing exploit risk dependent on out-of-repo settings. Pokud Sheet sharing je `Anyone with link can view` (na PROD `1RBc…` sheet which is public-known per SEC-001), full LEADS data dump je zero-click. Audit nemůže ověřit settings (MC-SEC-O-01). Risk je binary: pokud restricted = 0; pokud permissive = catastrophic. | P2 | SEC-001 evidence (Sheet ID public); MC-SEC-O-01 (sharing settings unverified) | Out-of-band setting determines whole exploit. | URGENT manual check MC-SEC-O-01. Pokud sharing permissive → P0 escalation. | SEC-001, SEC-002 |
| CC-SEC-005 | CC — Attacker | Vercel logs as token-leak surface. SEC-021 zmiňuje token v body, ale konkrétně Vercel Edge Function logs / Function logs default-include request body pokud není explicitně redacted. Pokud Vercel log retention > rotation cadence, token leaked = bearer until rotated. | P2 | SEC-021 evidence; MC-SEC-O-06 (Vercel logs token check) | Implicit credential leak via logging infrastructure. | Move token to Authorization header (SEC-021 fix) + audit Vercel log redaction settings. | SEC-017, SEC-021 |
| CC-SEC-006 | CC — Attacker | Sheet poisoning via lead row write. Kdokoli s sheet write access (= service account v repu, plus operator via `/api/leads/[id]/update`) může injektovat `<script>` payload do `sales_note` nebo `email_body_draft`. React JSX escapes ale Gmail drafts use raw text → email recipient může dostat HTML/JS payload. | P3 | `apps-script/PreviewPipeline.gs:composeDraft_ @ 61129bc` (template raw text); `crm-frontend/src/app/api/leads/[id]/update/route.ts:5000 char limit on salesNote @ 61129bc` | Limited risk (Gmail rendering blocks scripts) ale tracking pixels / phishing redirects funkční. | Sanitize HTML in email body builder + content security headers v Gmail draft creation. | SEC-015 |
| CC-SEC-007 | CC — Attacker | Source code exfiltration via clasp OAuth + committed scriptIds. SEC-002 zmiňuje clasp pull risk; CC-SEC-007 zdůrazňuje že OAuth token (z `.clasprc.json` ex-employee retains po revoke Google Workspace IF clasprc nebyl revoked separately — Google OAuth revocation je per-app). Plus Apps Script source obsahuje business logic (qualify rules, dedupe heuristics) které jsou competitive IP. | P2 | SEC-002 evidence; SEC-010 evidence (`.gitignore` missing `.clasprc.json`) | Competitive IP exfiltration risk. | Workspace offboarding must include "revoke clasp OAuth app" + Google Account → Security → Third-party access cleanup. | SEC-002, SEC-010 |
| CC-SEC-008 | CC — Attacker | No security headers in Vercel responses. Žádné CSP, X-Frame-Options, HSTS, Referrer-Policy. Public `/preview/<slug>` route by zejména měl mít CSP pro mitigaci future XSS. Browser-default je security-permissive. | P3 | `crm-frontend/next.config.ts:3-7 @ 61129bc` (empty config — BLD-018); no `headers()` function | Defense-in-depth gap. Future XSS or click-jacking attacks unmitigated. | `next.config.ts` přidat `headers()` async function s CSP, X-Frame-Options DENY, HSTS, Referrer-Policy strict-origin-when-cross-origin. | SEC-009, BLD-018 |
| CC-QA-001 | CC — QA | `docs/30-task-records/_template.md` neobsahuje "Acceptance criteria" section. Per Phase 11 review, jen 3 task records (B-04, B-05, B-06) mají explicit acceptance criteria. Většina tasks má "Code Changes" + "Docs Updated" ale neformální acceptance. QA nemá per-task verifikační seznam. | P1 | `docs/30-task-records/_template.md @ 61129bc` (no Acceptance section); cross-ref většina task records | QA testing gap. Tester does not know "what does done mean" per task. | Update `_template.md` s povinnou "Acceptance Criteria" section (BDD-style scenarios). Backfill recent task records. | DOC-022, BLD-014 |
| CC-QA-002 | CC — QA | End-to-end pipeline NOT testable. FF-001 + FF-002 zlomí scraper → _raw_import → LEADS link. QA nemůže spustit "full ingest acceptance test" — nelze. | P0 | FF-001, FF-002 evidence | Full funnel acceptance test impossible. | FF-001/2 fix priorities. Until then, document E2E gap explicitly v `docs/audits/12-summary.md`. | FF-001, FF-002 |
| CC-QA-003 | CC — QA | Outreach loop (send → reply → follow-up) je SPEC-only — žádný acceptance scenario možný. C-07 inbound, C-08 follow-up SPEC bez runtime → QA nemůže ověřit "lead reagoval, systém auto-tagged jako RESPONDED". Manual MailboxSync (FF-008) nelze acceptance-test bez waiting for real email. | P1 | FF-008, FF-012, FF-013 evidence | Bottom-half of funnel = untestable z QA perspective. | C-07/C-08 implementation priorities. Až runtime, acceptance criteria possible. | FF-008, FF-012, FF-013 |
| CC-QA-004 | CC — QA | No "Vercel restart preview integrity" regression test. IN-014 / FF-004 (preview store loss) je known issue ale není v test matrix. | P1 | IN-014, FF-004 evidence | Po každém Vercel deploy = N old `preview_url` v LEADS jsou broken. QA nemá způsob jak measure. | Pre/post deploy slug check script — count valid `preview_url` v LEADS, verify each resolves to 200. | IN-014, FF-004 |
| CC-QA-005 | CC — QA | No concurrency / race test pro `processPreviewQueue`. FF-003 + FF-019 — operator edit vs 15-min cron. Reproducer: manuálně nastavit `processPreviewQueue` na ~5 min interval, edit reviewa, observer overwrite. Manual-only test, žádný unit. | P1 | FF-003, FF-019 evidence | Concurrent edit data loss invisible until manifest. | Unit test pro `processPreviewQueue` se simulated concurrent calls (pomocí mock LockService). | FF-003, FF-019 |
| CC-QA-006 | CC — QA | No negative test pro identity verification conditional (IN-007). Apps Script `WebAppEndpoint.gs:81-94` přeskočí identity check pokud `payload.businessName/city` chybí. Žádný test "PATCH bez businessName by měl FAIL pro security" — frontend writer vždy posílá, takže happy-path-tested only. | P2 | IN-007 evidence; existing tests don't cover bez-identity case | Security defense-in-depth not tested. | Add test case do test-b06 (or equivalent) "doPost bez businessName must fail with identity error". | IN-007 |
| CC-QA-007 | CC — QA | Lifecycle state consistency cross-check missing. FF-015 — 4 separate state machines. QA nemá test "jsou stavy konzistentní?" (např. `outreach_stage=WON` ale `preview_stage=REJECTED` = invalid state). Žádné invariants documented or asserted. | P1 | FF-015 evidence; `apps-script/Config.gs:141-231 @ 61129bc` (4 enums) | Cross-state invariants drift undetected. | Define invariant matrix v `docs/21-business-process.md` (DOC-007 update) + audit script `node scripts/audit-lifecycle-consistency.mjs` to scan LEADS for invalid combinations. | FF-015 |
| CC-QA-008 | CC — QA | No load test / scale ceiling baseline. Apps Script má quotas, Sheets má ~50k rows performance threshold. QA nezná: kolik leads/day systém zvládne, p50/p95 latency, kdy se začne degradovat. Žádný load testing tooling v repu. | P2 | `docs/27-infrastructure-storage.md:43-46 @ 61129bc` (limits listed but no measurement) | Scale ceiling unknown until production hits it. | Synthetic load test: simulate 1k/10k leads, measure ingest+qualify+brief throughput, document v `docs/28-risks-bottlenecks-scaling.md`. | FF-014 |
| CC-QA-009 | CC — QA | No "test data fixtures" pro full happy path. Existuje sample brief fixtures (5 pro renderer testing) + scraper fixtures. Ale žádný unified "happy lead" test fixture který projde celou pipeline. QA musí ručně sestavit. | P2 | `crm-frontend/src/lib/mock/preview-brief.*.json @ 61129bc` (5 fixtures); `scripts/scraper/samples/` (scraper fixtures); no E2E fixture | Test fixture overhead. Each E2E test reinvents lead row. | Vytvořit `docs/audits/fixtures/happy-lead.json` + per-stage expected state. | — |
| CC-QA-010 | CC — QA | No regression matrix v audit docs. Existuje `docs/audits/MANUAL_CHECKS.md` (61+ items) ale není to regression matrix per release. QA potřebuje "before-each-release smoke test" subset (např. 10 critical items). | P2 | `docs/audits/MANUAL_CHECKS.md @ 61129bc` (61+ items, not categorized as smoke vs deep) | Pre-release verification gap. | Add `docs/audits/SMOKE-TEST.md` s 5-10 critical items (e.g., login, lead fetch, preview render, send draft, review writeback). | DOC-022 |
| CC-QA-011 | CC — QA | Test scripts používají console.log místo strukturovaného outputu. Per Phase 9 evidence, tests output je human-readable text. Pro CI integration / failure parsing by JSON output (`node:test --reporter=spec`) byl preferable. | P3 | `scripts/test-*.mjs @ 61129bc` (manual console-based assertion logging); BLD-013 cross-ref | CI parsing harder. Per-test failure tracking impossible without grep. | Migrate to `node:test` framework s `--reporter=spec` nebo `tap`. | BLD-013 |
| CC-QA-012 | CC — QA | Žádný `.mocharc` / test config v repu. Test scripts jsou self-contained `.mjs` files. Žádné common setup/teardown, žádný shared assertion helper. Drift mezi tests v stylu, custom assertions per file. | P3 | grep `assert` v `scripts/test-*.mjs` — 5+ different patterns | Test maintenance overhead. | Vytvořit `scripts/tests/_helpers.mjs` s shared assertion + setup utilities. Migrate tests one-by-one. | — |

---

## Severity distribution (progress tracker)

> **Updated 2026-04-29 (audit reconciliation pass).** Counts below show the original
> snapshot from baseline `61129bc` / `26d4802` / `69acc78`. Per the per-finding Status
> column updates above, **23 findings have been verified Resolved or In Progress**
> against current main. Open count dropped from 161 → ~138. New top P1 blockers
> after reconciliation: see Section E in `12-summary.md`. The original distribution
> table is preserved below for audit history; subtract Resolved per-domain to get
> current Open counts.

**Reconciliation rollup (2026-04-29):**

| Status | Count | Notes |
|--------|-------|-------|
| Resolved | 21 | Mostly P0/P1 quick wins: A-11 wave (FF-001/2/4/6/8/18/22, IN-014, FF-018), KROK 2/3/4/7 hygiene (BLD-001/2/3/4, BLD-010/15, SEC-012/16, DP-005/7, DP-021, SEC-011, DOC-017), envGuard wiring |
| In Progress | 4 | DP-002/SEC-002 (gitignored + example, history remains), DP-006 + SEC-010 partial (`.clasprc.json` ignored, broader patterns missing), BLD-011 (transitive resolved) |
| Open | 104 | Remaining P0/P1/P2/P3 across all domains |
| **Total** | **129** | (CC-* persona findings — 32 — not re-verified per task spec scope) |

**Original snapshot (audit history, unchanged):**

| | P0 | P1 | P2 | P3 | Celkem |
|---|----|----|----|----|--------|
| DM (Data Model) | 0 | 0 | 0 | 0 | 0 |
| AS (Apps Script) | 0 | 0 | 0 | 0 | 0 |
| FE (Frontend) | 0 | 0 | 0 | 0 | 0 |
| IN (Integration) | 0 | 6 | 11 | 1 | 18 |
| DP (Deploy Pipeline) | 2 | 8 | 11 | 0 | 21 |
| SEC (Security) | 2 | 11 | 9 | 0 | 22 |
| FF (Funnel Flow) | 2 | 9 | 11 | 0 | 22 |
| BLD (Buildability) | 3 | 7 | 8 | 3 | 21 |
| DOC (Docs & Onboarding) | 3 | 12 | 5 | 5 | 25 |
| CC-NB (Newbie) | 0 | 1 | 3 | 1 | 5 |
| CC-OPS (DevOps) | 0 | 3 | 4 | 0 | 7 |
| CC-SEC (Attacker) | 0 | 2 | 4 | 2 | 8 |
| CC-QA (QA) | 1 | 4 | 4 | 3 | 12 |
| **Total** | **13** | **63** | **70** | **15** | **161** |

---

## Notes

- Status `Open` = všechny findings (audit je read-only, nic se neopravuje)
- Evidence je vždy `soubor:řádek` odkaz na konkrétní místo
- Suggested Action je návrh, **neprovádí se** jako součást auditu
- Cross-refs: každý finding odkazuje na detailní analýzu v odpovídajícím `domains/XX-*.md` nebo `cross-check/11*-*.md`
- IN-016 je cross-domain s Phase 7 (Security) — primary owner SEC, IN- prefix je pro traceability v rámci kontraktu
- DP-001, DP-002, DP-006, DP-021 jsou cross-domain s Phase 7 (Security) — primary owner SEC, DP- prefix je pro traceability v rámci deploy pipeline
- DP-014, DP-015, DP-016 jsou cross-domain s Phase 10 (Docs & Onboarding) — primary owner DOC
- SEC-001 konsoliduje IN-016, DP-001 (a rozšiřuje plochu evidencí); původní findings zůstávají Open pro traceability
- SEC-002 konsoliduje DP-002; SEC-010 konsoliduje DP-006; SEC-011 konsoliduje DP-021; SEC-017 konsoliduje DP-019; SEC-022 konsoliduje DP-010 a DP-020
- SEC-014 (PII/GDPR), SEC-017 (rotation) jsou cross-domain s Phase 10 (Docs)
- FF-004 navazuje na IN-014 (preview store) na funnel-flow úrovni
- FF-019 navazuje na FF-003 (concurrency related)
- FF-009 navazuje na FF-006 (sendability gate runtime by řešil ad-hoc guards)
- FF-001, FF-002 jsou primary blockers pro end-to-end automation
- BLD-001 nový file path pro PROD ID (cross-ref SEC-001, DP-001) — `.env.example` byl v Phase 7 sandbox-blocked, Phase 9 ho zpřístupnil přes fresh clone
- BLD-007, BLD-008 pokračují DP-013 onboarding gap pro Apps Script
- BLD-010 je přímý cross-ref SEC-012 (CVE) — v Phase 9 validováno přes `npm audit --omit=dev` v fresh clone
- BLD-015, BLD-014 pokračují DP-005 missing CI gate gap
- BLD-016 je sub-finding DP-016 ohledně frontend README
- BLD-019 cross-ref BLD-005 (no Node version pinning)
- DOC-001 (CRM-SYSTEM-MAP broken refs) konsoliduje DP-016 z DOC perspective; DOC-015 je sub-finding pro `crm-frontend/README` link
- DOC-002 (governance broken) je nový — Phase 10-only; ovlivňuje všechny ostatní docs governance findings
- DOC-003/004/005 (path/count drifts) jsou nové discrete drift findings pro `docs/09`, `docs/10`, `docs/14`
- DOC-007/008/009/010 (stale canonical 21/25/26/28) jsou nové canonical-layer findings; DP-014/15 cover 22/27
- DOC-017/018/019/020/021/022 jsou DOC consolidation — missing onboarding artifacts napříč auditem (BLD-002, SEC-014, SEC-017, DP-018, DP-019)
- DOC-024/025 (audit framework stub, AS AGENTS missing) jsou nové
- Phase 10 = poslední domain phase — Phase 11 (cross-check by 4 personas) a Phase 12 (executive summary) zbývají
- CC-NB persona findings (5 new) — onboarding-specific gaps (mock mode silent fallback, AGENTS.md confusing for humans, no 30-sec project intro, CLAUDE.md ambiguity for humans, no release notes)
- CC-OPS persona findings (7 new) — operational gaps (incident response runbook, rotation cadence, deploy log, post-deploy verification, quota monitor, deployable units inventory, runtime kill switch)
- CC-SEC persona findings (8 new) — attack chains and threat-model gaps (login chain, public preview crawler, ex-employee window, sheet sharing exploit, Vercel token logs, sheet poisoning, source exfil, security headers)
- CC-QA persona findings (12 new) — testability gaps (acceptance criteria, E2E impossible, outreach SPEC-only, restart regression, race tests, identity negative, lifecycle invariants, load test, fixtures, smoke matrix, structured output, test config)
